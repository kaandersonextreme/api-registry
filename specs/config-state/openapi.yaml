components:
  schemas:
    AssetAccount:
      description: AssetAccount - DB model for Asset Account - This object represents
        the list devices with account information
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: The flag to indicate if the account is enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        is_secret_encrypted:
          description: The flag to indicate if the secret is encrypted.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        removable:
          description: The flag to indicate if the account is removable.
          type: boolean
        secret:
          description: The secret of the host/device.
          type: string
        type:
          description: The type of the account.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        user_name:
          description: The user name of the host/device.
          type: string
      required:
        - asset_device_id
        - secret
        - type
        - user_name
      type: object
    AssetAccountGetRequest:
      description: AssetAccountGetRequest represents the retrieval request for AssetAccount.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The flag to indicate if the
            account is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        is_secret_encrypted:
          description: >
            List of IsSecretEncrypted. IsSecretEncrypted represents The flag to
            indicate if the secret is encrypted.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        removable:
          description: >
            List of Removable. Removable represents The flag to indicate if the
            account is removable.
          items:
            type: boolean
          type: array
        secret:
          description: |
            List of Secret. Secret represents The secret of the host/device.
          items:
            type: string
          type: array
        type:
          description: |
            List of Type. Type represents The type of the account.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        user_name:
          description: >
            List of UserName. UserName represents The user name of the
            host/device.
          items:
            type: string
          type: array
      title: AssetAccountGetRequest
      type: object
    AssetAccountGetResponse:
      properties:
        AssetAccount:
          items:
            $ref: "#/components/schemas/AssetAccount"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAccountGetResponse
      type: object
    AssetAllOfLldpNeighborXmedInventory:
      description: AssetAllOfLldpNeighborXmedInventory - This object describes the
        lldp neighbor MED inventory state information list.
      nullable: true
      properties:
        asset_id:
          description: |-
            The vendor-specific asset tracking identifier
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.7.
          type: string
        asset_lldp_neighbor_xmed_id:
          description: Asset LLDP neighbor XMed ID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        firmware_rev:
          description: |-
            The vendor-specific firmware revision string
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          type: string
        hardware_rev:
          description: |-
            The vendor-specific hardware revision string
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.1.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mfg_name:
          description: |-
            The vendor-specific manufacturer name as advertised
            	by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.5.
          type: string
        model_name:
          description: |-
            The vendor-specific model name as advertised by
            	the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.6.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        serial_num:
          description: |-
            The vendor-specific serial number as advertised
            	by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.4.
          type: string
        software_rev:
          description: |-
            The vendor-specific software revision string
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.3.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetAllOfLldpNeighborXmedInventoryGetRequest:
      description: AssetAllOfLldpNeighborXmedInventoryGetRequest represents the
        retrieval request for AssetAllOfLldpNeighborXmedInventory.
      properties:
        asset_id:
          description: >
            List of AssetId. AssetId represents The vendor-specific asset
            tracking identifier
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.7.
          items:
            type: string
          type: array
        asset_lldp_neighbor_xmed_id:
          description: >
            List of AssetLldpNeighborXmedId. AssetLldpNeighborXmedId represents
            Asset LLDP neighbor XMed ID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        firmware_rev:
          description: >
            List of FirmwareRev. FirmwareRev represents The vendor-specific
            firmware revision string
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          items:
            type: string
          type: array
        hardware_rev:
          description: >
            List of HardwareRev. HardwareRev represents The vendor-specific
            hardware revision string
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.1.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mfg_name:
          description: >
            List of MfgName. MfgName represents The vendor-specific manufacturer
            name as advertised
            	by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.5.
          items:
            type: string
          type: array
        model_name:
          description: >
            List of ModelName. ModelName represents The vendor-specific model
            name as advertised by
            	the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.6.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        serial_num:
          description: >
            List of SerialNum. SerialNum represents The vendor-specific serial
            number as advertised
            	by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.4.
          items:
            type: string
          type: array
        software_rev:
          description: >
            List of SoftwareRev. SoftwareRev represents The vendor-specific
            software revision string
            	as advertised by the remote endpoint.
            	For details see ANSI/TIA-1057, Section 10.2.6.3.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetAllOfLldpNeighborXmedInventoryGetRequest
      type: object
    AssetAllOfLldpNeighborXmedInventoryGetResponse:
      properties:
        AssetAllOfLldpNeighborXmedInventory:
          items:
            $ref: "#/components/schemas/AssetAllOfLldpNeighborXmedInventory"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAllOfLldpNeighborXmedInventoryGetResponse
      type: object
    AssetAuthenticationConfig:
      description: AssetAuthenticationConfig - DB model for Authentication
        configuration PortSettings can be created while creating
        AssetAuthenticationConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        auth_protocol_order:
          description: List of authentication protocol order
          items:
            type: string
          nullable: true
          type: array
        auto_nsi_offset:
          description: Auto NSI Offset
          type: integer
        auto_nsi_offset_enabled:
          description: Auto NSI Offset enabled state
          type: boolean
        cep_cdp_nsi:
          description: CEP CDP NSI
          type: integer
        cep_cdp_role:
          description: CEP CDP role
          type: integer
        cep_cdp_vlan:
          description: CEP CDP Vlan
          type: integer
        cep_lldp_nsi:
          description: CEP LLDP NSI
          type: integer
        cep_lldp_role:
          description: CEP LLDP role
          type: integer
        cep_lldp_vlan:
          description: CEP LLDP Vlan
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        default_vlan:
          description: Default Vlan
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dot1x_auth_feature_enabled:
          description: 802.1X authentication feature enabled state
          type: boolean
        eapol_feature_enabled:
          description: Eapol feature enabled state
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mac_auth_feature_enabled:
          description: Mac authentication feature enabled state
          type: boolean
        mac_auth_order:
          description: Mac authentication order
          format: int64
          type: integer
        mac_list_default:
          description: Mac list default state
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        password_case:
          description: Password case
          format: int64
          type: integer
        password_separator:
          description: Password separator
          format: int64
          type: integer
        policy_cep_feature_enabled:
          description: Policy cep feature enabled state
          type: boolean
        policy_feature_enabled:
          description: Policy feature enabled state
          type: boolean
        port_list:
          description: Port list
          items:
            type: string
          nullable: true
          type: array
        port_settings:
          items:
            $ref: "#/components/schemas/AssetAuthenticationPortSettingConfig"
          nullable: true
          type: array
        radius_pwd_fmt:
          description: List of RADIUS password format
          items:
            type: string
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        web_auth_feature_enabled:
          description: Web authentication feature enabled state
          type: boolean
      required:
        - asset_device_id
      type: object
    AssetAuthenticationConfigGetRequest:
      description: AssetAuthenticationConfigGetRequest represents the retrieval
        request for AssetAuthenticationConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        auth_protocol_order:
          description: >
            List of AuthProtocolOrder. AuthProtocolOrder represents List of
            authentication protocol order
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        auto_nsi_offset:
          description: |
            List of AutoNsiOffset. AutoNsiOffset represents Auto NSI Offset
          items:
            type: integer
          type: array
        auto_nsi_offset_enabled:
          description: >
            List of AutoNsiOffsetEnabled. AutoNsiOffsetEnabled represents Auto
            NSI Offset enabled state
          items:
            type: boolean
          type: array
        cep_cdp_nsi:
          description: |
            List of CepCdpNsi. CepCdpNsi represents CEP CDP NSI
          items:
            type: integer
          type: array
        cep_cdp_role:
          description: |
            List of CepCdpRole. CepCdpRole represents CEP CDP role
          items:
            type: integer
          type: array
        cep_cdp_vlan:
          description: |
            List of CepCdpVlan. CepCdpVlan represents CEP CDP Vlan
          items:
            type: integer
          type: array
        cep_lldp_nsi:
          description: |
            List of CepLldpNsi. CepLldpNsi represents CEP LLDP NSI
          items:
            type: integer
          type: array
        cep_lldp_role:
          description: |
            List of CepLldpRole. CepLldpRole represents CEP LLDP role
          items:
            type: integer
          type: array
        cep_lldp_vlan:
          description: |
            List of CepLldpVlan. CepLldpVlan represents CEP LLDP Vlan
          items:
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        default_vlan:
          description: |
            List of DefaultVlan. DefaultVlan represents Default Vlan
          items:
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dot1x_auth_feature_enabled:
          description: >
            List of Dot1xAuthFeatureEnabled. Dot1xAuthFeatureEnabled represents
            802.1X authentication feature enabled state
          items:
            type: boolean
          type: array
        eapol_feature_enabled:
          description: >
            List of EapolFeatureEnabled. EapolFeatureEnabled represents Eapol
            feature enabled state
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mac_auth_feature_enabled:
          description: >
            List of MacAuthFeatureEnabled. MacAuthFeatureEnabled represents Mac
            authentication feature enabled state
          items:
            type: boolean
          type: array
        mac_auth_order:
          description: >
            List of MacAuthOrder. MacAuthOrder represents Mac authentication
            order
          items:
            format: int64
            type: integer
          type: array
        mac_list_default:
          description: >
            List of MacListDefault. MacListDefault represents Mac list default
            state
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        password_case:
          description: |
            List of PasswordCase. PasswordCase represents Password case
          items:
            format: int64
            type: integer
          type: array
        password_separator:
          description: >
            List of PasswordSeparator. PasswordSeparator represents Password
            separator
          items:
            format: int64
            type: integer
          type: array
        policy_cep_feature_enabled:
          description: >
            List of PolicyCepFeatureEnabled. PolicyCepFeatureEnabled represents
            Policy cep feature enabled state
          items:
            type: boolean
          type: array
        policy_feature_enabled:
          description: >
            List of PolicyFeatureEnabled. PolicyFeatureEnabled represents Policy
            feature enabled state
          items:
            type: boolean
          type: array
        port_list:
          description: |
            List of PortList. PortList represents Port list
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        radius_pwd_fmt:
          description: >
            List of RadiusPwdFmt. RadiusPwdFmt represents List of RADIUS
            password format
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        web_auth_feature_enabled:
          description: >
            List of WebAuthFeatureEnabled. WebAuthFeatureEnabled represents Web
            authentication feature enabled state
          items:
            type: boolean
          type: array
      title: AssetAuthenticationConfigGetRequest
      type: object
    AssetAuthenticationConfigGetResponse:
      properties:
        AssetAuthenticationConfig:
          items:
            $ref: "#/components/schemas/AssetAuthenticationConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAuthenticationConfigGetResponse
      type: object
    AssetAuthenticationPortSettingConfig:
      description: AssetAuthenticationPortSettingConfig - DB model for Authentication
        port setting configuration
      properties:
        asset_interface_id:
          description: Interface Id
          type: string
        auth_config_id:
          description: Authentication config id
          type: string
        auth_failure_vlan:
          description: Authentication failure Vlan
          type: integer
        auth_failure_vlan_enabled:
          description: Authentication failure Vlan enabled state
          type: boolean
        auth_mode:
          description: Authentication mode
          format: int64
          type: integer
        auth_optional:
          description: Auth optional state
          type: boolean
        cep_cdp_enabled:
          description: CEP CDP enabled state
          type: boolean
        cep_lldp_enabled:
          default: false
          description: CEP LLDP enabled state
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dot1x_auth_max_macs:
          description: 802.1X authenticated max Macs
          type: integer
        dot1x_auth_type:
          default: 1
          description: 802.1X authentication type
          format: int64
          type: integer
        dot1x_reauth_enabled:
          description: 802.1X reauthetication enabled state
          type: boolean
        dot1x_reauth_origin:
          description: 802.1X reauthentication origin
          format: int64
          type: integer
        dot1x_reauth_period:
          description: 802.1X reauthentication period
          type: integer
        dot1x_reauth_period_origin:
          description: 802.1X reauthentication period origin
          format: int64
          type: integer
        dynamic_auth_enabled:
          description: Dynamic authentication enabled state
          type: boolean
        guest_nsi:
          description: Guest NSI
          type: integer
        guest_vlan:
          description: Guest Vlan
          type: integer
        guest_vlan_enabled:
          description: Guest Vlan enabled state
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        mac_auth_enabled:
          default: false
          description: Mac auth enabled state
          type: boolean
        mac_auth_max_macs:
          description: Mac authenticated max Macs
          type: integer
        mac_reauth_enabled:
          description: Mac reauthentication enabled state
          type: boolean
        mac_reauth_period:
          description: Mac reauthentication period
          type: integer
        max_request:
          description: Maximum requests
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        quiet_interval:
          default: 60
          description: Quiet interval time
          type: integer
        service_unavail_nsi:
          description: Service unavailable NSI
          type: integer
        service_unavail_vlan:
          description: Service unavailable Vlan
          type: integer
        service_unavail_vlan_enabled:
          description: Service unavailable Vlan enabled state
          type: boolean
        total_auth_max_macs:
          description: Total authenticated max Macs
          type: integer
        traffic_control:
          description: Traffic control settings
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        web_auth_enabled:
          description: Web auth enabled state
          type: boolean
      required:
        - asset_interface_id
        - auth_config_id
        - cep_lldp_enabled
        - dot1x_auth_type
        - dot1x_reauth_enabled
        - guest_vlan
        - interface_name
        - mac_auth_enabled
        - service_unavail_vlan
      type: object
    AssetAuthenticationPortSettingConfigGetRequest:
      description: AssetAuthenticationPortSettingConfigGetRequest represents the
        retrieval request for AssetAuthenticationPortSettingConfig.
      properties:
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        auth_config_id:
          description: >
            List of AuthConfigId. AuthConfigId represents Authentication config
            id
          items:
            type: string
          type: array
        auth_failure_vlan:
          description: >
            List of AuthFailureVlan. AuthFailureVlan represents Authentication
            failure Vlan
          items:
            type: integer
          type: array
        auth_failure_vlan_enabled:
          description: >
            List of AuthFailureVlanEnabled. AuthFailureVlanEnabled represents
            Authentication failure Vlan enabled state
          items:
            type: boolean
          type: array
        auth_mode:
          description: |
            List of AuthMode. AuthMode represents Authentication mode
          items:
            format: int64
            type: integer
          type: array
        auth_optional:
          description: |
            List of AuthOptional. AuthOptional represents Auth optional state
          items:
            type: boolean
          type: array
        cep_cdp_enabled:
          description: >
            List of CepCdpEnabled. CepCdpEnabled represents CEP CDP enabled state
          items:
            type: boolean
          type: array
        cep_lldp_enabled:
          description: >
            List of CepLldpEnabled. CepLldpEnabled represents CEP LLDP enabled
            state
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dot1x_auth_max_macs:
          description: >
            List of Dot1xAuthMaxMacs. Dot1xAuthMaxMacs represents 802.1X
            authenticated max Macs
          items:
            type: integer
          type: array
        dot1x_auth_type:
          description: >
            List of Dot1xAuthType. Dot1xAuthType represents 802.1X
            authentication type
          items:
            format: int64
            type: integer
          type: array
        dot1x_reauth_enabled:
          description: >
            List of Dot1xReauthEnabled. Dot1xReauthEnabled represents 802.1X
            reauthetication enabled state
          items:
            type: boolean
          type: array
        dot1x_reauth_origin:
          description: >
            List of Dot1xReauthOrigin. Dot1xReauthOrigin represents 802.1X
            reauthentication origin
          items:
            format: int64
            type: integer
          type: array
        dot1x_reauth_period:
          description: >
            List of Dot1xReauthPeriod. Dot1xReauthPeriod represents 802.1X
            reauthentication period
          items:
            type: integer
          type: array
        dot1x_reauth_period_origin:
          description: >
            List of Dot1xReauthPeriodOrigin. Dot1xReauthPeriodOrigin represents
            802.1X reauthentication period origin
          items:
            format: int64
            type: integer
          type: array
        dynamic_auth_enabled:
          description: >
            List of DynamicAuthEnabled. DynamicAuthEnabled represents Dynamic
            authentication enabled state
          items:
            type: boolean
          type: array
        guest_nsi:
          description: |
            List of GuestNsi. GuestNsi represents Guest NSI
          items:
            type: integer
          type: array
        guest_vlan:
          description: |
            List of GuestVlan. GuestVlan represents Guest Vlan
          items:
            type: integer
          type: array
        guest_vlan_enabled:
          description: >
            List of GuestVlanEnabled. GuestVlanEnabled represents Guest Vlan
            enabled state
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        mac_auth_enabled:
          description: >
            List of MacAuthEnabled. MacAuthEnabled represents Mac auth enabled
            state
          items:
            type: boolean
          type: array
        mac_auth_max_macs:
          description: >
            List of MacAuthMaxMacs. MacAuthMaxMacs represents Mac authenticated
            max Macs
          items:
            type: integer
          type: array
        mac_reauth_enabled:
          description: >
            List of MacReauthEnabled. MacReauthEnabled represents Mac
            reauthentication enabled state
          items:
            type: boolean
          type: array
        mac_reauth_period:
          description: >
            List of MacReauthPeriod. MacReauthPeriod represents Mac
            reauthentication period
          items:
            type: integer
          type: array
        max_request:
          description: |
            List of MaxRequest. MaxRequest represents Maximum requests
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        quiet_interval:
          description: |
            List of QuietInterval. QuietInterval represents Quiet interval time
          items:
            type: integer
          type: array
        service_unavail_nsi:
          description: >
            List of ServiceUnavailNsi. ServiceUnavailNsi represents Service
            unavailable NSI
          items:
            type: integer
          type: array
        service_unavail_vlan:
          description: >
            List of ServiceUnavailVlan. ServiceUnavailVlan represents Service
            unavailable Vlan
          items:
            type: integer
          type: array
        service_unavail_vlan_enabled:
          description: >
            List of ServiceUnavailVlanEnabled. ServiceUnavailVlanEnabled
            represents Service unavailable Vlan enabled state
          items:
            type: boolean
          type: array
        total_auth_max_macs:
          description: >
            List of TotalAuthMaxMacs. TotalAuthMaxMacs represents Total
            authenticated max Macs
          items:
            type: integer
          type: array
        traffic_control:
          description: >
            List of TrafficControl. TrafficControl represents Traffic control
            settings
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        web_auth_enabled:
          description: >
            List of WebAuthEnabled. WebAuthEnabled represents Web auth enabled
            state
          items:
            type: boolean
          type: array
      title: AssetAuthenticationPortSettingConfigGetRequest
      type: object
    AssetAuthenticationPortSettingConfigGetResponse:
      properties:
        AssetAuthenticationPortSettingConfig:
          items:
            $ref: "#/components/schemas/AssetAuthenticationPortSettingConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAuthenticationPortSettingConfigGetResponse
      type: object
    AssetAuthenticationState:
      description: >-
        AssetAuthenticationState - DB model for fabric EAPOL - This object
        represents the list the hosts authenticated on the port,

        indexed by MAC address
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        auth_status:
          description: >-
            The authentication status for the host. For Switch Engine the values
            are SUCCESS, FAIL, AUTHENTICATING, RADIUS_SERVER_NOT_REACHABLE and
            	DISCONNECTED. Fabric Engine supports SUCCESS, FAIL, PENDING, RADIUS_REQUEST_DROPPED, RADIUS_SERVER_NOT_REACHABLE, HELD,
            	AUTHENTICATING for NEAP hosts and SUCCESS, AUTHENTICATING, INITIALIZE,
            	DISCONNECTED, CONNECTING, ABORTING, HELD, FORCE_AUTH, FORCE_UNAUTH, UNAUTHENTICATED for EAP hosts.
          type: string
        auth_time:
          description: The date/time the host was authenticated on the port. This
            attribute applies to Switch Engine only.
          type: string
        auth_type:
          description: The type of authentication supported on the port. This attribute
            applies to Switch Engine only.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        idle_time:
          description: >-
            The idle timeout value for the session in seconds. The idle timer
            starts after the session timeout expires and if there
            	is no traffic from the host for the duration of the idle timeout period, the session will be deleted. This attribute applies to Switch Engine only.
          format: int64
          type: integer
        mac_address:
          description: The MAC address of the host.
          type: string
        oper_traffic_control:
          default: IN_OUT
          description: |-
            This option specifies the operational traffic blocking
            	directions for a port when there are no authenticated
            	clients for that port.This setting only applies to Fabric Engine.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        policy_profile:
          description: This attribute represents the policy profile index. This attribute
            applies to Switch Engine only.
          format: int64
          type: integer
        port:
          description: |-
            Name of the interface, slot and port are required

            	to ensure deterministic results, sub_port is optional.

            	If the optional slot number for the management port is

            	not provided on a stacked system, the slot will default

            	to the master slot.

            	[slot]:[port][:sub_port]

            	'1:1:1'     slot=1, port=1, sub_port=1

            	'12:101'    slot=12, port=101

            	'1:s1'      insight port

            	'ge2'       port=ge2

            	'wifi1.3'   radio=1, ssid=3

            	'mgmt'      oob mgmt port

            	'2:mgmt'    mgmt port with optional slot

            	'1:stack2'  stack port, slot=1, port=2
          type: string
        session_duration:
          description: This attribute tracks how many seconds the host has been
            authenticated for. This attribute applies to Switch Engine only.
          format: int64
          type: integer
        sw_uni_bindings:
          description: Bindings of Vlan and ISID for a host. This applies to Fabric Engine
            only.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: The identifier of the VLAN
          format: int64
          type: integer
      required:
        - asset_device_id
      type: object
    AssetAuthenticationStateGetRequest:
      description: AssetAuthenticationStateGetRequest represents the retrieval request
        for AssetAuthenticationState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        auth_status:
          description: >
            List of AuthStatus. AuthStatus represents The authentication status
            for the host. For Switch Engine the values are SUCCESS, FAIL,
            AUTHENTICATING, RADIUS_SERVER_NOT_REACHABLE and
            	DISCONNECTED. Fabric Engine supports SUCCESS, FAIL, PENDING, RADIUS_REQUEST_DROPPED, RADIUS_SERVER_NOT_REACHABLE, HELD,
            	AUTHENTICATING for NEAP hosts and SUCCESS, AUTHENTICATING, INITIALIZE,
            	DISCONNECTED, CONNECTING, ABORTING, HELD, FORCE_AUTH, FORCE_UNAUTH, UNAUTHENTICATED for EAP hosts.
          items:
            type: string
          type: array
        auth_time:
          description: >
            List of AuthTime. AuthTime represents The date/time the host was
            authenticated on the port. This attribute applies to Switch Engine
            only.
          items:
            type: string
          type: array
        auth_type:
          description: >
            List of AuthType. AuthType represents The type of authentication
            supported on the port. This attribute applies to Switch Engine only.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        idle_time:
          description: >
            List of IdleTime. IdleTime represents The idle timeout value for the
            session in seconds. The idle timer starts after the session timeout
            expires and if there
            	is no traffic from the host for the duration of the idle timeout period, the session will be deleted. This attribute applies to Switch Engine only.
          items:
            format: int64
            type: integer
          type: array
        mac_address:
          description: >
            List of MacAddress. MacAddress represents The MAC address of the
            host.
          items:
            type: string
          type: array
        oper_traffic_control:
          description: >
            List of OperTrafficControl. OperTrafficControl represents This
            option specifies the operational traffic blocking
            	directions for a port when there are no authenticated
            	clients for that port.This setting only applies to Fabric Engine.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        policy_profile:
          description: >
            List of PolicyProfile. PolicyProfile represents This attribute
            represents the policy profile index. This attribute applies to
            Switch Engine only.
          items:
            format: int64
            type: integer
          type: array
        port:
          description: >
            List of Port. Port represents Name of the interface, slot and port
            are required

            	to ensure deterministic results, sub_port is optional.

            	If the optional slot number for the management port is

            	not provided on a stacked system, the slot will default

            	to the master slot.

            	[slot]:[port][:sub_port]

            	'1:1:1'     slot=1, port=1, sub_port=1

            	'12:101'    slot=12, port=101

            	'1:s1'      insight port

            	'ge2'       port=ge2

            	'wifi1.3'   radio=1, ssid=3

            	'mgmt'      oob mgmt port

            	'2:mgmt'    mgmt port with optional slot

            	'1:stack2'  stack port, slot=1, port=2
          items:
            type: string
          type: array
        session_duration:
          description: >
            List of SessionDuration. SessionDuration represents This attribute
            tracks how many seconds the host has been authenticated for. This
            attribute applies to Switch Engine only.
          items:
            format: int64
            type: integer
          type: array
        sw_uni_bindings:
          description: >
            List of SwUniBindings. SwUniBindings represents  Bindings of Vlan
            and ISID for a host. This applies to Fabric Engine only.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents The identifier of the VLAN
          items:
            format: int64
            type: integer
          type: array
      title: AssetAuthenticationStateGetRequest
      type: object
    AssetAuthenticationStateGetResponse:
      properties:
        AssetAuthenticationState:
          items:
            $ref: "#/components/schemas/AssetAuthenticationState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAuthenticationStateGetResponse
      type: object
    AssetAutoSenseFabricAttach:
      description: AssetAutoSenseFabricAttach - Fabric attach authentication key for
        auto-sense ports.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        auth_is_encrypted:
          description: Specifies if the secret is encrypted (true) or clear text (false)
          type: boolean
        auth_value:
          description: >-
            The fabric attach authentication key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 32 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          type: string
        camera_dot1x_state:
          description: >-
            Specifies the fabric attach 802.1x status for camera I-SID used by
            auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          type: string
        camera_isid:
          description: |-
            Specifies the fabric attach camera I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        msg_auth_enabled:
          description: Enable fabric attach MsgAuth for auto-sense ports.
          type: boolean
        ovs_isid:
          description: |-
            Specifies the fabric attach OVS I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        ovs_status:
          description: >-
            Specifies the fabric attach 802.1x status for OVS I-SID used by
            auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        proxy_mgmt_cvid:
          description: >-
            Specifies the fabric attach proxy management Cvid used by auto-sense
            ports.
            	Cvid is set only for tagged traffic.
          format: int64
          type: integer
        proxy_mgmt_isid:
          description: >-
            Specifies the fabric attach proxy management I-SID used by
            auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        proxy_no_auth_isid:
          description: >-
            Specifies the fabric attach proxy no-auth I-SID used by auto-sense
            ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        wap_type1_isid:
          description: >-
            Specifies the fabric attach WAP type-1 I-SID used by auto-sense
            ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        wap_type1_status:
          description: >-
            Specifies the fabric attach 802.1x status for WAP type-1 I-SID used
            by auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          type: string
      required:
        - asset_device_id
      type: object
    AssetAutoSenseFabricAttachGetRequest:
      description: AssetAutoSenseFabricAttachGetRequest represents the retrieval
        request for AssetAutoSenseFabricAttach.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        auth_is_encrypted:
          description: >
            List of AuthIsEncrypted. AuthIsEncrypted represents Specifies if the
            secret is encrypted (true) or clear text (false)
          items:
            type: boolean
          type: array
        auth_value:
          description: >
            List of AuthValue. AuthValue represents  The fabric attach
            authentication key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 32 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          items:
            type: string
          type: array
        camera_dot1x_state:
          description: >
            List of CameraDot1xState. CameraDot1xState represents Specifies the
            fabric attach 802.1x status for camera I-SID used by auto-sense
            ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          items:
            type: string
          type: array
        camera_isid:
          description: >
            List of CameraIsid. CameraIsid represents Specifies the fabric
            attach camera I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        msg_auth_enabled:
          description: >
            List of MsgAuthEnabled. MsgAuthEnabled represents Enable fabric
            attach MsgAuth for auto-sense ports.
          items:
            type: boolean
          type: array
        ovs_isid:
          description: >
            List of OvsIsid. OvsIsid represents Specifies the fabric attach OVS
            I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        ovs_status:
          description: >
            List of OvsStatus. OvsStatus represents Specifies the fabric attach
            802.1x status for OVS I-SID used by auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        proxy_mgmt_cvid:
          description: >
            List of ProxyMgmtCvid. ProxyMgmtCvid represents Specifies the fabric
            attach proxy management Cvid used by auto-sense ports.
            	Cvid is set only for tagged traffic.
          items:
            format: int64
            type: integer
          type: array
        proxy_mgmt_isid:
          description: >
            List of ProxyMgmtIsid. ProxyMgmtIsid represents Specifies the fabric
            attach proxy management I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        proxy_no_auth_isid:
          description: >
            List of ProxyNoAuthIsid. ProxyNoAuthIsid represents Specifies the
            fabric attach proxy no-auth I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        wap_type1_isid:
          description: >
            List of WapType1Isid. WapType1Isid represents Specifies the fabric
            attach WAP type-1 I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        wap_type1_status:
          description: >
            List of WapType1Status. WapType1Status represents Specifies the
            fabric attach 802.1x status for WAP type-1 I-SID used by auto-sense
            ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          items:
            type: string
          type: array
      title: AssetAutoSenseFabricAttachGetRequest
      type: object
    AssetAutoSenseFabricAttachGetResponse:
      properties:
        AssetAutoSenseFabricAttach:
          items:
            $ref: "#/components/schemas/AssetAutoSenseFabricAttach"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAutoSenseFabricAttachGetResponse
      type: object
    AssetAutoSenseGlobal:
      description: AssetAutoSenseGlobal - This object represents the global auto-sense
        settings.
      properties:
        access_diff_serv_enabled:
          description: Set the diff-srv type as 'access' for auto-sense ports.
          type: boolean
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        data_isid:
          description: |-
            Specifies the data I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dhcp_detection_enabled:
          description: Enable DHCP detection for auto-sense ports.
          type: boolean
        dot1p_override_enabled:
          description: Enable 802.1p Override for auto-sense ports.
          type: boolean
        dot1x_eap_mac_max:
          description: Specifies maximum 802.1x clients allowed on the port at one time.
          format: int64
          type: integer
        dot1x_mac_max:
          description: Specifies maximum MAC clients supported on 802.1x enabled ports.
          format: int64
          type: integer
        dot1x_non_eap_mac_max:
          description: Specifies maximum non 802.1x clients allowed on the port at one time.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        onboarding_isid:
          description: |-
            Specifies the onboarding I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        voice_cvid:
          description: |-
            Specifies the voice Cvid associated with
            	the voice I-SID used by auto-sense ports. Cvid is
            	set only for tagged voice traffic. This needs to be
            	set along the I-SID.
          format: int64
          type: integer
        voice_dot1x_lldp_auth_enabled:
          description: Enable 802.1x LLDP authentication for voice auto-sense ports.
          type: boolean
        voice_isid:
          description: |-
            Specifies the voice I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        wait_interval:
          description: Specifies the wait interval in seconds for the 'WAIT' state of
            auto-sense's finite state machine.
          format: int64
          type: integer
      required:
        - asset_device_id
      type: object
    AssetAutoSenseGlobalGetRequest:
      description: AssetAutoSenseGlobalGetRequest represents the retrieval request for
        AssetAutoSenseGlobal.
      properties:
        access_diff_serv_enabled:
          description: >
            List of AccessDiffServEnabled. AccessDiffServEnabled represents Set
            the diff-srv type as 'access' for auto-sense ports.
          items:
            type: boolean
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        data_isid:
          description: >
            List of DataIsid. DataIsid represents Specifies the data I-SID used
            by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dhcp_detection_enabled:
          description: >
            List of DhcpDetectionEnabled. DhcpDetectionEnabled represents Enable
            DHCP detection for auto-sense ports.
          items:
            type: boolean
          type: array
        dot1p_override_enabled:
          description: >
            List of Dot1pOverrideEnabled. Dot1pOverrideEnabled represents Enable
            802.1p Override for auto-sense ports.
          items:
            type: boolean
          type: array
        dot1x_eap_mac_max:
          description: >
            List of Dot1xEapMacMax. Dot1xEapMacMax represents Specifies maximum
            802.1x clients allowed on the port at one time.
          items:
            format: int64
            type: integer
          type: array
        dot1x_mac_max:
          description: >
            List of Dot1xMacMax. Dot1xMacMax represents Specifies maximum MAC
            clients supported on 802.1x enabled ports.
          items:
            format: int64
            type: integer
          type: array
        dot1x_non_eap_mac_max:
          description: >
            List of Dot1xNonEapMacMax. Dot1xNonEapMacMax represents Specifies
            maximum non 802.1x clients allowed on the port at one time.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        onboarding_isid:
          description: >
            List of OnboardingIsid. OnboardingIsid represents Specifies the
            onboarding I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        voice_cvid:
          description: >
            List of VoiceCvid. VoiceCvid represents Specifies the voice Cvid
            associated with
            	the voice I-SID used by auto-sense ports. Cvid is
            	set only for tagged voice traffic. This needs to be
            	set along the I-SID.
          items:
            format: int64
            type: integer
          type: array
        voice_dot1x_lldp_auth_enabled:
          description: >
            List of VoiceDot1xLldpAuthEnabled. VoiceDot1xLldpAuthEnabled
            represents Enable 802.1x LLDP authentication for voice auto-sense
            ports.
          items:
            type: boolean
          type: array
        voice_isid:
          description: >
            List of VoiceIsid. VoiceIsid represents Specifies the voice I-SID
            used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        wait_interval:
          description: >
            List of WaitInterval. WaitInterval represents Specifies the wait
            interval in seconds for the 'WAIT' state of auto-sense's finite
            state machine.
          items:
            format: int64
            type: integer
          type: array
      title: AssetAutoSenseGlobalGetRequest
      type: object
    AssetAutoSenseGlobalGetResponse:
      properties:
        AssetAutoSenseGlobal:
          items:
            $ref: "#/components/schemas/AssetAutoSenseGlobal"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAutoSenseGlobalGetResponse
      type: object
    AssetAutoSenseIsis:
      description: AssetAutoSenseIsis - This object represents the global auto-sense
        ISIS settings.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        hello_auth_is_encrypted:
          description: Specifies if the secret is encrypted (true) or clear text (false)
          type: boolean
        hello_auth_value:
          description: >-
            The ISIS HelloAuth key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 16 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          type: string
        hello_key_id:
          description: |-
            Specifies ISIS HelloAuth key ID for auto-sense ports.
            	This needs to be set along helloAuthType and helloAuthKey.
          format: int64
          type: integer
        hello_type:
          description: Set ISIS HelloAuth type for auto-sense ports.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l1_metric:
          description: The ISIS l1 metric value applied to all auto-sense interfaces.
          format: int64
          type: integer
        l1_metric_auto_enabled:
          description: Specifies whether l1 metric is automatically calculated for all
            auto-sense interfaces.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetAutoSenseIsisGetRequest:
      description: AssetAutoSenseIsisGetRequest represents the retrieval request for
        AssetAutoSenseIsis.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        hello_auth_is_encrypted:
          description: >
            List of HelloAuthIsEncrypted. HelloAuthIsEncrypted represents
            Specifies if the secret is encrypted (true) or clear text (false)
          items:
            type: boolean
          type: array
        hello_auth_value:
          description: >
            List of HelloAuthValue. HelloAuthValue represents The ISIS HelloAuth
            key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 16 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          items:
            type: string
          type: array
        hello_key_id:
          description: >
            List of HelloKeyId. HelloKeyId represents Specifies ISIS HelloAuth
            key ID for auto-sense ports.
            	This needs to be set along helloAuthType and helloAuthKey.
          items:
            format: int64
            type: integer
          type: array
        hello_type:
          description: >
            List of HelloType. HelloType represents Set ISIS HelloAuth type for
            auto-sense ports.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        l1_metric:
          description: >
            List of L1Metric. L1Metric represents The ISIS l1 metric value
            applied to all auto-sense interfaces.
          items:
            format: int64
            type: integer
          type: array
        l1_metric_auto_enabled:
          description: >
            List of L1MetricAutoEnabled. L1MetricAutoEnabled represents
            Specifies whether l1 metric is automatically calculated for all
            auto-sense interfaces.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetAutoSenseIsisGetRequest
      type: object
    AssetAutoSenseIsisGetResponse:
      properties:
        AssetAutoSenseIsis:
          items:
            $ref: "#/components/schemas/AssetAutoSenseIsis"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAutoSenseIsisGetResponse
      type: object
    AssetAutoSensePort:
      description: AssetAutoSensePort - This object represents the auto-sense settings
        for a port.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enable:
          description: Status of auto-sense on a port.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        nsi:
          description: The network service (I-SID) id.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: State of the auto-sense on a port.
          type: string
        state:
          description: State of the auto-sense on a port.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        wait_interval:
          description: Timeout interval in seconds
          format: int64
          type: integer
      required:
        - asset_device_id
        - port_name
      type: object
    AssetAutoSensePortGetRequest:
      description: AssetAutoSensePortGetRequest represents the retrieval request for
        AssetAutoSensePort.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enable:
          description: |
            List of Enable. Enable represents Status of auto-sense on a port.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        nsi:
          description: |
            List of Nsi. Nsi represents The network service (I-SID) id.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents State of the auto-sense on a
            port.
          items:
            type: string
          type: array
        state:
          description: |
            List of State. State represents State of the auto-sense on a port.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        wait_interval:
          description: >
            List of WaitInterval. WaitInterval represents Timeout interval in
            seconds
          items:
            format: int64
            type: integer
          type: array
      title: AssetAutoSensePortGetRequest
      type: object
    AssetAutoSensePortGetResponse:
      properties:
        AssetAutoSensePort:
          items:
            $ref: "#/components/schemas/AssetAutoSensePort"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetAutoSensePortGetResponse
      type: object
    AssetCdpNeighbor:
      description: AssetCdpNeighbor - DB model for CDP neighbor state information
      properties:
        address:
          description: >-
            The string value used to identify the management address component
            associated with the
            	remote system.  It is reported in the Address TLV of the most recently received CDP
            	message and it is associated with addressType.
          type: string
        address_type:
          description: IANA defined list of address family numbers
          format: int64
          type: integer
        asset_device_id:
          description: UUID of the device
          type: string
        call_servers:
          description: Indicates remote call server IPs received on this port - Fabric
            Engine (VOSS) only
          type: string
        capabilities:
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id_type:
          description: >-
            The type of encoding used to identify the chassis associated with
            the local system as
            	described by IEEE 802.1AB-2005 9.5.2.2.
          format: int64
          type: integer
        file_servers:
          description: Indicates remote file server IPs received on this port - Fabric
            Engine (VOSS) only
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: >-
            An arbitrary local integer value used to identify a particular
            connection instance,
            	unique only for the indicated remote system
          type: integer
        last_change_time:
          description: >-
            Indicates the system time(in hundredths of a second) when this cache
            entry was last
            	changed - Fabric Engine (VOSS) only
          type: integer
        neighbor_device_id:
          description: Remote device ID as reported in CDP message
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        platform:
          description: >-
            The Device's Hardware Platform as reported in the most recent CDP
            message. The
            	zero-length string indicates that no Platform field (TLV) was reported in the most recent
            	CDP message.
          type: string
        port_id:
          description: >-
            The string value used to identify the remote port component
            associated with the remote
            	system.
          type: string
        port_name:
          description: Identifies the local port on which the remote system information is
            received.
          type: string
        sys_name:
          description: The string value used to identify the system name of the remote
            system.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        version:
          description: The Version string as reported in CDP message for system software.
          type: string
      required:
        - asset_device_id
        - index
        - port_name
      type: object
    AssetCdpNeighborGetRequest:
      description: AssetCdpNeighborGetRequest represents the retrieval request for
        AssetCdpNeighbor.
      properties:
        address:
          description: >
            List of Address. Address represents The string value used to
            identify the management address component associated with the
            	remote system.  It is reported in the Address TLV of the most recently received CDP
            	message and it is associated with addressType.
          items:
            type: string
          type: array
        address_type:
          description: >
            List of AddressType. AddressType represents IANA defined list of
            address family numbers
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        call_servers:
          description: >
            List of CallServers. CallServers represents Indicates remote call
            server IPs received on this port - Fabric Engine (VOSS) only
          items:
            type: string
          type: array
        capabilities:
          description: >
            List of Capabilities. Capabilities represents The CDP capabilities
            of the remote system. Describes the The type of neighboring network
            devices such as the router, switch, or repeater.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id_type:
          description: >
            List of DeviceIdType. DeviceIdType represents The type of encoding
            used to identify the chassis associated with the local system as
            	described by IEEE 802.1AB-2005 9.5.2.2.
          items:
            format: int64
            type: integer
          type: array
        file_servers:
          description: >
            List of FileServers. FileServers represents Indicates remote file
            server IPs received on this port - Fabric Engine (VOSS) only
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: >
            List of Index. Index represents An arbitrary local integer value
            used to identify a particular connection instance,
            	unique only for the indicated remote system
          items:
            type: integer
          type: array
        last_change_time:
          description: >
            List of LastChangeTime. LastChangeTime represents Indicates the
            system time(in hundredths of a second) when this cache entry was
            last
            	changed - Fabric Engine (VOSS) only
          items:
            type: integer
          type: array
        neighbor_device_id:
          description: >
            List of NeighborDeviceId. NeighborDeviceId represents Remote device
            ID as reported in CDP message
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        platform:
          description: >
            List of Platform. Platform represents The Device's Hardware Platform
            as reported in the most recent CDP message. The
            	zero-length string indicates that no Platform field (TLV) was reported in the most recent
            	CDP message.
          items:
            type: string
          type: array
        port_id:
          description: >
            List of PortId. PortId represents The string value used to identify
            the remote port component associated with the remote
            	system.
          items:
            type: string
          type: array
        port_name:
          description: >
            List of PortName. PortName represents Identifies the local port on
            which the remote system information is received.
          items:
            type: string
          type: array
        sys_name:
          description: >
            List of SysName. SysName represents The string value used to
            identify the system name of the remote system.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        version:
          description: >
            List of Version. Version represents The Version string as reported
            in CDP message for system software.
          items:
            type: string
          type: array
      title: AssetCdpNeighborGetRequest
      type: object
    AssetCdpNeighborGetResponse:
      properties:
        AssetCdpNeighbor:
          items:
            $ref: "#/components/schemas/AssetCdpNeighbor"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetCdpNeighborGetResponse
      type: object
    AssetCdpPortsConfig:
      description: AssetCdpPortsConfig - DB model for CDP ports configuration
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dot1p_voice_vlan_advertised:
          description: >-
            A value of true indicates the voice VLAN reply TLV is advertised as
            priority tagged -
            	Switch Engine (EXOS) only
          type: boolean
        enabled:
          description: The admin enabled setting that indicates if CDP is enabled on this
            port
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: The name of the interface
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        power_available_advertised:
          description: >-
            A value of true indicates the power available TLV is advertised -
            Switch Engine (EXOS)
            	only
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        voice_vlan_advertised:
          description: >-
            A value of true indicates the voice VLAN reply TLV is advertised -
            Switch Engine (EXOS)
            	only. If voiceVlanId is provided, it is included in the TLV
          type: boolean
        voice_vlan_id:
          description: >-
            VLAN to be included when the voice VLAN reply TLV is advertised -
            Switch Engine (EXOS)
            	only. Use a value of 0 to unconfigure the VLAN
          type: integer
      required:
        - asset_device_id
        - asset_interface_id
        - dot1p_voice_vlan_advertised
        - enabled
        - interface_name
        - power_available_advertised
        - voice_vlan_advertised
        - voice_vlan_id
      type: object
    AssetCdpPortsConfigGetRequest:
      description: AssetCdpPortsConfigGetRequest represents the retrieval request for
        AssetCdpPortsConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dot1p_voice_vlan_advertised:
          description: >
            List of Dot1pVoiceVlanAdvertised. Dot1pVoiceVlanAdvertised
            represents A value of true indicates the voice VLAN reply TLV is
            advertised as priority tagged -
            	Switch Engine (EXOS) only
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The admin enabled setting that
            indicates if CDP is enabled on this port
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents The name of the
            interface
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        power_available_advertised:
          description: >
            List of PowerAvailableAdvertised. PowerAvailableAdvertised
            represents A value of true indicates the power available TLV is
            advertised - Switch Engine (EXOS)
            	only
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        voice_vlan_advertised:
          description: >
            List of VoiceVlanAdvertised. VoiceVlanAdvertised represents A value
            of true indicates the voice VLAN reply TLV is advertised - Switch
            Engine (EXOS)
            	only. If voiceVlanId is provided, it is included in the TLV
          items:
            type: boolean
          type: array
        voice_vlan_id:
          description: >
            List of VoiceVlanId. VoiceVlanId represents VLAN to be included when
            the voice VLAN reply TLV is advertised - Switch Engine (EXOS)
            	only. Use a value of 0 to unconfigure the VLAN
          items:
            type: integer
          type: array
      title: AssetCdpPortsConfigGetRequest
      type: object
    AssetCdpPortsConfigGetResponse:
      properties:
        AssetCdpPortsConfig:
          items:
            $ref: "#/components/schemas/AssetCdpPortsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetCdpPortsConfigGetResponse
      type: object
    AssetDevice:
      description: AssetDevice - The object that represents settings for a device
        AssetDeviceConnectionDetail,AssetLocation,AssetSlot can be created while
        creating AssetDevice.
      properties:
        admin_status:
          description: Device administrative status. UP, DOWN, TESTING and RESET are
            applicable to Fabric Engine, ENABLED is applicable to Switch Engine.
          format: int64
          type: integer
        asset_device_connection_detail:
          $ref: "#/components/schemas/AssetDeviceConnectionDetail"
        asset_location:
          $ref: "#/components/schemas/AssetLocation"
        asset_slot:
          items:
            $ref: "#/components/schemas/AssetSlot"
          nullable: true
          type: array
        base_mac_address:
          description: Mac Address
          type: string
        brand_name:
          description: Device brand name.
          type: string
        chassis_id:
          description: >-
            The string value used to identify the chassis component
            	associated with the local system as described by IEEE 802.1AB-2005 9.5.2.3.
          type: string
        chassis_id_subtype:
          description: The type of encoding used to identify the chassis associated with
            the local system as described by IEEE 802.1AB-2005 9.5.2.2.
          format: int64
          type: integer
        collection_done_timestamp:
          description: Timestamp of initial data collection completion.
          format: date-time
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        description:
          description: Device type name (must be unique within a given profile).
          type: string
        firmware_version:
          description: |-
            The vendor-specific firmware revision
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          type: string
        hardware_rev:
          description: Device hardware revision level.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        initial_collection_done:
          description: Flag set after initial collection from the device is done.
          type: boolean
        ip_address:
          description: IP Address (Ipv4 or Ipv6)
          type: string
        is_digital_twin:
          description: Digital Twin state.
          type: boolean
        is_power_enabled:
          description: Administratively turn on or off power on this slot. only applicable
            on Standby CP & IO and SF Card.
          type: boolean
        is_stack:
          default: false
          description: Stack state. This attribute is Switch Engine (EXOS) applicable only.
          type: boolean
        mac_address_capacity:
          description: MAC address capacity.
          format: int64
          type: integer
        model_name:
          description: Device model name.
          type: string
        name:
          description: Device type name (must be unique within a given profile).
          type: string
        node_state:
          description: Device brand name.
          format: int64
          type: integer
        num_ports:
          description: The total number of ports currently installed in the chassis.
          format: int64
          minimum: 0
          type: integer
        num_slots:
          default: 1
          description: The number of slots in the chassis available for plug-in cards.
          format: int64
          minimum: 0
          type: integer
        operation_status:
          description: Device brand name.
          format: int64
          type: integer
        os_type:
          description: Operating System Type
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        part_number:
          description: Device part number.
          type: string
        registered_with_scheduler:
          description: Registered With Scheduler
          type: boolean
        serial_number:
          description: Device unique serial number.
          type: string
        sys_build_time:
          description: System build time.
          type: string
        sys_up_time:
          description: |-
            The time (in hundredths of a second) since the network
            	management portion of the system was last re-initialized.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - chassis_id
        - serial_number
      type: object
    AssetDeviceConnectionDetail:
      description: AssetDeviceConnectionDetail - DB Model for settings for Asset
        device connection details AssetDeviceConnectionDetailStackInfo can be
        created while creating AssetDeviceConnectionDetail.
      properties:
        asset_device_connection_detail_stack_info:
          $ref: "#/components/schemas/AssetDeviceConnectionDetailStackInfo"
        asset_device_id:
          description: Device Id
          type: string
        connection_create_time:
          description: Connection Create Time
          format: date-time
          type: string
        connection_device_id:
          description: Connection device id
          format: int64
          type: integer
        connection_id:
          description: Connection Id
          format: int64
          minimum: 0
          type: integer
        connection_time_stamp:
          description: Connection Timestamp
          format: date-time
          type: string
        connection_update_time:
          description: Connection Update Time
          format: date-time
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_make:
          description: Device Make
          type: string
        device_model:
          description: Device Model
          type: string
        host_name:
          description: Name of the host
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inlets_server_url:
          description: Inlets Server Url
          type: string
        ip_address:
          description: IP Address (Ipv4 or Ipv6)
          type: string
        is_stack_device:
          default: false
          description: Is Stack device.
          type: boolean
        license_level:
          description: Device license level
          type: string
        license_tier:
          description: Device license tier level
          type: string
        mac_address:
          description: Mac address of the device
          type: string
        max_ssh_connections:
          description: Max SSH Connection
          format: int64
          minimum: 0
          type: integer
        nos_auth_pwd:
          description: Nos Auth Password
          type: string
        nos_auth_user_name:
          description: Nos Auth UserName
          type: string
        os_version:
          description: Operating system version of the device
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        routing_label:
          description: Routing Label
          type: string
        routing_port:
          description: Routing Port
          type: string
        routing_server:
          description: Routing Server
          type: string
        serial_number:
          description: Serial number
          type: string
        snmp_community_string:
          description: Snmp Community String
          type: string
        snmp_connection_header:
          description: SNMP Connection Header
          type: string
        ssh_connection_header:
          description: SSH Connection Header
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetDeviceConnectionDetailGetRequest:
      description: AssetDeviceConnectionDetailGetRequest represents the retrieval
        request for AssetDeviceConnectionDetail.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        connection_create_time:
          description: >
            List of ConnectionCreateTime. ConnectionCreateTime represents
            Connection Create Time
          items:
            format: date-time
            type: string
          type: array
        connection_device_id:
          description: >
            List of ConnectionDeviceId. ConnectionDeviceId represents Connection
            device id
          items:
            format: int64
            type: integer
          type: array
        connection_id:
          description: |
            List of ConnectionId. ConnectionId represents Connection Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        connection_time_stamp:
          description: >
            List of ConnectionTimeStamp. ConnectionTimeStamp represents
            Connection Timestamp
          items:
            format: date-time
            type: string
          type: array
        connection_update_time:
          description: >
            List of ConnectionUpdateTime. ConnectionUpdateTime represents
            Connection Update Time
          items:
            format: date-time
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_make:
          description: |
            List of DeviceMake. DeviceMake represents Device Make
          items:
            type: string
          type: array
        device_model:
          description: |
            List of DeviceModel. DeviceModel represents Device Model
          items:
            type: string
          type: array
        host_name:
          description: |
            List of HostName. HostName represents Name of the host
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        inlets_server_url:
          description: >
            List of InletsServerUrl. InletsServerUrl represents Inlets Server Url
          items:
            type: string
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents IP Address (Ipv4 or Ipv6)
          items:
            type: string
          type: array
        is_stack_device:
          description: |
            List of IsStackDevice. IsStackDevice represents Is Stack device.
          items:
            type: boolean
          type: array
        license_level:
          description: |
            List of LicenseLevel. LicenseLevel represents Device license level
          items:
            type: string
          type: array
        license_tier:
          description: >
            List of LicenseTier. LicenseTier represents Device license tier level
          items:
            type: string
          type: array
        mac_address:
          description: |
            List of MacAddress. MacAddress represents Mac address of the device
          items:
            type: string
          type: array
        max_ssh_connections:
          description: >
            List of MaxSshConnections. MaxSshConnections represents Max SSH
            Connection
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        nos_auth_pwd:
          description: |
            List of NosAuthPwd. NosAuthPwd represents Nos Auth Password
          items:
            type: string
          type: array
        nos_auth_user_name:
          description: >
            List of NosAuthUserName. NosAuthUserName represents Nos Auth UserName
          items:
            type: string
          type: array
        os_version:
          description: >
            List of OsVersion. OsVersion represents Operating system version of
            the device
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        routing_label:
          description: |
            List of RoutingLabel. RoutingLabel represents Routing Label
          items:
            type: string
          type: array
        routing_port:
          description: |
            List of RoutingPort. RoutingPort represents Routing Port
          items:
            type: string
          type: array
        routing_server:
          description: |
            List of RoutingServer. RoutingServer represents Routing Server
          items:
            type: string
          type: array
        serial_number:
          description: |
            List of SerialNumber. SerialNumber represents Serial number
          items:
            type: string
          type: array
        snmp_community_string:
          description: >
            List of SnmpCommunityString. SnmpCommunityString represents Snmp
            Community String
          items:
            type: string
          type: array
        snmp_connection_header:
          description: >
            List of SnmpConnectionHeader. SnmpConnectionHeader represents SNMP
            Connection Header
          items:
            type: string
          type: array
        ssh_connection_header:
          description: >
            List of SshConnectionHeader. SshConnectionHeader represents SSH
            Connection Header
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDeviceConnectionDetailGetRequest
      type: object
    AssetDeviceConnectionDetailGetResponse:
      properties:
        AssetDeviceConnectionDetail:
          items:
            $ref: "#/components/schemas/AssetDeviceConnectionDetail"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDeviceConnectionDetailGetResponse
      type: object
    AssetDeviceConnectionDetailPhysicalStack:
      description: AssetDeviceConnectionDetailPhysicalStack - The object that
        represents the connection details of the device on a physical stack.
      nullable: true
      properties:
        asset_stack_info_id:
          description: Asset Stack Info Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mac_address:
          description: Mac Address.
          type: string
        node_type:
          description: Node type.
          format: int64
          type: integer
        onboarded_to_cloud:
          description: Indicates if the stack is onboarded to cloud
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        serial_number:
          description: Device unique serial number.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_stack_info_id
      type: object
    AssetDeviceConnectionDetailPhysicalStackGetRequest:
      description: AssetDeviceConnectionDetailPhysicalStackGetRequest represents the
        retrieval request for AssetDeviceConnectionDetailPhysicalStack.
      properties:
        asset_stack_info_id:
          description: >
            List of AssetStackInfoId. AssetStackInfoId represents Asset Stack
            Info Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mac_address:
          description: |
            List of MacAddress. MacAddress represents Mac Address.
          items:
            type: string
          type: array
        node_type:
          description: |
            List of NodeType. NodeType represents Node type.
          items:
            format: int64
            type: integer
          type: array
        onboarded_to_cloud:
          description: >
            List of OnboardedToCloud. OnboardedToCloud represents Indicates if
            the stack is onboarded to cloud
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDeviceConnectionDetailPhysicalStackGetRequest
      type: object
    AssetDeviceConnectionDetailPhysicalStackGetResponse:
      properties:
        AssetDeviceConnectionDetailPhysicalStack:
          items:
            $ref: "#/components/schemas/AssetDeviceConnectionDetailPhysicalStack"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDeviceConnectionDetailPhysicalStackGetResponse
      type: object
    AssetDeviceConnectionDetailStackInfo:
      description: >-
        AssetDeviceConnectionDetailStackInfo - The object that represents
        settings for a Device connection details on the Stack.

        This consists of details of multiple physical stacks.
        AssetDeviceConnectionDetailPhysicalStack can be created while creating
        AssetDeviceConnectionDetailStackInfo.
      nullable: true
      properties:
        asset_device_connection_detail_id:
          description: Asset Device Connection Detail Id
          type: string
        asset_device_connection_detail_physical_stack:
          items:
            $ref: "#/components/schemas/AssetDeviceConnectionDetailPhysicalStack"
          nullable: true
          type: array
        chassis_id:
          description: Stacked device chassis ID.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        member_serial_numbers:
          description: The serial numbers of all members
          items:
            type: string
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        serial_numbers_onboarded_to_cloud:
          description: The serial numbers onboarded to cloud.
          items:
            type: string
          nullable: true
          type: array
        stack_id:
          description: Stack Id
          format: int64
          type: integer
        stack_message_type:
          description: Stack Message Type.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_connection_detail_id
      type: object
    AssetDeviceConnectionDetailStackInfoGetRequest:
      description: AssetDeviceConnectionDetailStackInfoGetRequest represents the
        retrieval request for AssetDeviceConnectionDetailStackInfo.
      properties:
        asset_device_connection_detail_id:
          description: >
            List of AssetDeviceConnectionDetailId. AssetDeviceConnectionDetailId
            represents Asset Device Connection Detail Id
          items:
            type: string
          type: array
        chassis_id:
          description: |
            List of ChassisId. ChassisId represents Stacked device chassis ID.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        member_serial_numbers:
          description: >
            List of MemberSerialNumbers. MemberSerialNumbers represents The
            serial numbers of all members
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        serial_numbers_onboarded_to_cloud:
          description: >
            List of SerialNumbersOnboardedToCloud. SerialNumbersOnboardedToCloud
            represents The serial numbers onboarded to cloud.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        stack_id:
          description: |
            List of StackId. StackId represents Stack Id
          items:
            format: int64
            type: integer
          type: array
        stack_message_type:
          description: >
            List of StackMessageType. StackMessageType represents Stack Message
            Type.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDeviceConnectionDetailStackInfoGetRequest
      type: object
    AssetDeviceConnectionDetailStackInfoGetResponse:
      properties:
        AssetDeviceConnectionDetailStackInfo:
          items:
            $ref: "#/components/schemas/AssetDeviceConnectionDetailStackInfo"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDeviceConnectionDetailStackInfoGetResponse
      type: object
    AssetDeviceGetRequest:
      description: AssetDeviceGetRequest represents the retrieval request for AssetDevice.
      properties:
        admin_status:
          description: >
            List of AdminStatus. AdminStatus represents Device administrative
            status. UP, DOWN, TESTING and RESET are applicable to Fabric Engine,
            ENABLED is applicable to Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        base_mac_address:
          description: |
            List of BaseMacAddress. BaseMacAddress represents Mac Address
          items:
            type: string
          type: array
        brand_name:
          description: |
            List of BrandName. BrandName represents Device brand name.
          items:
            type: string
          type: array
        chassis_id:
          description: >
            List of ChassisId. ChassisId represents The string value used to
            identify the chassis component
            	associated with the local system as described by IEEE 802.1AB-2005 9.5.2.3.
          items:
            type: string
          type: array
        chassis_id_subtype:
          description: >
            List of ChassisIdSubtype. ChassisIdSubtype represents The type of
            encoding used to identify the chassis associated with the local
            system as described by IEEE 802.1AB-2005 9.5.2.2.
          items:
            format: int64
            type: integer
          type: array
        collection_done_timestamp:
          description: >
            List of CollectionDoneTimestamp. CollectionDoneTimestamp represents
            Timestamp of initial data collection completion.
          items:
            format: date-time
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        description:
          description: >
            List of Description. Description represents Device type name (must
            be unique within a given profile).
          items:
            type: string
          type: array
        firmware_version:
          description: >
            List of FirmwareVersion. FirmwareVersion represents The
            vendor-specific firmware revision
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          items:
            type: string
          type: array
        hardware_rev:
          description: >
            List of HardwareRev. HardwareRev represents Device hardware revision
            level.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        initial_collection_done:
          description: >
            List of InitialCollectionDone. InitialCollectionDone represents Flag
            set after initial collection from the device is done.
          items:
            type: boolean
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents IP Address (Ipv4 or Ipv6)
          items:
            type: string
          type: array
        is_digital_twin:
          description: |
            List of IsDigitalTwin. IsDigitalTwin represents Digital Twin state.
          items:
            type: boolean
          type: array
        is_power_enabled:
          description: >
            List of IsPowerEnabled. IsPowerEnabled represents Administratively
            turn on or off power on this slot. only applicable on Standby CP &
            IO and SF Card.
          items:
            type: boolean
          type: array
        is_stack:
          description: >
            List of IsStack. IsStack represents Stack state. This attribute is
            Switch Engine (EXOS) applicable only.
          items:
            type: boolean
          type: array
        mac_address_capacity:
          description: >
            List of MacAddressCapacity. MacAddressCapacity represents MAC
            address capacity.
          items:
            format: int64
            type: integer
          type: array
        model_name:
          description: |
            List of ModelName. ModelName represents Device model name.
          items:
            type: string
          type: array
        name:
          description: >
            List of Name. Name represents Device type name (must be unique
            within a given profile).
          items:
            type: string
          type: array
        node_state:
          description: |
            List of NodeState. NodeState represents Device brand name.
          items:
            format: int64
            type: integer
          type: array
        num_ports:
          description: >
            List of NumPorts. NumPorts represents The total number of ports
            currently installed in the chassis.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        num_slots:
          description: >
            List of NumSlots. NumSlots represents The number of slots in the
            chassis available for plug-in cards.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        operation_status:
          description: >
            List of OperationStatus. OperationStatus represents Device brand
            name.
          items:
            format: int64
            type: integer
          type: array
        os_type:
          description: |
            List of OsType. OsType represents Operating System Type
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        part_number:
          description: |
            List of PartNumber. PartNumber represents Device part number.
          items:
            type: string
          type: array
        registered_with_scheduler:
          description: >
            List of RegisteredWithScheduler. RegisteredWithScheduler represents
            Registered With Scheduler
          items:
            type: boolean
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        sys_build_time:
          description: |
            List of SysBuildTime. SysBuildTime represents System build time.
          items:
            type: string
          type: array
        sys_up_time:
          description: >
            List of SysUpTime. SysUpTime represents The time (in hundredths of a
            second) since the network
            	management portion of the system was last re-initialized.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDeviceGetRequest
      type: object
    AssetDeviceGetResponse:
      properties:
        AssetDevice:
          items:
            $ref: "#/components/schemas/AssetDevice"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDeviceGetResponse
      type: object
    AssetDhcpRelayVlanConfig:
      description: AssetDhcpRelayVlanConfig - DB Model for Vlan level DHCP relay
        configuration Servers can be created while creating
        AssetDhcpRelayVlanConfig.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        dhcp_smart_relay:
          default: 1
          description: >-
            The setting represents the setting of DHCP smart relay on a per VR
            level.
            	This setting extends the DHCP relay configuration and is supported at both
            	VLAN and VR levels by Switch Engine.
          format: int64
          type: integer
        enable:
          description: |-
            DHCP Relay state on VLAN interface. This setting shows the status
            	of dhcp-relay on a VLAN interface.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        servers:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVlanServerConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config_id:
          description: VLAN Config Id
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - enable
        - vlan_config_id
        - vlan_number
      type: object
    AssetDhcpRelayVlanConfigGetRequest:
      description: AssetDhcpRelayVlanConfigGetRequest represents the retrieval request
        for AssetDhcpRelayVlanConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        dhcp_smart_relay:
          description: >
            List of DhcpSmartRelay. DhcpSmartRelay represents The setting
            represents the setting of DHCP smart relay on a per VR level.
            	This setting extends the DHCP relay configuration and is supported at both
            	VLAN and VR levels by Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        enable:
          description: >
            List of Enable. Enable represents DHCP Relay state on VLAN
            interface. This setting shows the status
            	of dhcp-relay on a VLAN interface.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_config_id:
          description: |
            List of VlanConfigId. VlanConfigId represents VLAN Config Id
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetDhcpRelayVlanConfigGetRequest
      type: object
    AssetDhcpRelayVlanConfigGetResponse:
      properties:
        AssetDhcpRelayVlanConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpRelayVlanConfigGetResponse
      type: object
    AssetDhcpRelayVlanServerConfig:
      description: AssetDhcpRelayVlanServerConfig - DB Model for Vlan level dhcp relay
        servers configuration (Switch engine specific)
      properties:
        asset_dhcp_relay_vlan_config_id:
          description: Dhcp Relay Vlan Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        enable:
          default: false
          description: Server state. This is Fabric Engine specific within the context of
            vrf settings.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_address:
          description: >-
            Ipv4 address of the DHCP Relay server. On Fabric Engine only
            available within
            	the context of vrf settings.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        src_ip_address:
          description: >-
            Primary Ipv4 address of the VLAN interface. This is Fabric Engine
            specific
            	within the context of vrf settings.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_dhcp_relay_vlan_config_id
        - device_id
        - ip_address
        - src_ip_address
        - vlan_number
      type: object
    AssetDhcpRelayVlanServerConfigGetRequest:
      description: AssetDhcpRelayVlanServerConfigGetRequest represents the retrieval
        request for AssetDhcpRelayVlanServerConfig.
      properties:
        asset_dhcp_relay_vlan_config_id:
          description: >
            List of AssetDhcpRelayVlanConfigId. AssetDhcpRelayVlanConfigId
            represents Dhcp Relay Vlan Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        enable:
          description: >
            List of Enable. Enable represents Server state. This is Fabric
            Engine specific within the context of vrf settings.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_address:
          description: >
            List of IpAddress. IpAddress represents Ipv4 address of the DHCP
            Relay server. On Fabric Engine only available within
            	the context of vrf settings.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        src_ip_address:
          description: >
            List of SrcIpAddress. SrcIpAddress represents Primary Ipv4 address
            of the VLAN interface. This is Fabric Engine specific
            	within the context of vrf settings.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetDhcpRelayVlanServerConfigGetRequest
      type: object
    AssetDhcpRelayVlanServerConfigGetResponse:
      properties:
        AssetDhcpRelayVlanServerConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVlanServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpRelayVlanServerConfigGetResponse
      type: object
    AssetDhcpRelayVrfConfig:
      description: AssetDhcpRelayVrfConfig - DB Model for Vrf level DHCP relay
        configuration list Servers can be created while creating
        AssetDhcpRelayVrfConfig.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        dhcp_smart_relay:
          description: >-
            The setting represents the setting of DHCP smart relay on a per VR
            level.
            	This setting extends the DHCP relay configuration and is supported at both
            	VLAN and VR levels by Switch Engine.
          format: int64
          type: integer
        enable:
          description: State of DHCP Relay on a VRF. This is Switch Engine specific.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        servers:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVrfServerConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_config_id:
          description: VRF Config Id
          type: string
        vrf_name:
          description: VRF name
          type: string
      required:
        - device_id
        - dhcp_smart_relay
        - enable
        - vrf_config_id
        - vrf_name
      type: object
    AssetDhcpRelayVrfConfigGetRequest:
      description: AssetDhcpRelayVrfConfigGetRequest represents the retrieval request
        for AssetDhcpRelayVrfConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        dhcp_smart_relay:
          description: >
            List of DhcpSmartRelay. DhcpSmartRelay represents The setting
            represents the setting of DHCP smart relay on a per VR level.
            	This setting extends the DHCP relay configuration and is supported at both
            	VLAN and VR levels by Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        enable:
          description: >
            List of Enable. Enable represents State of DHCP Relay on a VRF. This
            is Switch Engine specific.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_config_id:
          description: |
            List of VrfConfigId. VrfConfigId represents VRF Config Id
          items:
            type: string
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name
          items:
            type: string
          type: array
      title: AssetDhcpRelayVrfConfigGetRequest
      type: object
    AssetDhcpRelayVrfConfigGetResponse:
      properties:
        AssetDhcpRelayVrfConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVrfConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpRelayVrfConfigGetResponse
      type: object
    AssetDhcpRelayVrfServerConfig:
      description: AssetDhcpRelayVrfServerConfig - DB Model for Vrf level DHCP relay
        servers configuration list
      properties:
        asset_dhcp_relay_vrf_config_id:
          description: Dhcp relay VRF Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        enable:
          description: Server state. This is Fabric Engine specific within the context of
            vrf settings.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_address:
          description: >-
            Ipv4 address of the DHCP Relay server. On Fabric Engine only
            available within
            	the context of vrf settings.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        src_ip_address:
          description: >-
            Primary Ipv4 address of the VLAN interface. This is Fabric Engine
            specific
            	within the context of vrf settings.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_name:
          description: VRF name
          type: string
      required:
        - asset_dhcp_relay_vrf_config_id
        - device_id
        - enable
        - ip_address
        - src_ip_address
        - vrf_name
      type: object
    AssetDhcpRelayVrfServerConfigGetRequest:
      description: AssetDhcpRelayVrfServerConfigGetRequest represents the retrieval
        request for AssetDhcpRelayVrfServerConfig.
      properties:
        asset_dhcp_relay_vrf_config_id:
          description: >
            List of AssetDhcpRelayVrfConfigId. AssetDhcpRelayVrfConfigId
            represents Dhcp relay VRF Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        enable:
          description: >
            List of Enable. Enable represents Server state. This is Fabric
            Engine specific within the context of vrf settings.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_address:
          description: >
            List of IpAddress. IpAddress represents Ipv4 address of the DHCP
            Relay server. On Fabric Engine only available within
            	the context of vrf settings.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        src_ip_address:
          description: >
            List of SrcIpAddress. SrcIpAddress represents Primary Ipv4 address
            of the VLAN interface. This is Fabric Engine specific
            	within the context of vrf settings.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name
          items:
            type: string
          type: array
      title: AssetDhcpRelayVrfServerConfigGetRequest
      type: object
    AssetDhcpRelayVrfServerConfigGetResponse:
      properties:
        AssetDhcpRelayVrfServerConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVrfServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpRelayVrfServerConfigGetResponse
      type: object
    AssetDhcpRelayVrfState:
      description: AssetDhcpRelayVrfState - DB Model for Vrf level DHCP relay state
        information
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        num_requests:
          description: The total number of DHCP requests received.
          format: int64
          minimum: 0
          type: integer
        num_responses:
          description: The total number of DHCP responses received.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_name:
          description: VRF name
          type: string
        vrf_state_id:
          description: VRF State Id
          type: string
      required:
        - device_id
        - num_requests
        - num_responses
        - vrf_name
        - vrf_state_id
      type: object
    AssetDhcpRelayVrfStateGetRequest:
      description: AssetDhcpRelayVrfStateGetRequest represents the retrieval request
        for AssetDhcpRelayVrfState.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        num_requests:
          description: >
            List of NumRequests. NumRequests represents The total number of DHCP
            requests received.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        num_responses:
          description: >
            List of NumResponses. NumResponses represents The total number of
            DHCP responses received.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name
          items:
            type: string
          type: array
        vrf_state_id:
          description: |
            List of VrfStateId. VrfStateId represents VRF State Id
          items:
            type: string
          type: array
      title: AssetDhcpRelayVrfStateGetRequest
      type: object
    AssetDhcpRelayVrfStateGetResponse:
      properties:
        AssetDhcpRelayVrfState:
          items:
            $ref: "#/components/schemas/AssetDhcpRelayVrfState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpRelayVrfStateGetResponse
      type: object
    AssetDhcpSnoopConfig:
      description: AssetDhcpSnoopConfig - DB Model for DHCP snooping configuration
        list AllDynamicVlansConfig,VlanConfig can be created while creating
        AssetDhcpSnoopConfig.
      properties:
        all_dynamic_vlans_config:
          $ref: "#/components/schemas/AssetDhcpSnoopDynamicVlansConfig"
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        global_enable:
          description: DHCP Snooping Global Enable config attribute for Fabric Engine
            devices only. A value of true indicates the DHCP Snooping feature is
            globally enabled on the device.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        trusted_ports:
          description: DHCP Snoop trusted port list
          items:
            type: string
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopVlansConfig"
          nullable: true
          type: array
      required:
        - asset_device_id
      type: object
    AssetDhcpSnoopConfigGetRequest:
      description: AssetDhcpSnoopConfigGetRequest represents the retrieval request for
        AssetDhcpSnoopConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        global_enable:
          description: >
            List of GlobalEnable. GlobalEnable represents DHCP Snooping Global
            Enable config attribute for Fabric Engine devices only. A value of
            true indicates the DHCP Snooping feature is globally enabled on the
            device.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        trusted_ports:
          description: >
            List of TrustedPorts. TrustedPorts represents DHCP Snoop trusted
            port list
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDhcpSnoopConfigGetRequest
      type: object
    AssetDhcpSnoopConfigGetResponse:
      properties:
        AssetDhcpSnoopConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpSnoopConfigGetResponse
      type: object
    AssetDhcpSnoopDynamicVlansConfig:
      description: AssetDhcpSnoopDynamicVlansConfig - DB model for DHCP snooping
        dynamic vlans configurations PortSettings can be created while creating
        AssetDhcpSnoopDynamicVlansConfig.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dhcp_snoop_config_id:
          description: Dhcp Snoop Config Id
          type: string
        enabled:
          description: DHCP snooping state. A value of true indicates the DHCP snoop is
            enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_settings:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopPortsConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - dhcp_snoop_config_id
        - enabled
      type: object
    AssetDhcpSnoopDynamicVlansConfigGetRequest:
      description: AssetDhcpSnoopDynamicVlansConfigGetRequest represents the retrieval
        request for AssetDhcpSnoopDynamicVlansConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dhcp_snoop_config_id:
          description: >
            List of DhcpSnoopConfigId. DhcpSnoopConfigId represents Dhcp Snoop
            Config Id
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents DHCP snooping state. A value of
            true indicates the DHCP snoop is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDhcpSnoopDynamicVlansConfigGetRequest
      type: object
    AssetDhcpSnoopDynamicVlansConfigGetResponse:
      properties:
        AssetDhcpSnoopDynamicVlansConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopDynamicVlansConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpSnoopDynamicVlansConfigGetResponse
      type: object
    AssetDhcpSnoopPortsConfig:
      description: AssetDhcpSnoopPortsConfig - DB model for DHCP snooping port
        configurations
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dhcp_snoop_dynamic_vlan_config_id:
          description: Dhcp snoop dynamic Vlan Config Id
          type: string
        dhcp_snoop_vlan_config_id:
          description: Dhcp Snoop Vlan Config Id
          type: string
        duration:
          description: >-
            Duration of violationAction in seconds. A duration of 0 indicates
            permanent.
            	This attribute applies only to BLOCK_MAC and BLOCK_PORT violationActions.
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: Port name of the port whose DHCP Snoop settings are stored.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        violation_action:
          description: Available DHCP Snoop violation action values.
          format: int64
          type: integer
      required:
        - duration
        - violation_action
      type: object
    AssetDhcpSnoopPortsConfigGetRequest:
      description: AssetDhcpSnoopPortsConfigGetRequest represents the retrieval
        request for AssetDhcpSnoopPortsConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dhcp_snoop_dynamic_vlan_config_id:
          description: >
            List of DhcpSnoopDynamicVlanConfigId. DhcpSnoopDynamicVlanConfigId
            represents Dhcp snoop dynamic Vlan Config Id
          items:
            type: string
          type: array
        dhcp_snoop_vlan_config_id:
          description: >
            List of DhcpSnoopVlanConfigId. DhcpSnoopVlanConfigId represents Dhcp
            Snoop Vlan Config Id
          items:
            type: string
          type: array
        duration:
          description: >
            List of Duration. Duration represents Duration of violationAction in
            seconds. A duration of 0 indicates permanent.
            	This attribute applies only to BLOCK_MAC and BLOCK_PORT violationActions.
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents Port name of the port whose
            DHCP Snoop settings are stored.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        violation_action:
          description: >
            List of ViolationAction. ViolationAction represents Available DHCP
            Snoop violation action values.
          items:
            format: int64
            type: integer
          type: array
      title: AssetDhcpSnoopPortsConfigGetRequest
      type: object
    AssetDhcpSnoopPortsConfigGetResponse:
      properties:
        AssetDhcpSnoopPortsConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopPortsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpSnoopPortsConfigGetResponse
      type: object
    AssetDhcpSnoopVlansConfig:
      description: AssetDhcpSnoopVlansConfig - DB model for DHCP snooping vlans
        configurations PortSettings can be created while creating
        AssetDhcpSnoopVlansConfig.
      properties:
        asset_vlan_config_id:
          description: VLAN Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dhcp_snoop_config_id:
          description: Dhcp Snoop Config Id
          type: string
        enabled:
          description: DHCP snooping state. A value of true indicates the DHCP snoop is
            enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_settings:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopPortsConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_vlan_config_id
        - dhcp_snoop_config_id
        - enabled
        - vlan_number
      type: object
    AssetDhcpSnoopVlansConfigGetRequest:
      description: AssetDhcpSnoopVlansConfigGetRequest represents the retrieval
        request for AssetDhcpSnoopVlansConfig.
      properties:
        asset_vlan_config_id:
          description: >
            List of AssetVlanConfigId. AssetVlanConfigId represents VLAN Config
            Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dhcp_snoop_config_id:
          description: >
            List of DhcpSnoopConfigId. DhcpSnoopConfigId represents Dhcp Snoop
            Config Id
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents DHCP snooping state. A value of
            true indicates the DHCP snoop is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetDhcpSnoopVlansConfigGetRequest
      type: object
    AssetDhcpSnoopVlansConfigGetResponse:
      properties:
        AssetDhcpSnoopVlansConfig:
          items:
            $ref: "#/components/schemas/AssetDhcpSnoopVlansConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDhcpSnoopVlansConfigGetResponse
      type: object
    AssetDnsConfig:
      description: AssetDnsConfig - DB model for DNS configuration Servers can be
        created while creating AssetDnsConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        domains:
          description: >-
            The domain suffix used by DNS client. DNS domain suffix was learned
            dynamically through
            	DHCP. Fabric Engine supports a single domain
          items:
            type: string
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        servers:
          items:
            $ref: "#/components/schemas/AssetDnsVrfServerConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetDnsConfigGetRequest:
      description: AssetDnsConfigGetRequest represents the retrieval request for
        AssetDnsConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        domains:
          description: >
            List of Domains. Domains represents The domain suffix used by DNS
            client. DNS domain suffix was learned dynamically through
            	DHCP. Fabric Engine supports a single domain
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetDnsConfigGetRequest
      type: object
    AssetDnsConfigGetResponse:
      properties:
        AssetDnsConfig:
          items:
            $ref: "#/components/schemas/AssetDnsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDnsConfigGetResponse
      type: object
    AssetDnsVrfServerConfig:
      description: AssetDnsVrfServerConfig - DB model for DNS vrf server configuration
      nullable: true
      properties:
        asset_dns_config_id:
          description: Id of the DNS configuration
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: VRF configuration index
          format: int64
          minimum: 0
          type: integer
        ip_address:
          description: IP address of the DNS server. DNS server was learned dynamically
            through DHCP
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_name:
          description: Name of the vrf
          type: string
      required:
        - asset_dns_config_id
        - ip_address
        - vrf_name
      type: object
    AssetDnsVrfServerConfigGetRequest:
      description: AssetDnsVrfServerConfigGetRequest represents the retrieval request
        for AssetDnsVrfServerConfig.
      properties:
        asset_dns_config_id:
          description: >
            List of AssetDnsConfigId. AssetDnsConfigId represents Id of the DNS
            configuration
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: |
            List of Index. Index represents VRF configuration index
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        ip_address:
          description: >
            List of IpAddress. IpAddress represents IP address of the DNS
            server. DNS server was learned dynamically through DHCP
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents Name of the vrf
          items:
            type: string
          type: array
      title: AssetDnsVrfServerConfigGetRequest
      type: object
    AssetDnsVrfServerConfigGetResponse:
      properties:
        AssetDnsVrfServerConfig:
          items:
            $ref: "#/components/schemas/AssetDnsVrfServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetDnsVrfServerConfigGetResponse
      type: object
    AssetElrpConfig:
      description: AssetElrpConfig - DB Model for ELRP Global config list
        ElrpDynamicVlans,ElrpVlans can be created while creating
        AssetElrpConfig.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        disable_ports_excluded:
          description: ELRP Exclude Port List.
          items:
            type: string
          nullable: true
          type: array
        elrp_dynamic_vlans:
          items:
            $ref: "#/components/schemas/AssetElrpDynamicVlanConfig"
          nullable: true
          type: array
        elrp_vlans:
          items:
            $ref: "#/components/schemas/AssetElrpVlanConfig"
          nullable: true
          type: array
        enabled:
          default: false
          description: ELRP protocol enabled/disabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - enabled
      type: object
    AssetElrpConfigGetRequest:
      description: AssetElrpConfigGetRequest represents the retrieval request for
        AssetElrpConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        disable_ports_excluded:
          description: >
            List of DisablePortsExcluded. DisablePortsExcluded represents ELRP
            Exclude Port List.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents ELRP protocol enabled/disabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetElrpConfigGetRequest
      type: object
    AssetElrpConfigGetResponse:
      properties:
        AssetElrpConfig:
          items:
            $ref: "#/components/schemas/AssetElrpConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetElrpConfigGetResponse
      type: object
    AssetElrpDynamicVlanConfig:
      description: AssetElrpDynamicVlanConfig - DB Model for Vrf level DHCP relay
        configuration list
      properties:
        action_port:
          default: 1
          description: |-
            Port on which ELRP action is to be taken. This field is
            	ignored when the port action type is NONE.
          format: int64
          type: integer
        action_type:
          default: 1
          description: Type of disable port action.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        elrp_config_id:
          description: ELRP Config Id.
          type: string
        enabled:
          default: false
          description: ELRP protocol enabled/disabled on Vlan.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        protocol:
          default: 1
          description: ELRP protocol type.
          format: int64
          type: integer
        reporting:
          default: 3
          description: The type associated with ELRP reporting.
          format: int64
          type: integer
        temporary_duration:
          default: 30
          description: |-
            Length of time in seconds that a port is kept down when
            	TEMPORARY disable port action is configured. This value is ignored
            	when NONE or PERMANENT disable port action is configured.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - action_port
        - action_type
        - elrp_config_id
        - enabled
        - protocol
        - reporting
        - temporary_duration
      type: object
    AssetElrpDynamicVlanConfigGetRequest:
      description: AssetElrpDynamicVlanConfigGetRequest represents the retrieval
        request for AssetElrpDynamicVlanConfig.
      properties:
        action_port:
          description: >
            List of ActionPort. ActionPort represents Port on which ELRP action
            is to be taken. This field is
            	ignored when the port action type is NONE.
          items:
            format: int64
            type: integer
          type: array
        action_type:
          description: >
            List of ActionType. ActionType represents Type of disable port
            action.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        elrp_config_id:
          description: |
            List of ElrpConfigId. ElrpConfigId represents ELRP Config Id.
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents ELRP protocol enabled/disabled
            on Vlan.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        protocol:
          description: |
            List of Protocol. Protocol represents ELRP protocol type.
          items:
            format: int64
            type: integer
          type: array
        reporting:
          description: >
            List of Reporting. Reporting represents The type associated with
            ELRP reporting.
          items:
            format: int64
            type: integer
          type: array
        temporary_duration:
          description: >
            List of TemporaryDuration. TemporaryDuration represents Length of
            time in seconds that a port is kept down when
            	TEMPORARY disable port action is configured. This value is ignored
            	when NONE or PERMANENT disable port action is configured.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetElrpDynamicVlanConfigGetRequest
      type: object
    AssetElrpDynamicVlanConfigGetResponse:
      properties:
        AssetElrpDynamicVlanConfig:
          items:
            $ref: "#/components/schemas/AssetElrpDynamicVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetElrpDynamicVlanConfigGetResponse
      type: object
    AssetElrpVlanConfig:
      description: AssetElrpVlanConfig - DB Model for ELRP VLANs config list
      properties:
        action_port:
          default: 1
          description: |-
            Port on which ELRP action is to be taken. This field is
            	ignored when the port action type is NONE.
          format: int64
          type: integer
        action_type:
          default: 1
          description: Type of disable port action.
          format: int64
          type: integer
        asset_vlan_config_id:
          description: VLAN Config Id.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        elrp_config_id:
          description: ELRP Config Id.
          type: string
        enabled:
          default: false
          description: ELRP protocol enabled/disabled on Vlan.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        ports:
          description: List of ELRP enabled ports on this VLAN.
          items:
            type: string
          nullable: true
          type: array
        reporting:
          default: 3
          description: The type associated with ELRP reporting.
          format: int64
          type: integer
        temporary_duration:
          default: 30
          description: |-
            Length of time in seconds that a port is kept down when
            	TEMPORARY disable port action is configured. This value is ignored
            	when NONE or PERMANENT disable port action is configured.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - action_port
        - action_type
        - asset_vlan_config_id
        - elrp_config_id
        - enabled
        - reporting
        - temporary_duration
        - vlan_id
      type: object
    AssetElrpVlanConfigGetRequest:
      description: AssetElrpVlanConfigGetRequest represents the retrieval request for
        AssetElrpVlanConfig.
      properties:
        action_port:
          description: >
            List of ActionPort. ActionPort represents Port on which ELRP action
            is to be taken. This field is
            	ignored when the port action type is NONE.
          items:
            format: int64
            type: integer
          type: array
        action_type:
          description: >
            List of ActionType. ActionType represents Type of disable port
            action.
          items:
            format: int64
            type: integer
          type: array
        asset_vlan_config_id:
          description: >
            List of AssetVlanConfigId. AssetVlanConfigId represents VLAN Config
            Id.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        elrp_config_id:
          description: |
            List of ElrpConfigId. ElrpConfigId represents ELRP Config Id.
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents ELRP protocol enabled/disabled
            on Vlan.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        ports:
          description: >
            List of Ports. Ports represents List of ELRP enabled ports on this
            VLAN.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        reporting:
          description: >
            List of Reporting. Reporting represents The type associated with
            ELRP reporting.
          items:
            format: int64
            type: integer
          type: array
        temporary_duration:
          description: >
            List of TemporaryDuration. TemporaryDuration represents Length of
            time in seconds that a port is kept down when
            	TEMPORARY disable port action is configured. This value is ignored
            	when NONE or PERMANENT disable port action is configured.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents This object represents a VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetElrpVlanConfigGetRequest
      type: object
    AssetElrpVlanConfigGetResponse:
      properties:
        AssetElrpVlanConfig:
          items:
            $ref: "#/components/schemas/AssetElrpVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetElrpVlanConfigGetResponse
      type: object
    AssetFabricAttachConfig:
      description: AssetFabricAttachConfig - DB Model for  ELRP VLANs configuration
        list AssetFabricAttachConfigPort,AssetFabricAttachConfigZeroTouchClient
        can be created while creating AssetFabricAttachConfig.
      properties:
        asset_device_id:
          description: This object represents the fabric attach settings for a device.
          type: string
        asset_fabric_attach_config_port:
          items:
            $ref: "#/components/schemas/AssetFabricAttachConfigPort"
          nullable: true
          type: array
        asset_fabric_attach_config_zero_touch_client:
          items:
            $ref: "#/components/schemas/AssetFabricAttachConfigZeroTouchClient"
          nullable: true
          type: array
        assignment_timeout:
          description: Fabric Attach assignment timeout. For Switch Engine it is a read
            only attribute.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        discovery_timeout:
          description: Fabric Attach discovery timeout. This setting applies to Fabric
            Engine only.
          format: int64
          type: integer
        enabled:
          description: |-
            The status of the Fabric Attach service. On Switch
            	Engine, this setting is read only.
          type: boolean
        forwarding:
          description: >-
            When set to true Fabric Attach Proxy switches send management VLAN
            data to clients.
            	This setting applies to Switch Engine only.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mgmt_vlan:
          description: |-
            The VLAN advertised to fabric attach clients for them to use
            	as the management VLAN. Value set to 0 indicates that no management
            	VLAN is configured. Value set to 4095 indicates that Management
            	traffic should be sent untagged. This setting applies
            	to Switch Engine only.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        uplink_port:
          description: >-
            The uplink port name for standalone proxy operation. Name of the
            interface, slot and port are required to ensure deterministic
            results, sub_port is optional. This definition allows for the string
            'None' to unconfigure the uplink port. Switch Engine only.

            	 [slot]:[port][:sub_port]
            	'1:1:1'    slot=1, port=1, sub_port=1
            	'12:101'   slot=12, port=101
          type: string
      required:
        - asset_device_id
        - enabled
      type: object
    AssetFabricAttachConfigGetRequest:
      description: AssetFabricAttachConfigGetRequest represents the retrieval request
        for AssetFabricAttachConfig.
      properties:
        asset_device_id:
          description: >
            List of AssetDeviceId. AssetDeviceId represents This object
            represents the fabric attach settings for a device.
          items:
            type: string
          type: array
        assignment_timeout:
          description: >
            List of AssignmentTimeout. AssignmentTimeout represents Fabric
            Attach assignment timeout. For Switch Engine it is a read only
            attribute.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        discovery_timeout:
          description: >
            List of DiscoveryTimeout. DiscoveryTimeout represents Fabric Attach
            discovery timeout. This setting applies to Fabric Engine only.
          items:
            format: int64
            type: integer
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The status of the Fabric Attach
            service. On Switch
            	Engine, this setting is read only.
          items:
            type: boolean
          type: array
        forwarding:
          description: >
            List of Forwarding. Forwarding represents When set to true Fabric
            Attach Proxy switches send management VLAN data to clients.
            	This setting applies to Switch Engine only.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mgmt_vlan:
          description: >
            List of MgmtVlan. MgmtVlan represents The VLAN advertised to fabric
            attach clients for them to use
            	as the management VLAN. Value set to 0 indicates that no management
            	VLAN is configured. Value set to 4095 indicates that Management
            	traffic should be sent untagged. This setting applies
            	to Switch Engine only.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        uplink_port:
          description: >
            List of UplinkPort. UplinkPort represents The uplink port name for
            standalone proxy operation. Name of the interface, slot and port are
            required to ensure deterministic results, sub_port is optional. This
            definition allows for the string 'None' to unconfigure the uplink
            port. Switch Engine only.

            	 [slot]:[port][:sub_port]
            	'1:1:1'    slot=1, port=1, sub_port=1
            	'12:101'   slot=12, port=101
          items:
            type: string
          type: array
      title: AssetFabricAttachConfigGetRequest
      type: object
    AssetFabricAttachConfigGetResponse:
      properties:
        AssetFabricAttachConfig:
          items:
            $ref: "#/components/schemas/AssetFabricAttachConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFabricAttachConfigGetResponse
      type: object
    AssetFabricAttachConfigPort:
      description: AssetFabricAttachConfigPort - DB Model for fabric attach config port
      properties:
        authentication_key:
          description: |-
            Provides access to the Fabric Attach  message authentication key
            	for the associated interface. Switch Engine allows configuration
            	of both cleartext and encrypted keys. The maximum length for
            	configuration of a cleartext key is 32 characters. For Switch
            	Engine this corresponds to a maximum encrypted length of 110
            	characters.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Status of Fabric Attach on a port.
          type: boolean
        fabric_attach_config_id:
          description: This object represents the fabric attach settings for a port.
          type: string
        forwarding:
          description: >-
            When set to true Fabric Attach Proxy switches send management VLAN
            data to clients.
            	This setting applies to Switch Engine only.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        key_encrypted:
          description: >-
            A value of true indicates the key encryption in enabled for
            	the associated interface. This setting applies to Switch Engine only.
          type: boolean
        message_authentication_enabled:
          description: Status of the Fabric Attach message authentication for the
            associated interface.
          type: boolean
        mgmt_cvid:
          description: |-
            Fabric Attach management customer vid for the associated interface.
            	4095 is not used. 4096 is reserved for untagged case. Zero means
            	the management i-sid feature is not enabled. This setting applies
            	to Fabric Engine only.
          format: int64
          minimum: 0
          type: integer
        mgmt_nsi:
          description: >-
            Fabric Attach management network service (i-sid) id
            	for the associated interface. Zero means the management i-sid feature is
            	not enabled. Values of 16000000 and greater are reserved for internal use.
            	This setting applies to Fabric Engine only.
          format: int64
          minimum: 0
          type: integer
        origin:
          description: >-
            Fabric Attach management network service (i-sid) id
            	for the associated interface. Zero means the management i-sid feature is
            	not enabled. Values of 16000000 and greater are reserved for internal use.
            	This setting applies to Fabric Engine only.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: PortName string value
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - fabric_attach_config_id
        - port_name
      type: object
    AssetFabricAttachConfigPortGetRequest:
      description: AssetFabricAttachConfigPortGetRequest represents the retrieval
        request for AssetFabricAttachConfigPort.
      properties:
        authentication_key:
          description: >
            List of AuthenticationKey. AuthenticationKey represents Provides
            access to the Fabric Attach  message authentication key
            	for the associated interface. Switch Engine allows configuration
            	of both cleartext and encrypted keys. The maximum length for
            	configuration of a cleartext key is 32 characters. For Switch
            	Engine this corresponds to a maximum encrypted length of 110
            	characters.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents Status of Fabric Attach on a
            port.
          items:
            type: boolean
          type: array
        fabric_attach_config_id:
          description: >
            List of FabricAttachConfigId. FabricAttachConfigId represents This
            object represents the fabric attach settings for a port.
          items:
            type: string
          type: array
        forwarding:
          description: >
            List of Forwarding. Forwarding represents When set to true Fabric
            Attach Proxy switches send management VLAN data to clients.
            	This setting applies to Switch Engine only.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        key_encrypted:
          description: >
            List of KeyEncrypted. KeyEncrypted represents A value of true
            indicates the key encryption in enabled for
            	the associated interface. This setting applies to Switch Engine only.
          items:
            type: boolean
          type: array
        message_authentication_enabled:
          description: >
            List of MessageAuthenticationEnabled. MessageAuthenticationEnabled
            represents Status of the Fabric Attach message authentication for
            the associated interface.
          items:
            type: boolean
          type: array
        mgmt_cvid:
          description: >
            List of MgmtCvid. MgmtCvid represents Fabric Attach management
            customer vid for the associated interface.
            	4095 is not used. 4096 is reserved for untagged case. Zero means
            	the management i-sid feature is not enabled. This setting applies
            	to Fabric Engine only.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        mgmt_nsi:
          description: >
            List of MgmtNsi. MgmtNsi represents Fabric Attach management network
            service (i-sid) id
            	for the associated interface. Zero means the management i-sid feature is
            	not enabled. Values of 16000000 and greater are reserved for internal use.
            	This setting applies to Fabric Engine only.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        origin:
          description: >
            List of Origin. Origin represents Fabric Attach management network
            service (i-sid) id
            	for the associated interface. Zero means the management i-sid feature is
            	not enabled. Values of 16000000 and greater are reserved for internal use.
            	This setting applies to Fabric Engine only.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: |
            List of PortName. PortName represents PortName string value
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetFabricAttachConfigPortGetRequest
      type: object
    AssetFabricAttachConfigPortGetResponse:
      properties:
        AssetFabricAttachConfigPort:
          items:
            $ref: "#/components/schemas/AssetFabricAttachConfigPort"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFabricAttachConfigPortGetResponse
      type: object
    AssetFabricAttachConfigZeroTouchClient:
      description: AssetFabricAttachConfigZeroTouchClient - DB Model for fabric attach
        config zero touch client
      properties:
        asset_fabric_attach_config_id:
          description: FabricAttachConfigID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dot1p:
          description: >-
            Usage of dot1p port priority for untagged packets. If dot1p is True,
            	priority must be -1.  If dot1p is False, priority must be 0-7. Dot1p
            	is True by default. Switch Engine only.
          type: boolean
        enabled:
          description: The status of the Zero Touch Client. Switch Engine only.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        nsi:
          description: |-
            The management network service (i-sid) of the Zero Touch Client
            	auto-attach specification. On Fabric Engine the maximum value is
            	16000000.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        priority:
          description: |-
            Priority to be used for untagged packets. Priority value of -1
            	indicates dot1p is True. Switch Engine only
          format: int64
          type: integer
        type:
          description: Zero Touch Client element type
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: >-
            VLAN ID component of the Zero Touch Client auto-attach
            specification.
            	4095 is not used. 4096 is reserved for untagged case. Fabric Engine
            	exclusively supports untagged traffic.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_fabric_attach_config_id
        - nsi
        - type
      type: object
    AssetFabricAttachConfigZeroTouchClientGetRequest:
      description: AssetFabricAttachConfigZeroTouchClientGetRequest represents the
        retrieval request for AssetFabricAttachConfigZeroTouchClient.
      properties:
        asset_fabric_attach_config_id:
          description: >
            List of AssetFabricAttachConfigId. AssetFabricAttachConfigId
            represents FabricAttachConfigID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dot1p:
          description: >
            List of Dot1p. Dot1p represents Usage of dot1p port priority for
            untagged packets. If dot1p is True,
            	priority must be -1.  If dot1p is False, priority must be 0-7. Dot1p
            	is True by default. Switch Engine only.
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The status of the Zero Touch
            Client. Switch Engine only.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        nsi:
          description: >
            List of Nsi. Nsi represents The management network service (i-sid)
            of the Zero Touch Client
            	auto-attach specification. On Fabric Engine the maximum value is
            	16000000.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents Priority to be used for
            untagged packets. Priority value of -1
            	indicates dot1p is True. Switch Engine only
          items:
            format: int64
            type: integer
          type: array
        type:
          description: |
            List of Type. Type represents Zero Touch Client element type
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents VLAN ID component of the
            Zero Touch Client auto-attach specification.
            	4095 is not used. 4096 is reserved for untagged case. Fabric Engine
            	exclusively supports untagged traffic.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetFabricAttachConfigZeroTouchClientGetRequest
      type: object
    AssetFabricAttachConfigZeroTouchClientGetResponse:
      properties:
        AssetFabricAttachConfigZeroTouchClient:
          items:
            $ref: "#/components/schemas/AssetFabricAttachConfigZeroTouchClient"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFabricAttachConfigZeroTouchClientGetResponse
      type: object
    AssetFabricAttachState:
      description: AssetFabricAttachState - DB Model for fabric attach state
        AssetFabricAttachStateDiscoveryElement,AssetFabricAttachStateVlanAssignment
        can be created while creating AssetFabricAttachState.
      properties:
        asset_device_id:
          description: Fabric Attach state information
          type: string
        asset_fabric_attach_state_discovery_element:
          items:
            $ref: "#/components/schemas/AssetFabricAttachStateDiscoveryElement"
          nullable: true
          type: array
        asset_fabric_attach_state_vlan_assignment:
          items:
            $ref: "#/components/schemas/AssetFabricAttachStateVlanAssignment"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fa_role:
          description: Fabric attach role.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        primary_server_system_id:
          description: >-
            Fabric Attach primary server system Id. It represents an id of the
            server to which the FA Proxy is attached to. This data will only be
            used when the device is a proxy.
            	This setting applies to Switch Engine only.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetFabricAttachStateDiscoveryElement:
      description: AssetFabricAttachStateDiscoveryElement - DB Model for fabric attach
        state discovery element list
      nullable: true
      properties:
        assign_auth_status:
          description: |-
            Fabric Attach discovered element assignment authentication status.
            	This setting applies to Fabric Engine only.
          type: string
        assign_oper_auth_status:
          description: |-
            Fabric Attach discovered element assignment authentication status.
            	This setting applies to Fabric Engine only.
          type: string
        auth_status:
          description: |-
            Fabric Attach discovered element authentication status. This
            	setting applies to Fabric Engine only.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_attach_state_id:
          description: Fabric Attach state information
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mgmt_vlan:
          description: |-
            The VLAN advertised to fabric attach clients for them to use
            	as the management VLAN. Value set to 0 indicates that no management
            	VLAN is configured. Value set to 4095 indicates that Management
            	traffic should be sent untagged. This setting applies
            	to Switch Engine only.
          format: int64
          minimum: 0
          type: integer
        oper_auth_status:
          description: >-
            Operational Fabric Attach elements authentication status for the
            	   associated interface.
            	   "NONE" indicates that no packets have been received yet.
            	   "SUCCESS_NO_AUTH" indicates success with no-auth on either local or remote.
            	   "SUCCESS_AUTH" indicates success with authentication on both local and remote.
            	   "FAIL_MISMATCHED_KEYS" indicates failure due to key mismatch.
            	   "FAIL_LOCAL_AUTH_REMOTE_NO_AUTH" indicates failure due to local authentication
            	   and remote no-authentication.
            	   "FAIL_LOCAL_NO_AUTH_REMOTE_AUTH" indicates failure due to local no-authentication
            	   and remote authentication.
            	   This setting applies to Fabric Engine only.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: |-
            Name of the interface, slot and port are required

            	to ensure deterministic results, sub_port is optional. This
            	definition allows for the string 'None' to accommodate situations
            	in which the port name parameter is undefined.

            	[slot]:[port][:sub_port]

            	'1:1:1'    slot=1, port=1, sub_port=1

            	'12:101'   slot=12, port=101
          type: string
        provision:
          description: Exports the provision mode associated with the discovered Fabric
            Attach element.
          type: string
        system_id:
          description: Exports the Chassis ID associated with the discovered Fabric Attach
            element as advertised through LLDP.
          type: string
        tag:
          description: Exports the tag state associated with the discovered Fabric Attach
            element.
          type: string
        type:
          description: Fabric Attach element type as advertised through LLDP. UNKNOWN
            applies to Switch Engine Only
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - fabric_attach_state_id
        - port_name
        - system_id
      type: object
    AssetFabricAttachStateDiscoveryElementGetRequest:
      description: AssetFabricAttachStateDiscoveryElementGetRequest represents the
        retrieval request for AssetFabricAttachStateDiscoveryElement.
      properties:
        assign_auth_status:
          description: >
            List of AssignAuthStatus. AssignAuthStatus represents Fabric Attach
            discovered element assignment authentication status.
            	This setting applies to Fabric Engine only.
          items:
            type: string
          type: array
        assign_oper_auth_status:
          description: >
            List of AssignOperAuthStatus. AssignOperAuthStatus represents Fabric
            Attach discovered element assignment authentication status.
            	This setting applies to Fabric Engine only.
          items:
            type: string
          type: array
        auth_status:
          description: >
            List of AuthStatus. AuthStatus represents Fabric Attach discovered
            element authentication status. This
            	setting applies to Fabric Engine only.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_attach_state_id:
          description: >
            List of FabricAttachStateId. FabricAttachStateId represents Fabric
            Attach state information
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mgmt_vlan:
          description: >
            List of MgmtVlan. MgmtVlan represents The VLAN advertised to fabric
            attach clients for them to use
            	as the management VLAN. Value set to 0 indicates that no management
            	VLAN is configured. Value set to 4095 indicates that Management
            	traffic should be sent untagged. This setting applies
            	to Switch Engine only.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        oper_auth_status:
          description: >
            List of OperAuthStatus. OperAuthStatus represents Operational Fabric
            Attach elements authentication status for the
            	   associated interface.
            	   "NONE" indicates that no packets have been received yet.
            	   "SUCCESS_NO_AUTH" indicates success with no-auth on either local or remote.
            	   "SUCCESS_AUTH" indicates success with authentication on both local and remote.
            	   "FAIL_MISMATCHED_KEYS" indicates failure due to key mismatch.
            	   "FAIL_LOCAL_AUTH_REMOTE_NO_AUTH" indicates failure due to local authentication
            	   and remote no-authentication.
            	   "FAIL_LOCAL_NO_AUTH_REMOTE_AUTH" indicates failure due to local no-authentication
            	   and remote authentication.
            	   This setting applies to Fabric Engine only.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents Name of the interface, slot
            and port are required

            	to ensure deterministic results, sub_port is optional. This
            	definition allows for the string 'None' to accommodate situations
            	in which the port name parameter is undefined.

            	[slot]:[port][:sub_port]

            	'1:1:1'    slot=1, port=1, sub_port=1

            	'12:101'   slot=12, port=101
          items:
            type: string
          type: array
        provision:
          description: >
            List of Provision. Provision represents Exports the provision mode
            associated with the discovered Fabric Attach element.
          items:
            type: string
          type: array
        system_id:
          description: >
            List of SystemId. SystemId represents Exports the Chassis ID
            associated with the discovered Fabric Attach element as advertised
            through LLDP.
          items:
            type: string
          type: array
        tag:
          description: >
            List of Tag. Tag represents Exports the tag state associated with
            the discovered Fabric Attach element.
          items:
            type: string
          type: array
        type:
          description: >
            List of Type. Type represents Fabric Attach element type as
            advertised through LLDP. UNKNOWN applies to Switch Engine Only
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetFabricAttachStateDiscoveryElementGetRequest
      type: object
    AssetFabricAttachStateDiscoveryElementGetResponse:
      properties:
        AssetFabricAttachStateDiscoveryElement:
          items:
            $ref: "#/components/schemas/AssetFabricAttachStateDiscoveryElement"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFabricAttachStateDiscoveryElementGetResponse
      type: object
    AssetFabricAttachStateGetRequest:
      description: AssetFabricAttachStateGetRequest represents the retrieval request
        for AssetFabricAttachState.
      properties:
        asset_device_id:
          description: >
            List of AssetDeviceId. AssetDeviceId represents Fabric Attach state
            information
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fa_role:
          description: |
            List of FaRole. FaRole represents Fabric attach role.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        primary_server_system_id:
          description: >
            List of PrimaryServerSystemId. PrimaryServerSystemId represents
            Fabric Attach primary server system Id. It represents an id of the
            server to which the FA Proxy is attached to. This data will only be
            used when the device is a proxy.
            	This setting applies to Switch Engine only.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetFabricAttachStateGetRequest
      type: object
    AssetFabricAttachStateGetResponse:
      properties:
        AssetFabricAttachState:
          items:
            $ref: "#/components/schemas/AssetFabricAttachState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFabricAttachStateGetResponse
      type: object
    AssetFabricAttachStateVlanAssignment:
      description: AssetFabricAttachStateVlanAssignment - DB Model for fabric attach
        state VLAN assignment
      nullable: true
      properties:
        asset_fabric_attach_config_id:
          description: This object contains the nsi <-> vlan assignments configured.
            Switch Engine only
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        nsi:
          description: nsi component of the nsi <-> VLAN assignment.
          format: int64
          minimum: 0
          type: integer
        nsi_name:
          description: |-
            A value that uniquely identifies the nsi.
            	This setting applies to Fabric Engine only.
          type: string
        origin:
          description: |-
            Fabric Attach Origin indicates how the FA interface was created:
            	manual (CLI/SNMP) or ZTF (Auto-Sense).
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: |-
            Name of the interface, slot and port are required

            	to ensure deterministic results, sub_port is optional. This
            	definition allows for the string 'None' to accommodate situations
            	in which the port name parameter is undefined.

            	[slot]:[port][:sub_port]

            	'1:1:1'    slot=1, port=1, sub_port=1

            	'12:101'   slot=12, port=101
          type: string
        state:
          description: The Fabric Attach state information.
          type: string
        type:
          description: Fabric Attach assignment type. This setting applies to Switch
            Engine only.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: |-
            A value that uniquely identifies the Customer VLAN.
            	For Switch Engine, the maximum value is 4094.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_fabric_attach_config_id
        - port_name
        - vlan_number
      type: object
    AssetFabricAttachStateVlanAssignmentGetRequest:
      description: AssetFabricAttachStateVlanAssignmentGetRequest represents the
        retrieval request for AssetFabricAttachStateVlanAssignment.
      properties:
        asset_fabric_attach_config_id:
          description: >
            List of AssetFabricAttachConfigId. AssetFabricAttachConfigId
            represents This object contains the nsi <-> vlan assignments
            configured. Switch Engine only
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        nsi:
          description: >
            List of Nsi. Nsi represents nsi component of the nsi <-> VLAN
            assignment.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        nsi_name:
          description: >
            List of NsiName. NsiName represents A value that uniquely identifies
            the nsi.
            	This setting applies to Fabric Engine only.
          items:
            type: string
          type: array
        origin:
          description: >
            List of Origin. Origin represents Fabric Attach Origin indicates how
            the FA interface was created:
            	manual (CLI/SNMP) or ZTF (Auto-Sense).
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents Name of the interface, slot
            and port are required

            	to ensure deterministic results, sub_port is optional. This
            	definition allows for the string 'None' to accommodate situations
            	in which the port name parameter is undefined.

            	[slot]:[port][:sub_port]

            	'1:1:1'    slot=1, port=1, sub_port=1

            	'12:101'   slot=12, port=101
          items:
            type: string
          type: array
        state:
          description: |
            List of State. State represents The Fabric Attach state information.
          items:
            type: string
          type: array
        type:
          description: >
            List of Type. Type represents Fabric Attach assignment type. This
            setting applies to Switch Engine only.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents A value that uniquely
            identifies the Customer VLAN.
            	For Switch Engine, the maximum value is 4094.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetFabricAttachStateVlanAssignmentGetRequest
      type: object
    AssetFabricAttachStateVlanAssignmentGetResponse:
      properties:
        AssetFabricAttachStateVlanAssignment:
          items:
            $ref: "#/components/schemas/AssetFabricAttachStateVlanAssignment"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFabricAttachStateVlanAssignmentGetResponse
      type: object
    AssetFeTunnelConfig:
      description: AssetFeTunnelConfig - DB Model for logical inft config list
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        asset_interface_id:
          description: UUID of the interface
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dest_if_name:
          description: The interface name to which this logical interface corresponds.
            Applies for LAYER2 type only and it can be port or LAG name.
          type: string
        dest_ip_addr:
          description: The logical interface Destination Ipv4 address.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_type:
          description: InterfaceType string value
          format: int64
          type: integer
        isis_mtu:
          description: The value specifies MTU of ISIS control packets on logical
            interface tunnel. This attribute cannot be configured on XA1400.
          type: integer
        logical_if_id:
          description: ISIS area configuration
          type: string
        mtu:
          description: The value specifies MTU of logical interface tunnel. This attribute
            can only be configured on XA1400.
          type: integer
        name:
          description: An administratively-assigned name for the logical interface.
          type: string
        next_hop_vr_name:
          description: Next hop vr name to reach logical tunnel dest-ip. This attribute
            cannot be configured on XA1400.
          type: string
        origin:
          description: Indicates how the logical interface was created.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        src_ip_addr:
          description: The logical interface Source Ipv4 address. This attribute cannot be
            configured on XA1400.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - logical_if_id
      type: object
    AssetFeTunnelConfigGetRequest:
      description: AssetFeTunnelConfigGetRequest represents the retrieval request for
        AssetFeTunnelConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        asset_interface_id:
          description: >
            List of AssetInterfaceId. AssetInterfaceId represents UUID of the
            interface
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dest_if_name:
          description: >
            List of DestIfName. DestIfName represents The interface name to
            which this logical interface corresponds. Applies for LAYER2 type
            only and it can be port or LAG name.
          items:
            type: string
          type: array
        dest_ip_addr:
          description: >
            List of DestIpAddr. DestIpAddr represents The logical interface
            Destination Ipv4 address.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents InterfaceType string
            value
          items:
            format: int64
            type: integer
          type: array
        isis_mtu:
          description: >
            List of IsisMtu. IsisMtu represents The value specifies MTU of ISIS
            control packets on logical interface tunnel. This attribute cannot
            be configured on XA1400.
          items:
            type: integer
          type: array
        logical_if_id:
          description: |
            List of LogicalIfId. LogicalIfId represents ISIS area configuration
          items:
            type: string
          type: array
        mtu:
          description: >
            List of Mtu. Mtu represents The value specifies MTU of logical
            interface tunnel. This attribute can only be configured on XA1400.
          items:
            type: integer
          type: array
        name:
          description: >
            List of Name. Name represents An administratively-assigned name for
            the logical interface.
          items:
            type: string
          type: array
        next_hop_vr_name:
          description: >
            List of NextHopVrName. NextHopVrName represents Next hop vr name to
            reach logical tunnel dest-ip. This attribute cannot be configured on
            XA1400.
          items:
            type: string
          type: array
        origin:
          description: >
            List of Origin. Origin represents Indicates how the logical
            interface was created.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        src_ip_addr:
          description: >
            List of SrcIpAddr. SrcIpAddr represents The logical interface Source
            Ipv4 address. This attribute cannot be configured on XA1400.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetFeTunnelConfigGetRequest
      type: object
    AssetFeTunnelConfigGetResponse:
      properties:
        AssetFeTunnelConfig:
          items:
            $ref: "#/components/schemas/AssetFeTunnelConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetFeTunnelConfigGetResponse
      type: object
    AssetIgmpVlanConfig:
      description: AssetIgmpVlanConfig - DB Model for VLAN related settings for Igmp
      properties:
        asset_device_id:
          description: Device Id
          type: string
        asset_vlan_config_id:
          description: Asset VLAN Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fast_leave_enabled:
          description: A value of true indicates the fast leave feature is enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        report_suppression_enabled:
          description: |-
            A value of true indicates the report suppression feature
            	is enabled - Switch Engine (EXOS) only.
          type: boolean
        snooping_enabled:
          description: |-
            The admin enabled setting that indicates if IGMP snooping
            	is enabled on this VLAN.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_device_id
        - asset_vlan_config_id
        - fast_leave_enabled
        - report_suppression_enabled
        - snooping_enabled
        - vlan_number
      type: object
    AssetIgmpVlanConfigGetRequest:
      description: AssetIgmpVlanConfigGetRequest represents the retrieval request for
        AssetIgmpVlanConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        asset_vlan_config_id:
          description: >
            List of AssetVlanConfigId. AssetVlanConfigId represents Asset VLAN
            Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fast_leave_enabled:
          description: >
            List of FastLeaveEnabled. FastLeaveEnabled represents A value of
            true indicates the fast leave feature is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        report_suppression_enabled:
          description: >
            List of ReportSuppressionEnabled. ReportSuppressionEnabled
            represents A value of true indicates the report suppression feature
            	is enabled - Switch Engine (EXOS) only.
          items:
            type: boolean
          type: array
        snooping_enabled:
          description: >
            List of SnoopingEnabled. SnoopingEnabled represents The admin
            enabled setting that indicates if IGMP snooping
            	is enabled on this VLAN.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetIgmpVlanConfigGetRequest
      type: object
    AssetIgmpVlanConfigGetResponse:
      properties:
        AssetIgmpVlanConfig:
          items:
            $ref: "#/components/schemas/AssetIgmpVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIgmpVlanConfigGetResponse
      type: object
    AssetIgmpVrConfig:
      description: AssetIgmpVrConfig - DB Model for VPN routing related config for IGMP
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        report_suppression_enabled:
          description: |-
            A value of true indicates the report suppression feature
            	is enabled - Switch Engine (EXOS) only.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_name:
          description: The name of the VR
          type: string
      required:
        - asset_device_id
        - report_suppression_enabled
        - vr_name
      type: object
    AssetIgmpVrConfigGetRequest:
      description: AssetIgmpVrConfigGetRequest represents the retrieval request for
        AssetIgmpVrConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        report_suppression_enabled:
          description: >
            List of ReportSuppressionEnabled. ReportSuppressionEnabled
            represents A value of true indicates the report suppression feature
            	is enabled - Switch Engine (EXOS) only.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_name:
          description: |
            List of VrName. VrName represents The name of the VR
          items:
            type: string
          type: array
      title: AssetIgmpVrConfigGetRequest
      type: object
    AssetIgmpVrConfigGetResponse:
      properties:
        AssetIgmpVrConfig:
          items:
            $ref: "#/components/schemas/AssetIgmpVrConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIgmpVrConfigGetResponse
      type: object
    AssetInstantPortActionConfig:
      description: AssetInstantPortActionConfig - DB Model for the action
        configuration for IPP
      properties:
        action_type:
          description: Specifies which Action Type is being configured.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_types_config_id:
          description: Instant Port Device Types Config Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        rate_limit_broadcast:
          description: |-
            The rate limit in packets per second (pps) associated with an
            	instant-port storm control action for broadcast packets
          type: integer
        rate_limit_multicast:
          description: |-
            The rate limit in packets per second (pps) associated with an
            	instant-port storm control action for multicast packets
          type: integer
        rate_limit_unicast:
          description: |-
            The rate limit in packets per second (pps) associated with an
            	instant-port storm control action for unicast packets
          type: integer
        tagged_vlan_list:
          description: |-
            The set of VLANs which should transmit egress packets as tagged on
            	ports for which this role is active.
          items:
            type: string
          nullable: true
          type: array
        untagged_vlan_list:
          description: |-
            The set of VLANs which should transmit egress packets as untagged on
            	ports for which this role is active.
          items:
            type: string
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - action_type
        - device_types_config_id
      type: object
    AssetInstantPortActionConfigGetRequest:
      description: AssetInstantPortActionConfigGetRequest represents the retrieval
        request for AssetInstantPortActionConfig.
      properties:
        action_type:
          description: >
            List of ActionType. ActionType represents Specifies which Action
            Type is being configured.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_types_config_id:
          description: >
            List of DeviceTypesConfigId. DeviceTypesConfigId represents Instant
            Port Device Types Config Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        rate_limit_broadcast:
          description: >
            List of RateLimitBroadcast. RateLimitBroadcast represents The rate
            limit in packets per second (pps) associated with an
            	instant-port storm control action for broadcast packets
          items:
            type: integer
          type: array
        rate_limit_multicast:
          description: >
            List of RateLimitMulticast. RateLimitMulticast represents The rate
            limit in packets per second (pps) associated with an
            	instant-port storm control action for multicast packets
          items:
            type: integer
          type: array
        rate_limit_unicast:
          description: >
            List of RateLimitUnicast. RateLimitUnicast represents The rate limit
            in packets per second (pps) associated with an
            	instant-port storm control action for unicast packets
          items:
            type: integer
          type: array
        tagged_vlan_list:
          description: >
            List of TaggedVlanList. TaggedVlanList represents The set of VLANs
            which should transmit egress packets as tagged on
            	ports for which this role is active.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        untagged_vlan_list:
          description: >
            List of UntaggedVlanList. UntaggedVlanList represents The set of
            VLANs which should transmit egress packets as untagged on
            	ports for which this role is active.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInstantPortActionConfigGetRequest
      type: object
    AssetInstantPortActionConfigGetResponse:
      properties:
        AssetInstantPortActionConfig:
          items:
            $ref: "#/components/schemas/AssetInstantPortActionConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInstantPortActionConfigGetResponse
      type: object
    AssetInstantPortConfig:
      description: AssetInstantPortConfig - DB Model for instant port related config
        information ProfileConfig can be created while creating
        AssetInstantPortConfig.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        learning_vlan_id:
          description: |-
            The VLAN ID of the instant-port learning VLAN. A value
            	of zero indicates the learning VLAN is unset.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        profile_config:
          items:
            $ref: "#/components/schemas/AssetInstantPortProfileConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - learning_vlan_id
      type: object
    AssetInstantPortConfigGetRequest:
      description: AssetInstantPortConfigGetRequest represents the retrieval request
        for AssetInstantPortConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        learning_vlan_id:
          description: >
            List of LearningVlanId. LearningVlanId represents The VLAN ID of the
            instant-port learning VLAN. A value
            	of zero indicates the learning VLAN is unset.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInstantPortConfigGetRequest
      type: object
    AssetInstantPortConfigGetResponse:
      properties:
        AssetInstantPortConfig:
          items:
            $ref: "#/components/schemas/AssetInstantPortConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInstantPortConfigGetResponse
      type: object
    AssetInstantPortDeviceTypesConfig:
      description: AssetInstantPortDeviceTypesConfig - DB Model for device type
        configuration for IPP Actions,MatchConditions can be created while
        creating AssetInstantPortDeviceTypesConfig.
      nullable: true
      properties:
        actions:
          items:
            $ref: "#/components/schemas/AssetInstantPortActionConfig"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: Device type index.
          format: int64
          minimum: 0
          type: integer
        match_conditions:
          items:
            $ref: "#/components/schemas/AssetInstantPortMatchConditionConfig"
          nullable: true
          type: array
        name:
          description: Device type name (must be unique within a given profile).
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        profile_config_id:
          description: Instant port profile Config Id
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - index
        - name
        - profile_config_id
      type: object
    AssetInstantPortDeviceTypesConfigGetRequest:
      description: AssetInstantPortDeviceTypesConfigGetRequest represents the
        retrieval request for AssetInstantPortDeviceTypesConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: |
            List of Index. Index represents Device type index.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        name:
          description: >
            List of Name. Name represents Device type name (must be unique
            within a given profile).
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        profile_config_id:
          description: >
            List of ProfileConfigId. ProfileConfigId represents Instant port
            profile Config Id
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInstantPortDeviceTypesConfigGetRequest
      type: object
    AssetInstantPortDeviceTypesConfigGetResponse:
      properties:
        AssetInstantPortDeviceTypesConfig:
          items:
            $ref: "#/components/schemas/AssetInstantPortDeviceTypesConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInstantPortDeviceTypesConfigGetResponse
      type: object
    AssetInstantPortMatchConditionConfig:
      description: AssetInstantPortMatchConditionConfig - DB Model for the match
        conditions configuration for IPP
      nullable: true
      properties:
        category:
          description: >-
            The match category associated with the Instant Port device type. The
            	LLDP_MAC_DEVTYPE category can not be used for profile creation or
            	modification. In this case the LLDP_MAC and LLDP_DEVTYPE categories and
            	data are provided as separate entries in a matchList. The combined
            	LLDP_MAC_DEVTYPE category is used to display state information
            	to indicate that a user has matched on both categories.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_types_config_id:
          description: Instant Port Device Types Config Id
          type: string
        extreme_switch_type:
          description: Single device type for use with the match category EXTREME_SWITCH.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        instant_port_role:
          description: |-
            The role to be matched in an authentication response. The role '*'
            	is considered to be a wildcard role and therefore matches any role
            	contained in the authentication response. This role is specified in
            	the RADIUS Extreme Security Profile VSA by the 'Role=' tag.
          type: string
        lldp_device_type:
          description: The device types available for use with the match category
            LLDP_DEVTYPE.
          format: int64
          type: integer
        mac_address_and_mask:
          description: >-
            MAC address followed by mask. This is represented as two MAC
            addresses
            	separated by a forward slash. Each MAC address is colon separated and
            	can be upper or lower case.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - category
        - device_types_config_id
      type: object
    AssetInstantPortMatchConditionConfigGetRequest:
      description: AssetInstantPortMatchConditionConfigGetRequest represents the
        retrieval request for AssetInstantPortMatchConditionConfig.
      properties:
        category:
          description: >
            List of Category. Category represents The match category associated
            with the Instant Port device type. The
            	LLDP_MAC_DEVTYPE category can not be used for profile creation or
            	modification. In this case the LLDP_MAC and LLDP_DEVTYPE categories and
            	data are provided as separate entries in a matchList. The combined
            	LLDP_MAC_DEVTYPE category is used to display state information
            	to indicate that a user has matched on both categories.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_types_config_id:
          description: >
            List of DeviceTypesConfigId. DeviceTypesConfigId represents Instant
            Port Device Types Config Id
          items:
            type: string
          type: array
        extreme_switch_type:
          description: >
            List of ExtremeSwitchType. ExtremeSwitchType represents Single
            device type for use with the match category EXTREME_SWITCH.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instant_port_role:
          description: >
            List of InstantPortRole. InstantPortRole represents The role to be
            matched in an authentication response. The role '*'
            	is considered to be a wildcard role and therefore matches any role
            	contained in the authentication response. This role is specified in
            	the RADIUS Extreme Security Profile VSA by the 'Role=' tag.
          items:
            type: string
          type: array
        lldp_device_type:
          description: >
            List of LldpDeviceType. LldpDeviceType represents The device types
            available for use with the match category LLDP_DEVTYPE.
          items:
            format: int64
            type: integer
          type: array
        mac_address_and_mask:
          description: >
            List of MacAddressAndMask. MacAddressAndMask represents MAC address
            followed by mask. This is represented as two MAC addresses
            	separated by a forward slash. Each MAC address is colon separated and
            	can be upper or lower case.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInstantPortMatchConditionConfigGetRequest
      type: object
    AssetInstantPortMatchConditionConfigGetResponse:
      properties:
        AssetInstantPortMatchConditionConfig:
          items:
            $ref: "#/components/schemas/AssetInstantPortMatchConditionConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInstantPortMatchConditionConfigGetResponse
      type: object
    AssetInstantPortProfileConfig:
      description: AssetInstantPortProfileConfig - DB Model for profile settings for
        instant port DeviceTypes can be created while creating
        AssetInstantPortProfileConfig.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_types:
          items:
            $ref: "#/components/schemas/AssetInstantPortDeviceTypesConfig"
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        instant_port_config_id:
          description: Instant port Config Id
          type: string
        name:
          description: Instant port profile name (must be unique on the device).
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_list:
          description: An object representing a list of ports assigned to a profile.
          items:
            type: string
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - instant_port_config_id
        - name
      type: object
    AssetInstantPortProfileConfigGetRequest:
      description: AssetInstantPortProfileConfigGetRequest represents the retrieval
        request for AssetInstantPortProfileConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instant_port_config_id:
          description: >
            List of InstantPortConfigId. InstantPortConfigId represents Instant
            port Config Id
          items:
            type: string
          type: array
        name:
          description: >
            List of Name. Name represents Instant port profile name (must be
            unique on the device).
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_list:
          description: >
            List of PortList. PortList represents An object representing a list
            of ports assigned to a profile.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInstantPortProfileConfigGetRequest
      type: object
    AssetInstantPortProfileConfigGetResponse:
      properties:
        AssetInstantPortProfileConfig:
          items:
            $ref: "#/components/schemas/AssetInstantPortProfileConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInstantPortProfileConfigGetResponse
      type: object
    AssetInterIsisUnicastFibState:
      description: AssetInterIsisUnicastFibState - The object that represents state of
        ISIS IP Unicast-FIB entries.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        destination_ip_address:
          description: Destination IP Address (Ipv4 or Ipv6).
          type: string
        destination_ip_version:
          description: IP Version.
          format: int64
          type: integer
        destination_is_id:
          description: >-
            Destination ISID. A value that uniquely identifies the Layer 2 and
            Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        destination_mask_length:
          description: Destination prefix-length of the IP Unicast FIB entry.
          format: int64
          minimum: 0
          type: integer
        device_id:
          description: Device Id.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        is_id:
          description: >-
            ISID. A value that uniquely identifies the Layer 2 and Layer 3
            services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        metric_type:
          description: IP Metric Type of the IP Unicast FIB entry.
          format: int64
          type: integer
        next_hop_bmac:
          description: Next hop bmac. Colon separated MAC address in upper or lower case.
          type: string
        next_hop_name:
          description: The nexthop hostname of the IP Unicast FIB entry.
          type: string
        out_going_port:
          description: |-
            Outgoing port of the IP Unicast FIB entry; it can be one and only
            	one physical port in format [slot]:[port] in case it is not mlt
            	member or, in case it is mlt member, this field displays mlt name
            	instead of physical port.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        preference:
          description: IP Route preference of the IP Unicast FIB entry.
          format: int64
          minimum: 0
          type: integer
        prefix_cost:
          description: Prefix cost of the IP Unicast FIB entry.
          format: int64
          minimum: 0
          type: integer
        sp_bm_cost:
          description: BMAC cost of the IP Unicast FIB entry.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
        vrf_name:
          description: VRF name.
          type: string
      required:
        - destination_ip_address
        - device_id
        - vlan_number
      type: object
    AssetInterIsisUnicastFibStateGetRequest:
      description: AssetInterIsisUnicastFibStateGetRequest represents the retrieval
        request for AssetInterIsisUnicastFibState.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        destination_ip_address:
          description: >
            List of DestinationIpAddress. DestinationIpAddress represents
            Destination IP Address (Ipv4 or Ipv6).
          items:
            type: string
          type: array
        destination_ip_version:
          description: >
            List of DestinationIpVersion. DestinationIpVersion represents IP
            Version.
          items:
            format: int64
            type: integer
          type: array
        destination_is_id:
          description: >
            List of DestinationIsId. DestinationIsId represents Destination
            ISID. A value that uniquely identifies the Layer 2 and Layer 3
            services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        destination_mask_length:
          description: >
            List of DestinationMaskLength. DestinationMaskLength represents
            Destination prefix-length of the IP Unicast FIB entry.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        is_id:
          description: >
            List of IsId. IsId represents ISID. A value that uniquely identifies
            the Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        metric_type:
          description: >
            List of MetricType. MetricType represents IP Metric Type of the IP
            Unicast FIB entry.
          items:
            format: int64
            type: integer
          type: array
        next_hop_bmac:
          description: >
            List of NextHopBmac. NextHopBmac represents Next hop bmac. Colon
            separated MAC address in upper or lower case.
          items:
            type: string
          type: array
        next_hop_name:
          description: >
            List of NextHopName. NextHopName represents The nexthop hostname of
            the IP Unicast FIB entry.
          items:
            type: string
          type: array
        out_going_port:
          description: >
            List of OutGoingPort. OutGoingPort represents Outgoing port of the
            IP Unicast FIB entry; it can be one and only
            	one physical port in format [slot]:[port] in case it is not mlt
            	member or, in case it is mlt member, this field displays mlt name
            	instead of physical port.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        preference:
          description: >
            List of Preference. Preference represents IP Route preference of the
            IP Unicast FIB entry.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        prefix_cost:
          description: >
            List of PrefixCost. PrefixCost represents Prefix cost of the IP
            Unicast FIB entry.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sp_bm_cost:
          description: >
            List of SpBmCost. SpBmCost represents BMAC cost of the IP Unicast
            FIB entry.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name.
          items:
            type: string
          type: array
      title: AssetInterIsisUnicastFibStateGetRequest
      type: object
    AssetInterIsisUnicastFibStateGetResponse:
      properties:
        AssetInterIsisUnicastFibState:
          items:
            $ref: "#/components/schemas/AssetInterIsisUnicastFibState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInterIsisUnicastFibStateGetResponse
      type: object
    AssetInterface:
      description: AssetInterface - This object represents the list of Interfaces
        information.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - interface_name
        - interface_type
      type: object
    AssetInterfaceGetRequest:
      description: AssetInterfaceGetRequest represents the retrieval request for
        AssetInterface.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInterfaceGetRequest
      type: object
    AssetInterfaceGetResponse:
      properties:
        AssetInterface:
          items:
            $ref: "#/components/schemas/AssetInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInterfaceGetResponse
      type: object
    AssetInterfaceIpAddress:
      description: AssetInterfaceIpAddress - DB Model for VRRP interface ip address
      properties:
        address:
          description: IP address (Ipv4 or Ipv6) based on the IP-version.
          type: string
        asset_interface_id:
          description: Interface Id.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dynamic:
          description: Is Dynamic flag.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_version:
          description: IP Version.
          format: int64
          type: integer
        is_primary:
          description: Is primary flag.
          type: boolean
        mask_length:
          description: Prefix-length for the IP address.
          format: int64
          minimum: 0
          type: integer
        name:
          description: Interface Ip Address name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - address
        - asset_interface_id
        - ip_version
      type: object
    AssetInterfaceIpAddressGetRequest:
      description: AssetInterfaceIpAddressGetRequest represents the retrieval request
        for AssetInterfaceIpAddress.
      properties:
        address:
          description: >
            List of Address. Address represents IP address (Ipv4 or Ipv6) based
            on the IP-version.
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dynamic:
          description: |
            List of Dynamic. Dynamic represents Is Dynamic flag.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_version:
          description: |
            List of IpVersion. IpVersion represents IP Version.
          items:
            format: int64
            type: integer
          type: array
        is_primary:
          description: |
            List of IsPrimary. IsPrimary represents Is primary flag.
          items:
            type: boolean
          type: array
        mask_length:
          description: >
            List of MaskLength. MaskLength represents Prefix-length for the IP
            address.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        name:
          description: |
            List of Name. Name represents Interface Ip Address name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInterfaceIpAddressGetRequest
      type: object
    AssetInterfaceIpAddressGetResponse:
      properties:
        AssetInterfaceIpAddress:
          items:
            $ref: "#/components/schemas/AssetInterfaceIpAddress"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInterfaceIpAddressGetResponse
      type: object
    AssetInterfaceVlanMap:
      description: AssetInterfaceVlanMap - The object that represents settings for
        Interface Vlan Map configuration.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_vlan_properties_id:
          description: Interface Vlan Properties Id
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config_id:
          description: Vlan Config Id
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vlan_types:
          items:
            format: int64
            type: integer
          type: array
      required:
        - interface_vlan_properties_id
        - vlan_config_id
        - vlan_number
        - vlan_types
      type: object
    AssetInterfaceVlanMapGetRequest:
      description: AssetInterfaceVlanMapGetRequest represents the retrieval request
        for AssetInterfaceVlanMap.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_vlan_properties_id:
          description: >
            List of InterfaceVlanPropertiesId. InterfaceVlanPropertiesId
            represents Interface Vlan Properties Id
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_config_id:
          description: |
            List of VlanConfigId. VlanConfigId represents Vlan Config Id
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_types:
          description: >
            List of VlanTypes. VlanTypes represents Vlan interface map vlan
            types.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
      title: AssetInterfaceVlanMapGetRequest
      type: object
    AssetInterfaceVlanMapGetResponse:
      properties:
        AssetInterfaceVlanMap:
          items:
            $ref: "#/components/schemas/AssetInterfaceVlanMap"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInterfaceVlanMapGetResponse
      type: object
    AssetInterfaceVlanProperties:
      description: AssetInterfaceVlanProperties - This object represents the
        interface-vlan settings. Vlans can be created while creating
        AssetInterfaceVlanProperties.
      properties:
        allow_dynamic:
          description: >-
            A value of true indicates that this port is statically added as a
            tagged uplink port
            	to all dynamically created VLAN's. This property only applies to Switch Engine.
          type: boolean
        asset_interface_id:
          description: Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        is_brouter:
          default: false
          description: A value of true indicates this port is part of a BROUTER type VLAN.
            This is VOSS specific.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_type:
          description: >-
            The port type (ACCESS or TRUNK). An access port can not be assigned
            to a VLAN as tagged.
            	For VOSS, tagging can be enabled on the port which sets it as a trunk port.  For EXOS,
            	there is no explicit configuration of a trunk port so this setting can be set to trunk
            	or set to access if the port is not assigned to a VLAN as tagged.
          format: int64
          type: integer
        port_vlan:
          description: >-
            Untagged packets received on this port are assigned to the VLAN
            specified by this property. A value of 0,
            	is used to unset this parameter.
          format: int64
          minimum: 0
          type: integer
        pvlan:
          description: |-
            The private vlan setting for the port. This is a Fabric Engine
            	specific setting.
          format: int64
          type: integer
        spb_mcast_enabled:
          default: false
          description: |-
            A value of true indicates that this port is SPB Multicast enabled.
            	This is a Fabric Engine specific setting.
          type: boolean
        untagged_egress:
          default: false
          description: >-
            A value of true indicates that this port will transmit the portVlan
            as untagged on a
            	trunk port. This is a Fabric Engine specific setting.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlans:
          items:
            $ref: "#/components/schemas/AssetInterfaceVlanMap"
          nullable: true
          type: array
      required:
        - allow_dynamic
        - device_id
        - interface_name
        - interface_type
        - port_type
      type: object
    AssetInterfaceVlanPropertiesGetRequest:
      description: AssetInterfaceVlanPropertiesGetRequest represents the retrieval
        request for AssetInterfaceVlanProperties.
      properties:
        allow_dynamic:
          description: >
            List of AllowDynamic. AllowDynamic represents A value of true
            indicates that this port is statically added as a tagged uplink port
            	to all dynamically created VLAN's. This property only applies to Switch Engine.
          items:
            type: boolean
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        is_brouter:
          description: >
            List of IsBrouter. IsBrouter represents A value of true indicates
            this port is part of a BROUTER type VLAN. This is VOSS specific.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_type:
          description: >
            List of PortType. PortType represents The port type (ACCESS or
            TRUNK). An access port can not be assigned to a VLAN as tagged.
            	For VOSS, tagging can be enabled on the port which sets it as a trunk port.  For EXOS,
            	there is no explicit configuration of a trunk port so this setting can be set to trunk
            	or set to access if the port is not assigned to a VLAN as tagged.
          items:
            format: int64
            type: integer
          type: array
        port_vlan:
          description: >
            List of PortVlan. PortVlan represents Untagged packets received on
            this port are assigned to the VLAN specified by this property. A
            value of 0,
            	is used to unset this parameter.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        pvlan:
          description: >
            List of Pvlan. Pvlan represents The private vlan setting for the
            port. This is a Fabric Engine
            	specific setting.
          items:
            format: int64
            type: integer
          type: array
        spb_mcast_enabled:
          description: >
            List of SpbMcastEnabled. SpbMcastEnabled represents A value of true
            indicates that this port is SPB Multicast enabled.
            	This is a Fabric Engine specific setting.
          items:
            type: boolean
          type: array
        untagged_egress:
          description: >
            List of UntaggedEgress. UntaggedEgress represents A value of true
            indicates that this port will transmit the portVlan as untagged on a
            	trunk port. This is a Fabric Engine specific setting.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetInterfaceVlanPropertiesGetRequest
      type: object
    AssetInterfaceVlanPropertiesGetResponse:
      properties:
        AssetInterfaceVlanProperties:
          items:
            $ref: "#/components/schemas/AssetInterfaceVlanProperties"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetInterfaceVlanPropertiesGetResponse
      type: object
    AssetIpArpState:
      description: AssetIpArpState - This object represents the list of the Ip-Arp
        State information.
      properties:
        arp_type:
          description: Type of the ARP entry
          format: int64
          type: integer
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name.
          type: string
        interface_type:
          description: This attribute indicates the interface type.
          type: string
        ip_address:
          description: Ipv4 Address
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        physical_address:
          description: |-
            The media-dependent physical address. In the ethernet
            	case it is a MAC address.
          type: string
        port:
          description: |-
            The port on which the ARP is learned. This can also be a
            	Multi-Link Trunk (MLT) - Fabric Engine (FE) only
          type: string
        ttl:
          description: |-
            The numeric value of the ARP Time To Live (TTL) in seconds.
            	This field only applies to dynamic entries. For Switch Engine
            	(EXOS) this field only applies to entries with timeout enabled.
          type: integer
        tunnel:
          description: |-
            If the ARP is learned from a SPBM network, instead of using
            	port to track where the ARP comes from, the backbone MAC address
            	is used to track it - Fabric Engine (FE) only
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_name:
          description: This object represents a Virtual Router (VR) name
          type: string
      required:
        - arp_type
        - asset_device_id
        - interface_name
        - interface_type
        - ip_address
        - physical_address
        - vrf_name
      type: object
    AssetIpArpStateGetRequest:
      description: AssetIpArpStateGetRequest represents the retrieval request for
        AssetIpArpState.
      properties:
        arp_type:
          description: |
            List of ArpType. ArpType represents Type of the ARP entry
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents This attribute
            indicates the interface type.
          items:
            type: string
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents Ipv4 Address
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        physical_address:
          description: >
            List of PhysicalAddress. PhysicalAddress represents The
            media-dependent physical address. In the ethernet
            	case it is a MAC address.
          items:
            type: string
          type: array
        port:
          description: >
            List of Port. Port represents The port on which the ARP is learned.
            This can also be a
            	Multi-Link Trunk (MLT) - Fabric Engine (FE) only
          items:
            type: string
          type: array
        ttl:
          description: >
            List of Ttl. Ttl represents The numeric value of the ARP Time To
            Live (TTL) in seconds.
            	This field only applies to dynamic entries. For Switch Engine
            	(EXOS) this field only applies to entries with timeout enabled.
          items:
            type: integer
          type: array
        tunnel:
          description: >
            List of Tunnel. Tunnel represents If the ARP is learned from a SPBM
            network, instead of using
            	port to track where the ARP comes from, the backbone MAC address
            	is used to track it - Fabric Engine (FE) only
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_name:
          description: >
            List of VrfName. VrfName represents This object represents a Virtual
            Router (VR) name
          items:
            type: string
          type: array
      title: AssetIpArpStateGetRequest
      type: object
    AssetIpArpStateGetResponse:
      properties:
        AssetIpArpState:
          items:
            $ref: "#/components/schemas/AssetIpArpState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIpArpStateGetResponse
      type: object
    AssetIsidMacFdbState:
      description: AssetIsidMacFdbState - This object represents the State information
        for the Forwarding Database based on ISID.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        cvid:
          description: >-
            Used to identify customer vid of the switched uni port that
            	this entry is associated with. 4095 value is not used. 4096 value is
            	reserved for untagged case. 0 means there is no switched uni association.
            	This attribute applies to Fabric Engine only.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: |-
            Either the value 0, or the port name or associated LAG ID
            	of the port on which a frame having a source address equal to the
            	value of the corresponding MAC addrress has been seen.
            	A value of 0 indicates that the port number has not been
            	learned but that the bridge does have some forwarding/filtering
            	information about this address.
            	A value of 'cpp' indicates a self-assigned MAC address.
          type: string
        interface_type:
          description: |-
            This attribute indicates the interface type on which the MAC
            	is learnt. LAG type is applicable for Fabric Engine only.
          format: int64
          type: integer
        is_id:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        mac_address:
          description: |-
            A unicast MAC address for which the bridge has
            	forwarding and/or filtering information.
          type: string
        origin:
          description: |-
            This attribute indicates whether the MAC address was
            	learned locally or on NNI port from a remote destination.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        remote_mac_bvlan_id:
          description: |-
            The value indicates the backbone VLAN ID for this MAC address
            	if it is learned remotely. Otherwise a value of 0 is displayed.
          format: int64
          minimum: 0
          type: integer
        remote_mac_destination_address:
          description: >-
            A provider MAC address for which the bridge has forwarding
            	and/or filtering information. This value is displayed only if the port
            	type is remote otherwise a value of 0 is displayed.
          type: string
        remote_mac_destination_system_name:
          description: |-
            Primary system name of the node where MAC address entry was
            	learned from in case of remote otherwise NULL is shown.
          type: string
        status:
          description: |-
            This attribute indicates the status of the Forwarding
            	Database entry. This attribute applies to Fabric Engine only.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - interface_name
        - interface_type
        - is_id
        - mac_address
        - status
      type: object
    AssetIsidMacFdbStateGetRequest:
      description: AssetIsidMacFdbStateGetRequest represents the retrieval request for
        AssetIsidMacFdbState.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        cvid:
          description: >
            List of Cvid. Cvid represents Used to identify customer vid of the
            switched uni port that
            	this entry is associated with. 4095 value is not used. 4096 value is
            	reserved for untagged case. 0 means there is no switched uni association.
            	This attribute applies to Fabric Engine only.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Either the value 0,
            or the port name or associated LAG ID
            	of the port on which a frame having a source address equal to the
            	value of the corresponding MAC addrress has been seen.
            	A value of 0 indicates that the port number has not been
            	learned but that the bridge does have some forwarding/filtering
            	information about this address.
            	A value of 'cpp' indicates a self-assigned MAC address.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents This attribute
            indicates the interface type on which the MAC
            	is learnt. LAG type is applicable for Fabric Engine only.
          items:
            format: int64
            type: integer
          type: array
        is_id:
          description: >
            List of IsId. IsId represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        mac_address:
          description: >
            List of MacAddress. MacAddress represents A unicast MAC address for
            which the bridge has
            	forwarding and/or filtering information.
          items:
            type: string
          type: array
        origin:
          description: >
            List of Origin. Origin represents This attribute indicates whether
            the MAC address was
            	learned locally or on NNI port from a remote destination.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        remote_mac_bvlan_id:
          description: >
            List of RemoteMacBvlanId. RemoteMacBvlanId represents The value
            indicates the backbone VLAN ID for this MAC address
            	if it is learned remotely. Otherwise a value of 0 is displayed.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        remote_mac_destination_address:
          description: >
            List of RemoteMacDestinationAddress. RemoteMacDestinationAddress
            represents A provider MAC address for which the bridge has
            forwarding
            	and/or filtering information. This value is displayed only if the port
            	type is remote otherwise a value of 0 is displayed.
          items:
            type: string
          type: array
        remote_mac_destination_system_name:
          description: >
            List of RemoteMacDestinationSystemName.
            RemoteMacDestinationSystemName represents Primary system name of the
            node where MAC address entry was
            	learned from in case of remote otherwise NULL is shown.
          items:
            type: string
          type: array
        status:
          description: >
            List of Status. Status represents This attribute indicates the
            status of the Forwarding
            	Database entry. This attribute applies to Fabric Engine only.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsidMacFdbStateGetRequest
      type: object
    AssetIsidMacFdbStateGetResponse:
      properties:
        AssetIsidMacFdbState:
          items:
            $ref: "#/components/schemas/AssetIsidMacFdbState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsidMacFdbStateGetResponse
      type: object
    AssetIsisAdjacency:
      description: >-
        AssetIsisAdjacency - This object represents the state object of
        Intermediate System to

        Intermediate System(ISIS) adjacency per ISIS area.
      properties:
        area_name:
          description: |-
            The ISIS system area-name. This attribute is available in both areas
            	and if the device does not support multi-area, it won`t be included
            	in the home response.
          type: string
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_device_id:
          description: Device Id
          type: string
        asset_isis_global_config_id:
          description: ISIS Global Config Id
          type: string
        asset_isis_global_state_id:
          description: ISIS Global State Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        hold_timer:
          description: The hold time in seconds for this adjacency.
          format: int64
          type: integer
        host_name:
          description: |-
            The hostname listed in LSP or the system name if host
            	name is not configured.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: The name of the interface to which the circuit corresponds.
          type: string
        last_up_time:
          description: >-
            Time elapsed, measured in hundredths of a second,
            	since the moment the adjacency most recently entered the state 'up'.
          format: int64
          type: integer
        neighbor_priority:
          description: |-
            Priority of the neighboring Intermediate System for
            	becoming the Designated Intermediate System..
          format: int64
          type: integer
        neighbor_sys_id:
          description: The system ID of the neighboring Intermediate System.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        parallel_active_flag:
          description: |-
            A flag to indicate current adjacency is active or not
            	among all the parallel adjacencies between two nodes.
          type: boolean
        state:
          description: The state of the adjacency.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        usage:
          description: The ISIS level of the circuit. Currently Level 1 is supported.
          format: int64
          type: integer
      required:
        - asset_device_id
        - asset_isis_global_config_id
      type: object
    AssetIsisAdjacencyGetRequest:
      description: AssetIsisAdjacencyGetRequest represents the retrieval request for
        AssetIsisAdjacency.
      properties:
        area_name:
          description: >
            List of AreaName. AreaName represents The ISIS system area-name.
            This attribute is available in both areas
            	and if the device does not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        asset_isis_global_config_id:
          description: >
            List of AssetIsisGlobalConfigId. AssetIsisGlobalConfigId represents
            ISIS Global Config Id
          items:
            type: string
          type: array
        asset_isis_global_state_id:
          description: >
            List of AssetIsisGlobalStateId. AssetIsisGlobalStateId represents
            ISIS Global State Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        hold_timer:
          description: >
            List of HoldTimer. HoldTimer represents The hold time in seconds for
            this adjacency.
          items:
            format: int64
            type: integer
          type: array
        host_name:
          description: >
            List of HostName. HostName represents  The hostname listed in LSP or
            the system name if host
            	name is not configured.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents The name of the
            interface to which the circuit corresponds.
          items:
            type: string
          type: array
        last_up_time:
          description: >
            List of LastUpTime. LastUpTime represents Time elapsed, measured in
            hundredths of a second,
            	since the moment the adjacency most recently entered the state 'up'.
          items:
            format: int64
            type: integer
          type: array
        neighbor_priority:
          description: >
            List of NeighborPriority. NeighborPriority represents Priority of
            the neighboring Intermediate System for
            	becoming the Designated Intermediate System..
          items:
            format: int64
            type: integer
          type: array
        neighbor_sys_id:
          description: >
            List of NeighborSysId. NeighborSysId represents The system ID of the
            neighboring Intermediate System.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        parallel_active_flag:
          description: >
            List of ParallelActiveFlag. ParallelActiveFlag represents  A flag to
            indicate current adjacency is active or not
            	among all the parallel adjacencies between two nodes.
          items:
            type: boolean
          type: array
        state:
          description: |
            List of State. State represents The state of the adjacency.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        usage:
          description: >
            List of Usage. Usage represents The ISIS level of the circuit.
            Currently Level 1 is supported.
          items:
            format: int64
            type: integer
          type: array
      title: AssetIsisAdjacencyGetRequest
      type: object
    AssetIsisAdjacencyGetResponse:
      properties:
        AssetIsisAdjacency:
          items:
            $ref: "#/components/schemas/AssetIsisAdjacency"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisAdjacencyGetResponse
      type: object
    AssetIsisCircuitConfig:
      description: AssetIsisCircuitConfig - This object represents the ISIS
        configuration per interface. AssetIsisCircuitSpbmSetting can be created
        while creating AssetIsisCircuitConfig.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_interface_id:
          description: Interface Id
          type: string
        asset_isis_circuit_spbm_setting:
          $ref: "#/components/schemas/AssetIsisCircuitSpbmSetting"
        asset_isis_global_config_id:
          description: ISIS Global Config Id
          type: string
        auth_key_id:
          description: |-
            Authentication key id related to IIH key.
            	This can be set only if the authKeyType is HMAC_MD5.
            	The value of 0 indicates key ID is not configured.
          type: integer
        auth_key_is_encrypted:
          description: >-
            Specifies if the authentication key is encrypted (true) or clear
            text (false).
            	Currently, this attribute is read-only on Fabric Engine.
          type: boolean
        auth_key_type:
          description: |-
            Authentication type for IIH to be sent/received
            	on the circuit/interface.
          format: int64
          type: integer
        auth_key_value:
          description: |-
            Authentication key.
            	Currently, the configured authentication key is always
            	encrypted upon retrieval on Fabric Engine.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: ISIS circuit state.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: ISIS circuit type.
          format: int64
          type: integer
        l1_hello_multiplier:
          description: |-
            This value is multiplied by the corresponding HelloTimer
            	and the result in seconds (rounded up) is used as the
            	holding time in transmitted hellos, to be used by
            	receivers of hello packets from this IS.
          type: integer
        l1_hello_timer:
          description: |-
            Maximum period, in milliseconds, between IIH PDUs
            	on multiaccess networks at this level for LANs.
            	The value at L1 is used as the period between
            	Hellos on L1L2 point to point circuits.  Setting
            	this value at level 2 on an L1L2 point to point
            	circuit will result in an error of InconsistentValue.
            	This object follows the resettingTimer behavior.
          type: integer
        l1_is_priority:
          description: |-
            The priority for becoming LAN Designated
            	Intermediate System at this level.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - area_type
        - asset_isis_global_config_id
        - interface_name
        - interface_type
      type: object
    AssetIsisCircuitConfigGetRequest:
      description: AssetIsisCircuitConfigGetRequest represents the retrieval request
        for AssetIsisCircuitConfig.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        asset_isis_global_config_id:
          description: >
            List of AssetIsisGlobalConfigId. AssetIsisGlobalConfigId represents
            ISIS Global Config Id
          items:
            type: string
          type: array
        auth_key_id:
          description: >
            List of AuthKeyId. AuthKeyId represents Authentication key id
            related to IIH key.
            	This can be set only if the authKeyType is HMAC_MD5.
            	The value of 0 indicates key ID is not configured.
          items:
            type: integer
          type: array
        auth_key_is_encrypted:
          description: >
            List of AuthKeyIsEncrypted. AuthKeyIsEncrypted represents Specifies
            if the authentication key is encrypted (true) or clear text (false).
            	Currently, this attribute is read-only on Fabric Engine.
          items:
            type: boolean
          type: array
        auth_key_type:
          description: >
            List of AuthKeyType. AuthKeyType represents Authentication type for
            IIH to be sent/received
            	on the circuit/interface.
          items:
            format: int64
            type: integer
          type: array
        auth_key_value:
          description: |
            List of AuthKeyValue. AuthKeyValue represents Authentication key.
            	Currently, the configured authentication key is always
            	encrypted upon retrieval on Fabric Engine.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents ISIS circuit state.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents ISIS circuit type.
          items:
            format: int64
            type: integer
          type: array
        l1_hello_multiplier:
          description: >
            List of L1HelloMultiplier. L1HelloMultiplier represents This value
            is multiplied by the corresponding HelloTimer
            	and the result in seconds (rounded up) is used as the
            	holding time in transmitted hellos, to be used by
            	receivers of hello packets from this IS.
          items:
            type: integer
          type: array
        l1_hello_timer:
          description: >
            List of L1HelloTimer. L1HelloTimer represents Maximum period, in
            milliseconds, between IIH PDUs
            	on multiaccess networks at this level for LANs.
            	The value at L1 is used as the period between
            	Hellos on L1L2 point to point circuits.  Setting
            	this value at level 2 on an L1L2 point to point
            	circuit will result in an error of InconsistentValue.
            	This object follows the resettingTimer behavior.
          items:
            type: integer
          type: array
        l1_is_priority:
          description: >
            List of L1IsPriority. L1IsPriority represents The priority for
            becoming LAN Designated
            	Intermediate System at this level.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisCircuitConfigGetRequest
      type: object
    AssetIsisCircuitConfigGetResponse:
      properties:
        AssetIsisCircuitConfig:
          items:
            $ref: "#/components/schemas/AssetIsisCircuitConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisCircuitConfigGetResponse
      type: object
    AssetIsisCircuitSpbmSetting:
      description: AssetIsisCircuitSpbmSetting - This object represents the ISIS SPBM
        interface configuration.
      properties:
        asset_isis_circuit_config_id:
          description: ISIS Circuit Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: SPBM Instance State.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_type:
          description: ISIS circuit type.
          format: int64
          type: integer
        l1_metric:
          description: The metric value of this circuit for this level.
          type: integer
        l1_metric_auto:
          description: The l1 metric auto state of this circuit for this level.
          type: boolean
        l1_metric_operational:
          description: The operational metric value of this circuit for this level.
          type: integer
        origin:
          description: SPBM instance origin.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        spbm_id:
          description: SPBM Id
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_isis_circuit_config_id
      type: object
    AssetIsisCircuitSpbmSettingGetRequest:
      description: AssetIsisCircuitSpbmSettingGetRequest represents the retrieval
        request for AssetIsisCircuitSpbmSetting.
      properties:
        asset_isis_circuit_config_id:
          description: >
            List of AssetIsisCircuitConfigId. AssetIsisCircuitConfigId
            represents ISIS Circuit Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SPBM Instance State.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents ISIS circuit type.
          items:
            format: int64
            type: integer
          type: array
        l1_metric:
          description: >
            List of L1Metric. L1Metric represents The metric value of this
            circuit for this level.
          items:
            type: integer
          type: array
        l1_metric_auto:
          description: >
            List of L1MetricAuto. L1MetricAuto represents The l1 metric auto
            state of this circuit for this level.
          items:
            type: boolean
          type: array
        l1_metric_operational:
          description: >
            List of L1MetricOperational. L1MetricOperational represents The
            operational metric value of this circuit for this level.
          items:
            type: integer
          type: array
        origin:
          description: |
            List of Origin. Origin represents SPBM instance origin.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        spbm_id:
          description: |
            List of SpbmId. SpbmId represents SPBM Id
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisCircuitSpbmSettingGetRequest
      type: object
    AssetIsisCircuitSpbmSettingGetResponse:
      properties:
        AssetIsisCircuitSpbmSetting:
          items:
            $ref: "#/components/schemas/AssetIsisCircuitSpbmSetting"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisCircuitSpbmSettingGetResponse
      type: object
    AssetIsisCircuitState:
      description: AssetIsisCircuitState - This object represents the ISIS operational
        information per interface.
      properties:
        adjancencies_num:
          description: Number of adjacencies.
          type: integer
        adjancencies_up_num:
          description: Number of adjacencies which are up.
          type: integer
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_interface_id:
          description: Interface Id
          type: string
        asset_isis_global_config_id:
          description: ISIS Global Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled_operational:
          description: ISIS circuit operational enable state.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: ISIS circuit type.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        sd_wan_state:
          description: Identifies SD-WAN operational state.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - area_type
        - asset_isis_global_config_id
        - interface_name
        - interface_type
      type: object
    AssetIsisCircuitStateGetRequest:
      description: AssetIsisCircuitStateGetRequest represents the retrieval request
        for AssetIsisCircuitState.
      properties:
        adjancencies_num:
          description: >
            List of AdjancenciesNum. AdjancenciesNum represents Number of
            adjacencies.
          items:
            type: integer
          type: array
        adjancencies_up_num:
          description: >
            List of AdjancenciesUpNum. AdjancenciesUpNum represents Number of
            adjacencies which are up.
          items:
            type: integer
          type: array
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        asset_isis_global_config_id:
          description: >
            List of AssetIsisGlobalConfigId. AssetIsisGlobalConfigId represents
            ISIS Global Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled_operational:
          description: >
            List of EnabledOperational. EnabledOperational represents ISIS
            circuit operational enable state.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents ISIS circuit type.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sd_wan_state:
          description: >
            List of SdWanState. SdWanState represents Identifies SD-WAN
            operational state.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisCircuitStateGetRequest
      type: object
    AssetIsisCircuitStateGetResponse:
      properties:
        AssetIsisCircuitState:
          items:
            $ref: "#/components/schemas/AssetIsisCircuitState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisCircuitStateGetResponse
      type: object
    AssetIsisGlobalConfig:
      description: >-
        AssetIsisGlobalConfig - This object represents the list of ISIS global
        settings configured per each ISIS area

        type: home and remote area. The area type is present in the payload and
        based on that,

        the attributes for each type is different. For devices which do not
        support multi-area,

        response will not include remote area and in the home area will not
        include areaName,

        areaVNodeNickname, areaVNodeSysId and areaVNodeSysName AssetSpbmInstance
        can be created while creating AssetIsisGlobalConfig.
      properties:
        area_name:
          description: |-
            The ISIS system area-name. This attribute is available in both areas
            	and if the device does not support multi-area, it won`t be included
            	in the home response.
          type: string
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        area_vnode_nickname:
          description: >-
            Virtual node's nickname is used to calculate ISID multicast mac
            address.
            	Values to be represented as x.xx.xx. This attribute is available in both
            	areas and if the device does not support multi-area, it won`t be included
            	in the home response.
          type: string
        area_vnode_sys_id:
          description: >-
            The ISIS system id of the virtual node. This attribute is available
            in both
            	areas and if the device does not support multi-area, it won`t be included
            	in the home response.
          type: string
        area_vnode_sys_name:
          description: >-
            The ISIS system name of the virtual node. This attribute is
            available in both
            	areas and if the device does not support multi-area, it won`t be included
            	in the home response.
          type: string
        asset_device_id:
          description: Device Id
          type: string
        asset_spbm_instance:
          items:
            $ref: "#/components/schemas/AssetSpbmInstance"
          nullable: true
          type: array
        backbone_enabled:
          description: |-
            A value of True will allow a non DVR BEB to join the DVR
            	backbone tree in order to receive redistributed DVR Host
            	Routes from all the different DVR Controllers in the SPB
            	Network. This attribute is available in home area.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_source_address:
          description: |-
            ISIS SPBM Ipv4 address. Define an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          type: string
        ip_tunnel_overlay_enabled:
          description: |-
            A value of True will provide the ability of setting the
            	tunnel-source-address on interfaces belonging to a VRF
            	with I-SID attached. This attribute is available in home
            	area.
          type: boolean
        ip_tunnel_source_address:
          description: |-
            ISIS tunnel source Ipv4 address. This attribute is
            	available in home area.
          type: string
        ip_tunnel_vrf:
          description: |-
            VRF name associated with IP tunnel. This attribute is
            	available in home area.
          type: string
        manual_area_address:
          description: |-
            A manually configured area address for this system. This
            	attribute is available in both areas.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        sys_admin_state:
          description: |-
            The administrative state of this Intermediate System.
            	Setting this object to the value 'on' when its current
            	value is 'off' enables the Intermediate System. This
            	attribute is available in both areas.
          format: int64
          type: integer
        sys_id:
          description: |-
            The ID for this Intermediate System. This value is
            	appended to each of the area addresses to form the
            	Network Entity Titles. This attribute is available
            	in both areas.
          type: string
        sys_level_set_overload_enabled:
          description: |-
            A value of True will set the overload bit for the level.
            	The overload bit will continue to be set if the
            	implementation runs out of memory, independent of
            	this variable. It may also be set manually independent
            	of this variable, using the sysLevelSetOverloadUntil.
            	This attribute is available in home area.
          type: boolean
        sys_level_set_overload_threshold:
          description: |-
            If this object is non-zero, the overload bit is set at
            	this level when the sysAdminState variable goes to
            	state 'on' for this Intermediate System. The overload bit
            	remains set for sysLevelSetOverloadThreshold seconds.
            	When sysLevelSetOverloadUntil seconds have elapsed,
            	the overload flag remains set if the implementation has
            	run out of memory, or if it is set manually using the
            	isisSysLevelSetOverload object. If sysLevelSetOverload
            	is false, the system clears the overload bit when
            	sysLevelSetOverloadUntil seconds have elapsed, if the
            	system has not run out of memory. This attribute is
            	available in home area.
          type: integer
        sys_name:
          description: |-
            The hostname listed in LSP, or the system name if host
            	name is not configured.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetIsisGlobalConfigGetRequest:
      description: AssetIsisGlobalConfigGetRequest represents the retrieval request
        for AssetIsisGlobalConfig.
      properties:
        area_name:
          description: >
            List of AreaName. AreaName represents The ISIS system area-name.
            This attribute is available in both areas
            	and if the device does not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        area_vnode_nickname:
          description: >
            List of AreaVnodeNickname. AreaVnodeNickname represents Virtual
            node's nickname is used to calculate ISID multicast mac address.
            	Values to be represented as x.xx.xx. This attribute is available in both
            	areas and if the device does not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        area_vnode_sys_id:
          description: >
            List of AreaVnodeSysId. AreaVnodeSysId represents The ISIS system id
            of the virtual node. This attribute is available in both
            	areas and if the device does not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        area_vnode_sys_name:
          description: >
            List of AreaVnodeSysName. AreaVnodeSysName represents The ISIS
            system name of the virtual node. This attribute is available in both
            	areas and if the device does not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        backbone_enabled:
          description: >
            List of BackboneEnabled. BackboneEnabled represents A value of True
            will allow a non DVR BEB to join the DVR
            	backbone tree in order to receive redistributed DVR Host
            	Routes from all the different DVR Controllers in the SPB
            	Network. This attribute is available in home area.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_source_address:
          description: >
            List of IpSourceAddress. IpSourceAddress represents ISIS SPBM Ipv4
            address. Define an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          items:
            type: string
          type: array
        ip_tunnel_overlay_enabled:
          description: >
            List of IpTunnelOverlayEnabled. IpTunnelOverlayEnabled represents A
            value of True will provide the ability of setting the
            	tunnel-source-address on interfaces belonging to a VRF
            	with I-SID attached. This attribute is available in home
            	area.
          items:
            type: boolean
          type: array
        ip_tunnel_source_address:
          description: >
            List of IpTunnelSourceAddress. IpTunnelSourceAddress represents ISIS
            tunnel source Ipv4 address. This attribute is
            	available in home area.
          items:
            type: string
          type: array
        ip_tunnel_vrf:
          description: >
            List of IpTunnelVrf. IpTunnelVrf represents VRF name associated with
            IP tunnel. This attribute is
            	available in home area.
          items:
            type: string
          type: array
        manual_area_address:
          description: >
            List of ManualAreaAddress. ManualAreaAddress represents A manually
            configured area address for this system. This
            	attribute is available in both areas.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sys_admin_state:
          description: >
            List of SysAdminState. SysAdminState represents The administrative
            state of this Intermediate System.
            	Setting this object to the value 'on' when its current
            	value is 'off' enables the Intermediate System. This
            	attribute is available in both areas.
          items:
            format: int64
            type: integer
          type: array
        sys_id:
          description: >
            List of SysId. SysId represents The ID for this Intermediate System.
            This value is
            	appended to each of the area addresses to form the
            	Network Entity Titles. This attribute is available
            	in both areas.
          items:
            type: string
          type: array
        sys_level_set_overload_enabled:
          description: >
            List of SysLevelSetOverloadEnabled. SysLevelSetOverloadEnabled
            represents A value of True will set the overload bit for the level.
            	The overload bit will continue to be set if the
            	implementation runs out of memory, independent of
            	this variable. It may also be set manually independent
            	of this variable, using the sysLevelSetOverloadUntil.
            	This attribute is available in home area.
          items:
            type: boolean
          type: array
        sys_level_set_overload_threshold:
          description: >
            List of SysLevelSetOverloadThreshold. SysLevelSetOverloadThreshold
            represents If this object is non-zero, the overload bit is set at
            	this level when the sysAdminState variable goes to
            	state 'on' for this Intermediate System. The overload bit
            	remains set for sysLevelSetOverloadThreshold seconds.
            	When sysLevelSetOverloadUntil seconds have elapsed,
            	the overload flag remains set if the implementation has
            	run out of memory, or if it is set manually using the
            	isisSysLevelSetOverload object. If sysLevelSetOverload
            	is false, the system clears the overload bit when
            	sysLevelSetOverloadUntil seconds have elapsed, if the
            	system has not run out of memory. This attribute is
            	available in home area.
          items:
            type: integer
          type: array
        sys_name:
          description: >
            List of SysName. SysName represents The hostname listed in LSP, or
            the system name if host
            	name is not configured.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisGlobalConfigGetRequest
      type: object
    AssetIsisGlobalConfigGetResponse:
      properties:
        AssetIsisGlobalConfig:
          items:
            $ref: "#/components/schemas/AssetIsisGlobalConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisGlobalConfigGetResponse
      type: object
    AssetIsisGlobalState:
      description: AssetIsisGlobalState - This object represents the retrieval of list
        of ISIS global settings states.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        default_area_address:
          description: Default area reported in a level 1 LSP.
          type: string
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dynamically_learned_area:
          description: Dynamically learned area throuh ISIS hello packets.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        multi_area_oper_state:
          description: |-
            Multi-area operational state. Value is set to designated or
            	non-designated when the device becomes a Multi-area Boundary Node.
            	This attribute is relevant only for multi-area devices.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetIsisGlobalStateGetRequest:
      description: AssetIsisGlobalStateGetRequest represents the retrieval request for
        AssetIsisGlobalState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        default_area_address:
          description: >
            List of DefaultAreaAddress. DefaultAreaAddress represents Default
            area reported in a level 1 LSP.
          items:
            type: string
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dynamically_learned_area:
          description: >
            List of DynamicallyLearnedArea. DynamicallyLearnedArea represents
            Dynamically learned area throuh ISIS hello packets.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        multi_area_oper_state:
          description: >
            List of MultiAreaOperState. MultiAreaOperState represents Multi-area
            operational state. Value is set to designated or
            	non-designated when the device becomes a Multi-area Boundary Node.
            	This attribute is relevant only for multi-area devices.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisGlobalStateGetRequest
      type: object
    AssetIsisGlobalStateGetResponse:
      properties:
        AssetIsisGlobalState:
          items:
            $ref: "#/components/schemas/AssetIsisGlobalState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisGlobalStateGetResponse
      type: object
    AssetIsisL2MultiAreaIsidList:
      description: >-
        AssetIsisL2MultiAreaIsidList - This object represents the ISIS L2 ISID
        list and is available only on multi-area devices.

        This holds ISIS L2 ISID lists name and the ISID ids in each list.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          type: integer
        isid_list_name:
          description: The ISID values in the list.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
      type: object
    AssetIsisL2MultiAreaIsidListGetRequest:
      description: AssetIsisL2MultiAreaIsidListGetRequest represents the retrieval
        request for AssetIsisL2MultiAreaIsidList.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            type: integer
          type: array
        isid_list_name:
          description: >
            List of IsidListName. IsidListName represents The ISID values in the
            list.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisL2MultiAreaIsidListGetRequest
      type: object
    AssetIsisL2MultiAreaIsidListGetResponse:
      properties:
        AssetIsisL2MultiAreaIsidList:
          items:
            $ref: "#/components/schemas/AssetIsisL2MultiAreaIsidList"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisL2MultiAreaIsidListGetResponse
      type: object
    AssetIsisL2RedistributeMultiAreaSnoop:
      description: >-
        AssetIsisL2RedistributeMultiAreaSnoop - This object specifies the
        attributes based on ISIS L2 multi-area snoop

        redistribution type.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        direction:
          description: Specifies the ISIS redistribution configuration direction
          type: string
        enabled:
          description: A value of True enables the redistribution of the specified
            protocol into the SPBM network.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          format: int64
          type: integer
        isid_list_name:
          description: The ISID values in the list.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        route_policy:
          description: Configures the route policy to apply the redistributes routes.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
      type: object
    AssetIsisL2RedistributeMultiAreaSnoopGetRequest:
      description: AssetIsisL2RedistributeMultiAreaSnoopGetRequest represents the
        retrieval request for AssetIsisL2RedistributeMultiAreaSnoop.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        direction:
          description: >
            List of Direction. Direction represents Specifies the ISIS
            redistribution configuration direction
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of True enables the
            redistribution of the specified protocol into the SPBM network.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          items:
            format: int64
            type: integer
          type: array
        isid_list_name:
          description: >
            List of IsidListName. IsidListName represents The ISID values in the
            list.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        route_policy:
          description: >
            List of RoutePolicy. RoutePolicy represents Configures the route
            policy to apply the redistributes routes.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisL2RedistributeMultiAreaSnoopGetRequest
      type: object
    AssetIsisL2RedistributeMultiAreaSnoopGetResponse:
      properties:
        AssetIsisL2RedistributeMultiAreaSnoop:
          items:
            $ref: "#/components/schemas/AssetIsisL2RedistributeMultiAreaSnoop"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisL2RedistributeMultiAreaSnoopGetResponse
      type: object
    AssetIsisL3AcceptPolicyConfig:
      description: AssetIsisL3AcceptPolicyConfig - This object describes the ISIS L3
        accept policies config list.
      properties:
        backbone_route_map:
          description: >-
            Backbone Route map name. The ISIS routes are selectively accepted
            based on the route policy.
            	An empty string will allow to delete a route map added previously.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        destination_is_id:
          description: |-
            Specifies an ISID number to represent a local Layer 3 VSN to which
            	the ISIS accept policy applies. A value of 0 represents the global
            	routing table(GRT).
          format: int64
          minimum: 0
          type: integer
        device_id:
          description: Device Id
          type: string
        enabled:
          description: A value of True will enable filtering.
          type: boolean
        filter_is_id:
          description: |-
            Specifies an ISID number to represent a local Layer 3 VSN to which
            	the ISIS accept policy applies. A value of 0 represents the global
            	routing table(GRT).
          format: int64
          minimum: 0
          type: integer
        filter_is_id_list_name:
          description: |-
            Name of ISID list. The ISID list contains all the ISID ids from
            	which routes will be accepted.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_version:
          description: IP Version.
          format: int64
          type: integer
        nick_name:
          description: SPBM nickname.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        policy_type:
          description: Policy type.
          format: int64
          type: integer
        route_map:
          description: >-
            Route map name. The ISIS routes are selectively accepted based on
            the route policy.
            	An empty string will allow to delete a route map added previously.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - ip_version
        - policy_type
      type: object
    AssetIsisL3AcceptPolicyConfigGetRequest:
      description: AssetIsisL3AcceptPolicyConfigGetRequest represents the retrieval
        request for AssetIsisL3AcceptPolicyConfig.
      properties:
        backbone_route_map:
          description: >
            List of BackboneRouteMap. BackboneRouteMap represents Backbone Route
            map name. The ISIS routes are selectively accepted based on the
            route policy.
            	An empty string will allow to delete a route map added previously.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        destination_is_id:
          description: >
            List of DestinationIsId. DestinationIsId represents Specifies an
            ISID number to represent a local Layer 3 VSN to which
            	the ISIS accept policy applies. A value of 0 represents the global
            	routing table(GRT).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of True will enable
            filtering.
          items:
            type: boolean
          type: array
        filter_is_id:
          description: >
            List of FilterIsId. FilterIsId represents Specifies an ISID number
            to represent a local Layer 3 VSN to which
            	the ISIS accept policy applies. A value of 0 represents the global
            	routing table(GRT).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        filter_is_id_list_name:
          description: >
            List of FilterIsIdListName. FilterIsIdListName represents Name of
            ISID list. The ISID list contains all the ISID ids from
            	which routes will be accepted.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_version:
          description: |
            List of IpVersion. IpVersion represents IP Version.
          items:
            format: int64
            type: integer
          type: array
        nick_name:
          description: |
            List of NickName. NickName represents SPBM nickname.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        policy_type:
          description: |
            List of PolicyType. PolicyType represents Policy type.
          items:
            format: int64
            type: integer
          type: array
        route_map:
          description: >
            List of RouteMap. RouteMap represents Route map name. The ISIS
            routes are selectively accepted based on the route policy.
            	An empty string will allow to delete a route map added previously.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisL3AcceptPolicyConfigGetRequest
      type: object
    AssetIsisL3AcceptPolicyConfigGetResponse:
      properties:
        AssetIsisL3AcceptPolicyConfig:
          items:
            $ref: "#/components/schemas/AssetIsisL3AcceptPolicyConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisL3AcceptPolicyConfigGetResponse
      type: object
    AssetIsisL3Redistribute:
      description: AssetIsisL3Redistribute - This object describes the ISIS L3
        redistribute list.
      properties:
        afi:
          description: Specifies the authority and format indicator
          type: string
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: A value of True enables the redistribution of the specified
            protocol into the ISIS network.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          format: int64
          type: integer
        metric:
          description: Cost to apply to redistributed routes.
          format: int64
          type: integer
        metric_type:
          description: Specifies the type of route to import from the protocol.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        protocol:
          description: Specifies the protocol from which the routes are redistributed in
            ISIS
          type: string
        route_policy:
          description: Configures the route policy to apply the redistributes routes.
          type: string
        subnets:
          description: >-
            Indicates whether the subnets are advertised individually or
            aggregated
            	to their classful subnet. Choose suppress to advertise subnets
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
      type: object
    AssetIsisL3RedistributeGetRequest:
      description: AssetIsisL3RedistributeGetRequest represents the retrieval request
        for AssetIsisL3Redistribute.
      properties:
        afi:
          description: >
            List of Afi. Afi represents Specifies the authority and format
            indicator
          items:
            type: string
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of True enables the
            redistribution of the specified protocol into the ISIS network.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          items:
            format: int64
            type: integer
          type: array
        metric:
          description: >
            List of Metric. Metric represents Cost to apply to redistributed
            routes.
          items:
            format: int64
            type: integer
          type: array
        metric_type:
          description: >
            List of MetricType. MetricType represents Specifies the type of
            route to import from the protocol.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        protocol:
          description: >
            List of Protocol. Protocol represents Specifies the protocol from
            which the routes are redistributed in ISIS
          items:
            type: string
          type: array
        route_policy:
          description: >
            List of RoutePolicy. RoutePolicy represents Configures the route
            policy to apply the redistributes routes.
          items:
            type: string
          type: array
        subnets:
          description: >
            List of Subnets. Subnets represents Indicates whether the subnets
            are advertised individually or aggregated
            	to their classful subnet. Choose suppress to advertise subnets
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisL3RedistributeGetRequest
      type: object
    AssetIsisL3RedistributeGetResponse:
      properties:
        AssetIsisL3Redistribute:
          items:
            $ref: "#/components/schemas/AssetIsisL3Redistribute"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisL3RedistributeGetResponse
      type: object
    AssetIsisL3RedistributeMultiAreaIsidRedist:
      description: AssetIsisL3RedistributeMultiAreaIsidRedist - This object describes
        the ISIS L3 redistribute multi-area ISID list.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        direction:
          description: Specifies the ISIS redistribution configuration direction
          type: string
        enabled:
          description: >-
            Indicates whether the subnets are advertised individually or
            aggregated
            	to their classful subnet. Choose suppress to advertise subnets
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        protocol_type:
          description: Specifies the protocol from which the routes are redistributed in
            ISIS
          type: string
        route_policy:
          description: Configures the route policy to apply the redistributes routes.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
      type: object
    AssetIsisL3RedistributeMultiAreaIsidRedistGetRequest:
      description: AssetIsisL3RedistributeMultiAreaIsidRedistGetRequest represents the
        retrieval request for AssetIsisL3RedistributeMultiAreaIsidRedist.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        direction:
          description: >
            List of Direction. Direction represents Specifies the ISIS
            redistribution configuration direction
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents Indicates whether the subnets
            are advertised individually or aggregated
            	to their classful subnet. Choose suppress to advertise subnets
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        protocol_type:
          description: >
            List of ProtocolType. ProtocolType represents Specifies the protocol
            from which the routes are redistributed in ISIS
          items:
            type: string
          type: array
        route_policy:
          description: >
            List of RoutePolicy. RoutePolicy represents Configures the route
            policy to apply the redistributes routes.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisL3RedistributeMultiAreaIsidRedistGetRequest
      type: object
    AssetIsisL3RedistributeMultiAreaIsidRedistGetResponse:
      properties:
        AssetIsisL3RedistributeMultiAreaIsidRedist:
          items:
            $ref: "#/components/schemas/AssetIsisL3RedistributeMultiAreaIsidRedist"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisL3RedistributeMultiAreaIsidRedistGetResponse
      type: object
    AssetIsisLspState:
      description: >-
        AssetIsisLspState - The object that represents the state object for ISIS
        LSP. This is

        Fabric Engine specific.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        host_name:
          description: Hostname of the node where multicast FIB entry originates from.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        level:
          description: |-
            This attributes describes at which the system displays the LSP.
            	L1 is for area and L2 is for domain.
          format: int64
          type: integer
        life_time_remain:
          description: The remaining lifetime in seconds for this LSP.
          format: int64
          minimum: 0
          type: integer
        lsp_id:
          description: >-
            Indicates the 8 byte LSP ID, consisting of the SystemID, Circuit ID,
            and Fragment Number.
            	The format of LSP ID is xxxx.xxxx.xxxx.xx-xx(20 characters)
          type: string
        nick_name:
          description: Nick name of node where this ISID got configured or discovered.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        sequence_number:
          description: The sequence number for this LSP in hexadecimal format with 0x
            prefix included.
          type: string
        smlt_bmac:
          description: Smlt bmac. Colon separated MAC address in upper or lower case.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - lsp_id
      type: object
    AssetIsisLspStateGetRequest:
      description: AssetIsisLspStateGetRequest represents the retrieval request for
        AssetIsisLspState.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        host_name:
          description: >
            List of HostName. HostName represents Hostname of the node where
            multicast FIB entry originates from.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        level:
          description: >
            List of Level. Level represents This attributes describes at which
            the system displays the LSP.
            	L1 is for area and L2 is for domain.
          items:
            format: int64
            type: integer
          type: array
        life_time_remain:
          description: >
            List of LifeTimeRemain. LifeTimeRemain represents The remaining
            lifetime in seconds for this LSP.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        lsp_id:
          description: >
            List of LspId. LspId represents Indicates the 8 byte LSP ID,
            consisting of the SystemID, Circuit ID, and Fragment Number.
            	The format of LSP ID is xxxx.xxxx.xxxx.xx-xx(20 characters)
          items:
            type: string
          type: array
        nick_name:
          description: >
            List of NickName. NickName represents Nick name of node where this
            ISID got configured or discovered.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sequence_number:
          description: >
            List of SequenceNumber. SequenceNumber represents The sequence
            number for this LSP in hexadecimal format with 0x prefix included.
          items:
            type: string
          type: array
        smlt_bmac:
          description: >
            List of SmltBmac. SmltBmac represents Smlt bmac. Colon separated MAC
            address in upper or lower case.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisLspStateGetRequest
      type: object
    AssetIsisLspStateGetResponse:
      properties:
        AssetIsisLspState:
          items:
            $ref: "#/components/schemas/AssetIsisLspState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisLspStateGetResponse
      type: object
    AssetIsisMulticastFibState:
      description: AssetIsisMulticastFibState - The object that represents state of
        SPBM IP Multicast-FIB entries. IncomingInterface,OutgoingInterfaces can
        be created while creating AssetIsisMulticastFibState.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        destination_mac:
          description: Destination mac for Isis Unicast.
          type: string
        device_id:
          description: Device Id.
          type: string
        host_name:
          description: Hostname of the node where multicast FIB entry originates from.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        incoming_interface:
          $ref: "#/components/schemas/AssetMulticastFibInterface"
        is_id:
          description: >-
            ISID. A value that uniquely identifies the Layer 2 and Layer 3
            services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        is_id_name:
          description: A value that uniquely identifies the ISID.
          type: string
        nick_name:
          description: Nick name of node where this ISID got configured or discovered.
          type: string
        outgoing_interfaces:
          items:
            $ref: "#/components/schemas/AssetMulticastFibInterface"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        system_id:
          description: System ID of the node where Multicast FIB entry come from.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - destination_mac
        - device_id
        - vlan_number
      type: object
    AssetIsisMulticastFibStateGetRequest:
      description: AssetIsisMulticastFibStateGetRequest represents the retrieval
        request for AssetIsisMulticastFibState.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        destination_mac:
          description: >
            List of DestinationMac. DestinationMac represents Destination mac
            for Isis Unicast.
          items:
            type: string
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        host_name:
          description: >
            List of HostName. HostName represents Hostname of the node where
            multicast FIB entry originates from.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        is_id:
          description: >
            List of IsId. IsId represents ISID. A value that uniquely identifies
            the Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        is_id_name:
          description: >
            List of IsIdName. IsIdName represents A value that uniquely
            identifies the ISID.
          items:
            type: string
          type: array
        nick_name:
          description: >
            List of NickName. NickName represents Nick name of node where this
            ISID got configured or discovered.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        system_id:
          description: >
            List of SystemId. SystemId represents System ID of the node where
            Multicast FIB entry come from.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetIsisMulticastFibStateGetRequest
      type: object
    AssetIsisMulticastFibStateGetResponse:
      properties:
        AssetIsisMulticastFibState:
          items:
            $ref: "#/components/schemas/AssetIsisMulticastFibState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisMulticastFibStateGetResponse
      type: object
    AssetIsisMulticastRouteState:
      description: >-
        AssetIsisMulticastRouteState - The object that represents the state
        object for ISIS ISID

        Multicast IP Routes entries. Ports can be created while creating
        AssetIsisMulticastRouteState.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        bvlan_number:
          description: This object represents a backbone VLAN ID.
          format: int64
          minimum: 0
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        data_is_id:
          description: >-
            Specifies the data I-SID for the IP Multicastroute. A a BEB receives
            IP multicast data from a sender,
            	a BEB allocates a data I-SID in the range of 16,000,000 to 16,512,000 for the stream. The stream is
            	identified by the source IP address, group IP address, and the local VLAN the stream is received on.
            	The data I-SID is a child of the scope or VSN I-SID.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        group_ip:
          description: >-
            The Ipv4 multicast group address for which this entry contains
            multicast
            	routing information.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mcast_type:
          description: Indicates the type of multicast.
          format: int64
          type: integer
        nick_name:
          description: Nick name of node where this ISID got configured or discovered.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        ports:
          items:
            $ref: "#/components/schemas/AssetNetworkToNetworkInterface"
          nullable: true
          type: array
        source_beb:
          description: Source Backbone Edge Bridge name.
          type: string
        source_ip:
          description: >-
            The Ipv4 address of the multicast source for which this entry
            contains
            	multicast routing information.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
        vrf_name:
          description: VRF name.
          type: string
        vsn_is_id:
          description: |-
            Virtual Service Network ISID. Layer 2 VSN and Layer 3 VSN each
            	require a VSN I-SID.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - group_ip
        - source_ip
        - vlan_number
      type: object
    AssetIsisMulticastRouteStateGetRequest:
      description: AssetIsisMulticastRouteStateGetRequest represents the retrieval
        request for AssetIsisMulticastRouteState.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        bvlan_number:
          description: >
            List of BvlanNumber. BvlanNumber represents This object represents a
            backbone VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        data_is_id:
          description: >
            List of DataIsId. DataIsId represents Specifies the data I-SID for
            the IP Multicastroute. A a BEB receives IP multicast data from a
            sender,
            	a BEB allocates a data I-SID in the range of 16,000,000 to 16,512,000 for the stream. The stream is
            	identified by the source IP address, group IP address, and the local VLAN the stream is received on.
            	The data I-SID is a child of the scope or VSN I-SID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        group_ip:
          description: >
            List of GroupIp. GroupIp represents The Ipv4 multicast group address
            for which this entry contains multicast
            	routing information.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mcast_type:
          description: >
            List of McastType. McastType represents Indicates the type of
            multicast.
          items:
            format: int64
            type: integer
          type: array
        nick_name:
          description: >
            List of NickName. NickName represents Nick name of node where this
            ISID got configured or discovered.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        source_beb:
          description: >
            List of SourceBeb. SourceBeb represents Source Backbone Edge Bridge
            name.
          items:
            type: string
          type: array
        source_ip:
          description: >
            List of SourceIp. SourceIp represents The Ipv4 address of the
            multicast source for which this entry contains
            	multicast routing information.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name.
          items:
            type: string
          type: array
        vsn_is_id:
          description: >
            List of VsnIsId. VsnIsId represents Virtual Service Network ISID.
            Layer 2 VSN and Layer 3 VSN each
            	require a VSN I-SID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetIsisMulticastRouteStateGetRequest
      type: object
    AssetIsisMulticastRouteStateGetResponse:
      properties:
        AssetIsisMulticastRouteState:
          items:
            $ref: "#/components/schemas/AssetIsisMulticastRouteState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisMulticastRouteStateGetResponse
      type: object
    AssetIsisRedistributeMultiArea:
      description: AssetIsisRedistributeMultiArea - This object describes the ISIS
        redistribute multi area list.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dvr_enabled:
          description: A value of True will enable DvR backbone redistribution.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l2_except_isid_list:
          description: >-
            Used to configure inter-area L2 ISID redistribution exception
            isid-list name.
            	ISID values contained by this list will not be subject to the permit/deny rule.
          type: string
        l2_permit_type:
          description: Used to set inter-area L2 ISID redistribution policy to permit or
            deny.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetIsisRedistributeMultiAreaGetRequest:
      description: AssetIsisRedistributeMultiAreaGetRequest represents the retrieval
        request for AssetIsisRedistributeMultiArea.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dvr_enabled:
          description: >
            List of DvrEnabled. DvrEnabled represents A value of True will
            enable DvR backbone redistribution.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        l2_except_isid_list:
          description: >
            List of L2ExceptIsidList. L2ExceptIsidList represents Used to
            configure inter-area L2 ISID redistribution exception isid-list
            name.
            	ISID values contained by this list will not be subject to the permit/deny rule.
          items:
            type: string
          type: array
        l2_permit_type:
          description: >
            List of L2PermitType. L2PermitType represents Used to set inter-area
            L2 ISID redistribution policy to permit or deny.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisRedistributeMultiAreaGetRequest
      type: object
    AssetIsisRedistributeMultiAreaGetResponse:
      properties:
        AssetIsisRedistributeMultiArea:
          items:
            $ref: "#/components/schemas/AssetIsisRedistributeMultiArea"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisRedistributeMultiAreaGetResponse
      type: object
    AssetIsisRedistributeMultiAreaStaticIsid:
      description: AssetIsisRedistributeMultiAreaStaticIsid - This object describes
        the ISIS redistribute multi area static ISID list.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        direction:
          description: Specifies the ISIS redistribution configuration direction
          type: string
        enabled:
          description: >-
            Indicates whether the subnets are advertised individually or
            aggregated
            	to their classful subnet. Choose suppress to advertise subnets
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid_list_name:
          description: The ISID values in the list.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - direction
        - isid_list_name
      type: object
    AssetIsisRedistributeMultiAreaStaticIsidGetRequest:
      description: AssetIsisRedistributeMultiAreaStaticIsidGetRequest represents the
        retrieval request for AssetIsisRedistributeMultiAreaStaticIsid.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        direction:
          description: >
            List of Direction. Direction represents Specifies the ISIS
            redistribution configuration direction
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents Indicates whether the subnets
            are advertised individually or aggregated
            	to their classful subnet. Choose suppress to advertise subnets
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid_list_name:
          description: >
            List of IsidListName. IsidListName represents The ISID values in the
            list.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetIsisRedistributeMultiAreaStaticIsidGetRequest
      type: object
    AssetIsisRedistributeMultiAreaStaticIsidGetResponse:
      properties:
        AssetIsisRedistributeMultiAreaStaticIsid:
          items:
            $ref: "#/components/schemas/AssetIsisRedistributeMultiAreaStaticIsid"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisRedistributeMultiAreaStaticIsidGetResponse
      type: object
    AssetIsisUnicastFibState:
      description: AssetIsisUnicastFibState - The object that represents settings for
        ISIS IP Unicast-FIB entries.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        cost:
          description: The cost of the IP Unicast FIB entry.
          format: int64
          minimum: 0
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        destination_mac:
          description: Destination mac for Isis Unicast.
          type: string
        device_id:
          description: Device Id.
          type: string
        host_name:
          description: Host name of the node where Unicast FIB entry come from.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        out_going_port:
          description: |-
            Outgoing port of the IP Unicast FIB entry; it can be one and only
            	one physical port in format [slot]:[port] in case it is not mlt
            	member or, in case it is mlt member, this field displays mlt name
            	instead of physical port.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        system_id:
          description: System ID of the node where Unicast FIB entry come from.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - destination_mac
        - device_id
        - vlan_number
      type: object
    AssetIsisUnicastFibStateGetRequest:
      description: AssetIsisUnicastFibStateGetRequest represents the retrieval request
        for AssetIsisUnicastFibState.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        cost:
          description: |
            List of Cost. Cost represents The cost of the IP Unicast FIB entry.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        destination_mac:
          description: >
            List of DestinationMac. DestinationMac represents Destination mac
            for Isis Unicast.
          items:
            type: string
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        host_name:
          description: >
            List of HostName. HostName represents Host name of the node where
            Unicast FIB entry come from.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        out_going_port:
          description: >
            List of OutGoingPort. OutGoingPort represents Outgoing port of the
            IP Unicast FIB entry; it can be one and only
            	one physical port in format [slot]:[port] in case it is not mlt
            	member or, in case it is mlt member, this field displays mlt name
            	instead of physical port.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        system_id:
          description: >
            List of SystemId. SystemId represents System ID of the node where
            Unicast FIB entry come from.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetIsisUnicastFibStateGetRequest
      type: object
    AssetIsisUnicastFibStateGetResponse:
      properties:
        AssetIsisUnicastFibState:
          items:
            $ref: "#/components/schemas/AssetIsisUnicastFibState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetIsisUnicastFibStateGetResponse
      type: object
    AssetL2VsnCvlanConfig:
      description: AssetL2VsnCvlanConfig - This object represents the CVLAN
        configuration on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        isid_name:
          description: A value that uniquely identifies the ISID.
          type: string
        isid_origin:
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        platform_vlan_number:
          description: The identifier of the VLAN.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
        - isid_name
        - platform_vlan_number
      type: object
    AssetL2VsnCvlanConfigGetRequest:
      description: AssetL2VsnCvlanConfigGetRequest represents the retrieval request
        for AssetL2VsnCvlanConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_name:
          description: >
            List of IsidName. IsidName represents A value that uniquely
            identifies the ISID.
          items:
            type: string
          type: array
        isid_origin:
          description: |
            List of IsidOrigin. IsidOrigin represents Type of ISID origin.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        platform_vlan_number:
          description: >
            List of PlatformVlanNumber. PlatformVlanNumber represents The
            identifier of the VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetL2VsnCvlanConfigGetRequest
      type: object
    AssetL2VsnCvlanConfigGetResponse:
      properties:
        AssetL2VsnCvlanConfig:
          items:
            $ref: "#/components/schemas/AssetL2VsnCvlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnCvlanConfigGetResponse
      type: object
    AssetL2VsnSuniConfig:
      description: AssetL2VsnSuniConfig - This object represents the SUNI
        configuration on the device. EndPoints can be created while creating
        AssetL2VsnSuniConfig.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        end_points:
          items:
            $ref: "#/components/schemas/AssetL2VsnSuniConfigEndPoint"
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        isid_name:
          description: A value that uniquely identifies the ISID.
          type: string
        isid_origin:
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        platform_vlan_number:
          description: The identifier of the VLAN.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
        - isid_name
      type: object
    AssetL2VsnSuniConfigEndPoint:
      description: >-
        AssetL2VsnSuniConfigEndPoint - This object represents updating the
        Customer Vlan Id and other

        endpoints to specified ISID.
      properties:
        bpdu_enabled:
          description: |-
            A value of True will enable bpdu for untagged end point.
            	This is applicable only for untagged traffic(cvid 4096).
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        customer_vlan_number:
          description: The identifier of the Customer VLAN.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        l2_vsn_suni_config_id:
          description: L2 VSN Tuni Config Id
          type: string
        mac_based:
          description: |-
            It shows if the current displayed entry is associated to a
            	mac-based SUNI
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - bpdu_enabled
        - customer_vlan_number
        - device_id
        - interface_name
        - interface_type
        - isid
        - l2_vsn_suni_config_id
        - mac_based
      type: object
    AssetL2VsnSuniConfigEndPointGetRequest:
      description: AssetL2VsnSuniConfigEndPointGetRequest represents the retrieval
        request for AssetL2VsnSuniConfigEndPoint.
      properties:
        bpdu_enabled:
          description: >
            List of BpduEnabled. BpduEnabled represents A value of True will
            enable bpdu for untagged end point.
            	This is applicable only for untagged traffic(cvid 4096).
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        customer_vlan_number:
          description: >
            List of CustomerVlanNumber. CustomerVlanNumber represents The
            identifier of the Customer VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l2_vsn_suni_config_id:
          description: >
            List of L2VsnSuniConfigId. L2VsnSuniConfigId represents L2 VSN Tuni
            Config Id
          items:
            type: string
          type: array
        mac_based:
          description: >
            List of MacBased. MacBased represents It shows if the current
            displayed entry is associated to a
            	mac-based SUNI
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetL2VsnSuniConfigEndPointGetRequest
      type: object
    AssetL2VsnSuniConfigEndPointGetResponse:
      properties:
        AssetL2VsnSuniConfigEndPoint:
          items:
            $ref: "#/components/schemas/AssetL2VsnSuniConfigEndPoint"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnSuniConfigEndPointGetResponse
      type: object
    AssetL2VsnSuniConfigGetRequest:
      description: AssetL2VsnSuniConfigGetRequest represents the retrieval request for
        AssetL2VsnSuniConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_name:
          description: >
            List of IsidName. IsidName represents A value that uniquely
            identifies the ISID.
          items:
            type: string
          type: array
        isid_origin:
          description: |
            List of IsidOrigin. IsidOrigin represents Type of ISID origin.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        platform_vlan_number:
          description: >
            List of PlatformVlanNumber. PlatformVlanNumber represents The
            identifier of the VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetL2VsnSuniConfigGetRequest
      type: object
    AssetL2VsnSuniConfigGetResponse:
      properties:
        AssetL2VsnSuniConfig:
          items:
            $ref: "#/components/schemas/AssetL2VsnSuniConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnSuniConfigGetResponse
      type: object
    AssetL2VsnSuniState:
      description: AssetL2VsnSuniState - The object that represents settings for L2
        VSN Suni State. L2VsnSuniStateMemberIntfs can be created while creating
        AssetL2VsnSuniState.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        l2_vsn_suni_state_member_intfs:
          items:
            $ref: "#/components/schemas/AssetL2VsnSuniStateMemberIntfs"
          nullable: true
          type: array
        mac_based:
          description: It shows if the current displayed entry is associated to a
            mac-based SUNI
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_device_id
        - isid
        - mac_based
        - vlan_number
      type: object
    AssetL2VsnSuniStateGetRequest:
      description: AssetL2VsnSuniStateGetRequest represents the retrieval request for
        AssetL2VsnSuniState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        mac_based:
          description: >
            List of MacBased. MacBased represents It shows if the current
            displayed entry is associated to a mac-based SUNI
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetL2VsnSuniStateGetRequest
      type: object
    AssetL2VsnSuniStateGetResponse:
      properties:
        AssetL2VsnSuniState:
          items:
            $ref: "#/components/schemas/AssetL2VsnSuniState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnSuniStateGetResponse
      type: object
    AssetL2VsnSuniStateMemberIntfs:
      description: AssetL2VsnSuniStateMemberIntfs - The object that represents
        settings for L2 VSN Suni State member interfaces.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intf_name:
          description: Interface name
          type: string
        intf_type:
          description: The type of interface.
          format: int64
          type: integer
        isid:
          default: 0
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999. Default is set to 0 to support the migration case
          format: int64
          minimum: 0
          type: integer
        l2_vsn_suni_state_id:
          description: L2 VSN Suni State Id
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - intf_name
        - intf_type
        - isid
        - l2_vsn_suni_state_id
      type: object
    AssetL2VsnSuniStateMemberIntfsGetRequest:
      description: AssetL2VsnSuniStateMemberIntfsGetRequest represents the retrieval
        request for AssetL2VsnSuniStateMemberIntfs.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intf_name:
          description: |
            List of IntfName. IntfName represents Interface name
          items:
            type: string
          type: array
        intf_type:
          description: |
            List of IntfType. IntfType represents The type of interface.
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999. Default is set to 0 to support the migration case
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l2_vsn_suni_state_id:
          description: >
            List of L2VsnSuniStateId. L2VsnSuniStateId represents L2 VSN Suni
            State Id
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetL2VsnSuniStateMemberIntfsGetRequest
      type: object
    AssetL2VsnSuniStateMemberIntfsGetResponse:
      properties:
        AssetL2VsnSuniStateMemberIntfs:
          items:
            $ref: "#/components/schemas/AssetL2VsnSuniStateMemberIntfs"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnSuniStateMemberIntfsGetResponse
      type: object
    AssetL2VsnTuniConfig:
      description: AssetL2VsnTuniConfig - This object represents the TUNI
        configuration on the device. MemberInterface can be created while
        creating AssetL2VsnTuniConfig.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        isid_name:
          description: A value that uniquely identifies the ISID.
          type: string
        isid_origin:
          items:
            format: int64
            type: integer
          type: array
        member_interface:
          items:
            $ref: "#/components/schemas/AssetL2VsnTuniConfigMemberIntf"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - isid
        - isid_name
      type: object
    AssetL2VsnTuniConfigGetRequest:
      description: AssetL2VsnTuniConfigGetRequest represents the retrieval request for
        AssetL2VsnTuniConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_name:
          description: >
            List of IsidName. IsidName represents A value that uniquely
            identifies the ISID.
          items:
            type: string
          type: array
        isid_origin:
          description: |
            List of IsidOrigin. IsidOrigin represents Type of ISID origin.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetL2VsnTuniConfigGetRequest
      type: object
    AssetL2VsnTuniConfigGetResponse:
      properties:
        AssetL2VsnTuniConfig:
          items:
            $ref: "#/components/schemas/AssetL2VsnTuniConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnTuniConfigGetResponse
      type: object
    AssetL2VsnTuniConfigMemberIntf:
      description: >-
        AssetL2VsnTuniConfigMemberIntf - This object represents the list of
        different interfaces like Ports, Lag for the

        specified TUNI Configuration Id.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intf_name:
          description: Interface name
          type: string
        intf_type:
          description: The type of interface.
          format: int64
          type: integer
        l2_vsn_tuni_config_id:
          description: L2 VSN Tuni Config Id
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - intf_name
        - intf_type
        - l2_vsn_tuni_config_id
      type: object
    AssetL2VsnTuniConfigMemberIntfGetRequest:
      description: AssetL2VsnTuniConfigMemberIntfGetRequest represents the retrieval
        request for AssetL2VsnTuniConfigMemberIntf.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intf_name:
          description: |
            List of IntfName. IntfName represents Interface name
          items:
            type: string
          type: array
        intf_type:
          description: |
            List of IntfType. IntfType represents The type of interface.
          items:
            format: int64
            type: integer
          type: array
        l2_vsn_tuni_config_id:
          description: >
            List of L2VsnTuniConfigId. L2VsnTuniConfigId represents L2 VSN Tuni
            Config Id
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetL2VsnTuniConfigMemberIntfGetRequest
      type: object
    AssetL2VsnTuniConfigMemberIntfGetResponse:
      properties:
        AssetL2VsnTuniConfigMemberIntf:
          items:
            $ref: "#/components/schemas/AssetL2VsnTuniConfigMemberIntf"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL2VsnTuniConfigMemberIntfGetResponse
      type: object
    AssetL3VsnConfig:
      description: >-
        AssetL3VsnConfig - This object represents the L3VSN configuration
        information for each VRF

        on the device. This lists the VPN node settings.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_vpn_type:
          description: IP VPN Type.
          format: int64
          type: integer
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
            	Value 0 is used to unset the ISID number.
          format: int64
          minimum: 0
          type: integer
        isid_name:
          description: A value that uniquely identifies the ISID.
          type: string
        mvpn_enabled:
          description: The status of MVPN on a VRF.
          type: boolean
        mvpn_forward_cache_timeout:
          description: MVPN Forward Cache Timeout in seconds.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vpn_enabled:
          description: The enabled status of IP VPN node.
          type: boolean
        vrf_config_id:
          description: VRF Config Id.
          type: string
        vrf_name:
          description: This object represents a Virtual Router (VR) name
          type: string
      required:
        - device_id
        - isid
        - vrf_config_id
        - vrf_name
      type: object
    AssetL3VsnConfigGetRequest:
      description: AssetL3VsnConfigGetRequest represents the retrieval request for
        AssetL3VsnConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_vpn_type:
          description: |
            List of IpVpnType. IpVpnType represents IP VPN Type.
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
            	Value 0 is used to unset the ISID number.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_name:
          description: >
            List of IsidName. IsidName represents A value that uniquely
            identifies the ISID.
          items:
            type: string
          type: array
        mvpn_enabled:
          description: >
            List of MvpnEnabled. MvpnEnabled represents The status of MVPN on a
            VRF.
          items:
            type: boolean
          type: array
        mvpn_forward_cache_timeout:
          description: >
            List of MvpnForwardCacheTimeout. MvpnForwardCacheTimeout represents
            MVPN Forward Cache Timeout in seconds.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vpn_enabled:
          description: >
            List of VpnEnabled. VpnEnabled represents The enabled status of IP
            VPN node.
          items:
            type: boolean
          type: array
        vrf_config_id:
          description: |
            List of VrfConfigId. VrfConfigId represents VRF Config Id.
          items:
            type: string
          type: array
        vrf_name:
          description: >
            List of VrfName. VrfName represents This object represents a Virtual
            Router (VR) name
          items:
            type: string
          type: array
      title: AssetL3VsnConfigGetRequest
      type: object
    AssetL3VsnConfigGetResponse:
      properties:
        AssetL3VsnConfig:
          items:
            $ref: "#/components/schemas/AssetL3VsnConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL3VsnConfigGetResponse
      type: object
    AssetL3VsnIsidListConfig:
      description: >-
        AssetL3VsnIsidListConfig - This object represents the L3VSN
        configuration to create ISIS ISID list for the specified VRF

        on the device.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
            	Value 0 is used to unset the ISID number.
          format: int64
          minimum: 0
          type: integer
        isid_list_name:
          description: A value that uniquely identifies the ISID.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_config_id:
          description: VRF Config Id.
          type: string
        vrf_name:
          description: This object represents a Virtual Router (VR) name
          type: string
      required:
        - device_id
        - isid
        - isid_list_name
        - vrf_config_id
        - vrf_name
      type: object
    AssetL3VsnIsidListConfigGetRequest:
      description: AssetL3VsnIsidListConfigGetRequest represents the retrieval request
        for AssetL3VsnIsidListConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
            	Value 0 is used to unset the ISID number.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_list_name:
          description: >
            List of IsidListName. IsidListName represents A value that uniquely
            identifies the ISID.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_config_id:
          description: |
            List of VrfConfigId. VrfConfigId represents VRF Config Id.
          items:
            type: string
          type: array
        vrf_name:
          description: >
            List of VrfName. VrfName represents This object represents a Virtual
            Router (VR) name
          items:
            type: string
          type: array
      title: AssetL3VsnIsidListConfigGetRequest
      type: object
    AssetL3VsnIsidListConfigGetResponse:
      properties:
        AssetL3VsnIsidListConfig:
          items:
            $ref: "#/components/schemas/AssetL3VsnIsidListConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetL3VsnIsidListConfigGetResponse
      type: object
    AssetLagConfig:
      description: >-
        AssetLagConfig - This object represents the Lag configuration on the
        device with

        the specified name and id. MemberPorts can be created while creating
        AssetLagConfig.
      properties:
        asset_device_id:
          description: Generated Device Id
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dynamic:
          description: Is Dynamic
          type: boolean
        enabled:
          description: A value of true indicates the lag admin state is enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lacp_key:
          description: |-
            The current operational value of the key for the Aggregation Port.
            	Ports with the same key will be aggregated as a single trunk. It
            	is used only by Fabric Engine(VOSS). This attribute is not used in
            	case of STATIC mode.
          type: string
        lag_number:
          description: Lag Number.
          type: string
        load_balance_algo:
          description: LAG load balancing algorithm. VOSS will always return/set CUSTOM.
          format: int64
          type: integer
        member_ports:
          items:
            $ref: "#/components/schemas/AssetLagConfigMemberPort"
          nullable: true
          type: array
        mode:
          description: >-
            The LAG mode that is currently running. The supported modes for
            Fabric Engine (VOSS)
            	are STATIC, LACP and VLACP. The supported modes for Switch Engine(EXOS) are STATIC,
            	LACP and HEALTH_CHECK.
          format: int64
          type: integer
        name:
          description: Lag Name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - asset_interface_id
        - lag_number
      type: object
    AssetLagConfigGetRequest:
      description: AssetLagConfigGetRequest represents the retrieval request for
        AssetLagConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Generated Device Id
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dynamic:
          description: |
            List of Dynamic. Dynamic represents Is Dynamic
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates the
            lag admin state is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lacp_key:
          description: >
            List of LacpKey. LacpKey represents The current operational value of
            the key for the Aggregation Port.
            	Ports with the same key will be aggregated as a single trunk. It
            	is used only by Fabric Engine(VOSS). This attribute is not used in
            	case of STATIC mode.
          items:
            type: string
          type: array
        lag_number:
          description: |
            List of LagNumber. LagNumber represents Lag Number.
          items:
            type: string
          type: array
        load_balance_algo:
          description: >
            List of LoadBalanceAlgo. LoadBalanceAlgo represents LAG load
            balancing algorithm. VOSS will always return/set CUSTOM.
          items:
            format: int64
            type: integer
          type: array
        mode:
          description: >
            List of Mode. Mode represents The LAG mode that is currently
            running. The supported modes for Fabric Engine (VOSS)
            	are STATIC, LACP and VLACP. The supported modes for Switch Engine(EXOS) are STATIC,
            	LACP and HEALTH_CHECK.
          items:
            format: int64
            type: integer
          type: array
        name:
          description: |
            List of Name. Name represents Lag Name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLagConfigGetRequest
      type: object
    AssetLagConfigGetResponse:
      properties:
        AssetLagConfig:
          items:
            $ref: "#/components/schemas/AssetLagConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLagConfigGetResponse
      type: object
    AssetLagConfigMemberPort:
      description: >-
        AssetLagConfigMemberPort - This object represents the member ports and
        their information configured

        on the Lag.
      properties:
        asset_lag_config_id:
          description: Lag Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_lag_config_id
      type: object
    AssetLagConfigMemberPortGetRequest:
      description: AssetLagConfigMemberPortGetRequest represents the retrieval request
        for AssetLagConfigMemberPort.
      properties:
        asset_lag_config_id:
          description: |
            List of AssetLagConfigId. AssetLagConfigId represents Lag Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLagConfigMemberPortGetRequest
      type: object
    AssetLagConfigMemberPortGetResponse:
      properties:
        AssetLagConfigMemberPort:
          items:
            $ref: "#/components/schemas/AssetLagConfigMemberPort"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLagConfigMemberPortGetResponse
      type: object
    AssetLagState:
      description: >-
        AssetLagState - This object represents the retrieval of LAG state
        information for

        the entire switch. MemberPorts can be created while creating
        AssetLagState.
      properties:
        asset_device_id:
          description: Generated Device Id
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lag_number:
          description: Lag Number.
          type: string
        member_ports:
          items:
            $ref: "#/components/schemas/AssetLagStateMemberPort"
          nullable: true
          type: array
        name:
          description: Lag Name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - asset_interface_id
        - lag_number
      type: object
    AssetLagStateGetRequest:
      description: AssetLagStateGetRequest represents the retrieval request for
        AssetLagState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Generated Device Id
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lag_number:
          description: |
            List of LagNumber. LagNumber represents Lag Number.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Lag Name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLagStateGetRequest
      type: object
    AssetLagStateGetResponse:
      properties:
        AssetLagState:
          items:
            $ref: "#/components/schemas/AssetLagState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLagStateGetResponse
      type: object
    AssetLagStateMemberPort:
      description: >-
        AssetLagStateMemberPort - This object represents the state of member
        ports and their information

        configured on the Lag.
      properties:
        asset_lag_state_id:
          description: Lag State Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_lag_state_id
      type: object
    AssetLagStateMemberPortGetRequest:
      description: AssetLagStateMemberPortGetRequest represents the retrieval request
        for AssetLagStateMemberPort.
      properties:
        asset_lag_state_id:
          description: |
            List of AssetLagStateId. AssetLagStateId represents Lag State Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLagStateMemberPortGetRequest
      type: object
    AssetLagStateMemberPortGetResponse:
      properties:
        AssetLagStateMemberPort:
          items:
            $ref: "#/components/schemas/AssetLagStateMemberPort"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLagStateMemberPortGetResponse
      type: object
    AssetLldpConfig:
      description: AssetLldpConfig - DB model for LLDP Global Config Ports can be
        created while creating AssetLldpConfig.
      properties:
        advertisement_interval:
          default: 30
          description: >-
            The advertisement interval determines how frequently LLDP-enabled
            devices send
            	advertisement frames to their neighbors. By default, this interval is set to 30 seconds,
            	but it can be configured to range from 5 to 32,768 seconds.
          type: integer
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fast_start_repeat_count:
          default: 3
          description: >-
            The fast-start-repeat-count specifies how many times LLDP Media
            Endpoint Discovery
            	(LLDP-MED) packets are sent at 1-second intervals when a new LLDP-MED device is detected.
            	This helps the device quickly learn about its neighbors. The range is from 1 to 10, with a
            	default value of 3
          type: integer
        hold_multiplier:
          default: 4
          description: >-
            The hold multiplier determines how long the information about a
            device is retained by its
            	neighbors. It is a multiplier applied to the advertisement interval to calculate the
            	Time-to-Live (TTL) for the LLDP data. By default, the hold multiplier is set to 4, but it
            	can be configured to range from 2 to 10
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        init_delay_seconds:
          default: 2
          description: >-
            The initialization delay (init delay) specifies the minimum number
            of seconds a device
            	will wait after LLDP is disabled on a port before it will re-enable LLDP on that port. The
            	default value for this delay is 2 seconds, but it can typically be configured to range
            	from 1 to 10 seconds
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        ports:
          items:
            $ref: "#/components/schemas/AssetLldpPortConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - advertisement_interval
        - asset_device_id
        - fast_start_repeat_count
        - hold_multiplier
        - init_delay_seconds
      type: object
    AssetLldpConfigGetRequest:
      description: AssetLldpConfigGetRequest represents the retrieval request for
        AssetLldpConfig.
      properties:
        advertisement_interval:
          description: >
            List of AdvertisementInterval. AdvertisementInterval represents The
            advertisement interval determines how frequently LLDP-enabled
            devices send
            	advertisement frames to their neighbors. By default, this interval is set to 30 seconds,
            	but it can be configured to range from 5 to 32,768 seconds.
          items:
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fast_start_repeat_count:
          description: >
            List of FastStartRepeatCount. FastStartRepeatCount represents The
            fast-start-repeat-count specifies how many times LLDP Media Endpoint
            Discovery
            	(LLDP-MED) packets are sent at 1-second intervals when a new LLDP-MED device is detected.
            	This helps the device quickly learn about its neighbors. The range is from 1 to 10, with a
            	default value of 3
          items:
            type: integer
          type: array
        hold_multiplier:
          description: >
            List of HoldMultiplier. HoldMultiplier represents The hold
            multiplier determines how long the information about a device is
            retained by its
            	neighbors. It is a multiplier applied to the advertisement interval to calculate the
            	Time-to-Live (TTL) for the LLDP data. By default, the hold multiplier is set to 4, but it
            	can be configured to range from 2 to 10
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        init_delay_seconds:
          description: >
            List of InitDelaySeconds. InitDelaySeconds represents The
            initialization delay (init delay) specifies the minimum number of
            seconds a device
            	will wait after LLDP is disabled on a port before it will re-enable LLDP on that port. The
            	default value for this delay is 2 seconds, but it can typically be configured to range
            	from 1 to 10 seconds
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpConfigGetRequest
      type: object
    AssetLldpConfigGetResponse:
      properties:
        AssetLldpConfig:
          items:
            $ref: "#/components/schemas/AssetLldpConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpConfigGetResponse
      type: object
    AssetLldpDot1PortProtocolVlan:
      description: AssetLldpDot1PortProtocolVlan - DB model for LLDP Dot1 Port
        Protocol Vlan Config
      nullable: true
      properties:
        advertise:
          description: A value of true allows a device to advertise VLAN information to
            its neighbors.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lldp_port_advertise_settings_config_id:
          description: UUID of the lldp-dot1-port-protocol-vlan
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_list:
          description: The VLAN list information
          items:
            type: string
          nullable: true
          type: array
      required:
        - advertise
        - lldp_port_advertise_settings_config_id
      type: object
    AssetLldpDot1PortProtocolVlanGetRequest:
      description: AssetLldpDot1PortProtocolVlanGetRequest represents the retrieval
        request for AssetLldpDot1PortProtocolVlan.
      properties:
        advertise:
          description: >
            List of Advertise. Advertise represents A value of true allows a
            device to advertise VLAN information to its neighbors.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lldp_port_advertise_settings_config_id:
          description: >
            List of LldpPortAdvertiseSettingsConfigId.
            LldpPortAdvertiseSettingsConfigId represents UUID of the
            lldp-dot1-port-protocol-vlan
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_list:
          description: |
            List of VlanList. VlanList represents The VLAN list information
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
      title: AssetLldpDot1PortProtocolVlanGetRequest
      type: object
    AssetLldpDot1PortProtocolVlanGetResponse:
      properties:
        AssetLldpDot1PortProtocolVlan:
          items:
            $ref: "#/components/schemas/AssetLldpDot1PortProtocolVlan"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpDot1PortProtocolVlanGetResponse
      type: object
    AssetLldpDot1VlanObject:
      description: AssetLldpDot1VlanObject - DB model for LLDP Dot1 Vlan Config
      properties:
        advertise:
          description: A value of true allows a device to advertise VLAN information to
            its neighbors.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lldp_port_advertise_settings_config_id:
          description: UUID of the lldp-dot1-vlan-object
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_list:
          description: The VLAN list information
          items:
            type: string
          nullable: true
          type: array
      required:
        - advertise
        - lldp_port_advertise_settings_config_id
      type: object
    AssetLldpDot1VlanObjectGetRequest:
      description: AssetLldpDot1VlanObjectGetRequest represents the retrieval request
        for AssetLldpDot1VlanObject.
      properties:
        advertise:
          description: >
            List of Advertise. Advertise represents A value of true allows a
            device to advertise VLAN information to its neighbors.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lldp_port_advertise_settings_config_id:
          description: >
            List of LldpPortAdvertiseSettingsConfigId.
            LldpPortAdvertiseSettingsConfigId represents UUID of the
            lldp-dot1-vlan-object
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_list:
          description: |
            List of VlanList. VlanList represents The VLAN list information
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
      title: AssetLldpDot1VlanObjectGetRequest
      type: object
    AssetLldpDot1VlanObjectGetResponse:
      properties:
        AssetLldpDot1VlanObject:
          items:
            $ref: "#/components/schemas/AssetLldpDot1VlanObject"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpDot1VlanObjectGetResponse
      type: object
    AssetLldpNeighborFabricAttach:
      description: AssetLldpNeighborFabricAttach - This object describes the lldp
        neighbor fabric attach state info list.
        AssetLldpNeighborFabricAttachVlanIsidMap can be created while creating
        AssetLldpNeighborFabricAttach.
      properties:
        active_vlan_isid_maps:
          description: Active VLAN and ISID mappings
          format: int64
          type: integer
        asset_device_id:
          description: Device Id
          type: string
        asset_lldp_neighbor_fabric_attach_vlan_isid_map:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborFabricAttachVlanIsidMap"
          nullable: true
          type: array
        asset_lldp_neighbor_state_id:
          description: ID of the parent lldp-neighbor-state
          type: string
        auth_enabled:
          description: Advertised fabric attach authentication state.
          type: boolean
        auth_passed:
          description: |-
            Advertised fabric attach authentication result.
            	This attribute is applicable to Switch Engine only.
          type: boolean
        connection_type:
          description: |-
            Advertised connection type.
            	This attribute is applicable to Switch Engine only.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lag_id:
          description: |-
            This attribute is applicable to Switch Engine only.
            	A value of 0 means the LAG ID is not set.
          type: integer
        link_info:
          description: This attribute is applicable to Switch Engine only.
          type: string
        mac_address:
          description: Colon separated MAC address in upper or lower case
          type: string
        mgmt_vlan_id:
          description: A value of 0 means the management VLAN ID is not set.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        pending_vlan_isid_maps:
          description: Pending VLAN and ISID mappings
          format: int64
          type: integer
        port_name:
          description: This attribute is applicable to Switch Engine only.
          type: string
        rejected_vlan_isid_maps:
          description: Rejected VLAN and ISID mappings
          format: int64
          type: integer
        reserved:
          description: |-
            Advertised fabric attach reserved value.
            	This attribute is applicable to Fabric Engine only.
          type: integer
        smlt_id:
          description: |-
            This attribute is applicable to Switch Engine only.
            	A value of 0 means the SMLT ID is not set.
          type: integer
        state:
          description: |-
            Advertised fabric attach state.
            	Integer value obtained from TLV, representing a combination of
            	TAG and Auto Provision states.
          type: integer
        total_vlan_isid_maps:
          description: Total VLAN and ISID mappings
          format: int64
          type: integer
        type:
          description: Advertised fabric attach type.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetLldpNeighborFabricAttachGetRequest:
      description: AssetLldpNeighborFabricAttachGetRequest represents the retrieval
        request for AssetLldpNeighborFabricAttach.
      properties:
        active_vlan_isid_maps:
          description: >
            List of ActiveVlanIsidMaps. ActiveVlanIsidMaps represents Active
            VLAN and ISID mappings
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        asset_lldp_neighbor_state_id:
          description: >
            List of AssetLldpNeighborStateId. AssetLldpNeighborStateId
            represents ID of the parent lldp-neighbor-state
          items:
            type: string
          type: array
        auth_enabled:
          description: >
            List of AuthEnabled. AuthEnabled represents Advertised fabric attach
            authentication state.
          items:
            type: boolean
          type: array
        auth_passed:
          description: >
            List of AuthPassed. AuthPassed represents Advertised fabric attach
            authentication result.
            	This attribute is applicable to Switch Engine only.
          items:
            type: boolean
          type: array
        connection_type:
          description: >
            List of ConnectionType. ConnectionType represents Advertised
            connection type.
            	This attribute is applicable to Switch Engine only.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lag_id:
          description: >
            List of LagId. LagId represents This attribute is applicable to
            Switch Engine only.
            	A value of 0 means the LAG ID is not set.
          items:
            type: integer
          type: array
        link_info:
          description: >
            List of LinkInfo. LinkInfo represents This attribute is applicable
            to Switch Engine only.
          items:
            type: string
          type: array
        mac_address:
          description: >
            List of MacAddress. MacAddress represents Colon separated MAC
            address in upper or lower case
          items:
            type: string
          type: array
        mgmt_vlan_id:
          description: >
            List of MgmtVlanId. MgmtVlanId represents A value of 0 means the
            management VLAN ID is not set.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        pending_vlan_isid_maps:
          description: >
            List of PendingVlanIsidMaps. PendingVlanIsidMaps represents Pending
            VLAN and ISID mappings
          items:
            format: int64
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents This attribute is applicable
            to Switch Engine only.
          items:
            type: string
          type: array
        rejected_vlan_isid_maps:
          description: >
            List of RejectedVlanIsidMaps. RejectedVlanIsidMaps represents
            Rejected VLAN and ISID mappings
          items:
            format: int64
            type: integer
          type: array
        reserved:
          description: >
            List of Reserved. Reserved represents Advertised fabric attach
            reserved value.
            	This attribute is applicable to Fabric Engine only.
          items:
            type: integer
          type: array
        smlt_id:
          description: >
            List of SmltId. SmltId represents This attribute is applicable to
            Switch Engine only.
            	A value of 0 means the SMLT ID is not set.
          items:
            type: integer
          type: array
        state:
          description: |
            List of State. State represents Advertised fabric attach state.
            	Integer value obtained from TLV, representing a combination of
            	TAG and Auto Provision states.
          items:
            type: integer
          type: array
        total_vlan_isid_maps:
          description: >
            List of TotalVlanIsidMaps. TotalVlanIsidMaps represents Total VLAN
            and ISID mappings
          items:
            format: int64
            type: integer
          type: array
        type:
          description: |
            List of Type. Type represents Advertised fabric attach type.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborFabricAttachGetRequest
      type: object
    AssetLldpNeighborFabricAttachGetResponse:
      properties:
        AssetLldpNeighborFabricAttach:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborFabricAttach"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborFabricAttachGetResponse
      type: object
    AssetLldpNeighborFabricAttachVlanIsidMap:
      description: >-
        AssetLldpNeighborFabricAttachVlanIsidMap - This object describes the
        lldp neighbor fabric attach vlan isid

        mapping state info list.
      nullable: true
      properties:
        asset_lldp_neighbor_fabric_attach_id:
          description: Asset Lldp Neighbor Fabric Attach ID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        status:
          description: Status Enum
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: The identifier of the VLAN
          type: integer
      type: object
    AssetLldpNeighborFabricAttachVlanIsidMapGetRequest:
      description: AssetLldpNeighborFabricAttachVlanIsidMapGetRequest represents the
        retrieval request for AssetLldpNeighborFabricAttachVlanIsidMap.
      properties:
        asset_lldp_neighbor_fabric_attach_id:
          description: >
            List of AssetLldpNeighborFabricAttachId.
            AssetLldpNeighborFabricAttachId represents Asset Lldp Neighbor
            Fabric Attach ID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        status:
          description: |
            List of Status. Status represents Status Enum
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents The identifier of the VLAN
          items:
            type: integer
          type: array
      title: AssetLldpNeighborFabricAttachVlanIsidMapGetRequest
      type: object
    AssetLldpNeighborFabricAttachVlanIsidMapGetResponse:
      properties:
        AssetLldpNeighborFabricAttachVlanIsidMap:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborFabricAttachVlanIsidMap"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborFabricAttachVlanIsidMapGetResponse
      type: object
    AssetLldpNeighborManagementAddressList:
      description: AssetLldpNeighborManagementAddressList - This object describes the
        lldp neighbor management address list.
      properties:
        address:
          description: |-
            The string value used to identify the management address
            	component associated with the remote system.  The purpose
            	of this address is to contact the management entity.
            	Value is defined by IEEE 802.1AB-2005 9.5.9.4.
          type: string
        address_if_id:
          description: |-
            The integer value used to identify the interface number
            	regarding the management address component associated
            	with the remote system. Value is defined by IEEE
            	802.1AB-2005 9.5.9.6.
          type: integer
        address_if_subtype:
          description: |-
            The type of interface associated with the management
            	address as defined by IEEE 802.1AB-2005 9.5.9.5.
            	IFINDEX         - interface identifier
            	SYS_PORT_NUMBER - interface identifier based on the system
            	port numbering convention
            	UNKNOWN         - interface is not known
          format: int64
          type: integer
        address_subtype:
          description: Address Subtype
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lldp_neighbor_state_id:
          description: ID of the parent lldp-neighbor-state
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - address
        - address_subtype
      type: object
    AssetLldpNeighborManagementAddressListGetRequest:
      description: AssetLldpNeighborManagementAddressListGetRequest represents the
        retrieval request for AssetLldpNeighborManagementAddressList.
      properties:
        address:
          description: >
            List of Address. Address represents The string value used to
            identify the management address
            	component associated with the remote system.  The purpose
            	of this address is to contact the management entity.
            	Value is defined by IEEE 802.1AB-2005 9.5.9.4.
          items:
            type: string
          type: array
        address_if_id:
          description: >
            List of AddressIfId. AddressIfId represents The integer value used
            to identify the interface number
            	regarding the management address component associated
            	with the remote system. Value is defined by IEEE
            	802.1AB-2005 9.5.9.6.
          items:
            type: integer
          type: array
        address_if_subtype:
          description: >
            List of AddressIfSubtype. AddressIfSubtype represents The type of
            interface associated with the management
            	address as defined by IEEE 802.1AB-2005 9.5.9.5.
            	IFINDEX         - interface identifier
            	SYS_PORT_NUMBER - interface identifier based on the system
            	port numbering convention
            	UNKNOWN         - interface is not known
          items:
            format: int64
            type: integer
          type: array
        address_subtype:
          description: |
            List of AddressSubtype. AddressSubtype represents Address Subtype
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lldp_neighbor_state_id:
          description: >
            List of LldpNeighborStateId. LldpNeighborStateId represents ID of
            the parent lldp-neighbor-state
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborManagementAddressListGetRequest
      type: object
    AssetLldpNeighborManagementAddressListGetResponse:
      properties:
        AssetLldpNeighborManagementAddressList:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborManagementAddressList"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborManagementAddressListGetResponse
      type: object
    AssetLldpNeighborState:
      description: AssetLldpNeighborState - This object describes the lldp neighbor
        state info list.
        AssetLldpNeighborFabricAttach,AssetLldpNeighborManagementAddressList,AssetLldpNeighborXmed,AssetLldpNeighborZtf
        can be created while creating AssetLldpNeighborState.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        asset_lldp_neighbor_fabric_attach:
          $ref: "#/components/schemas/AssetLldpNeighborFabricAttach"
        asset_lldp_neighbor_management_address_list:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborManagementAddressList"
          nullable: true
          type: array
        asset_lldp_neighbor_xmed:
          $ref: "#/components/schemas/AssetLldpNeighborXmed"
        asset_lldp_neighbor_ztf:
          $ref: "#/components/schemas/AssetLldpNeighborZtf"
        chassis_id:
          description: |-
            The type of encoding used to identify the chassis
            	associated with the local system as described by IEEE
            	802.1AB-2005 9.5.2.3.
          type: string
        chassis_id_subtype:
          description: |-
            The type of encoding used to identify the chassis
            	associated with the local system as described by IEEE
            	802.1AB-2005 9.5.2.2.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: |-
            An arbitrary local integer value used to identify a particular
            	connection instance, unique only for the indicated remote system.
          type: integer
        ip_address_list:
          description: The neighbor IP address (Ipv4 or Ipv6) list.
          items:
            type: string
          nullable: true
          type: array
        local_interface_name:
          description: |-
            Identifies the local port on which the remote system
            	information is received.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_id_subtype:
          description: |-
            The type of port identifier encoding used in the
            	associated LldpPortId schema as defined by IEEE
            	802.1AB-2005 9.5.3.2.
          format: int64
          type: integer
        remote_chassis_id:
          description: ID of the remote chassis
          type: string
        remote_interface_name:
          description: |-
            String value used to identify the port component associated
            	with the remote system as defined by IEEE 802.1AB-2005
            	9.5.3.3.
          type: string
        remote_port_description:
          description: |-
            String value used to identify the description of the given
            	port associated with the remote system as defined by IEEE
            	802.1AB-2005 9.5.5.2.
          type: string
        remote_system_description:
          description: description of the remote system
          type: string
        remote_system_name:
          description: Name of the remote system
          type: string
        sys_cap_enabled:
          items:
            format: int64
            type: integer
          type: array
        sys_cap_supported:
          items:
            format: int64
            type: integer
          type: array
        sys_name:
          description: |-
            The string value used to identify the
            	system name of the remote system as defined
            	by IEEE 802.1AB-2005 9.5.6.2.
          type: string
        time_mark:
          description: TimeFilter value as defined by IETF RFC 2021 section 6.
          type: integer
        ttl:
          description: Time To Live
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - index
        - time_mark
      type: object
    AssetLldpNeighborStateGetRequest:
      description: AssetLldpNeighborStateGetRequest represents the retrieval request
        for AssetLldpNeighborState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        chassis_id:
          description: >
            List of ChassisId. ChassisId represents The type of encoding used to
            identify the chassis
            	associated with the local system as described by IEEE
            	802.1AB-2005 9.5.2.3.
          items:
            type: string
          type: array
        chassis_id_subtype:
          description: >
            List of ChassisIdSubtype. ChassisIdSubtype represents The type of
            encoding used to identify the chassis
            	associated with the local system as described by IEEE
            	802.1AB-2005 9.5.2.2.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: >
            List of Index. Index represents An arbitrary local integer value
            used to identify a particular
            	connection instance, unique only for the indicated remote system.
          items:
            type: integer
          type: array
        ip_address_list:
          description: >
            List of IpAddressList. IpAddressList represents The neighbor IP
            address (Ipv4 or Ipv6) list.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        local_interface_name:
          description: >
            List of LocalInterfaceName. LocalInterfaceName represents Identifies
            the local port on which the remote system
            	information is received.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_id_subtype:
          description: >
            List of PortIdSubtype. PortIdSubtype represents The type of port
            identifier encoding used in the
            	associated LldpPortId schema as defined by IEEE
            	802.1AB-2005 9.5.3.2.
          items:
            format: int64
            type: integer
          type: array
        remote_chassis_id:
          description: >
            List of RemoteChassisId. RemoteChassisId represents ID of the remote
            chassis
          items:
            type: string
          type: array
        remote_interface_name:
          description: >
            List of RemoteInterfaceName. RemoteInterfaceName represents String
            value used to identify the port component associated
            	with the remote system as defined by IEEE 802.1AB-2005
            	9.5.3.3.
          items:
            type: string
          type: array
        remote_port_description:
          description: >
            List of RemotePortDescription. RemotePortDescription represents
            String value used to identify the description of the given
            	port associated with the remote system as defined by IEEE
            	802.1AB-2005 9.5.5.2.
          items:
            type: string
          type: array
        remote_system_description:
          description: >
            List of RemoteSystemDescription. RemoteSystemDescription represents
            description of the remote system
          items:
            type: string
          type: array
        remote_system_name:
          description: >
            List of RemoteSystemName. RemoteSystemName represents Name of the
            remote system
          items:
            type: string
          type: array
        sys_cap_enabled:
          description: >
            List of SysCapEnabled. SysCapEnabled represents The list of system
            capabilities enabled
            	on the remote system.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        sys_cap_supported:
          description: >
            List of SysCapSupported. SysCapSupported represents The list of
            system capabilities supported
            	on the remote system.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        sys_name:
          description: >
            List of SysName. SysName represents The string value used to
            identify the
            	system name of the remote system as defined
            	by IEEE 802.1AB-2005 9.5.6.2.
          items:
            type: string
          type: array
        time_mark:
          description: >
            List of TimeMark. TimeMark represents TimeFilter value as defined by
            IETF RFC 2021 section 6.
          items:
            type: integer
          type: array
        ttl:
          description: |
            List of Ttl. Ttl represents Time To Live
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborStateGetRequest
      type: object
    AssetLldpNeighborStateGetResponse:
      properties:
        AssetLldpNeighborState:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborStateGetResponse
      type: object
    AssetLldpNeighborXmed:
      description: AssetLldpNeighborXmed - This object describes the lldp neighbor MED
        state info list.
        AssetAllOfLldpNeighborXmedInventory,AssetLldpNeighborXmedPoePd,AssetLldpNeighborXmedPoePse,AssetLldpNeighborXmedPolicy,AssetPortLldpLocation
        can be created while creating AssetLldpNeighborXmed.
      nullable: true
      properties:
        asset_all_of_lldp_neighbor_xmed_inventory:
          $ref: "#/components/schemas/AssetAllOfLldpNeighborXmedInventory"
        asset_lldp_neighbor_state_id:
          description: ID of the parent lldp-neighbor-state
          type: string
        asset_lldp_neighbor_xmed_poe_pd:
          $ref: "#/components/schemas/AssetLldpNeighborXmedPoePd"
        asset_lldp_neighbor_xmed_poe_pse:
          $ref: "#/components/schemas/AssetLldpNeighborXmedPoePse"
        asset_lldp_neighbor_xmed_policy:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborXmedPolicy"
          nullable: true
          type: array
        asset_port_lldp_location:
          $ref: "#/components/schemas/AssetPortLldpLocation"
        cap_current:
          items:
            format: int64
            type: integer
          type: array
        cap_supported:
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_class:
          description: |-
            Device Class as advertised by the device remotely connected
            	to the port. For details see ANSI/TIA-1057, Section 10.2.2.2.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetLldpNeighborXmedGetRequest:
      description: AssetLldpNeighborXmedGetRequest represents the retrieval request
        for AssetLldpNeighborXmed.
      properties:
        asset_lldp_neighbor_state_id:
          description: >
            List of AssetLldpNeighborStateId. AssetLldpNeighborStateId
            represents ID of the parent lldp-neighbor-state
          items:
            type: string
          type: array
        cap_current:
          description: >
            List of CapCurrent. CapCurrent represents MED organizationally
            defined set of LLDP TLVs whose transmission
            	is possible on the LLDP agent of the remote device connected to this port.
            	Each TLV type corresponds to a specific TIA TR41.4 MED optional TLV.
            	For details see ANSI/TIA-1057, Sections 10.2.2.1.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        cap_supported:
          description: >
            List of CapSupported. CapSupported represents MED organizationally
            defined set of LLDP TLVs whose
            	transmission is possible on the LLDP agent of the remote device
            	connected to this port. Each bit in the bitmap corresponds to an
            	LLDP-MED subtype associated with a specific TIA TR41.4 MED optional
            	TLV. For details see ANSI/TIA-1057, Sections 10.2.2.1.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_class:
          description: >
            List of DeviceClass. DeviceClass represents Device Class as
            advertised by the device remotely connected
            	to the port. For details see ANSI/TIA-1057, Section 10.2.2.2.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborXmedGetRequest
      type: object
    AssetLldpNeighborXmedGetResponse:
      properties:
        AssetLldpNeighborXmed:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborXmed"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborXmedGetResponse
      type: object
    AssetLldpNeighborXmedPoePd:
      description: AssetLldpNeighborXmedPoePd - This object describes the lldp
        neighbor MED POE PD info list.
      nullable: true
      properties:
        asset_lldp_neighbor_xmed_id:
          description: Asset LLDP neighbor XMed ID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_type:
          description: >-
            The type of Power-via-MDI (Power over Ethernet) advertised by the
            remote device.
            	PD_DEVICE  - the device is advertised as a Powered Device (PD)
            	PSE_DEVICE - the device is advertised as a Power Sourcing Entity (PSE)
            	For detailes see ANSI/TIA-1057, Section 10.2.5.1.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        power_priority:
          description: >-
            Priority of the port from the point of view of a power management
            algorithm.
            	This is used by control mechanism that prevents over current situations by
            	disconnecting first ports with lower power priority.
            	CRITICAL - power priority is critical, as per RFC 3621
            	HIGH     - power priority is high, as per RFC 3621
            	LOW      - power priority is low, as per RFC 3621
            	For details see ANSI/TIA-1057, Section 10.2.5.3.
          type: string
        power_req:
          description: >-
            The value of the power required by a PD connected remotely to the
            port
            	expressed in units of 0.1 watts.
            	For details see ANSI/TIA-1057, Section 10.2.5.4.
          type: integer
        power_source:
          description: >-
            Defines the type of Power Source advertised as being used by the
            device
            	connected remotely to the port.
            	LOCAL     - the device advertises its power source as local
            	LOCAL_PSE - the device advertises its power source as using both local and PSE power
            	PSE       - the device advertises its power source as received from a PSE
            	For details see ANSI/TIA-1057, Section 10.2.5.2.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetLldpNeighborXmedPoePdGetRequest:
      description: AssetLldpNeighborXmedPoePdGetRequest represents the retrieval
        request for AssetLldpNeighborXmedPoePd.
      properties:
        asset_lldp_neighbor_xmed_id:
          description: >
            List of AssetLldpNeighborXmedId. AssetLldpNeighborXmedId represents
            Asset LLDP neighbor XMed ID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_type:
          description: >
            List of DeviceType. DeviceType represents The type of Power-via-MDI
            (Power over Ethernet) advertised by the remote device.
            	PD_DEVICE  - the device is advertised as a Powered Device (PD)
            	PSE_DEVICE - the device is advertised as a Power Sourcing Entity (PSE)
            	For detailes see ANSI/TIA-1057, Section 10.2.5.1.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        power_priority:
          description: >
            List of PowerPriority. PowerPriority represents Priority of the port
            from the point of view of a power management algorithm.
            	This is used by control mechanism that prevents over current situations by
            	disconnecting first ports with lower power priority.
            	CRITICAL - power priority is critical, as per RFC 3621
            	HIGH     - power priority is high, as per RFC 3621
            	LOW      - power priority is low, as per RFC 3621
            	For details see ANSI/TIA-1057, Section 10.2.5.3.
          items:
            type: string
          type: array
        power_req:
          description: >
            List of PowerReq. PowerReq represents The value of the power
            required by a PD connected remotely to the port
            	expressed in units of 0.1 watts.
            	For details see ANSI/TIA-1057, Section 10.2.5.4.
          items:
            type: integer
          type: array
        power_source:
          description: >
            List of PowerSource. PowerSource represents Defines the type of
            Power Source advertised as being used by the device
            	connected remotely to the port.
            	LOCAL     - the device advertises its power source as local
            	LOCAL_PSE - the device advertises its power source as using both local and PSE power
            	PSE       - the device advertises its power source as received from a PSE
            	For details see ANSI/TIA-1057, Section 10.2.5.2.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborXmedPoePdGetRequest
      type: object
    AssetLldpNeighborXmedPoePdGetResponse:
      properties:
        AssetLldpNeighborXmedPoePd:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborXmedPoePd"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborXmedPoePdGetResponse
      type: object
    AssetLldpNeighborXmedPoePse:
      description: AssetLldpNeighborXmedPoePse - This object describes the lldp
        neighbor MED POE PSE list.
      nullable: true
      properties:
        asset_lldp_neighbor_xmed_id:
          description: Asset LLDP neighbor XMed ID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_type:
          description: >-
            The type of Power-via-MDI (Power over Ethernet) advertised by the
            remote device.
            	PD_DEVICE  - the device is advertised as a Powered Device (PD)
            	PSE_DEVICE - the device is advertised as a Power Sourcing Entity (PSE)
            	For detailes see ANSI/TIA-1057, Section 10.2.5.1.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        power_av:
          description: |-
            Contains the value of the power available from the PSE via this port
            	expressed in units of 0.1 watts on the remote device.
            	For details see ANSI/TIA-1057, Section 10.2.5.4.
          type: integer
        power_priority:
          description: >-
            Priority of the port from the point of view of a power management
            algorithm.
            	This is used by control mechanism that prevents over current situations by
            	disconnecting first ports with lower power priority.
            	CRITICAL - power priority is critical, as per RFC 3621
            	HIGH     - power priority is high, as per RFC 3621
            	LOW      - power priority is low, as per RFC 3621
            	For details see ANSI/TIA-1057, Section 10.2.5.3.
          type: string
        power_source:
          description: >-
            Defines the type of PSE Power Source advertised  by the remote
            device.
            	BACKUP  - the device advertises its power source as backup
            	PRIMARY - the device advertises its power source as primary
            	For details see ANSI/TIA-1057, Section 10.2.5.2.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetLldpNeighborXmedPoePseGetRequest:
      description: AssetLldpNeighborXmedPoePseGetRequest represents the retrieval
        request for AssetLldpNeighborXmedPoePse.
      properties:
        asset_lldp_neighbor_xmed_id:
          description: >
            List of AssetLldpNeighborXmedId. AssetLldpNeighborXmedId represents
            Asset LLDP neighbor XMed ID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_type:
          description: >
            List of DeviceType. DeviceType represents The type of Power-via-MDI
            (Power over Ethernet) advertised by the remote device.
            	PD_DEVICE  - the device is advertised as a Powered Device (PD)
            	PSE_DEVICE - the device is advertised as a Power Sourcing Entity (PSE)
            	For detailes see ANSI/TIA-1057, Section 10.2.5.1.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        power_av:
          description: >
            List of PowerAv. PowerAv represents Contains the value of the power
            available from the PSE via this port
            	expressed in units of 0.1 watts on the remote device.
            	For details see ANSI/TIA-1057, Section 10.2.5.4.
          items:
            type: integer
          type: array
        power_priority:
          description: >
            List of PowerPriority. PowerPriority represents Priority of the port
            from the point of view of a power management algorithm.
            	This is used by control mechanism that prevents over current situations by
            	disconnecting first ports with lower power priority.
            	CRITICAL - power priority is critical, as per RFC 3621
            	HIGH     - power priority is high, as per RFC 3621
            	LOW      - power priority is low, as per RFC 3621
            	For details see ANSI/TIA-1057, Section 10.2.5.3.
          items:
            type: string
          type: array
        power_source:
          description: >
            List of PowerSource. PowerSource represents Defines the type of PSE
            Power Source advertised  by the remote device.
            	BACKUP  - the device advertises its power source as backup
            	PRIMARY - the device advertises its power source as primary
            	For details see ANSI/TIA-1057, Section 10.2.5.2.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborXmedPoePseGetRequest
      type: object
    AssetLldpNeighborXmedPoePseGetResponse:
      properties:
        AssetLldpNeighborXmedPoePse:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborXmedPoePse"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborXmedPoePseGetResponse
      type: object
    AssetLldpNeighborXmedPolicy:
      description: AssetLldpNeighborXmedPolicy - This object describes the lldp
        neighbor MED policy state info list.
      nullable: true
      properties:
        app_type:
          description: >-
            The media type that defines the primary function of the
            	application for the policy advertised by the endpoint connected
            	remotely to this port. If type is UNKNOWN, vlanId, priority and dscp
            	attributes values are ignored.
            	According to Nos API yaml it is an Enum but here stored as space separated list of strings.
            	Possible Enum Values: [GUEST_VOICE, GUEST_VOICE_SIGNALING, SOFT_PHONE_VOICE, STREAMING_VIDEO, VIDEO_CONFERENCING, VIDEO_SIGNALING, VOICE, VOICE_SIGNALING]
          type: string
        asset_lldp_neighbor_xmed_id:
          description: Asset LLDP neighbor XMed ID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dscp:
          description: >-
            Contains the value of the Differentiated Service Code Point (DSCP)
            	as defined in IETF RFC 2474 and RFC 2475 which is associated with remote
            	system connected at the port.
            	For details see ANSI/TIA-1057, Section 10.2.3.7.
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        priority:
          description: |-
            Contains the value of the 802.1p priority which is associated
            	with the remote system connected at given port.
            	For details see ANSI/TIA-1057, Section 10.2.3.6.
          type: integer
        tagged_enabled:
          description: >-
            A value of 'true' indicates that the application is using a tagged
            VLAN.
            	A value of 'false' indicates that for the specific application the device either is
            	using an untagged VLAN or does not support port based VLAN operation. In this case,
            	both the vlanId and priority attributes are ignored and only the DSCP value has relevance.
            	For details see ANSI/TIA-1057, Section 10.2.3.3.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: >-
            An extension of the VLAN Identifier for the remote system connected
            to
            	this port, as defined in IEEE 802.1P-1998. A value of 1 through 4094 is used
            	to define a valid PVID. A value of 0 shall be used if the device is using
            	priority tagged frames, meaning that only the 802.1p priority level is significant
            	and the default VID of the ingress port is being used instead.
            	A value of 4095 is reserved for implementation use.
            	For details see ANSI/TIA-1057, Section 10.2.3.5.
          format: int64
          minimum: 0
          type: integer
      required:
        - app_type
      type: object
    AssetLldpNeighborXmedPolicyGetRequest:
      description: AssetLldpNeighborXmedPolicyGetRequest represents the retrieval
        request for AssetLldpNeighborXmedPolicy.
      properties:
        app_type:
          description: >
            List of AppType. AppType represents The media type that defines the
            primary function of the
            	application for the policy advertised by the endpoint connected
            	remotely to this port. If type is UNKNOWN, vlanId, priority and dscp
            	attributes values are ignored.
            	According to Nos API yaml it is an Enum but here stored as space separated list of strings.
            	Possible Enum Values: [GUEST_VOICE, GUEST_VOICE_SIGNALING, SOFT_PHONE_VOICE, STREAMING_VIDEO, VIDEO_CONFERENCING, VIDEO_SIGNALING, VOICE, VOICE_SIGNALING]
          items:
            type: string
          type: array
        asset_lldp_neighbor_xmed_id:
          description: >
            List of AssetLldpNeighborXmedId. AssetLldpNeighborXmedId represents
            Asset LLDP neighbor XMed ID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dscp:
          description: >
            List of Dscp. Dscp represents Contains the value of the
            Differentiated Service Code Point (DSCP)
            	as defined in IETF RFC 2474 and RFC 2475 which is associated with remote
            	system connected at the port.
            	For details see ANSI/TIA-1057, Section 10.2.3.7.
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents Contains the value of the
            802.1p priority which is associated
            	with the remote system connected at given port.
            	For details see ANSI/TIA-1057, Section 10.2.3.6.
          items:
            type: integer
          type: array
        tagged_enabled:
          description: >
            List of TaggedEnabled. TaggedEnabled represents A value of 'true'
            indicates that the application is using a tagged VLAN.
            	A value of 'false' indicates that for the specific application the device either is
            	using an untagged VLAN or does not support port based VLAN operation. In this case,
            	both the vlanId and priority attributes are ignored and only the DSCP value has relevance.
            	For details see ANSI/TIA-1057, Section 10.2.3.3.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: >
            List of VlanId. VlanId represents An extension of the VLAN
            Identifier for the remote system connected to
            	this port, as defined in IEEE 802.1P-1998. A value of 1 through 4094 is used
            	to define a valid PVID. A value of 0 shall be used if the device is using
            	priority tagged frames, meaning that only the 802.1p priority level is significant
            	and the default VID of the ingress port is being used instead.
            	A value of 4095 is reserved for implementation use.
            	For details see ANSI/TIA-1057, Section 10.2.3.5.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetLldpNeighborXmedPolicyGetRequest
      type: object
    AssetLldpNeighborXmedPolicyGetResponse:
      properties:
        AssetLldpNeighborXmedPolicy:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborXmedPolicy"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborXmedPolicyGetResponse
      type: object
    AssetLldpNeighborZtf:
      description: AssetLldpNeighborZtf - This object describes the lldp neighbor ZTF
        state info list. AssetLldpNeighborZtfTunnelGroup can be created while
        creating AssetLldpNeighborZtf.
      nullable: true
      properties:
        asset_lldp_neighbor_state_id:
          description: ID of the parent lldp-neighbor-state
          type: string
        asset_lldp_neighbor_ztf_tunnel_group:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborZtfTunnelGroup"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_extended_auth:
          description: Advertised fabric extend authentication state.
          format: int64
          type: integer
        gateway_ip_address:
          description: Advertised gateway IP address (Ipv4 or Ipv6).
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        local_ip_address:
          description: Advertised local IP address (Ipv4 or Ipv6) and mask.
          type: string
        mtu:
          description: Advertised Maximum Transmission Unit.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetLldpNeighborZtfGetRequest:
      description: AssetLldpNeighborZtfGetRequest represents the retrieval request for
        AssetLldpNeighborZtf.
      properties:
        asset_lldp_neighbor_state_id:
          description: >
            List of AssetLldpNeighborStateId. AssetLldpNeighborStateId
            represents ID of the parent lldp-neighbor-state
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_extended_auth:
          description: >
            List of FabricExtendedAuth. FabricExtendedAuth represents Advertised
            fabric extend authentication state.
          items:
            format: int64
            type: integer
          type: array
        gateway_ip_address:
          description: >
            List of GatewayIpAddress. GatewayIpAddress represents Advertised
            gateway IP address (Ipv4 or Ipv6).
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        local_ip_address:
          description: >
            List of LocalIpAddress. LocalIpAddress represents Advertised local
            IP address (Ipv4 or Ipv6) and mask.
          items:
            type: string
          type: array
        mtu:
          description: |
            List of Mtu. Mtu represents Advertised Maximum Transmission Unit.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborZtfGetRequest
      type: object
    AssetLldpNeighborZtfGetResponse:
      properties:
        AssetLldpNeighborZtf:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborZtf"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborZtfGetResponse
      type: object
    AssetLldpNeighborZtfTunnelGroup:
      description: AssetLldpNeighborZtfTunnelGroup - This object describes the lldp
        neighbor ZTF tunnel group state info list.
      nullable: true
      properties:
        asset_lldp_neighbor_ztf_id:
          description: ID of the parent lldp-neighbor-ztf
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        destination_tunnel_list:
          description: Destination tunnels list.
          items:
            type: string
          nullable: true
          type: array
        enabled:
          description: Enabled state.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        metric:
          description: Metric.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        tunnel_group_id:
          description: Group identifier.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetLldpNeighborZtfTunnelGroupGetRequest:
      description: AssetLldpNeighborZtfTunnelGroupGetRequest represents the retrieval
        request for AssetLldpNeighborZtfTunnelGroup.
      properties:
        asset_lldp_neighbor_ztf_id:
          description: >
            List of AssetLldpNeighborZtfId. AssetLldpNeighborZtfId represents ID
            of the parent lldp-neighbor-ztf
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        destination_tunnel_list:
          description: >
            List of DestinationTunnelList. DestinationTunnelList represents
            Destination tunnels list.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents Enabled state.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        metric:
          description: |
            List of Metric. Metric represents Metric.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        tunnel_group_id:
          description: |
            List of TunnelGroupId. TunnelGroupId represents Group identifier.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpNeighborZtfTunnelGroupGetRequest
      type: object
    AssetLldpNeighborZtfTunnelGroupGetResponse:
      properties:
        AssetLldpNeighborZtfTunnelGroup:
          items:
            $ref: "#/components/schemas/AssetLldpNeighborZtfTunnelGroup"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpNeighborZtfTunnelGroupGetResponse
      type: object
    AssetLldpPortAdvertiseSettingsConfig:
      description: AssetLldpPortAdvertiseSettingsConfig - DB model for LLDP Port
        Advertise Settings Config Dot1PortProtocolVlan,Dot1VlanObject can be
        created while creating AssetLldpPortAdvertiseSettingsConfig.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dot1_port_protocol_vlan:
          $ref: "#/components/schemas/AssetLldpDot1PortProtocolVlan"
        dot1_vlan_object:
          $ref: "#/components/schemas/AssetLldpDot1VlanObject"
        dot3_mac_phy:
          default: false
          description: >-
            Dot3 MAC/PHY refers to the TLV (Type-Length-Value) that advertises
            the MAC and physical
            	layer capabilities of a port.
          type: boolean
        dot3_power:
          default: false
          description: >-
            Dot3 power refers to the Power over Ethernet (PoE) TLV
            (Type-Length-Value) defined by
            	IEEE 802.3
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inventory:
          description: >-
            The inventory TLV (Type-Length-Value) is part of the LLDP-MED (Media
            Endpoint Discovery)
            	extension. For Fabric Engine, default value is True
          type: boolean
        lldp_port_config_id:
          description: UUID of the lldp-port-config
          type: string
        location:
          description: >-
            The location refers to the TLV (Type-Length-Value) that provides
            geographic or civic
            	address information of a device. For Fabric Engine, default value is True
          type: boolean
        management_address:
          description: >-
            The management address is a TLV (Type-Length-Value) that provides
            the IP address or other
            	network address of the device's management interface. For Fabric Engine, default value is
            	True
          type: boolean
        med_capabilities:
          description: >-
            MED capabilities refer to the capabilities of Media Endpoint
            Discovery (LLDP-MED). For
            	Fabric Engine, default value is True
          type: boolean
        med_power:
          description: >-
            MED power refers to the Power Management TLV (Type-Length-Value)
            used in LLDP-MED (Media
            	Endpoint Discovery). For Fabric Engine, default value is True
          type: boolean
        network_policy:
          description: >-
            The network policy TLV (Type-Length-Value) is used to communicate
            specific network
            	configurations for real-time applications like voice or video. For Fabric Engine, default
            	value is True
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_description:
          description: >-
            The port description is a TLV (Type-Length-Value) that provides
            information about the
            	specific port. This description typically includes details such as the port's identifier,
            	manufacturer, product name, and hardware or software version. For Fabric Engine, default
            	value is True
          type: boolean
        system_capabilities:
          description: >-
            The system capabilities refer to the primary functions that a device
            can perform and whether
            	these functions are enabled.
          type: boolean
        system_description:
          description: >-
            The system description is a TLV (Type-Length-Value) that provides a
            textual description of
            	the device.
          type: boolean
        system_name:
          description: The system name is a TLV (Type-Length-Value) that provides the name
            of the device.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - inventory
        - lldp_port_config_id
        - location
        - management_address
        - med_capabilities
        - med_power
        - network_policy
        - port_description
        - system_capabilities
      type: object
    AssetLldpPortAdvertiseSettingsConfigGetRequest:
      description: AssetLldpPortAdvertiseSettingsConfigGetRequest represents the
        retrieval request for AssetLldpPortAdvertiseSettingsConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dot3_mac_phy:
          description: >
            List of Dot3MacPhy. Dot3MacPhy represents Dot3 MAC/PHY refers to the
            TLV (Type-Length-Value) that advertises the MAC and physical
            	layer capabilities of a port.
          items:
            type: boolean
          type: array
        dot3_power:
          description: >
            List of Dot3Power. Dot3Power represents Dot3 power refers to the
            Power over Ethernet (PoE) TLV (Type-Length-Value) defined by
            	IEEE 802.3
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        inventory:
          description: >
            List of Inventory. Inventory represents The inventory TLV
            (Type-Length-Value) is part of the LLDP-MED (Media Endpoint
            Discovery)
            	extension. For Fabric Engine, default value is True
          items:
            type: boolean
          type: array
        lldp_port_config_id:
          description: >
            List of LldpPortConfigId. LldpPortConfigId represents UUID of the
            lldp-port-config
          items:
            type: string
          type: array
        location:
          description: >
            List of Location. Location represents The location refers to the TLV
            (Type-Length-Value) that provides geographic or civic
            	address information of a device. For Fabric Engine, default value is True
          items:
            type: boolean
          type: array
        management_address:
          description: >
            List of ManagementAddress. ManagementAddress represents The
            management address is a TLV (Type-Length-Value) that provides the IP
            address or other
            	network address of the device's management interface. For Fabric Engine, default value is
            	True
          items:
            type: boolean
          type: array
        med_capabilities:
          description: >
            List of MedCapabilities. MedCapabilities represents MED capabilities
            refer to the capabilities of Media Endpoint Discovery (LLDP-MED).
            For
            	Fabric Engine, default value is True
          items:
            type: boolean
          type: array
        med_power:
          description: >
            List of MedPower. MedPower represents MED power refers to the Power
            Management TLV (Type-Length-Value) used in LLDP-MED (Media
            	Endpoint Discovery). For Fabric Engine, default value is True
          items:
            type: boolean
          type: array
        network_policy:
          description: >
            List of NetworkPolicy. NetworkPolicy represents The network policy
            TLV (Type-Length-Value) is used to communicate specific network
            	configurations for real-time applications like voice or video. For Fabric Engine, default
            	value is True
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_description:
          description: >
            List of PortDescription. PortDescription represents The port
            description is a TLV (Type-Length-Value) that provides information
            about the
            	specific port. This description typically includes details such as the port's identifier,
            	manufacturer, product name, and hardware or software version. For Fabric Engine, default
            	value is True
          items:
            type: boolean
          type: array
        system_capabilities:
          description: >
            List of SystemCapabilities. SystemCapabilities represents The system
            capabilities refer to the primary functions that a device can
            perform and whether
            	these functions are enabled.
          items:
            type: boolean
          type: array
        system_description:
          description: >
            List of SystemDescription. SystemDescription represents The system
            description is a TLV (Type-Length-Value) that provides a textual
            description of
            	the device.
          items:
            type: boolean
          type: array
        system_name:
          description: >
            List of SystemName. SystemName represents The system name is a TLV
            (Type-Length-Value) that provides the name of the device.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpPortAdvertiseSettingsConfigGetRequest
      type: object
    AssetLldpPortAdvertiseSettingsConfigGetResponse:
      properties:
        AssetLldpPortAdvertiseSettingsConfig:
          items:
            $ref: "#/components/schemas/AssetLldpPortAdvertiseSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpPortAdvertiseSettingsConfigGetResponse
      type: object
    AssetLldpPortConfig:
      description: AssetLldpPortConfig - DB model for LLDP Port Config
        AdvertiseSettingsConfig,MedPolicySettingsConfig can be created while
        creating AssetLldpPortConfig.
      properties:
        advertise_settings_config:
          $ref: "#/components/schemas/AssetLldpPortAdvertiseSettingsConfig"
        civic_address:
          description: Physical location information of a device.
          type: string
        coordinate:
          description: Geographic location information of a device.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        ecs_elin:
          description: >-
            Emergency Call Service ELIN, which is a unique identifier used to
            specify the location of
            	a network device based on its Emergency Location Identification Number (ELIN).
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Name of the interface
          type: string
        lldp_config_id:
          description: UUID of the lldp-config
          type: string
        med_policy_settings_config:
          items:
            $ref: "#/components/schemas/AssetLldpPortMedPolicySettingsConfig"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        receive_enabled:
          description: >-
            The value of true indicates that the port is configured to accept
            and process LLDP
            	packets from neighboring devices. Default value for Fabric Engine devices is true.
          type: boolean
        transmit_enabled:
          description: >-
            The value of true indicates that the port is configured to send LLDP
            packets to its
            	neighbors. Default value for Fabric Engine devices is true.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - civic_address
        - coordinate
        - ecs_elin
        - interface_name
        - lldp_config_id
        - receive_enabled
        - transmit_enabled
      type: object
    AssetLldpPortConfigGetRequest:
      description: AssetLldpPortConfigGetRequest represents the retrieval request for
        AssetLldpPortConfig.
      properties:
        civic_address:
          description: >
            List of CivicAddress. CivicAddress represents Physical location
            information of a device.
          items:
            type: string
          type: array
        coordinate:
          description: >
            List of Coordinate. Coordinate represents Geographic location
            information of a device.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        ecs_elin:
          description: >
            List of EcsElin. EcsElin represents Emergency Call Service ELIN,
            which is a unique identifier used to specify the location of
            	a network device based on its Emergency Location Identification Number (ELIN).
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Name of the interface
          items:
            type: string
          type: array
        lldp_config_id:
          description: >
            List of LldpConfigId. LldpConfigId represents UUID of the lldp-config
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        receive_enabled:
          description: >
            List of ReceiveEnabled. ReceiveEnabled represents The value of true
            indicates that the port is configured to accept and process LLDP
            	packets from neighboring devices. Default value for Fabric Engine devices is true.
          items:
            type: boolean
          type: array
        transmit_enabled:
          description: >
            List of TransmitEnabled. TransmitEnabled represents The value of
            true indicates that the port is configured to send LLDP packets to
            its
            	neighbors. Default value for Fabric Engine devices is true.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLldpPortConfigGetRequest
      type: object
    AssetLldpPortConfigGetResponse:
      properties:
        AssetLldpPortConfig:
          items:
            $ref: "#/components/schemas/AssetLldpPortConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpPortConfigGetResponse
      type: object
    AssetLldpPortMedPolicySettingsConfig:
      description: AssetLldpPortMedPolicySettingsConfig - DB model for LLDP Port Med
        Policy Settings Config
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dscp:
          description: Specify the level of service for specific types of traffic
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lldp_port_config_id:
          description: UUID of the lldp-port-med-policy-settings-config
          type: string
        origin:
          description: Source of the network policy information. Fabric Engine (VOSS) only
            [CONFIG, AUTO_SENSE]
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        priority:
          description: >-
            The 802.1p priority level assigned to the traffic for a specific
            application type. Fabric
            	Engine (VOSS) only.
          type: integer
        priority_tagged:
          description: >-
            The combination of setting a priority level and tagging the VLAN
            traffic. Switch Engine
            	(EXOS) only
          type: boolean
        tagged:
          description: >-
            VLAN (Virtual Local Area Network) traffic is tagged or untagged.
            Fabric Engine (VOSS)
            	only
          type: boolean
        type:
          description: >-
            The type in the policy settings configuration refers to the specific
            application type for
            	which the network policy is being defined. LLDP-MED extends LLDP to support devices like
            	IP phones and provides additional TLVs (Type-Length-Value) for capabilities discovery,
            	network policy, Power over Ethernet, and inventory management.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: The id of the vlan
          type: integer
      required:
        - dscp
        - lldp_port_config_id
        - origin
        - priority
        - priority_tagged
        - tagged
        - type
        - vlan_id
      type: object
    AssetLldpPortMedPolicySettingsConfigGetRequest:
      description: AssetLldpPortMedPolicySettingsConfigGetRequest represents the
        retrieval request for AssetLldpPortMedPolicySettingsConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dscp:
          description: >
            List of Dscp. Dscp represents Specify the level of service for
            specific types of traffic
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lldp_port_config_id:
          description: >
            List of LldpPortConfigId. LldpPortConfigId represents UUID of the
            lldp-port-med-policy-settings-config
          items:
            type: string
          type: array
        origin:
          description: >
            List of Origin. Origin represents Source of the network policy
            information. Fabric Engine (VOSS) only [CONFIG, AUTO_SENSE]
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents The 802.1p priority level
            assigned to the traffic for a specific application type. Fabric
            	Engine (VOSS) only.
          items:
            type: integer
          type: array
        priority_tagged:
          description: >
            List of PriorityTagged. PriorityTagged represents The combination of
            setting a priority level and tagging the VLAN traffic. Switch Engine
            	(EXOS) only
          items:
            type: boolean
          type: array
        tagged:
          description: >
            List of Tagged. Tagged represents VLAN (Virtual Local Area Network)
            traffic is tagged or untagged. Fabric Engine (VOSS)
            	only
          items:
            type: boolean
          type: array
        type:
          description: >
            List of Type. Type represents The type in the policy settings
            configuration refers to the specific application type for
            	which the network policy is being defined. LLDP-MED extends LLDP to support devices like
            	IP phones and provides additional TLVs (Type-Length-Value) for capabilities discovery,
            	network policy, Power over Ethernet, and inventory management.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents The id of the vlan
          items:
            type: integer
          type: array
      title: AssetLldpPortMedPolicySettingsConfigGetRequest
      type: object
    AssetLldpPortMedPolicySettingsConfigGetResponse:
      properties:
        AssetLldpPortMedPolicySettingsConfig:
          items:
            $ref: "#/components/schemas/AssetLldpPortMedPolicySettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLldpPortMedPolicySettingsConfigGetResponse
      type: object
    AssetLocation:
      description: AssetLocation - This object describes the asset location list.
      nullable: true
      properties:
        asset_device_id:
          description: Device Id
          type: string
        building_id:
          description: The building ID where the device is located.
          format: int64
          minimum: 0
          type: integer
        building_latitude:
          description: The building latitude where the device is located.
          type: number
        building_longitude:
          description: The building longitude.
          type: number
        building_name:
          description: The building name where the device is located.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        floor_id:
          description: The floor ID where the device is located.
          format: int64
          minimum: 0
          type: integer
        floor_name:
          description: The floor name where the device is located.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        site_id:
          description: The site ID where the device is located.
          format: int64
          minimum: 0
          type: integer
        site_latitude:
          description: The site latitude.
          type: number
        site_longitude:
          description: The site longitude.
          type: number
        site_name:
          description: The site name where the device is located.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetLocationGetRequest:
      description: AssetLocationGetRequest represents the retrieval request for
        AssetLocation.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        building_id:
          description: >
            List of BuildingId. BuildingId represents The building ID where the
            device is located.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        building_latitude:
          description: >
            List of BuildingLatitude. BuildingLatitude represents The building
            latitude where the device is located.
          items:
            type: number
          type: array
        building_longitude:
          description: >
            List of BuildingLongitude. BuildingLongitude represents The building
            longitude.
          items:
            type: number
          type: array
        building_name:
          description: >
            List of BuildingName. BuildingName represents The building name
            where the device is located.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        floor_id:
          description: >
            List of FloorId. FloorId represents The floor ID where the device is
            located.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        floor_name:
          description: >
            List of FloorName. FloorName represents The floor name where the
            device is located.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        site_id:
          description: >
            List of SiteId. SiteId represents The site ID where the device is
            located.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        site_latitude:
          description: |
            List of SiteLatitude. SiteLatitude represents The site latitude.
          items:
            type: number
          type: array
        site_longitude:
          description: |
            List of SiteLongitude. SiteLongitude represents The site longitude.
          items:
            type: number
          type: array
        site_name:
          description: >
            List of SiteName. SiteName represents The site name where the device
            is located.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetLocationGetRequest
      type: object
    AssetLocationGetResponse:
      properties:
        AssetLocation:
          items:
            $ref: "#/components/schemas/AssetLocation"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetLocationGetResponse
      type: object
    AssetMacLockingConfig:
      description: AssetMacLockingConfig - DB model for MAC locking global settings
        configuration MacLockingPortEntries can be created while creating
        AssetMacLockingConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: A value of true indicates MAC-locking is globally enabled on this
            device.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mac_locking_port_entries:
          items:
            $ref: "#/components/schemas/AssetPortMacLockingConfig"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetMacLockingConfigGetRequest:
      description: AssetMacLockingConfigGetRequest represents the retrieval request
        for AssetMacLockingConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates
            MAC-locking is globally enabled on this device.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetMacLockingConfigGetRequest
      type: object
    AssetMacLockingConfigGetResponse:
      properties:
        AssetMacLockingConfig:
          items:
            $ref: "#/components/schemas/AssetMacLockingConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetMacLockingConfigGetResponse
      type: object
    AssetMlagPeerConfig:
      description: AssetMlagPeerConfig - This object describes the mlag peer config list.
      properties:
        asset_device_id:
          description: MLAG peer configuration object
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        lacp_smll_sys_id:
          description: LACP SMLT system identifier, this should be configured to the same
            value on both peers in case LACP is used on split multi-link trunk
            (SMLT) - Fabric Engine (VOSS) only
          type: string
        lag_id:
          description: MLT identifier of virtual-ist link -  used for simplified VIST -
            Fabric Engine (VOSS) only
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        peer_bmac:
          description: Virtual Backbone MAC of the VIST pair - Fabric Engine (VOSS) only
          type: string
        peer_id:
          description: MLAG peer identifier - Name of the MLAG peer for Switch Engine
            (EXOS), value is always "Default" for Fabric Engine (VOSS) since
            only one peer is supported
          type: string
        peer_ip_address:
          description: IP address of the MLAG peer - Ipv4 or Ipv6 for Switch Engine
            (EXOS), Ipv4 only for Fabric Engine (VOSS)
          type: string
        peer_ip_address_type:
          description: Specifies the type of IP address in the following property (Ipv4 or
            Ipv6)
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vist_vlan_id:
          description: VLAN identifier of virtual inter-switch trunk - Fabric Engine
            (VOSS) only
          format: int64
          type: integer
        vr_name:
          description: Virtual router identifier - Switch Engine (EXOS) only
          type: string
      required:
        - asset_device_id
        - peer_id
      type: object
    AssetMlagPeerConfigGetRequest:
      description: AssetMlagPeerConfigGetRequest represents the retrieval request for
        AssetMlagPeerConfig.
      properties:
        asset_device_id:
          description: >
            List of AssetDeviceId. AssetDeviceId represents MLAG peer
            configuration object
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        lacp_smll_sys_id:
          description: >
            List of LacpSmllSysId. LacpSmllSysId represents LACP SMLT system
            identifier, this should be configured to the same value on both
            peers in case LACP is used on split multi-link trunk (SMLT) - Fabric
            Engine (VOSS) only
          items:
            type: string
          type: array
        lag_id:
          description: >
            List of LagId. LagId represents MLT identifier of virtual-ist link
            -  used for simplified VIST - Fabric Engine (VOSS) only
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        peer_bmac:
          description: >
            List of PeerBmac. PeerBmac represents Virtual Backbone MAC of the
            VIST pair - Fabric Engine (VOSS) only
          items:
            type: string
          type: array
        peer_id:
          description: >
            List of PeerId. PeerId represents MLAG peer identifier - Name of the
            MLAG peer for Switch Engine (EXOS), value is always "Default" for
            Fabric Engine (VOSS) since only one peer is supported
          items:
            type: string
          type: array
        peer_ip_address:
          description: >
            List of PeerIpAddress. PeerIpAddress represents IP address of the
            MLAG peer - Ipv4 or Ipv6 for Switch Engine (EXOS), Ipv4 only for
            Fabric Engine (VOSS)
          items:
            type: string
          type: array
        peer_ip_address_type:
          description: >
            List of PeerIpAddressType. PeerIpAddressType represents Specifies
            the type of IP address in the following property (Ipv4 or Ipv6)
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vist_vlan_id:
          description: >
            List of VistVlanId. VistVlanId represents VLAN identifier of virtual
            inter-switch trunk - Fabric Engine (VOSS) only
          items:
            format: int64
            type: integer
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents Virtual router identifier - Switch
            Engine (EXOS) only
          items:
            type: string
          type: array
      title: AssetMlagPeerConfigGetRequest
      type: object
    AssetMlagPeerConfigGetResponse:
      properties:
        AssetMlagPeerConfig:
          items:
            $ref: "#/components/schemas/AssetMlagPeerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetMlagPeerConfigGetResponse
      type: object
    AssetMlagPeerCounters:
      description: AssetMlagPeerCounters - This object describes the mlag peer
        counters list.
      properties:
        asset_mlag_peer_state_id:
          description: MLAG peer counters
          type: string
        checkpoint_rx:
          description: Aggregated value for receive messages - Switch Engine (EXOS) only
          format: int64
          type: integer
        checkpoint_tx:
          description: Transmit counter for transmit messages - Switch Engine (EXOS) only
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        delete_mac_address_rx:
          description: The number of MAC address delete messages received - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        delete_mac_address_tx:
          description: The number of MAC address delete messages sent - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dialect_negotiate_rx:
          description: The number of dialect negotiate messages received - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        dialect_negotiate_tx:
          description: The number of dialect negotiate messages sent - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        entry_update_rx:
          description: The number of entry update messages received - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        entry_update_tx:
          description: The number of entry update messages sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        hello_rx:
          description: Receive counter for hello packets
          format: int64
          type: integer
        hello_timeouts:
          description: >-
            For Switch Engine (EXOS) - Number of times the hello packet detect
            timer has expired.
            	For Fabric Engine (VOSS) - Inter-Switch Trunk down.
          format: int64
          type: integer
        hello_tx:
          description: Transmit counter for hello packets
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        igmp_rx:
          description: The number of IGMP messages received - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        igmp_tx:
          description: The number of IGMP messages sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        learn_mac_address_rx:
          description: The number of MAC addresses received over VIST - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        learn_mac_address_tx:
          description: The number of MAC addresses sent over VIST - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        mac_address_ageout_rx:
          description: The number of MAC addresses ageout received - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        mac_address_ageout_tx:
          description: The number of MAC addresses ageout sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        mac_address_expired_rx:
          description: The number of MAC addresses expired receeived - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        mac_address_expired_tx:
          description: The number of MAC addresses expired sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        mlt_table_sync_request_rx:
          description: The number of MLT table sync requests reeived - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        mlt_table_sync_request_tx:
          description: The number of MLT table sync requests sent - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        mlt_table_sync_rx:
          description: The number of MLT table sync received - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        mlt_table_sync_tx:
          description: The number of MLT table sync sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        poll_count_high_watermark:
          description: The number of transaction queue high watermark messages - Fabric
            Engine (VOSS) only.
          format: int64
          type: integer
        port_down_rx:
          description: The number of port down messages received - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        port_down_tx:
          description: The number of port down messages sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        port_update_rx:
          description: The number of port update messages received - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        port_update_tx:
          description: The number of port update messages sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        request_mac_table_rx:
          description: The number of request MAC table messages received - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        request_mac_table_tx:
          description: The number of request MAC table messages sent - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        send_mac_address_rx:
          description: The number of send MAC address messages received - Fabric Engine
            (VOSS) only.
          format: int64
          type: integer
        send_mac_address_tx:
          description: The number of send MAC address messages sent - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        smlt_down_rx:
          description: The number of SMLT down messages received - Fabric Engine (VOSS)
            only.
          format: int64
          type: integer
        smlt_down_tx:
          description: The number of SMLT down messages sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        smlt_up_rx:
          description: The number of SMLT up messages received - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        smlt_up_tx:
          description: The number of SMLT up messages sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        transaction_queue_high_watermark:
          description: The number of transaction queue high watermark messages - Fabric
            Engine (VOSS) only.
          format: int64
          type: integer
        unknown_message_rx:
          description: The number of unknown message received - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        update_response_rx:
          description: The number of update response received - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        update_response_tx:
          description: The number of update response sent - Fabric Engine (VOSS) only.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_mlag_peer_state_id
      type: object
    AssetMlagPeerCountersGetRequest:
      description: AssetMlagPeerCountersGetRequest represents the retrieval request
        for AssetMlagPeerCounters.
      properties:
        asset_mlag_peer_state_id:
          description: >
            List of AssetMlagPeerStateId. AssetMlagPeerStateId represents MLAG
            peer counters
          items:
            type: string
          type: array
        checkpoint_rx:
          description: >
            List of CheckpointRx. CheckpointRx represents Aggregated value for
            receive messages - Switch Engine (EXOS) only
          items:
            format: int64
            type: integer
          type: array
        checkpoint_tx:
          description: >
            List of CheckpointTx. CheckpointTx represents Transmit counter for
            transmit messages - Switch Engine (EXOS) only
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        delete_mac_address_rx:
          description: >
            List of DeleteMacAddressRx. DeleteMacAddressRx represents The number
            of MAC address delete messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        delete_mac_address_tx:
          description: >
            List of DeleteMacAddressTx. DeleteMacAddressTx represents The number
            of MAC address delete messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dialect_negotiate_rx:
          description: >
            List of DialectNegotiateRx. DialectNegotiateRx represents The number
            of dialect negotiate messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        dialect_negotiate_tx:
          description: >
            List of DialectNegotiateTx. DialectNegotiateTx represents The number
            of dialect negotiate messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        entry_update_rx:
          description: >
            List of EntryUpdateRx. EntryUpdateRx represents The number of entry
            update messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        entry_update_tx:
          description: >
            List of EntryUpdateTx. EntryUpdateTx represents The number of entry
            update messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        hello_rx:
          description: >
            List of HelloRx. HelloRx represents Receive counter for hello packets
          items:
            format: int64
            type: integer
          type: array
        hello_timeouts:
          description: >
            List of HelloTimeouts. HelloTimeouts represents For Switch Engine
            (EXOS) - Number of times the hello packet detect timer has expired.
            	For Fabric Engine (VOSS) - Inter-Switch Trunk down.
          items:
            format: int64
            type: integer
          type: array
        hello_tx:
          description: >
            List of HelloTx. HelloTx represents Transmit counter for hello
            packets
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        igmp_rx:
          description: >
            List of IgmpRx. IgmpRx represents The number of IGMP messages
            received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        igmp_tx:
          description: >
            List of IgmpTx. IgmpTx represents The number of IGMP messages sent -
            Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        learn_mac_address_rx:
          description: >
            List of LearnMacAddressRx. LearnMacAddressRx represents The number
            of MAC addresses received over VIST - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        learn_mac_address_tx:
          description: >
            List of LearnMacAddressTx. LearnMacAddressTx represents The number
            of MAC addresses sent over VIST - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        mac_address_ageout_rx:
          description: >
            List of MacAddressAgeoutRx. MacAddressAgeoutRx represents The number
            of MAC addresses ageout received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        mac_address_ageout_tx:
          description: >
            List of MacAddressAgeoutTx. MacAddressAgeoutTx represents The number
            of MAC addresses ageout sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        mac_address_expired_rx:
          description: >
            List of MacAddressExpiredRx. MacAddressExpiredRx represents The
            number of MAC addresses expired receeived - Fabric Engine (VOSS)
            only.
          items:
            format: int64
            type: integer
          type: array
        mac_address_expired_tx:
          description: >
            List of MacAddressExpiredTx. MacAddressExpiredTx represents The
            number of MAC addresses expired sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        mlt_table_sync_request_rx:
          description: >
            List of MltTableSyncRequestRx. MltTableSyncRequestRx represents The
            number of MLT table sync requests reeived - Fabric Engine (VOSS)
            only.
          items:
            format: int64
            type: integer
          type: array
        mlt_table_sync_request_tx:
          description: >
            List of MltTableSyncRequestTx. MltTableSyncRequestTx represents The
            number of MLT table sync requests sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        mlt_table_sync_rx:
          description: >
            List of MltTableSyncRx. MltTableSyncRx represents The number of MLT
            table sync received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        mlt_table_sync_tx:
          description: >
            List of MltTableSyncTx. MltTableSyncTx represents The number of MLT
            table sync sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        poll_count_high_watermark:
          description: >
            List of PollCountHighWatermark. PollCountHighWatermark represents
            The number of transaction queue high watermark messages - Fabric
            Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        port_down_rx:
          description: >
            List of PortDownRx. PortDownRx represents The number of port down
            messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        port_down_tx:
          description: >
            List of PortDownTx. PortDownTx represents The number of port down
            messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        port_update_rx:
          description: >
            List of PortUpdateRx. PortUpdateRx represents The number of port
            update messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        port_update_tx:
          description: >
            List of PortUpdateTx. PortUpdateTx represents The number of port
            update messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        request_mac_table_rx:
          description: >
            List of RequestMacTableRx. RequestMacTableRx represents The number
            of request MAC table messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        request_mac_table_tx:
          description: >
            List of RequestMacTableTx. RequestMacTableTx represents The number
            of request MAC table messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        send_mac_address_rx:
          description: >
            List of SendMacAddressRx. SendMacAddressRx represents The number of
            send MAC address messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        send_mac_address_tx:
          description: >
            List of SendMacAddressTx. SendMacAddressTx represents The number of
            send MAC address messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        smlt_down_rx:
          description: >
            List of SmltDownRx. SmltDownRx represents The number of SMLT down
            messages received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        smlt_down_tx:
          description: >
            List of SmltDownTx. SmltDownTx represents The number of SMLT down
            messages sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        smlt_up_rx:
          description: >
            List of SmltUpRx. SmltUpRx represents The number of SMLT up messages
            received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        smlt_up_tx:
          description: >
            List of SmltUpTx. SmltUpTx represents The number of SMLT up messages
            sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        transaction_queue_high_watermark:
          description: >
            List of TransactionQueueHighWatermark. TransactionQueueHighWatermark
            represents The number of transaction queue high watermark messages -
            Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        unknown_message_rx:
          description: >
            List of UnknownMessageRx. UnknownMessageRx represents The number of
            unknown message received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        update_response_rx:
          description: >
            List of UpdateResponseRx. UpdateResponseRx represents The number of
            update response received - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        update_response_tx:
          description: >
            List of UpdateResponseTx. UpdateResponseTx represents The number of
            update response sent - Fabric Engine (VOSS) only.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetMlagPeerCountersGetRequest
      type: object
    AssetMlagPeerCountersGetResponse:
      properties:
        AssetMlagPeerCounters:
          items:
            $ref: "#/components/schemas/AssetMlagPeerCounters"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetMlagPeerCountersGetResponse
      type: object
    AssetMlagPeerState:
      description: AssetMlagPeerState - This object describes the mlag peer state
        list. AssetMlagPeerCounters,AssetMlagPortState can be created while
        creating AssetMlagPeerState.
      properties:
        asset_device_id:
          description: MLAG peer state object
          type: string
        asset_mlag_peer_counters:
          $ref: "#/components/schemas/AssetMlagPeerCounters"
        asset_mlag_port_state:
          items:
            $ref: "#/components/schemas/AssetMlagPortState"
          nullable: true
          type: array
        chkpt_state:
          description: Checkpointing status of the MLAG peer
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        local_identifier:
          description: >-
            For Fabric Engine (VOSS) this represents the ISIS system-id.
            	For Switch Engine (EXOS) this represents the MAC address of the switch.
          type: string
        local_ip_address:
          description: IP address of the local MLAG device - Ipv4 or Ipv6 for Switch
            Engine (EXOS), Ipv4 only for Fabric Engine (VOSS)
          type: string
        local_ip_address_type:
          description: Specifies the type of IP address in the following property (Ipv4 or
            Ipv6)
          format: int64
          type: integer
        local_vlan_id:
          description: VLAN identifier of Inter-Switch Connection
          format: int64
          type: integer
        mlt_vist_state:
          description: The status of the MLT used for the Inter-Switch Connection in
            Simplified VIST - Fabric Engine (VOSS) only
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        peer_id:
          description: MLAG peer identifier - Name of the MLAG peer for Switch Engine
            (EXOS), value is always "Default" for Fabric Engine (VOSS) since
            only one peer is supported
          type: string
        peer_identifier:
          description: >-
            For Fabric Engine (VOSS) this represents the ISIS SPBM SMLT peer`s
            system-id for the SPBM instance.
            	For Switch Engine (EXOS) this represents the MAC address of the peer.
          type: string
        peer_ip_address:
          description: IP address of the MLAG peer - Ipv4 or Ipv6 for Switch Engine
            (EXOS), Ipv4 only for Fabric Engine (VOSS)
          type: string
        peer_ip_address_type:
          description: Specifies the type of IP address in the following property (Ipv4 or
            Ipv6)
          format: int64
          type: integer
        smlt_split_beb:
          description: Role in VIST pair - Fabric Engine (VOSS) only
          type: string
        up_time:
          description: Number of seconds since the MLAG peer was UP - Switch Engine (EXOS)
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_name:
          description: Virtual router identifier - Switch Engine (EXOS) only
          type: string
      required:
        - asset_device_id
        - peer_id
      type: object
    AssetMlagPeerStateGetRequest:
      description: AssetMlagPeerStateGetRequest represents the retrieval request for
        AssetMlagPeerState.
      properties:
        asset_device_id:
          description: >
            List of AssetDeviceId. AssetDeviceId represents MLAG peer state
            object
          items:
            type: string
          type: array
        chkpt_state:
          description: >
            List of ChkptState. ChkptState represents Checkpointing status of
            the MLAG peer
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        local_identifier:
          description: >
            List of LocalIdentifier. LocalIdentifier represents For Fabric
            Engine (VOSS) this represents the ISIS system-id.
            	For Switch Engine (EXOS) this represents the MAC address of the switch.
          items:
            type: string
          type: array
        local_ip_address:
          description: >
            List of LocalIpAddress. LocalIpAddress represents IP address of the
            local MLAG device - Ipv4 or Ipv6 for Switch Engine (EXOS), Ipv4 only
            for Fabric Engine (VOSS)
          items:
            type: string
          type: array
        local_ip_address_type:
          description: >
            List of LocalIpAddressType. LocalIpAddressType represents Specifies
            the type of IP address in the following property (Ipv4 or Ipv6)
          items:
            format: int64
            type: integer
          type: array
        local_vlan_id:
          description: >
            List of LocalVlanId. LocalVlanId represents VLAN identifier of
            Inter-Switch Connection
          items:
            format: int64
            type: integer
          type: array
        mlt_vist_state:
          description: >
            List of MltVistState. MltVistState represents The status of the MLT
            used for the Inter-Switch Connection in Simplified VIST - Fabric
            Engine (VOSS) only
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        peer_id:
          description: >
            List of PeerId. PeerId represents MLAG peer identifier - Name of the
            MLAG peer for Switch Engine (EXOS), value is always "Default" for
            Fabric Engine (VOSS) since only one peer is supported
          items:
            type: string
          type: array
        peer_identifier:
          description: >
            List of PeerIdentifier. PeerIdentifier represents For Fabric Engine
            (VOSS) this represents the ISIS SPBM SMLT peer`s system-id for the
            SPBM instance.
            	For Switch Engine (EXOS) this represents the MAC address of the peer.
          items:
            type: string
          type: array
        peer_ip_address:
          description: >
            List of PeerIpAddress. PeerIpAddress represents IP address of the
            MLAG peer - Ipv4 or Ipv6 for Switch Engine (EXOS), Ipv4 only for
            Fabric Engine (VOSS)
          items:
            type: string
          type: array
        peer_ip_address_type:
          description: >
            List of PeerIpAddressType. PeerIpAddressType represents Specifies
            the type of IP address in the following property (Ipv4 or Ipv6)
          items:
            format: int64
            type: integer
          type: array
        smlt_split_beb:
          description: >
            List of SmltSplitBeb. SmltSplitBeb represents Role in VIST pair -
            Fabric Engine (VOSS) only
          items:
            type: string
          type: array
        up_time:
          description: >
            List of UpTime. UpTime represents Number of seconds since the MLAG
            peer was UP - Switch Engine (EXOS)
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents Virtual router identifier - Switch
            Engine (EXOS) only
          items:
            type: string
          type: array
      title: AssetMlagPeerStateGetRequest
      type: object
    AssetMlagPeerStateGetResponse:
      properties:
        AssetMlagPeerState:
          items:
            $ref: "#/components/schemas/AssetMlagPeerState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetMlagPeerStateGetResponse
      type: object
    AssetMlagPortState:
      description: AssetMlagPortState - This object describes the mlag port state list.
      properties:
        asset_mlag_peer_state_id:
          description: MLAG port state object
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        local_link_failures:
          description: LocalLinkFailures integer value
          format: int64
          type: integer
        local_link_state:
          description: Status of local MLAG link - Switch Engine (EXOS) only
          type: string
        mlag_id:
          description: MLAG ID - Switch Engine (EXOS) only, Fabric Engine (VOSS) uses MLT ID
          format: int64
          type: integer
        mlt_smlt_state:
          description: Operational status of the MLT - Fabric Engine (VOSS) only
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_id:
          description: MLAG port ID
          type: string
        remote_link_failures:
          description: RemoteLinkFailures integer value
          format: int64
          type: integer
        remote_link_state:
          description: Status of remote MLAG link - Switch Engine (EXOS) only
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_mlag_peer_state_id
        - port_id
      type: object
    AssetMlagPortStateGetRequest:
      description: AssetMlagPortStateGetRequest represents the retrieval request for
        AssetMlagPortState.
      properties:
        asset_mlag_peer_state_id:
          description: >
            List of AssetMlagPeerStateId. AssetMlagPeerStateId represents MLAG
            port state object
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        local_link_failures:
          description: >
            List of LocalLinkFailures. LocalLinkFailures represents
            LocalLinkFailures integer value
          items:
            format: int64
            type: integer
          type: array
        local_link_state:
          description: >
            List of LocalLinkState. LocalLinkState represents Status of local
            MLAG link - Switch Engine (EXOS) only
          items:
            type: string
          type: array
        mlag_id:
          description: >
            List of MlagId. MlagId represents MLAG ID - Switch Engine (EXOS)
            only, Fabric Engine (VOSS) uses MLT ID
          items:
            format: int64
            type: integer
          type: array
        mlt_smlt_state:
          description: >
            List of MltSmltState. MltSmltState represents Operational status of
            the MLT - Fabric Engine (VOSS) only
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_id:
          description: |
            List of PortId. PortId represents MLAG port ID
          items:
            type: string
          type: array
        remote_link_failures:
          description: >
            List of RemoteLinkFailures. RemoteLinkFailures represents
            RemoteLinkFailures integer value
          items:
            format: int64
            type: integer
          type: array
        remote_link_state:
          description: >
            List of RemoteLinkState. RemoteLinkState represents Status of remote
            MLAG link - Switch Engine (EXOS) only
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetMlagPortStateGetRequest
      type: object
    AssetMlagPortStateGetResponse:
      properties:
        AssetMlagPortState:
          items:
            $ref: "#/components/schemas/AssetMlagPortState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetMlagPortStateGetResponse
      type: object
    AssetMulticastFibInterface:
      description: AssetMulticastFibInterface - The object that represents the
        Multicast FIB interface.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: >-
            Specifies the name of ISIS interface depending upon its type. In
            case of
            	PORT interfaces, this name is in well-known physical port format 'slot:port';
            	for multi-area, values 33 and 34 indicate port 'PortVirtual' and value 0 indicates 'cpp'.
            	In case of LAG interfaces, this name indicates the mlt id of the LAG.
            	In case of logical interfaces, this name indicates the destination IP of the ISIS logical
            	interface tunnel.
          type: string
        interface_type:
          description: Specifies the type of ISIS interface.
          format: int64
          type: integer
        isis_multicast_fib_state_id:
          description: ISIS multicast Fib state id.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        traffic_flow_type:
          description: Specifies the type of traffic flow.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - interface_name
        - interface_type
        - isis_multicast_fib_state_id
        - traffic_flow_type
      type: object
    AssetMulticastFibInterfaceGetRequest:
      description: AssetMulticastFibInterfaceGetRequest represents the retrieval
        request for AssetMulticastFibInterface.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Specifies the name
            of ISIS interface depending upon its type. In case of
            	PORT interfaces, this name is in well-known physical port format 'slot:port';
            	for multi-area, values 33 and 34 indicate port 'PortVirtual' and value 0 indicates 'cpp'.
            	In case of LAG interfaces, this name indicates the mlt id of the LAG.
            	In case of logical interfaces, this name indicates the destination IP of the ISIS logical
            	interface tunnel.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents Specifies the type
            of ISIS interface.
          items:
            format: int64
            type: integer
          type: array
        isis_multicast_fib_state_id:
          description: >
            List of IsisMulticastFibStateId. IsisMulticastFibStateId represents
            ISIS multicast Fib state id.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        traffic_flow_type:
          description: >
            List of TrafficFlowType. TrafficFlowType represents Specifies the
            type of traffic flow.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetMulticastFibInterfaceGetRequest
      type: object
    AssetMulticastFibInterfaceGetResponse:
      properties:
        AssetMulticastFibInterface:
          items:
            $ref: "#/components/schemas/AssetMulticastFibInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetMulticastFibInterfaceGetResponse
      type: object
    AssetNeighDiscState:
      description: AssetNeighDiscState - This object describes the Ipv6 neighbor
        discovery state list.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: |-
            The name of the Ipv6 interface. For VLAN, CIRCUITLESS_IP
            	and TUNNEL the name represents the interface id.
          type: string
        interface_type:
          description: The type of the Ipv6 interface.
          format: int64
          type: integer
        ip_address:
          description: Ipv6 Address
          type: string
        last_update:
          description: >-
            The value of sysUpTime as defined by SystemState schema
            	(described in IETF RFC 3418) at the time this entry was last updated.
            	Fabric Engine (FE) only.
          type: integer
        nbr_detection_state:
          description: |-
            The Neighbor Unreachability Detection state for the interface.
            	Fabric Engine (FE) only.
          format: int64
          type: integer
        nbr_type:
          description: Type of the Ipv6 Neighbor Discovery entry.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        physical_address:
          description: |-
            The media-dependent physical address. In the ethernet
            	case it is a MAC address.
          type: string
        port:
          description: |-
            The port on which the Ipv6 Neigbor is learned. This can also be
            	a Multi-Link Trunk (MLT) - Fabric Engine (FE) only.
          type: string
        ttl:
          description: |-
            The numeric value of the Ipv6 Neighbor Time To Live (TTL)
            	in seconds. This field only aplies to dynamic entries.
            	Switch Engine (EXOS) only.
          type: integer
        tunnel:
          description: |-
            If the entry is learned from a SPBM network, instead of
            	using port to track where it comes from, the backbone MAC
            	address is used to track it - Fabric Engine (FE) only.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_name:
          description: This object represents a Virtual Router (VR) name
          type: string
      required:
        - asset_device_id
        - interface_name
        - interface_type
        - ip_address
        - nbr_detection_state
        - nbr_type
        - physical_address
        - port
        - vrf_name
      type: object
    AssetNeighDiscStateGetRequest:
      description: AssetNeighDiscStateGetRequest represents the retrieval request for
        AssetNeighDiscState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents The name of the Ipv6
            interface. For VLAN, CIRCUITLESS_IP
            	and TUNNEL the name represents the interface id.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of the Ipv6
            interface.
          items:
            format: int64
            type: integer
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents Ipv6 Address
          items:
            type: string
          type: array
        last_update:
          description: >
            List of LastUpdate. LastUpdate represents The value of sysUpTime as
            defined by SystemState schema
            	(described in IETF RFC 3418) at the time this entry was last updated.
            	Fabric Engine (FE) only.
          items:
            type: integer
          type: array
        nbr_detection_state:
          description: >
            List of NbrDetectionState. NbrDetectionState represents The Neighbor
            Unreachability Detection state for the interface.
            	Fabric Engine (FE) only.
          items:
            format: int64
            type: integer
          type: array
        nbr_type:
          description: >
            List of NbrType. NbrType represents Type of the Ipv6 Neighbor
            Discovery entry.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        physical_address:
          description: >
            List of PhysicalAddress. PhysicalAddress represents The
            media-dependent physical address. In the ethernet
            	case it is a MAC address.
          items:
            type: string
          type: array
        port:
          description: >
            List of Port. Port represents The port on which the Ipv6 Neigbor is
            learned. This can also be
            	a Multi-Link Trunk (MLT) - Fabric Engine (FE) only.
          items:
            type: string
          type: array
        ttl:
          description: >
            List of Ttl. Ttl represents The numeric value of the Ipv6 Neighbor
            Time To Live (TTL)
            	in seconds. This field only aplies to dynamic entries.
            	Switch Engine (EXOS) only.
          items:
            type: integer
          type: array
        tunnel:
          description: >
            List of Tunnel. Tunnel represents If the entry is learned from a
            SPBM network, instead of
            	using port to track where it comes from, the backbone MAC
            	address is used to track it - Fabric Engine (FE) only.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_name:
          description: >
            List of VrfName. VrfName represents This object represents a Virtual
            Router (VR) name
          items:
            type: string
          type: array
      title: AssetNeighDiscStateGetRequest
      type: object
    AssetNeighDiscStateGetResponse:
      properties:
        AssetNeighDiscState:
          items:
            $ref: "#/components/schemas/AssetNeighDiscState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetNeighDiscStateGetResponse
      type: object
    AssetNetworkToNetworkInterface:
      description: AssetNetworkToNetworkInterface - The object that represents the
        interface between networks.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: >-
            Specifies the name of ISIS interface depending upon its type. In
            case of
            	PORT interfaces, this name is in well-known physical port format 'slot:port';
            	for multi-area, values 33 and 34 indicate port 'PortVirtual' and value 0 indicates 'cpp'.
            	In case of LAG interfaces, this name indicates the mlt id of the LAG.
            	In case of logical interfaces, this name indicates the destination IP of the ISIS logical
            	interface tunnel.
          type: string
        interface_type:
          description: Specifies the type of ISIS interface.
          format: int64
          type: integer
        isis_multicast_route_state_id:
          description: ISIS multicast route state id.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - interface_name
        - interface_type
        - isis_multicast_route_state_id
      type: object
    AssetNetworkToNetworkInterfaceGetRequest:
      description: AssetNetworkToNetworkInterfaceGetRequest represents the retrieval
        request for AssetNetworkToNetworkInterface.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Specifies the name
            of ISIS interface depending upon its type. In case of
            	PORT interfaces, this name is in well-known physical port format 'slot:port';
            	for multi-area, values 33 and 34 indicate port 'PortVirtual' and value 0 indicates 'cpp'.
            	In case of LAG interfaces, this name indicates the mlt id of the LAG.
            	In case of logical interfaces, this name indicates the destination IP of the ISIS logical
            	interface tunnel.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents Specifies the type
            of ISIS interface.
          items:
            format: int64
            type: integer
          type: array
        isis_multicast_route_state_id:
          description: >
            List of IsisMulticastRouteStateId. IsisMulticastRouteStateId
            represents ISIS multicast route state id.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetNetworkToNetworkInterfaceGetRequest
      type: object
    AssetNetworkToNetworkInterfaceGetResponse:
      properties:
        AssetNetworkToNetworkInterface:
          items:
            $ref: "#/components/schemas/AssetNetworkToNetworkInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetNetworkToNetworkInterfaceGetResponse
      type: object
    AssetNodeAliasConfig:
      description: AssetNodeAliasConfig - DB model for node alias global settings
        configuration (eg enabled, global max entries etc) Ports can be created
        while creating AssetNodeAliasConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        bgp_enabled:
          description: A value of true indicates node alias global setting BGP enabled
            state.
          type: boolean
        bootpc_enabled:
          description: A value of true indicates node alias global setting BOOTPC enabled
            state.
          type: boolean
        bootps_enabled:
          description: A value of true indicates node alias global setting BOOTPS enabled
            state.
          type: boolean
        bpdu_enabled:
          description: A value of true indicates node alias global setting BPDU enabled
            state.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dhcp_enabled:
          description: A value of true indicates node alias global setting DHCP enabled
            state.
          type: boolean
        dhcpc_enabled:
          description: A value of true indicates node alias global setting DHCPC enabled
            state.
          type: boolean
        dhcps_enabled:
          description: A value of true indicates node alias global setting DHCPS enabled
            state.
          type: boolean
        enabled:
          description: A value of true indicates node alias global setting enabled state.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ipv4_enabled:
          description: A value of true indicates node alias global setting IPv4 enabled
            state.
          type: boolean
        ipv6_enabled:
          description: A value of true indicates node alias global setting IPv6 enabled
            state.
          type: boolean
        lldp_enabled:
          description: A value of true indicates node alias global setting LLDP enabled
            state.
          type: boolean
        llmnr_enabled:
          description: A value of true indicates node alias global setting LLMNR enabled
            state.
          type: boolean
        max_entries:
          description: Maximum number of alias entries per device
          type: integer
        mdns_enabled:
          description: A value of true indicates node alias global setting MDNS enabled
            state.
          type: boolean
        non_nt_bpdu_enabled:
          description: A value of true indicates node alias global setting NON NT BPDU
            enabled state.
          type: boolean
        ospf_enabled:
          description: A value of true indicates node alias global setting OSPF enabled
            state.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        ports:
          items:
            $ref: "#/components/schemas/AssetNodeAliasPortConfig"
          nullable: true
          type: array
        rip_enabled:
          description: A value of true indicates node alias global setting RIP enabled
            state.
          type: boolean
        ssdp_enabled:
          description: A value of true indicates node alias global setting SSDP enabled
            state.
          type: boolean
        udp_enabled:
          description: A value of true indicates node alias global setting UDP enabled
            state.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrrp_enabled:
          description: A value of true indicates node alias global setting VRRP enabled
            state.
          type: boolean
      required:
        - asset_device_id
      type: object
    AssetNodeAliasConfigGetRequest:
      description: AssetNodeAliasConfigGetRequest represents the retrieval request for
        AssetNodeAliasConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        bgp_enabled:
          description: >
            List of BgpEnabled. BgpEnabled represents A value of true indicates
            node alias global setting BGP enabled state.
          items:
            type: boolean
          type: array
        bootpc_enabled:
          description: >
            List of BootpcEnabled. BootpcEnabled represents A value of true
            indicates node alias global setting BOOTPC enabled state.
          items:
            type: boolean
          type: array
        bootps_enabled:
          description: >
            List of BootpsEnabled. BootpsEnabled represents A value of true
            indicates node alias global setting BOOTPS enabled state.
          items:
            type: boolean
          type: array
        bpdu_enabled:
          description: >
            List of BpduEnabled. BpduEnabled represents A value of true
            indicates node alias global setting BPDU enabled state.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dhcp_enabled:
          description: >
            List of DhcpEnabled. DhcpEnabled represents A value of true
            indicates node alias global setting DHCP enabled state.
          items:
            type: boolean
          type: array
        dhcpc_enabled:
          description: >
            List of DhcpcEnabled. DhcpcEnabled represents A value of true
            indicates node alias global setting DHCPC enabled state.
          items:
            type: boolean
          type: array
        dhcps_enabled:
          description: >
            List of DhcpsEnabled. DhcpsEnabled represents A value of true
            indicates node alias global setting DHCPS enabled state.
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates node
            alias global setting enabled state.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ipv4_enabled:
          description: >
            List of Ipv4Enabled. Ipv4Enabled represents A value of true
            indicates node alias global setting IPv4 enabled state.
          items:
            type: boolean
          type: array
        ipv6_enabled:
          description: >
            List of Ipv6Enabled. Ipv6Enabled represents A value of true
            indicates node alias global setting IPv6 enabled state.
          items:
            type: boolean
          type: array
        lldp_enabled:
          description: >
            List of LldpEnabled. LldpEnabled represents A value of true
            indicates node alias global setting LLDP enabled state.
          items:
            type: boolean
          type: array
        llmnr_enabled:
          description: >
            List of LlmnrEnabled. LlmnrEnabled represents A value of true
            indicates node alias global setting LLMNR enabled state.
          items:
            type: boolean
          type: array
        max_entries:
          description: >
            List of MaxEntries. MaxEntries represents Maximum number of alias
            entries per device
          items:
            type: integer
          type: array
        mdns_enabled:
          description: >
            List of MdnsEnabled. MdnsEnabled represents A value of true
            indicates node alias global setting MDNS enabled state.
          items:
            type: boolean
          type: array
        non_nt_bpdu_enabled:
          description: >
            List of NonNtBpduEnabled. NonNtBpduEnabled represents A value of
            true indicates node alias global setting NON NT BPDU enabled state.
          items:
            type: boolean
          type: array
        ospf_enabled:
          description: >
            List of OspfEnabled. OspfEnabled represents A value of true
            indicates node alias global setting OSPF enabled state.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        rip_enabled:
          description: >
            List of RipEnabled. RipEnabled represents A value of true indicates
            node alias global setting RIP enabled state.
          items:
            type: boolean
          type: array
        ssdp_enabled:
          description: >
            List of SsdpEnabled. SsdpEnabled represents A value of true
            indicates node alias global setting SSDP enabled state.
          items:
            type: boolean
          type: array
        udp_enabled:
          description: >
            List of UdpEnabled. UdpEnabled represents A value of true indicates
            node alias global setting UDP enabled state.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrrp_enabled:
          description: >
            List of VrrpEnabled. VrrpEnabled represents A value of true
            indicates node alias global setting VRRP enabled state.
          items:
            type: boolean
          type: array
      title: AssetNodeAliasConfigGetRequest
      type: object
    AssetNodeAliasConfigGetResponse:
      properties:
        AssetNodeAliasConfig:
          items:
            $ref: "#/components/schemas/AssetNodeAliasConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetNodeAliasConfigGetResponse
      type: object
    AssetNodeAliasPortConfig:
      description: AssetNodeAliasPortConfig - DB model for Node alias port settings
        configuration
      nullable: true
      properties:
        asset_interface_id:
          description: UUID of the interface
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: A value of true indicates node alias port setting enabled state.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: The name of the interface
          type: string
        max_entries:
          description: Maximum number of alias entries per port
          type: integer
        node_alias_config_id:
          description: UUID of the Node alias config
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_interface_id
        - interface_name
        - node_alias_config_id
      type: object
    AssetNodeAliasPortConfigGetRequest:
      description: AssetNodeAliasPortConfigGetRequest represents the retrieval request
        for AssetNodeAliasPortConfig.
      properties:
        asset_interface_id:
          description: >
            List of AssetInterfaceId. AssetInterfaceId represents UUID of the
            interface
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates node
            alias port setting enabled state.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents The name of the
            interface
          items:
            type: string
          type: array
        max_entries:
          description: >
            List of MaxEntries. MaxEntries represents Maximum number of alias
            entries per port
          items:
            type: integer
          type: array
        node_alias_config_id:
          description: >
            List of NodeAliasConfigId. NodeAliasConfigId represents UUID of the
            Node alias config
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetNodeAliasPortConfigGetRequest
      type: object
    AssetNodeAliasPortConfigGetResponse:
      properties:
        AssetNodeAliasPortConfig:
          items:
            $ref: "#/components/schemas/AssetNodeAliasPortConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetNodeAliasPortConfigGetResponse
      type: object
    AssetNtpConfig:
      description: AssetNtpConfig - DB model for NTP configuration Servers can be
        created while creating AssetNtpConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        broadcast_client_enabled:
          default: false
          description: Broadcast client enabled status of the NTP configuration
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          default: false
          description: NTP configuration enabled state
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        poll_interval:
          description: Poll interval time
          type: integer
        servers:
          items:
            $ref: "#/components/schemas/AssetNtpServerConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_name:
          description: Virtual Router (VR) instance on which NTP is enabled
          type: string
      required:
        - asset_device_id
        - broadcast_client_enabled
        - enabled
        - poll_interval
        - vr_name
      type: object
    AssetNtpConfigGetRequest:
      description: AssetNtpConfigGetRequest represents the retrieval request for
        AssetNtpConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        broadcast_client_enabled:
          description: >
            List of BroadcastClientEnabled. BroadcastClientEnabled represents
            Broadcast client enabled status of the NTP configuration
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents NTP configuration enabled state
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        poll_interval:
          description: |
            List of PollInterval. PollInterval represents Poll interval time
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents Virtual Router (VR) instance on
            which NTP is enabled
          items:
            type: string
          type: array
      title: AssetNtpConfigGetRequest
      type: object
    AssetNtpConfigGetResponse:
      properties:
        AssetNtpConfig:
          items:
            $ref: "#/components/schemas/AssetNtpConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetNtpConfigGetResponse
      type: object
    AssetNtpServerConfig:
      description: AssetNtpServerConfig - DB model for NTP server configuration
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: NTP server configuration enabled state
          type: boolean
        host_name_ip:
          description: Host Name or IP address (v4 or v6)
          type: string
        host_type:
          description: Type of the Host Name or IP address
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: NTP config index
          format: int64
          type: integer
        ntp_config_id:
          description: Id of the NTP configuration
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - enabled
        - host_name_ip
        - host_type
        - ntp_config_id
      type: object
    AssetNtpServerConfigGetRequest:
      description: AssetNtpServerConfigGetRequest represents the retrieval request for
        AssetNtpServerConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents NTP server configuration enabled
            state
          items:
            type: boolean
          type: array
        host_name_ip:
          description: >
            List of HostNameIp. HostNameIp represents Host Name or IP address
            (v4 or v6)
          items:
            type: string
          type: array
        host_type:
          description: >
            List of HostType. HostType represents Type of the Host Name or IP
            address
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: |
            List of Index. Index represents NTP config index
          items:
            format: int64
            type: integer
          type: array
        ntp_config_id:
          description: >
            List of NtpConfigId. NtpConfigId represents Id of the NTP
            configuration
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetNtpServerConfigGetRequest
      type: object
    AssetNtpServerConfigGetResponse:
      properties:
        AssetNtpServerConfig:
          items:
            $ref: "#/components/schemas/AssetNtpServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetNtpServerConfigGetResponse
      type: object
    AssetPoePowerConfig:
      description: AssetPoePowerConfig - This object represents the Poe power settings
        for device. AssetPoePowerPortsConfig,AssetPoePowerSlotSettingsConfig can
        be created while creating AssetPoePowerConfig.
      properties:
        asset_poe_power_ports_config:
          items:
            $ref: "#/components/schemas/AssetPoePowerPortsConfig"
          nullable: true
          type: array
        asset_poe_power_slot_settings_config:
          items:
            $ref: "#/components/schemas/AssetPoePowerSlotSettingsConfig"
          nullable: true
          type: array
        classification:
          description: >-
            Classification based on power consumption. On Switch Engine (EXOS),
            	it can be set per port level. On Fabric Engine (VOSS), it is read-only.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        detection:
          description: >-
            Type of power detection. This type can be set per port level in
            	Switch Engine (EXOS) and per device level in Fabric Engine(VOSS).
            	The values of BYPASS, AF_2_POINT_LEGACY, AF_ONLY, AF_4_POINT_LEGACY
            	apply to Switch Engine (EXOS) and the values of NONE, LEGACY_CAPACITIVE,
            	AF_4_POINT_ONLY, AF_4_POINT_LEGACY, AF_2_POINT, AF_2_POINT_LEGACY
            	apply to Fabric Engine(VOSS).
          format: int64
          type: integer
        device_id:
          description: Device ID.
          type: string
        enabled:
          description: >-
            The operational status of the main PSE. This property is
            configurable
            	only in Switch Engine (EXOS).
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        usage_threshold:
          description: >-
            The usage threshold expressed in percentage for comparing the
            measured
            	power and initiating an alarm if the threshold is exceeded.
          type: integer
      required:
        - device_id
      type: object
    AssetPoePowerConfigGetRequest:
      description: AssetPoePowerConfigGetRequest represents the retrieval request for
        AssetPoePowerConfig.
      properties:
        classification:
          description: >
            List of Classification. Classification represents Classification
            based on power consumption. On Switch Engine (EXOS),
            	it can be set per port level. On Fabric Engine (VOSS), it is read-only.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        detection:
          description: >
            List of Detection. Detection represents Type of power detection.
            This type can be set per port level in
            	Switch Engine (EXOS) and per device level in Fabric Engine(VOSS).
            	The values of BYPASS, AF_2_POINT_LEGACY, AF_ONLY, AF_4_POINT_LEGACY
            	apply to Switch Engine (EXOS) and the values of NONE, LEGACY_CAPACITIVE,
            	AF_4_POINT_ONLY, AF_4_POINT_LEGACY, AF_2_POINT, AF_2_POINT_LEGACY
            	apply to Fabric Engine(VOSS).
          items:
            format: int64
            type: integer
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device ID.
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The operational status of the
            main PSE. This property is configurable
            	only in Switch Engine (EXOS).
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        usage_threshold:
          description: >
            List of UsageThreshold. UsageThreshold represents The usage
            threshold expressed in percentage for comparing the measured
            	power and initiating an alarm if the threshold is exceeded.
          items:
            type: integer
          type: array
      title: AssetPoePowerConfigGetRequest
      type: object
    AssetPoePowerConfigGetResponse:
      properties:
        AssetPoePowerConfig:
          items:
            $ref: "#/components/schemas/AssetPoePowerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPoePowerConfigGetResponse
      type: object
    AssetPoePowerPortsConfig:
      description: AssetPoePowerPortsConfig - This object represents the Poe power
        settings for ports.
      nullable: true
      properties:
        asset_interface_id:
          description: Interface ID.
          type: string
        classification:
          description: >-
            Classification based on power consumption. On Switch Engine (EXOS),
            	it can be set per port level. On Fabric Engine (VOSS), it is read-only.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        detection:
          description: >-
            Type of power detection. This type can be set per port level in
            	Switch Engine (EXOS) and per device level in Fabric Engine(VOSS).
            	The values of BYPASS, AF_2_POINT_LEGACY, AF_ONLY, AF_4_POINT_LEGACY
            	apply to Switch Engine (EXOS) and the values of NONE, LEGACY_CAPACITIVE,
            	AF_4_POINT_ONLY, AF_4_POINT_LEGACY, AF_2_POINT, AF_2_POINT_LEGACY
            	apply to Fabric Engine(VOSS).
          format: int64
          type: integer
        enable:
          description: Status of PoE on a port.
          type: boolean
        fast_poe:
          description: >-
            Enable or disable Fast POE feature. This setting can be set both on
            	a device and per port level in Fabric Engine(VOSS), as well as in Switch Engine(EXOS).
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        perpetual_poe:
          description: >-
            Enable or disable Perpetual POE feature. This setting can be set per
            	port level in Fabric Engine(VOSS) and on a device level in Switch Engine(EXOS).
          type: boolean
        poe_power_config_id:
          description: POE power config ID.
          type: string
        power_limit:
          description: >-
            DTE Power limit per port in milliwatts. Range for VOSS is 3000-98000
            and
            	for EXOS ranges are 3000-16800(POE), 3000-30000(POE+), 3000-60000(POE++ Type 3),
            	3000-90000(POE++ Type 4).
          type: integer
        priority:
          default: 3
          description: >-
            Priority of the port from the point of view of a power management
            algorithm.
            	This is used by control mechanism that prevents over current situations by
            	disconnecting first ports with lower power priority.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_interface_id
        - interface_name
        - poe_power_config_id
      type: object
    AssetPoePowerPortsConfigGetRequest:
      description: AssetPoePowerPortsConfigGetRequest represents the retrieval request
        for AssetPoePowerPortsConfig.
      properties:
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface ID.
          items:
            type: string
          type: array
        classification:
          description: >
            List of Classification. Classification represents Classification
            based on power consumption. On Switch Engine (EXOS),
            	it can be set per port level. On Fabric Engine (VOSS), it is read-only.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        detection:
          description: >
            List of Detection. Detection represents Type of power detection.
            This type can be set per port level in
            	Switch Engine (EXOS) and per device level in Fabric Engine(VOSS).
            	The values of BYPASS, AF_2_POINT_LEGACY, AF_ONLY, AF_4_POINT_LEGACY
            	apply to Switch Engine (EXOS) and the values of NONE, LEGACY_CAPACITIVE,
            	AF_4_POINT_ONLY, AF_4_POINT_LEGACY, AF_2_POINT, AF_2_POINT_LEGACY
            	apply to Fabric Engine(VOSS).
          items:
            format: int64
            type: integer
          type: array
        enable:
          description: |
            List of Enable. Enable represents Status of PoE on a port.
          items:
            type: boolean
          type: array
        fast_poe:
          description: >
            List of FastPoe. FastPoe represents Enable or disable Fast POE
            feature. This setting can be set both on
            	a device and per port level in Fabric Engine(VOSS), as well as in Switch Engine(EXOS).
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        perpetual_poe:
          description: >
            List of PerpetualPoe. PerpetualPoe represents Enable or disable
            Perpetual POE feature. This setting can be set per
            	port level in Fabric Engine(VOSS) and on a device level in Switch Engine(EXOS).
          items:
            type: boolean
          type: array
        poe_power_config_id:
          description: >
            List of PoePowerConfigId. PoePowerConfigId represents POE power
            config ID.
          items:
            type: string
          type: array
        power_limit:
          description: >
            List of PowerLimit. PowerLimit represents DTE Power limit per port
            in milliwatts. Range for VOSS is 3000-98000 and
            	for EXOS ranges are 3000-16800(POE), 3000-30000(POE+), 3000-60000(POE++ Type 3),
            	3000-90000(POE++ Type 4).
          items:
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents Priority of the port from the
            point of view of a power management algorithm.
            	This is used by control mechanism that prevents over current situations by
            	disconnecting first ports with lower power priority.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPoePowerPortsConfigGetRequest
      type: object
    AssetPoePowerPortsConfigGetResponse:
      properties:
        AssetPoePowerPortsConfig:
          items:
            $ref: "#/components/schemas/AssetPoePowerPortsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPoePowerPortsConfigGetResponse
      type: object
    AssetPoePowerPortsState:
      description: AssetPoePowerPortsState - This object represents the Poe power port
        state list.
      properties:
        asset_interface_id:
          description: Interface ID.
          type: string
        class:
          description: Port Class.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device ID.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        power_used:
          description: Power used by the port.
          type: number
        standard:
          description: Port Standard.
          format: int64
          type: integer
        state:
          description: Port State.
          format: int64
          type: integer
        sufficient:
          description: Port Sufficient.
          type: boolean
        supported:
          description: Port Supported.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_interface_id
        - device_id
        - interface_name
      type: object
    AssetPoePowerPortsStateGetRequest:
      description: AssetPoePowerPortsStateGetRequest represents the retrieval request
        for AssetPoePowerPortsState.
      properties:
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface ID.
          items:
            type: string
          type: array
        class:
          description: |
            List of Class. Class represents Port Class.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device ID.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        power_used:
          description: |
            List of PowerUsed. PowerUsed represents Power used by the port.
          items:
            type: number
          type: array
        standard:
          description: |
            List of Standard. Standard represents Port Standard.
          items:
            format: int64
            type: integer
          type: array
        state:
          description: |
            List of State. State represents Port State.
          items:
            format: int64
            type: integer
          type: array
        sufficient:
          description: |
            List of Sufficient. Sufficient represents Port Sufficient.
          items:
            type: boolean
          type: array
        supported:
          description: |
            List of Supported. Supported represents Port Supported.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPoePowerPortsStateGetRequest
      type: object
    AssetPoePowerPortsStateGetResponse:
      properties:
        AssetPoePowerPortsState:
          items:
            $ref: "#/components/schemas/AssetPoePowerPortsState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPoePowerPortsStateGetResponse
      type: object
    AssetPoePowerSlotSettingsConfig:
      description: AssetPoePowerSlotSettingsConfig - This object represents the Poe
        power settings for slots.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fast_poe:
          description: >-
            Enable or disable Fast POE feature. This setting can be set both on
            	a device and per port level in Fabric Engine(VOSS), as well as in Switch Engine(EXOS).
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        perpetual_poe:
          description: >-
            Enable or disable Perpetual POE feature. This setting can be set per
            	port level in Fabric Engine(VOSS) and on a device level in Switch Engine(EXOS).
          type: boolean
        poe_power_config_id:
          description: POE power config ID.
          type: string
        slot_number:
          description: Slot number.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - poe_power_config_id
        - slot_number
      type: object
    AssetPoePowerSlotSettingsConfigGetRequest:
      description: AssetPoePowerSlotSettingsConfigGetRequest represents the retrieval
        request for AssetPoePowerSlotSettingsConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fast_poe:
          description: >
            List of FastPoe. FastPoe represents Enable or disable Fast POE
            feature. This setting can be set both on
            	a device and per port level in Fabric Engine(VOSS), as well as in Switch Engine(EXOS).
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        perpetual_poe:
          description: >
            List of PerpetualPoe. PerpetualPoe represents Enable or disable
            Perpetual POE feature. This setting can be set per
            	port level in Fabric Engine(VOSS) and on a device level in Switch Engine(EXOS).
          items:
            type: boolean
          type: array
        poe_power_config_id:
          description: >
            List of PoePowerConfigId. PoePowerConfigId represents POE power
            config ID.
          items:
            type: string
          type: array
        slot_number:
          description: |
            List of SlotNumber. SlotNumber represents Slot number.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPoePowerSlotSettingsConfigGetRequest
      type: object
    AssetPoePowerSlotSettingsConfigGetResponse:
      properties:
        AssetPoePowerSlotSettingsConfig:
          items:
            $ref: "#/components/schemas/AssetPoePowerSlotSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPoePowerSlotSettingsConfigGetResponse
      type: object
    AssetPolicyGlobal:
      description: AssetPolicyGlobal - DB model for Asset policy global - This object
        represents the list devices with policy global information
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        cep_enabled:
          description: The flag to indicate if the certificate enrollment policy is enabled.
          type: boolean
        conflict_response:
          description: The policy conflict response type.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: The flag to indicate if the policy is enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        invalid_request:
          description: The action type for invalid request.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        rule_model:
          description: The policy rule model.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vfp_app_signature_slices:
          description: The number of slices allocated for application signatures in the
            VFP (Virtual Forwarding Plane).
          format: int64
          type: integer
        vfp_tci_overwrite_slices:
          description: The number of slices used by a profile in the look-up stage TCAM
            resources with TCI (Tag Control Information) overwrite enabled
          format: int64
          type: integer
        vlan_authorization:
          description: The flag to indicate if the vlan authorization is enabled.
          type: boolean
      required:
        - asset_device_id
        - cep_enabled
        - enabled
        - vlan_authorization
      type: object
    AssetPolicyGlobalGetRequest:
      description: AssetPolicyGlobalGetRequest represents the retrieval request for
        AssetPolicyGlobal.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        cep_enabled:
          description: >
            List of CepEnabled. CepEnabled represents The flag to indicate if
            the certificate enrollment policy is enabled.
          items:
            type: boolean
          type: array
        conflict_response:
          description: >
            List of ConflictResponse. ConflictResponse represents The policy
            conflict response type.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The flag to indicate if the
            policy is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        invalid_request:
          description: >
            List of InvalidRequest. InvalidRequest represents The action type
            for invalid request.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        rule_model:
          description: |
            List of RuleModel. RuleModel represents The policy rule model.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vfp_app_signature_slices:
          description: >
            List of VfpAppSignatureSlices. VfpAppSignatureSlices represents The
            number of slices allocated for application signatures in the VFP
            (Virtual Forwarding Plane).
          items:
            format: int64
            type: integer
          type: array
        vfp_tci_overwrite_slices:
          description: >
            List of VfpTciOverwriteSlices. VfpTciOverwriteSlices represents The
            number of slices used by a profile in the look-up stage TCAM
            resources with TCI (Tag Control Information) overwrite enabled
          items:
            format: int64
            type: integer
          type: array
        vlan_authorization:
          description: >
            List of VlanAuthorization. VlanAuthorization represents The flag to
            indicate if the vlan authorization is enabled.
          items:
            type: boolean
          type: array
      title: AssetPolicyGlobalGetRequest
      type: object
    AssetPolicyGlobalGetResponse:
      properties:
        AssetPolicyGlobal:
          items:
            $ref: "#/components/schemas/AssetPolicyGlobal"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPolicyGlobalGetResponse
      type: object
    AssetPortCapabilities:
      description: AssetPortCapabilities - DB Model for Port Capabilities
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        auto_neg_off_supported_speed_duplex_list:
          description: >-
            This list contains all the speed-duplex combinations supported by
            the port to when
            	auto-negotiation is disabled. The configured speed and duplex are managed by
            	'EthernetInterface.speed; and 'EthernetInterface.duplex' when
            	'EthernetInterface.autoNegotiation=False'.
          items:
            type: string
          nullable: true
          type: array
        auto_neg_on_supported_adv_list:
          description: >-
            This list contains all the speed-duplex combinations the port is
            capable of advertising
            	when auto-negotiation is enabled. The configured list of advertisements is managed by
            	'EthernetInterface.autoAdvertisementsList' when 'EthernetInterface.autoNegotiation=True'.
          items:
            type: string
          nullable: true
          type: array
        auto_negotiation:
          description: >-
            The auto-negotiation setting for the port.
            	'configurable' means Auto-negotiation can be configured (true or false) for the port.
            	'on' means auto-negotiation is supported and cannot be disabled.
            	'off' means auto-negotiation is not supported.
          type: string
        channelization_sub_ports:
          description: >-
            If channelization is supported, shows the capability of channelized
            sub-ports.
            	For Fabric Engine (VOSS) - the partition is determined based on speed and
            	channelized attributes. For a GET operation a list of partition will be returned.
          items:
            type: string
          nullable: true
          type: array
        channelization_support:
          description: Indicates whether the port supports channelization
          type: boolean
        configurable:
          description: >-
            States if the port speed/duplex/autoNegotiationEnabled are
            configurable or not.
            	For Fabric Engine for COPPER ports, this field will always be true.
            	For FIBER ports, if port is not populated, it will not be configurable.
            	If populated, configurable will be True, but there are certain
            	scenarios for which those attributes are really configurable
            	- speed is configurable only when a dual-speed optic is inserted.
            	- duplex is configurable only when 25GbCX is inserted and auto-negotiation is disabled
            	- auto-negotiation is configurable mostly on DACs, except the 10Gb ones
            	Port description is configurable no matter if the port is populated or not
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        ddi_supported:
          description: >-
            States if Digital Diagnostic Interface is supported. If is
            applicable only for
            	FIBER ports and only if that port is populated.
          type: boolean
        default_auto_negotiation_enabled:
          description: >-
            Indicates whether this port is enabled for auto negotiation or not.
            This represents the
            	default value of auto negotiation per port. For Switch Engine plug-and-play is enabled
            	when configured speed, duplex and autoNegotiationEnabled are all at their default values.
            	This property is Switch Engine specific.
          type: boolean
        default_duplex:
          description: >-
            This represents the default value for duplex per port. For Switch
            Engine plug-and-play is enabled
            	when configured speed, duplex and autoNegotiationEnabled are all at their default values.
            	This property is Switch Engine specific.
          format: int64
          type: integer
        default_speed:
          description: >-
            This represents the default speed per port. For Switch Engine
            plug-and-play is enabled when
            	configured speed, duplex and autoNegotiationEnabled are all at their default values.
            	This property is Switch Engine specific.
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        ethernet:
          description: >-
            Indicates whether the port supports ethernet capability. This
            property is Switch Engine specific.
            	Standard front panel, universal, or alternate stacking ports may have a value of true.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        loopback_port:
          description: >-
            Indicates that the port is allocated for loopback. Required for
            advanced
            	feature functionality.
          type: boolean
        management_port:
          description: Management port for out-of-band management connection to the switch.
          type: boolean
        media_type:
          description: >-
            Indicates the types of media used by the port for data transmission.
            This property
            	is represented as a list as certain ports can support multiple media types.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: |-
            Name of the interface, slot and port are required

            	to ensure deterministic results, sub_port is optional.

            	If the optional slot number for the management port is

            	not provided on a stacked system, the slot will default

            	to the master slot.

            	[slot]:[port][:sub_port]

            	'1:1:1'     slot=1, port=1, sub_port=1

            	'12:101'    slot=12, port=101

            	'1:s1'      insight port

            	'ge2'       port=ge2

            	'wifi1.3'   radio=1, ssid=3

            	'mgmt'      oob mgmt port

            	'2:mgmt'    mgmt port with optional slot

            	'1:stack2'  stack port, slot=1, port=2
          type: string
        reserved_port:
          description: Reserved port for configuring advanced features. This is Fabric
            Engine specific.
          type: boolean
        stacking:
          description: >-
            Indicates whether the port supports stacking capability.
            	This property is Switch Engine specific. Dedicated stacking ports, universal,
            	or alternate stacking ports may have a value of true.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vim_port:
          description: |-
            Indicates whether the port is a Virtual Infrastructure Management
            	(VIM) port.
          type: boolean
      required:
        - asset_device_id
        - port_name
      type: object
    AssetPortCapabilitiesGetRequest:
      description: AssetPortCapabilitiesGetRequest represents the retrieval request
        for AssetPortCapabilities.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        auto_neg_off_supported_speed_duplex_list:
          description: >
            List of AutoNegOffSupportedSpeedDuplexList.
            AutoNegOffSupportedSpeedDuplexList represents This list contains all
            the speed-duplex combinations supported by the port to when
            	auto-negotiation is disabled. The configured speed and duplex are managed by
            	'EthernetInterface.speed; and 'EthernetInterface.duplex' when
            	'EthernetInterface.autoNegotiation=False'.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        auto_neg_on_supported_adv_list:
          description: >
            List of AutoNegOnSupportedAdvList. AutoNegOnSupportedAdvList
            represents This list contains all the speed-duplex combinations the
            port is capable of advertising
            	when auto-negotiation is enabled. The configured list of advertisements is managed by
            	'EthernetInterface.autoAdvertisementsList' when 'EthernetInterface.autoNegotiation=True'.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        auto_negotiation:
          description: >
            List of AutoNegotiation. AutoNegotiation represents The
            auto-negotiation setting for the port.
            	'configurable' means Auto-negotiation can be configured (true or false) for the port.
            	'on' means auto-negotiation is supported and cannot be disabled.
            	'off' means auto-negotiation is not supported.
          items:
            type: string
          type: array
        channelization_sub_ports:
          description: >
            List of ChannelizationSubPorts. ChannelizationSubPorts represents If
            channelization is supported, shows the capability of channelized
            sub-ports.
            	For Fabric Engine (VOSS) - the partition is determined based on speed and
            	channelized attributes. For a GET operation a list of partition will be returned.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        channelization_support:
          description: >
            List of ChannelizationSupport. ChannelizationSupport represents
            Indicates whether the port supports channelization
          items:
            type: boolean
          type: array
        configurable:
          description: >
            List of Configurable. Configurable represents States if the port
            speed/duplex/autoNegotiationEnabled are configurable or not.
            	For Fabric Engine for COPPER ports, this field will always be true.
            	For FIBER ports, if port is not populated, it will not be configurable.
            	If populated, configurable will be True, but there are certain
            	scenarios for which those attributes are really configurable
            	- speed is configurable only when a dual-speed optic is inserted.
            	- duplex is configurable only when 25GbCX is inserted and auto-negotiation is disabled
            	- auto-negotiation is configurable mostly on DACs, except the 10Gb ones
            	Port description is configurable no matter if the port is populated or not
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        ddi_supported:
          description: >
            List of DdiSupported. DdiSupported represents States if Digital
            Diagnostic Interface is supported. If is applicable only for
            	FIBER ports and only if that port is populated.
          items:
            type: boolean
          type: array
        default_auto_negotiation_enabled:
          description: >
            List of DefaultAutoNegotiationEnabled. DefaultAutoNegotiationEnabled
            represents Indicates whether this port is enabled for auto
            negotiation or not. This represents the
            	default value of auto negotiation per port. For Switch Engine plug-and-play is enabled
            	when configured speed, duplex and autoNegotiationEnabled are all at their default values.
            	This property is Switch Engine specific.
          items:
            type: boolean
          type: array
        default_duplex:
          description: >
            List of DefaultDuplex. DefaultDuplex represents This represents the
            default value for duplex per port. For Switch Engine plug-and-play
            is enabled
            	when configured speed, duplex and autoNegotiationEnabled are all at their default values.
            	This property is Switch Engine specific.
          items:
            format: int64
            type: integer
          type: array
        default_speed:
          description: >
            List of DefaultSpeed. DefaultSpeed represents This represents the
            default speed per port. For Switch Engine plug-and-play is enabled
            when
            	configured speed, duplex and autoNegotiationEnabled are all at their default values.
            	This property is Switch Engine specific.
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        ethernet:
          description: >
            List of Ethernet. Ethernet represents Indicates whether the port
            supports ethernet capability. This property is Switch Engine
            specific.
            	Standard front panel, universal, or alternate stacking ports may have a value of true.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        loopback_port:
          description: >
            List of LoopbackPort. LoopbackPort represents Indicates that the
            port is allocated for loopback. Required for advanced
            	feature functionality.
          items:
            type: boolean
          type: array
        management_port:
          description: >
            List of ManagementPort. ManagementPort represents Management port
            for out-of-band management connection to the switch.
          items:
            type: boolean
          type: array
        media_type:
          description: >
            List of MediaType. MediaType represents Indicates the types of media
            used by the port for data transmission. This property
            	is represented as a list as certain ports can support multiple media types.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents Name of the interface, slot
            and port are required

            	to ensure deterministic results, sub_port is optional.

            	If the optional slot number for the management port is

            	not provided on a stacked system, the slot will default

            	to the master slot.

            	[slot]:[port][:sub_port]

            	'1:1:1'     slot=1, port=1, sub_port=1

            	'12:101'    slot=12, port=101

            	'1:s1'      insight port

            	'ge2'       port=ge2

            	'wifi1.3'   radio=1, ssid=3

            	'mgmt'      oob mgmt port

            	'2:mgmt'    mgmt port with optional slot

            	'1:stack2'  stack port, slot=1, port=2
          items:
            type: string
          type: array
        reserved_port:
          description: >
            List of ReservedPort. ReservedPort represents Reserved port for
            configuring advanced features. This is Fabric Engine specific.
          items:
            type: boolean
          type: array
        stacking:
          description: >
            List of Stacking. Stacking represents Indicates whether the port
            supports stacking capability.
            	This property is Switch Engine specific. Dedicated stacking ports, universal,
            	or alternate stacking ports may have a value of true.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vim_port:
          description: >
            List of VimPort. VimPort represents Indicates whether the port is a
            Virtual Infrastructure Management
            	(VIM) port.
          items:
            type: boolean
          type: array
      title: AssetPortCapabilitiesGetRequest
      type: object
    AssetPortCapabilitiesGetResponse:
      properties:
        AssetPortCapabilities:
          items:
            $ref: "#/components/schemas/AssetPortCapabilities"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPortCapabilitiesGetResponse
      type: object
    AssetPortConfig:
      description: AssetPortConfig - This object represents the list of ports on a
        switch and their configuration.
      properties:
        asset_device_id:
          description: Generated Device Id
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        asset_slot_id:
          description: Slot Id
          type: string
        auto_advertisements:
          items:
            format: int64
            type: integer
          type: array
        auto_negotiation_enabled:
          description: |-
            Set to TRUE to request the interface to auto-negotiate
            	transmission parameters with its peer interface.  When
            	set to FALSE, the transmission parameters are specified
            	manually.
          type: boolean
        channelized:
          description: >-
            A value of true indicates channelization is enabled on the port.
            	This parameter applies to Fabric Engine.
            	For Switch Engine the partition property specifies the type of partitioning on the port.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        debounce_timer:
          description: >-
            The value of the port debounce timer in milliseconds.
            	This value represents the delay link in flap detection after a link goes down.
            	The range for Switch Engine is [0-5000] and the range for Fabric Engine is [0-300000].
            	A value of 0 is the default on both operating systems and this indicates there is no delay in link down detection.
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        description:
          description: Decsription.
          type: string
        duplex:
          description: >-
            Available transmissions type that can be set on the device when
            auto-negotiation is disabled
            	(autoNegotiationEnabled is set to false). Capabilities can vary per port type and per platform
            	(for example some platforms/ports do not offer half duplex).
          format: int64
          type: integer
        eee:
          description: >-
            A value of true indicates jumbo frames are enabled on the port and
            it's MTU is set to the global 'l2mtu' setting.
            	This setting is only applicable to Switch Engine
          type: boolean
        enabled:
          description: A value of true indicates the port is enabled.
          type: boolean
        fec:
          description: |-
            IEEE Forward Error Correction (FEC) options. The AUTO
            	setting automatically configures FEC based on port
            	speed and pluggable module type and is only applicable
            	to Fabric Engine. This attribute can be set only for FIBER
            	media type
          format: int64
          type: integer
        flow_control:
          description: >-
            Available flow control settings. Fabric Engine (VOSS) does
            	not support RX_ENABLE and PFC_ENABLE values. For Fabric Engine (VOSS)
            	this attribute can only be set when the boot config flag flow-control-mode
            	is enabled, otherwise it should not be specified in the payload.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_arp_inspection_trusted:
          description: >-
            A value of true indicates that the interface is trusted for ARP
            inspection purposes.
            	This parameter applies to Fabric Engine only.
          type: boolean
        jumbo_enabled:
          description: >-
            A value of true indicates jumbo frames are enabled on the port and
            it's MTU is set to the global 'l2mtu' setting.
            	This setting is only applicable to Switch Engine
          type: boolean
        name:
          description: |-
            Port number (maps to the front panel port of a switch);
            	also the key for the port list
          type: string
        native_vlan:
          description: >-
            The VLAN ID assigned to untagged frames received on this trunk port.
            	This field is meaningless when the port is not a trunk port (portMode is True).
            	This is Fabric Engine specific.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        partition:
          description: |-
            Available port partition settings. The options vary
            	depending on platform and port. For a GET operation
            	a single value will be returned. For a SET operation
            	a single value can be configured and this list represents
            	the possible values among all port types.
            	For Fabric Engine (VOSS) - the partition is determined
            	based on speed and channelized attributes, therefore
            	this attribute is available only in a GET operation,
            	but can't be configured in a SET operation. This attribute
            	is available only for ports with speed 20G, 40G and 100G
            	on Fabric Engine (VOSS). Values 2X50G, 3X25G, 3X10G and 3X1G
            	are not supported on Fabric Engine (VOSS).
          format: int64
          type: integer
        port_mode:
          description: >-
            A value of True will enable the port on the current vlan to perform
            tagging on the incoming and outgoing traffic.
            	This is Fabric Engine specific.
          type: boolean
        speed:
          description: >-
            Available speed values (in Mbps/Gbps) that can be set on the device
            when auto-negotiation is
            	disabled (autoNegotiationEnabled is set to false). Capabilities vary per port type and per platform.
            	The 'AUTO' value will be returned only in GET operations for those ports that are part of a
            	VIM and where the VIM Speed is automatically set. Fabric Engine (VOSS) does not support speed
            	values 20G and 50G and allows this attribute to be configured only for dual speed transceivers.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - asset_interface_id
      type: object
    AssetPortConfigGetRequest:
      description: AssetPortConfigGetRequest represents the retrieval request for
        AssetPortConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Generated Device Id
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        asset_slot_id:
          description: |
            List of AssetSlotId. AssetSlotId represents Slot Id
          items:
            type: string
          type: array
        auto_advertisements:
          description: >
            List of AutoAdvertisements. AutoAdvertisements represents The list
            of the port's autonegotiation advertisement settings.
            	This attribute can only be set on the device when autoNegotiationEnabled is true.
            	If autoNegotiationEnabled is true and a value is not provided,
            	the application should set the advertised speed and duplex based on the default value for the port type.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        auto_negotiation_enabled:
          description: >
            List of AutoNegotiationEnabled. AutoNegotiationEnabled represents
            Set to TRUE to request the interface to auto-negotiate
            	transmission parameters with its peer interface.  When
            	set to FALSE, the transmission parameters are specified
            	manually.
          items:
            type: boolean
          type: array
        channelized:
          description: >
            List of Channelized. Channelized represents A value of true
            indicates channelization is enabled on the port.
            	This parameter applies to Fabric Engine.
            	For Switch Engine the partition property specifies the type of partitioning on the port.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        debounce_timer:
          description: >
            List of DebounceTimer. DebounceTimer represents The value of the
            port debounce timer in milliseconds.
            	This value represents the delay link in flap detection after a link goes down.
            	The range for Switch Engine is [0-5000] and the range for Fabric Engine is [0-300000].
            	A value of 0 is the default on both operating systems and this indicates there is no delay in link down detection.
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        description:
          description: |
            List of Description. Description represents Decsription.
          items:
            type: string
          type: array
        duplex:
          description: >
            List of Duplex. Duplex represents Available transmissions type that
            can be set on the device when auto-negotiation is disabled
            	(autoNegotiationEnabled is set to false). Capabilities can vary per port type and per platform
            	(for example some platforms/ports do not offer half duplex).
          items:
            format: int64
            type: integer
          type: array
        eee:
          description: >
            List of Eee. Eee represents A value of true indicates jumbo frames
            are enabled on the port and it's MTU is set to the global 'l2mtu'
            setting.
            	This setting is only applicable to Switch Engine
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates the
            port is enabled.
          items:
            type: boolean
          type: array
        fec:
          description: >
            List of Fec. Fec represents IEEE Forward Error Correction (FEC)
            options. The AUTO
            	setting automatically configures FEC based on port
            	speed and pluggable module type and is only applicable
            	to Fabric Engine. This attribute can be set only for FIBER
            	media type
          items:
            format: int64
            type: integer
          type: array
        flow_control:
          description: >
            List of FlowControl. FlowControl represents Available flow control
            settings. Fabric Engine (VOSS) does
            	not support RX_ENABLE and PFC_ENABLE values. For Fabric Engine (VOSS)
            	this attribute can only be set when the boot config flag flow-control-mode
            	is enabled, otherwise it should not be specified in the payload.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_arp_inspection_trusted:
          description: >
            List of IpArpInspectionTrusted. IpArpInspectionTrusted represents A
            value of true indicates that the interface is trusted for ARP
            inspection purposes.
            	This parameter applies to Fabric Engine only.
          items:
            type: boolean
          type: array
        jumbo_enabled:
          description: >
            List of JumboEnabled. JumboEnabled represents A value of true
            indicates jumbo frames are enabled on the port and it's MTU is set
            to the global 'l2mtu' setting.
            	This setting is only applicable to Switch Engine
          items:
            type: boolean
          type: array
        name:
          description: >
            List of Name. Name represents Port number (maps to the front panel
            port of a switch);
            	also the key for the port list
          items:
            type: string
          type: array
        native_vlan:
          description: >
            List of NativeVlan. NativeVlan represents The VLAN ID assigned to
            untagged frames received on this trunk port.
            	This field is meaningless when the port is not a trunk port (portMode is True).
            	This is Fabric Engine specific.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        partition:
          description: >
            List of Partition. Partition represents Available port partition
            settings. The options vary
            	depending on platform and port. For a GET operation
            	a single value will be returned. For a SET operation
            	a single value can be configured and this list represents
            	the possible values among all port types.
            	For Fabric Engine (VOSS) - the partition is determined
            	based on speed and channelized attributes, therefore
            	this attribute is available only in a GET operation,
            	but can't be configured in a SET operation. This attribute
            	is available only for ports with speed 20G, 40G and 100G
            	on Fabric Engine (VOSS). Values 2X50G, 3X25G, 3X10G and 3X1G
            	are not supported on Fabric Engine (VOSS).
          items:
            format: int64
            type: integer
          type: array
        port_mode:
          description: >
            List of PortMode. PortMode represents A value of True will enable
            the port on the current vlan to perform tagging on the incoming and
            outgoing traffic.
            	This is Fabric Engine specific.
          items:
            type: boolean
          type: array
        speed:
          description: >
            List of Speed. Speed represents Available speed values (in
            Mbps/Gbps) that can be set on the device when auto-negotiation is
            	disabled (autoNegotiationEnabled is set to false). Capabilities vary per port type and per platform.
            	The 'AUTO' value will be returned only in GET operations for those ports that are part of a
            	VIM and where the VIM Speed is automatically set. Fabric Engine (VOSS) does not support speed
            	values 20G and 50G and allows this attribute to be configured only for dual speed transceivers.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPortConfigGetRequest
      type: object
    AssetPortConfigGetResponse:
      properties:
        AssetPortConfig:
          items:
            $ref: "#/components/schemas/AssetPortConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPortConfigGetResponse
      type: object
    AssetPortLldpLocation:
      description: AssetPortLldpLocation - This object describes the lldp location
        identification list.
      nullable: true
      properties:
        asset_lldp_neighbor_xmed_id:
          description: Asset LLDP neighbor XMed ID
          type: string
        civic_address:
          description: >-
            Civic based location information.
            	Switch Engine uses a flexible format but has a minimum
            	requirement of 6 bytes.
            	Fabric Engine uses civic address LCI data format.
            	A valid request for Fabric Engine should always start with country-code label
            	followed by 2 characters for the country code. Country-code is not
            	enough for setting the civic address, so another eligible label should
            	also be set.
            	All eligible labels that can be set in a civic address are:
            	additional-code, additional-information, apartment, block, building,
            	city, city-district, county, floor, house-number, house-number-suffix,
            	landmark, leading-street-direction, name, place-type, pobox,
            	postal-community-name, postal-zip-code, room-number, state, street,
            	street-suffix, trailing-street-suffix.
          type: string
        coordinate:
          description: >-
            Coordinate based location information that comprises of
            	latitude, longitude, altitude and reference datum.
            	Same format applies to both Switch Engine and Fabric Engine.
            	Switch Engine has a strict length requirement of 16 bytes.
            	A valid request for Fabric Engine should look like in the example.
            	Not all of latitude, longitude, latitude and datum have to be set in the same request.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        ecs_elin:
          description: |-
            Emergency call service (ECS) emergency line information
            	number (ELIN).
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    AssetPortLldpLocationGetRequest:
      description: AssetPortLldpLocationGetRequest represents the retrieval request
        for AssetPortLldpLocation.
      properties:
        asset_lldp_neighbor_xmed_id:
          description: >
            List of AssetLldpNeighborXmedId. AssetLldpNeighborXmedId represents
            Asset LLDP neighbor XMed ID
          items:
            type: string
          type: array
        civic_address:
          description: >
            List of CivicAddress. CivicAddress represents Civic based location
            information.
            	Switch Engine uses a flexible format but has a minimum
            	requirement of 6 bytes.
            	Fabric Engine uses civic address LCI data format.
            	A valid request for Fabric Engine should always start with country-code label
            	followed by 2 characters for the country code. Country-code is not
            	enough for setting the civic address, so another eligible label should
            	also be set.
            	All eligible labels that can be set in a civic address are:
            	additional-code, additional-information, apartment, block, building,
            	city, city-district, county, floor, house-number, house-number-suffix,
            	landmark, leading-street-direction, name, place-type, pobox,
            	postal-community-name, postal-zip-code, room-number, state, street,
            	street-suffix, trailing-street-suffix.
          items:
            type: string
          type: array
        coordinate:
          description: >
            List of Coordinate. Coordinate represents Coordinate based location
            information that comprises of
            	latitude, longitude, altitude and reference datum.
            	Same format applies to both Switch Engine and Fabric Engine.
            	Switch Engine has a strict length requirement of 16 bytes.
            	A valid request for Fabric Engine should look like in the example.
            	Not all of latitude, longitude, latitude and datum have to be set in the same request.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        ecs_elin:
          description: >
            List of EcsElin. EcsElin represents Emergency call service (ECS)
            emergency line information
            	number (ELIN).
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPortLldpLocationGetRequest
      type: object
    AssetPortLldpLocationGetResponse:
      properties:
        AssetPortLldpLocation:
          items:
            $ref: "#/components/schemas/AssetPortLldpLocation"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPortLldpLocationGetResponse
      type: object
    AssetPortMacLockingConfig:
      description: AssetPortMacLockingConfig - DB model for MAC locking port settings
        configuration
      properties:
        clear_on_link_down:
          description: MAC-locking action when port goes down. Clear the state of
            MAC-locking disabled ports.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: A value of true indicates to enable MAC-locking on this port.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: The name of the interface
          type: string
        mac_locking_config_id:
          description: UUID of the MAC locking config
          type: string
        max_fa_learn_limit:
          description: Maximum first arrival MAC addresses allowed to communicate on this
            port.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        remove_aged_macs:
          description: >-
            A value of true indicates MAC-locking should remove learned MAC
            Addresses when they age
            	out of the switch forwarding database.
          type: boolean
        shut_down_on_limit:
          description: >-
            A value of true indicates MAC-locking should disable this port when
            the maximum first
            	arrival limit is exceeded.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - clear_on_link_down
        - enabled
        - interface_name
        - mac_locking_config_id
        - max_fa_learn_limit
        - remove_aged_macs
        - shut_down_on_limit
      type: object
    AssetPortMacLockingConfigGetRequest:
      description: AssetPortMacLockingConfigGetRequest represents the retrieval
        request for AssetPortMacLockingConfig.
      properties:
        clear_on_link_down:
          description: >
            List of ClearOnLinkDown. ClearOnLinkDown represents MAC-locking
            action when port goes down. Clear the state of MAC-locking disabled
            ports.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates to
            enable MAC-locking on this port.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents The name of the
            interface
          items:
            type: string
          type: array
        mac_locking_config_id:
          description: >
            List of MacLockingConfigId. MacLockingConfigId represents UUID of
            the MAC locking config
          items:
            type: string
          type: array
        max_fa_learn_limit:
          description: >
            List of MaxFaLearnLimit. MaxFaLearnLimit represents Maximum first
            arrival MAC addresses allowed to communicate on this port.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        remove_aged_macs:
          description: >
            List of RemoveAgedMacs. RemoveAgedMacs represents A value of true
            indicates MAC-locking should remove learned MAC Addresses when they
            age
            	out of the switch forwarding database.
          items:
            type: boolean
          type: array
        shut_down_on_limit:
          description: >
            List of ShutDownOnLimit. ShutDownOnLimit represents A value of true
            indicates MAC-locking should disable this port when the maximum
            first
            	arrival limit is exceeded.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPortMacLockingConfigGetRequest
      type: object
    AssetPortMacLockingConfigGetResponse:
      properties:
        AssetPortMacLockingConfig:
          items:
            $ref: "#/components/schemas/AssetPortMacLockingConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPortMacLockingConfigGetResponse
      type: object
    AssetPortState:
      description: AssetPortState - This object represents the list of ports on a
        switch and their state.
      properties:
        asset_device_id:
          description: Generated Device Id
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        asset_slot_id:
          description: Slot Id
          type: string
        broadcast_rate_limit:
          description: Broadcast Rate Limit
          format: int64
          type: integer
        congestion_pkt_drops_cumulative:
          description: Congestion Packet Drops Cumulative
          format: int64
          type: integer
        connector_type:
          description: Type of port connector.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enable_broadcast_traffic:
          description: Enable Broadcast Traffic
          type: boolean
        enable_multicast_traffic:
          description: Enable Multicast Traffic
          type: boolean
        enable_unicast_traffic:
          description: Enable Unicast Traffic
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        if_index:
          description: >-
            A unique value, greater than zero, for each interface.  It
            	is recommended that values are assigned contiguously
            	starting from 1.  The value for each interface sub-layer
            	must remain constant at least from one re-initialization of
            	the entity's network management system to the next reinitialization.
          format: int64
          minimum: 0
          type: integer
        mac_address:
          description: |-
            Assigns a MAC address to the Ethernet interface.  If not
            	specified, the corresponding operational state leaf is
            	expected to show the system-assigned MAC address.
          type: string
        name:
          description: |-
            Port number (maps to the front panel port of a switch);
            	also the key for the port list
          type: string
        oper_duplex:
          description: Indicate this port's current duplex value.
          format: int64
          type: integer
        oper_speed:
          description: >-
            Indicate this port's current speed value. Fabric Engine (VOSS) does
            not support speed
            	values 20G and 50G
          format: int64
          type: integer
        oper_state:
          description: |-
            The current operational state of the interface.  The
            	testing state indicates that no operational packets can
            	be passed.  If admin state is down then oper state
            	should be down. If admin state is changed to up
            	then oper state should change to up and the interface is
            	ready to transmit and receive network traffic. It should
            	change to dormant if the interface is waiting for
            	external actions (such as a serial line waiting for an
            	incoming connection). It should remain in the down state
            	if and only if there is a fault that prevents it from going
            	to the up state; It should remain in the notPresent
            	state if the interface has missing (typically, hardware)
            	components
          format: int64
          type: integer
        operation_mode:
          description: Operation Mode
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        slpp_status:
          description: >-
            The SLPP safe guard status for the port.
            	SLPP guard state is set to MONITORING if the port is operational UP and
            	SLPP guard is enabled on that port. The state is set to BLOCKING when the
            	port is disabled by SLPP guard. If SLPP is not enabled on the port, the state
            	is set to NONE.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - asset_interface_id
      type: object
    AssetPortStateGetRequest:
      description: AssetPortStateGetRequest represents the retrieval request for
        AssetPortState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Generated Device Id
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        asset_slot_id:
          description: |
            List of AssetSlotId. AssetSlotId represents Slot Id
          items:
            type: string
          type: array
        broadcast_rate_limit:
          description: >
            List of BroadcastRateLimit. BroadcastRateLimit represents Broadcast
            Rate Limit
          items:
            format: int64
            type: integer
          type: array
        congestion_pkt_drops_cumulative:
          description: >
            List of CongestionPktDropsCumulative. CongestionPktDropsCumulative
            represents Congestion Packet Drops Cumulative
          items:
            format: int64
            type: integer
          type: array
        connector_type:
          description: >
            List of ConnectorType. ConnectorType represents Type of port
            connector.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enable_broadcast_traffic:
          description: >
            List of EnableBroadcastTraffic. EnableBroadcastTraffic represents
            Enable Broadcast Traffic
          items:
            type: boolean
          type: array
        enable_multicast_traffic:
          description: >
            List of EnableMulticastTraffic. EnableMulticastTraffic represents
            Enable Multicast Traffic
          items:
            type: boolean
          type: array
        enable_unicast_traffic:
          description: >
            List of EnableUnicastTraffic. EnableUnicastTraffic represents Enable
            Unicast Traffic
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        if_index:
          description: >
            List of IfIndex. IfIndex represents A unique value, greater than
            zero, for each interface.  It
            	is recommended that values are assigned contiguously
            	starting from 1.  The value for each interface sub-layer
            	must remain constant at least from one re-initialization of
            	the entity's network management system to the next reinitialization.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        mac_address:
          description: >
            List of MacAddress. MacAddress represents Assigns a MAC address to
            the Ethernet interface.  If not
            	specified, the corresponding operational state leaf is
            	expected to show the system-assigned MAC address.
          items:
            type: string
          type: array
        name:
          description: >
            List of Name. Name represents Port number (maps to the front panel
            port of a switch);
            	also the key for the port list
          items:
            type: string
          type: array
        oper_duplex:
          description: >
            List of OperDuplex. OperDuplex represents Indicate this port's
            current duplex value.
          items:
            format: int64
            type: integer
          type: array
        oper_speed:
          description: >
            List of OperSpeed. OperSpeed represents Indicate this port's current
            speed value. Fabric Engine (VOSS) does not support speed
            	values 20G and 50G
          items:
            format: int64
            type: integer
          type: array
        oper_state:
          description: >
            List of OperState. OperState represents The current operational
            state of the interface.  The
            	testing state indicates that no operational packets can
            	be passed.  If admin state is down then oper state
            	should be down. If admin state is changed to up
            	then oper state should change to up and the interface is
            	ready to transmit and receive network traffic. It should
            	change to dormant if the interface is waiting for
            	external actions (such as a serial line waiting for an
            	incoming connection). It should remain in the down state
            	if and only if there is a fault that prevents it from going
            	to the up state; It should remain in the notPresent
            	state if the interface has missing (typically, hardware)
            	components
          items:
            format: int64
            type: integer
          type: array
        operation_mode:
          description: |
            List of OperationMode. OperationMode represents Operation Mode
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        slpp_status:
          description: >
            List of SlppStatus. SlppStatus represents The SLPP safe guard status
            for the port.
            	SLPP guard state is set to MONITORING if the port is operational UP and
            	SLPP guard is enabled on that port. The state is set to BLOCKING when the
            	port is disabled by SLPP guard. If SLPP is not enabled on the port, the state
            	is set to NONE.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPortStateGetRequest
      type: object
    AssetPortStateGetResponse:
      properties:
        AssetPortState:
          items:
            $ref: "#/components/schemas/AssetPortState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPortStateGetResponse
      type: object
    AssetPortsGlobalConfig:
      description: AssetPortsGlobalConfig - DB Model for Ports Global config list
      properties:
        advancedfeaturebandwidthreservation:
          default: 2
          description: Ports Global AdvancedFeatureBandwidthReservation boot config flag
            setting.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        flowcontrolmode:
          description: Ports Global FlowControlMode boot config flag enabled/disabled.
          type: boolean
        flowcontrolmodeoperstatus:
          description: Ports Global FlowControlModeOperStatus operational status of
            FlowControlMode boot config flag, enabled/disabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - advancedfeaturebandwidthreservation
        - device_id
        - flowcontrolmode
        - flowcontrolmodeoperstatus
      type: object
    AssetPortsGlobalConfigGetRequest:
      description: AssetPortsGlobalConfigGetRequest represents the retrieval request
        for AssetPortsGlobalConfig.
      properties:
        advancedfeaturebandwidthreservation:
          description: >
            List of AdvancedFeatureBandwidthReservation.
            AdvancedFeatureBandwidthReservation represents Ports Global
            AdvancedFeatureBandwidthReservation boot config flag setting.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        flowcontrolmode:
          description: >
            List of FlowControlMode. FlowControlMode represents Ports Global
            FlowControlMode boot config flag enabled/disabled.
          items:
            type: boolean
          type: array
        flowcontrolmodeoperstatus:
          description: >
            List of FlowControlModeOperStatus. FlowControlModeOperStatus
            represents Ports Global FlowControlModeOperStatus operational status
            of FlowControlMode boot config flag, enabled/disabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetPortsGlobalConfigGetRequest
      type: object
    AssetPortsGlobalConfigGetResponse:
      properties:
        AssetPortsGlobalConfig:
          items:
            $ref: "#/components/schemas/AssetPortsGlobalConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetPortsGlobalConfigGetResponse
      type: object
    AssetRadiusServerConfig:
      description: AssetRadiusServerConfig - DB model Radius Server Config list
        Servers,SecureProfiles can be created while creating
        AssetRadiusServerConfig.
      properties:
        access_priority_attr:
          description: This attribute helps manage the order in which RADIUS servers are
            queried for authentication, authorization, and accounting
          type: integer
        account_attr:
          description: Define specific elements that will be included in the accounting
            records sent to the RADIUS server.
          type: integer
        account_cli_commands:
          description: Ensure that all executed CLI commands are logged and sent to the
            RADIUS server.
          type: boolean
        account_enabled:
          description: Allow to track and log user sessions, providing valuable data for
            auditing and monitoring purposes.
          type: boolean
        auth_info_attr:
          description: This attribute used to provide extra context or data required for
            the authentication process.
          type: integer
        clear_stat:
          description: Clear statistics on a RADIUS server.
          type: boolean
        cli_commands_attr:
          description: This attribute helps manage which commands a user is allowed to
            execute on network devices.
          type: integer
        cli_commands_count:
          description: This attribute helps manage and limit the command execution
            capabilities of users on network devices.
          type: integer
        cli_profile_enabled:
          description: This attribute in RADIUS server configuration is used to enable or
            disable the use of CLI profiles for command authorization
          type: boolean
        command_access_attr:
          description: This attribute helps manage which commands a user is allowed to
            execute on network devices.
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        dyn_auth_enabled:
          description: >-
            Allow the RADIUS server to dynamically manage user sessions,
            	including disconnecting sessions or changing authorization parameters like VLAN or user-role assignments
          type: boolean
        enabled:
          default: false
          description: The status of the Radius Server.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        igap_timeout_log_fsize:
          description: Manage the timeout settings and log file size for IGAP
            (Inter-Gateway Access Protocol) communications.
          type: integer
        keep_alive_timer:
          description: The interval at which keep-alive messages are sent to ensure the
            server is responsive and available.
          type: integer
        max_server:
          description: Specify the maximum number of RADIUS servers that can be configured
            or used for redundancy and load balancing.
          type: integer
        mcast_addr_attr:
          description: Specify multicast addresses for RADIUS communication.
          type: integer
        mgmt_acct_enabled:
          description: Allow to track and log management access attempts to network devices.
          type: boolean
        mgmt_auth_enabled:
          description: Allow administrators to authenticate management access to network
            devices using RADIUS
          type: boolean
        network_acct_enabled:
          description: Allow to track and log network access attempts, providing valuable
            data for auditing and monitoring purposes.
          type: boolean
        network_auth_enabled:
          description: Ensure that network access requests are authenticated using the
            RADIUS protocol.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        password:
          description: The password on the RADIUS server for authentication.
          type: string
        reachability_mode:
          description: Monitor the status of a RADIUS server's availability
          type: string
        retransmit_algo:
          description: Algorithm for network device to handles retransmission of requests
            to the RADIUS server.
          type: string
        retries:
          description: Number of times the network device will attempt to contact the
            RADIUS server before considering it unreachable
          type: integer
        secure_enabled:
          description: Enable secure communication between the RADIUS server and its clients
          type: boolean
        secure_profiles:
          items:
            $ref: "#/components/schemas/AssetRadiusServerSecureProfilesConfig"
          nullable: true
          type: array
        servers:
          items:
            $ref: "#/components/schemas/AssetRadiusServerHostConfig"
          nullable: true
          type: array
        timeout:
          description: How long the network device will wait for a response from the
            RADIUS server before trying the next server or considering the
            request failed.
          type: integer
        tls_ocsp_enabled:
          description: >-
            Enable TLS OCSP (Online Certificate Status Protocol) on a RADIUS
            server to allow for real-time verification of certificate status,
            	enhancing security by ensuring that revoked certificates are not accepted.
          type: boolean
        tls_ocsp_nonce:
          description: >-
            Option for a RADIUS server to determines whether the OCSP (Online
            Certificate Status Protocol) requests include a nonce.
            	A nonce is a unique identifier used to prevent replay attacks, enhancing security
          type: boolean
        tls_ocsp_override_url:
          description: Specify a custom URL for the OCSP (Online Certificate Status
            Protocol) responder.
          type: string
        tls_ocsp_signer_no_check:
          description: Option for a RADIUS server to bypass the verification of the OCSP
            (Online Certificate Status Protocol) responder's certificate.
          type: string
        tls_tcp_timeout:
          description: How long the network device will wait for a response over a TLS
            connection before considering the request failed.
          type: integer
        unreachable_timer:
          description: Specify the duration for which a RADIUS server is considered
            unreachable before it is retried.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        username:
          description: The username on the RADIUS server for authentication.
          type: string
      required:
        - device_id
        - enabled
      type: object
    AssetRadiusServerConfigGetRequest:
      description: AssetRadiusServerConfigGetRequest represents the retrieval request
        for AssetRadiusServerConfig.
      properties:
        access_priority_attr:
          description: >
            List of AccessPriorityAttr. AccessPriorityAttr represents This
            attribute helps manage the order in which RADIUS servers are queried
            for authentication, authorization, and accounting
          items:
            type: integer
          type: array
        account_attr:
          description: >
            List of AccountAttr. AccountAttr represents Define specific elements
            that will be included in the accounting records sent to the RADIUS
            server.
          items:
            type: integer
          type: array
        account_cli_commands:
          description: >
            List of AccountCliCommands. AccountCliCommands represents Ensure
            that all executed CLI commands are logged and sent to the RADIUS
            server.
          items:
            type: boolean
          type: array
        account_enabled:
          description: >
            List of AccountEnabled. AccountEnabled represents Allow to track and
            log user sessions, providing valuable data for auditing and
            monitoring purposes.
          items:
            type: boolean
          type: array
        auth_info_attr:
          description: >
            List of AuthInfoAttr. AuthInfoAttr represents This attribute used to
            provide extra context or data required for the authentication
            process.
          items:
            type: integer
          type: array
        clear_stat:
          description: >
            List of ClearStat. ClearStat represents Clear statistics on a RADIUS
            server.
          items:
            type: boolean
          type: array
        cli_commands_attr:
          description: >
            List of CliCommandsAttr. CliCommandsAttr represents This attribute
            helps manage which commands a user is allowed to execute on network
            devices.
          items:
            type: integer
          type: array
        cli_commands_count:
          description: >
            List of CliCommandsCount. CliCommandsCount represents This attribute
            helps manage and limit the command execution capabilities of users
            on network devices.
          items:
            type: integer
          type: array
        cli_profile_enabled:
          description: >
            List of CliProfileEnabled. CliProfileEnabled represents This
            attribute in RADIUS server configuration is used to enable or
            disable the use of CLI profiles for command authorization
          items:
            type: boolean
          type: array
        command_access_attr:
          description: >
            List of CommandAccessAttr. CommandAccessAttr represents This
            attribute helps manage which commands a user is allowed to execute
            on network devices.
          items:
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        dyn_auth_enabled:
          description: >
            List of DynAuthEnabled. DynAuthEnabled represents Allow the RADIUS
            server to dynamically manage user sessions,
            	including disconnecting sessions or changing authorization parameters like VLAN or user-role assignments
          items:
            type: boolean
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents The status of the Radius Server.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        igap_timeout_log_fsize:
          description: >
            List of IgapTimeoutLogFsize. IgapTimeoutLogFsize represents Manage
            the timeout settings and log file size for IGAP (Inter-Gateway
            Access Protocol) communications.
          items:
            type: integer
          type: array
        keep_alive_timer:
          description: >
            List of KeepAliveTimer. KeepAliveTimer represents The interval at
            which keep-alive messages are sent to ensure the server is
            responsive and available.
          items:
            type: integer
          type: array
        max_server:
          description: >
            List of MaxServer. MaxServer represents Specify the maximum number
            of RADIUS servers that can be configured or used for redundancy and
            load balancing.
          items:
            type: integer
          type: array
        mcast_addr_attr:
          description: >
            List of McastAddrAttr. McastAddrAttr represents Specify multicast
            addresses for RADIUS communication.
          items:
            type: integer
          type: array
        mgmt_acct_enabled:
          description: >
            List of MgmtAcctEnabled. MgmtAcctEnabled represents Allow to track
            and log management access attempts to network devices.
          items:
            type: boolean
          type: array
        mgmt_auth_enabled:
          description: >
            List of MgmtAuthEnabled. MgmtAuthEnabled represents Allow
            administrators to authenticate management access to network devices
            using RADIUS
          items:
            type: boolean
          type: array
        network_acct_enabled:
          description: >
            List of NetworkAcctEnabled. NetworkAcctEnabled represents Allow to
            track and log network access attempts, providing valuable data for
            auditing and monitoring purposes.
          items:
            type: boolean
          type: array
        network_auth_enabled:
          description: >
            List of NetworkAuthEnabled. NetworkAuthEnabled represents Ensure
            that network access requests are authenticated using the RADIUS
            protocol.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        password:
          description: >
            List of Password. Password represents The password on the RADIUS
            server for authentication.
          items:
            type: string
          type: array
        reachability_mode:
          description: >
            List of ReachabilityMode. ReachabilityMode represents Monitor the
            status of a RADIUS server's availability
          items:
            type: string
          type: array
        retransmit_algo:
          description: >
            List of RetransmitAlgo. RetransmitAlgo represents Algorithm for
            network device to handles retransmission of requests to the RADIUS
            server.
          items:
            type: string
          type: array
        retries:
          description: >
            List of Retries. Retries represents Number of times the network
            device will attempt to contact the RADIUS server before considering
            it unreachable
          items:
            type: integer
          type: array
        secure_enabled:
          description: >
            List of SecureEnabled. SecureEnabled represents Enable secure
            communication between the RADIUS server and its clients
          items:
            type: boolean
          type: array
        timeout:
          description: >
            List of Timeout. Timeout represents How long the network device will
            wait for a response from the RADIUS server before trying the next
            server or considering the request failed.
          items:
            type: integer
          type: array
        tls_ocsp_enabled:
          description: >
            List of TlsOcspEnabled. TlsOcspEnabled represents Enable TLS OCSP
            (Online Certificate Status Protocol) on a RADIUS server to allow for
            real-time verification of certificate status,
            	enhancing security by ensuring that revoked certificates are not accepted.
          items:
            type: boolean
          type: array
        tls_ocsp_nonce:
          description: >
            List of TlsOcspNonce. TlsOcspNonce represents Option for a RADIUS
            server to determines whether the OCSP (Online Certificate Status
            Protocol) requests include a nonce.
            	A nonce is a unique identifier used to prevent replay attacks, enhancing security
          items:
            type: boolean
          type: array
        tls_ocsp_override_url:
          description: >
            List of TlsOcspOverrideUrl. TlsOcspOverrideUrl represents Specify a
            custom URL for the OCSP (Online Certificate Status Protocol)
            responder.
          items:
            type: string
          type: array
        tls_ocsp_signer_no_check:
          description: >
            List of TlsOcspSignerNoCheck. TlsOcspSignerNoCheck represents Option
            for a RADIUS server to bypass the verification of the OCSP (Online
            Certificate Status Protocol) responder's certificate.
          items:
            type: string
          type: array
        tls_tcp_timeout:
          description: >
            List of TlsTcpTimeout. TlsTcpTimeout represents How long the network
            device will wait for a response over a TLS connection before
            considering the request failed.
          items:
            type: integer
          type: array
        unreachable_timer:
          description: >
            List of UnreachableTimer. UnreachableTimer represents Specify the
            duration for which a RADIUS server is considered unreachable before
            it is retried.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        username:
          description: >
            List of Username. Username represents The username on the RADIUS
            server for authentication.
          items:
            type: string
          type: array
      title: AssetRadiusServerConfigGetRequest
      type: object
    AssetRadiusServerConfigGetResponse:
      properties:
        AssetRadiusServerConfig:
          items:
            $ref: "#/components/schemas/AssetRadiusServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetRadiusServerConfigGetResponse
      type: object
    AssetRadiusServerHostConfig:
      description: AssetRadiusServerHostConfig - DB model Radius Server Host Config list
      nullable: true
      properties:
        account_enabled:
          description: Accounting enabled
          type: boolean
        account_port:
          description: Port number for accounting requests.
          type: integer
        auth_port:
          description: Port number for authentication requests.
          type: integer
        client_address:
          description: Client Address
          type: string
        client_address_type:
          description: Client address type
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Host config enabled
          type: boolean
        host_address:
          description: Host address
          type: string
        host_type:
          description: Radius server supported host types
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: Index
          type: integer
        interval_minimum:
          description: Minimum interval
          type: integer
        is_primary:
          description: It specifies if the RADIUS server which is intended to be updated
            it's a primary server. (Used only for Switch Engine)
          type: boolean
        is_secondary:
          description: It specifies if the RADIUS server which is intended to be updated
            it's a secondary server. (Used only for Switch Engine)
          type: boolean
        max_sessions:
          description: Max sessions
          type: integer
        nas_ip:
          description: This attribute allows you to specify an arbitrary IP address for
            the NAS (Network Access Server)
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port:
          description: Port
          type: integer
        priority:
          description: Configuration priority
          type: integer
        radius_server_config_id:
          description: UUID of the radius server configuration
          type: string
        retries:
          description: Number of retries
          type: integer
        secret_is_encrypted:
          description: Encryption status of secret value
          type: boolean
        secret_value:
          description: |-
            The unencrypted shared key used between the authentication
            	server and the device.
          type: string
        secure_enabled:
          description: Secure mode enabled
          type: boolean
        secure_log_level:
          description: Secure log level [ CRITICAL, ERROR, WARNING, INFO, DEBUG ]
          type: string
        secure_mode:
          description: Secure mode
          type: string
        secure_profile:
          description: Secure profile
          type: string
        server_mode:
          description: RADIUS server modes to handle different aspects of network security
            and management
          format: int64
          type: integer
        server_status:
          description: Server status
          type: string
        timeout:
          description: Timeout
          type: integer
        tls_connection_status:
          description: TLS connection status
          type: string
        update_interval:
          description: Update interval
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        used_by:
          description: Used by
          type: string
        vr_name:
          description: Virtual router Name
          type: string
      required:
        - client_address
        - client_address_type
        - host_address
        - host_type
        - port
        - radius_server_config_id
        - secret_value
        - secure_mode
        - server_mode
        - used_by
        - vr_name
      type: object
    AssetRadiusServerHostConfigGetRequest:
      description: AssetRadiusServerHostConfigGetRequest represents the retrieval
        request for AssetRadiusServerHostConfig.
      properties:
        account_enabled:
          description: |
            List of AccountEnabled. AccountEnabled represents Accounting enabled
          items:
            type: boolean
          type: array
        account_port:
          description: >
            List of AccountPort. AccountPort represents Port number for
            accounting requests.
          items:
            type: integer
          type: array
        auth_port:
          description: >
            List of AuthPort. AuthPort represents Port number for authentication
            requests.
          items:
            type: integer
          type: array
        client_address:
          description: |
            List of ClientAddress. ClientAddress represents Client Address
          items:
            type: string
          type: array
        client_address_type:
          description: >
            List of ClientAddressType. ClientAddressType represents Client
            address type
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents Host config enabled
          items:
            type: boolean
          type: array
        host_address:
          description: |
            List of HostAddress. HostAddress represents Host address
          items:
            type: string
          type: array
        host_type:
          description: >
            List of HostType. HostType represents Radius server supported host
            types
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: |
            List of Index. Index represents Index
          items:
            type: integer
          type: array
        interval_minimum:
          description: |
            List of IntervalMinimum. IntervalMinimum represents Minimum interval
          items:
            type: integer
          type: array
        is_primary:
          description: >
            List of IsPrimary. IsPrimary represents It specifies if the RADIUS
            server which is intended to be updated it's a primary server. (Used
            only for Switch Engine)
          items:
            type: boolean
          type: array
        is_secondary:
          description: >
            List of IsSecondary. IsSecondary represents It specifies if the
            RADIUS server which is intended to be updated it's a secondary
            server. (Used only for Switch Engine)
          items:
            type: boolean
          type: array
        max_sessions:
          description: |
            List of MaxSessions. MaxSessions represents Max sessions
          items:
            type: integer
          type: array
        nas_ip:
          description: >
            List of NasIp. NasIp represents This attribute allows you to specify
            an arbitrary IP address for the NAS (Network Access Server)
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port:
          description: |
            List of Port. Port represents Port
          items:
            type: integer
          type: array
        priority:
          description: |
            List of Priority. Priority represents Configuration priority
          items:
            type: integer
          type: array
        radius_server_config_id:
          description: >
            List of RadiusServerConfigId. RadiusServerConfigId represents UUID
            of the radius server configuration
          items:
            type: string
          type: array
        retries:
          description: |
            List of Retries. Retries represents Number of retries
          items:
            type: integer
          type: array
        secret_is_encrypted:
          description: >
            List of SecretIsEncrypted. SecretIsEncrypted represents Encryption
            status of secret value
          items:
            type: boolean
          type: array
        secret_value:
          description: >
            List of SecretValue. SecretValue represents The unencrypted shared
            key used between the authentication
            	server and the device.
          items:
            type: string
          type: array
        secure_enabled:
          description: |
            List of SecureEnabled. SecureEnabled represents Secure mode enabled
          items:
            type: boolean
          type: array
        secure_log_level:
          description: >
            List of SecureLogLevel. SecureLogLevel represents Secure log level [
            CRITICAL, ERROR, WARNING, INFO, DEBUG ]
          items:
            type: string
          type: array
        secure_mode:
          description: |
            List of SecureMode. SecureMode represents Secure mode
          items:
            type: string
          type: array
        secure_profile:
          description: |
            List of SecureProfile. SecureProfile represents Secure profile
          items:
            type: string
          type: array
        server_mode:
          description: >
            List of ServerMode. ServerMode represents RADIUS server modes to
            handle different aspects of network security and management
          items:
            format: int64
            type: integer
          type: array
        server_status:
          description: |
            List of ServerStatus. ServerStatus represents Server status
          items:
            type: string
          type: array
        timeout:
          description: |
            List of Timeout. Timeout represents Timeout
          items:
            type: integer
          type: array
        tls_connection_status:
          description: >
            List of TlsConnectionStatus. TlsConnectionStatus represents TLS
            connection status
          items:
            type: string
          type: array
        update_interval:
          description: |
            List of UpdateInterval. UpdateInterval represents Update interval
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        used_by:
          description: |
            List of UsedBy. UsedBy represents Used by
          items:
            type: string
          type: array
        vr_name:
          description: |
            List of VrName. VrName represents Virtual router Name
          items:
            type: string
          type: array
      title: AssetRadiusServerHostConfigGetRequest
      type: object
    AssetRadiusServerHostConfigGetResponse:
      properties:
        AssetRadiusServerHostConfig:
          items:
            $ref: "#/components/schemas/AssetRadiusServerHostConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetRadiusServerHostConfigGetResponse
      type: object
    AssetRadiusServerSecureProfilesConfig:
      description: AssetRadiusServerSecureProfilesConfig - DB model Radius Server
        Secure Profiles Config list
      properties:
        cert_hash:
          description: The hash of the server's certificate to ensure secure communication.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        name:
          description: Profile name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        radius_server_config_id:
          description: UUID of the radius server configuration
          type: string
        root_cert_hash:
          description: The hash of the server's root certificate to ensure secure
            communication.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - name
        - radius_server_config_id
      type: object
    AssetRadiusServerSecureProfilesConfigGetRequest:
      description: AssetRadiusServerSecureProfilesConfigGetRequest represents the
        retrieval request for AssetRadiusServerSecureProfilesConfig.
      properties:
        cert_hash:
          description: >
            List of CertHash. CertHash represents The hash of the server's
            certificate to ensure secure communication.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Profile name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        radius_server_config_id:
          description: >
            List of RadiusServerConfigId. RadiusServerConfigId represents UUID
            of the radius server configuration
          items:
            type: string
          type: array
        root_cert_hash:
          description: >
            List of RootCertHash. RootCertHash represents The hash of the
            server's root certificate to ensure secure communication.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetRadiusServerSecureProfilesConfigGetRequest
      type: object
    AssetRadiusServerSecureProfilesConfigGetResponse:
      properties:
        AssetRadiusServerSecureProfilesConfig:
          items:
            $ref: "#/components/schemas/AssetRadiusServerSecureProfilesConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetRadiusServerSecureProfilesConfigGetResponse
      type: object
    AssetRsmltLocalInstanceState:
      description: AssetRsmltLocalInstanceState - This object represents the list of
        state of RSMLT local instances.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        hold_down_time_left:
          description: The remaining time from Hold Down Time
          format: int64
          minimum: 0
          type: integer
        hold_up_time_left:
          description: The remaining time from Hold Up Time.
          format: int64
          minimum: 0
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mac_address:
          description: Mac Address
          type: string
        operation_status:
          description: The operation status of RSMLT instance.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        smlt_id:
          description: SMLT Id
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_device_id
        - mac_address
        - smlt_id
        - vlan_number
      type: object
    AssetRsmltLocalInstanceStateGetRequest:
      description: AssetRsmltLocalInstanceStateGetRequest represents the retrieval
        request for AssetRsmltLocalInstanceState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        hold_down_time_left:
          description: >
            List of HoldDownTimeLeft. HoldDownTimeLeft represents The remaining
            time from Hold Down Time
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        hold_up_time_left:
          description: >
            List of HoldUpTimeLeft. HoldUpTimeLeft represents The remaining time
            from Hold Up Time.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mac_address:
          description: |
            List of MacAddress. MacAddress represents Mac Address
          items:
            type: string
          type: array
        operation_status:
          description: >
            List of OperationStatus. OperationStatus represents The operation
            status of RSMLT instance.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        smlt_id:
          description: |
            List of SmltId. SmltId represents SMLT Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetRsmltLocalInstanceStateGetRequest
      type: object
    AssetRsmltLocalInstanceStateGetResponse:
      properties:
        AssetRsmltLocalInstanceState:
          items:
            $ref: "#/components/schemas/AssetRsmltLocalInstanceState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetRsmltLocalInstanceStateGetResponse
      type: object
    AssetRsmltRemoteInstanceState:
      description: AssetRsmltRemoteInstanceState - This object represents the list of
        state of RSMLT remote instances.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: The admin enabled setting of RSMLT instance.
          type: boolean
        hold_down_time_left:
          description: The remaining time from Hold Down Time
          format: int64
          minimum: 0
          type: integer
        hold_down_timer:
          description: |-
            Timer value, the range of value is from 0 to 3600.
            	The time period is in seconds.
          format: int64
          minimum: 0
          type: integer
        hold_up_time_left:
          description: The remaining time from Hold Up Time.
          format: int64
          minimum: 0
          type: integer
        hold_up_timer:
          description: |-
            The range of value is 0 to 3600 or 9999. 9999 means infinity.
            	The time period is in seconds.
          format: int64
          minimum: 0
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mac_address:
          description: Mac Address
          type: string
        operation_status:
          description: The operation status of RSMLT instance.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        smlt_id:
          description: SMLT Id
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_device_id
        - enabled
        - smlt_id
        - vlan_number
      type: object
    AssetRsmltRemoteInstanceStateGetRequest:
      description: AssetRsmltRemoteInstanceStateGetRequest represents the retrieval
        request for AssetRsmltRemoteInstanceState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The admin enabled setting of
            RSMLT instance.
          items:
            type: boolean
          type: array
        hold_down_time_left:
          description: >
            List of HoldDownTimeLeft. HoldDownTimeLeft represents The remaining
            time from Hold Down Time
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        hold_down_timer:
          description: >
            List of HoldDownTimer. HoldDownTimer represents Timer value, the
            range of value is from 0 to 3600.
            	The time period is in seconds.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        hold_up_time_left:
          description: >
            List of HoldUpTimeLeft. HoldUpTimeLeft represents The remaining time
            from Hold Up Time.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        hold_up_timer:
          description: >
            List of HoldUpTimer. HoldUpTimer represents The range of value is 0
            to 3600 or 9999. 9999 means infinity.
            	The time period is in seconds.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mac_address:
          description: |
            List of MacAddress. MacAddress represents Mac Address
          items:
            type: string
          type: array
        operation_status:
          description: >
            List of OperationStatus. OperationStatus represents The operation
            status of RSMLT instance.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        smlt_id:
          description: |
            List of SmltId. SmltId represents SMLT Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetRsmltRemoteInstanceStateGetRequest
      type: object
    AssetRsmltRemoteInstanceStateGetResponse:
      properties:
        AssetRsmltRemoteInstanceState:
          items:
            $ref: "#/components/schemas/AssetRsmltRemoteInstanceState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetRsmltRemoteInstanceStateGetResponse
      type: object
    AssetRsmltVlanInstanceConfig:
      description: AssetRsmltVlanInstanceConfig - This object represents the list of
        configuration of all RSMLT instances on all VLANs.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        enabled:
          description: The admin enabled setting of RSMLT instance.
          type: boolean
        hold_down_timer:
          description: |-
            Timer value, the range of value is from 0 to 3600.
            	The time period is in seconds.
          format: int64
          minimum: 0
          type: integer
        hold_up_timer:
          description: |-
            The range of value is 0 to 3600 or 9999. 9999 means infinity.
            	The time period is in seconds.
          format: int64
          minimum: 0
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config_id:
          description: VLAN Config Id
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - enabled
        - vlan_config_id
        - vlan_number
      type: object
    AssetRsmltVlanInstanceConfigGetRequest:
      description: AssetRsmltVlanInstanceConfigGetRequest represents the retrieval
        request for AssetRsmltVlanInstanceConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The admin enabled setting of
            RSMLT instance.
          items:
            type: boolean
          type: array
        hold_down_timer:
          description: >
            List of HoldDownTimer. HoldDownTimer represents Timer value, the
            range of value is from 0 to 3600.
            	The time period is in seconds.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        hold_up_timer:
          description: >
            List of HoldUpTimer. HoldUpTimer represents The range of value is 0
            to 3600 or 9999. 9999 means infinity.
            	The time period is in seconds.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_config_id:
          description: |
            List of VlanConfigId. VlanConfigId represents VLAN Config Id
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetRsmltVlanInstanceConfigGetRequest
      type: object
    AssetRsmltVlanInstanceConfigGetResponse:
      properties:
        AssetRsmltVlanInstanceConfig:
          items:
            $ref: "#/components/schemas/AssetRsmltVlanInstanceConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetRsmltVlanInstanceConfigGetResponse
      type: object
    AssetSlot:
      description: >-
        AssetSlot - This object represents the list of Slots on device. It also

        includes the information on card. AssetPortConfig,AssetVim can be
        created while creating AssetSlot.
      nullable: true
      properties:
        admin_status:
          description: >-
            Device administrative status. UP, DOWN, TESTING and RESET are
            applicable
            	to Fabric Engine, ENABLED is applicable to Switch Engine.
          format: int64
          type: integer
        asset_device_id:
          description: Device Id.
          type: string
        asset_port_config:
          items:
            $ref: "#/components/schemas/AssetPortConfig"
          nullable: true
          type: array
        asset_vim:
          items:
            $ref: "#/components/schemas/AssetVim"
          nullable: true
          type: array
        base_mac_address:
          description: Mac Address.
          type: string
        brand_name:
          description: Device brand name.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        firmware_version:
          description: |-
            The vendor-specific firmware revision.
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          type: string
        hardware_rev:
          description: Device hardware revision level.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        is_power_enabled:
          description: >-
            Administratively turn on or off power on this slot. only applicable
            on Standby
            	CP & IO and SF Card.
          type: boolean
        mac_address_capacity:
          description: MAC address capacity.
          format: int64
          minimum: 0
          type: integer
        model_name:
          description: Device model name.
          type: string
        node_state:
          description: >-
            Node state - Switch Engine (EXOS) only. Configuration commands are
            	prevented when the stack's backup node is in BACKUP state.
            	Administrative failover operations are prevented when the stack's backup
            	node is in BACKUP or BACKUP_CONFIG_SYNCED states. It is safe to perform
            	a failover when the stack's backup node is in BACKUP_IN_SYNC state.
          format: int64
          type: integer
        num_ports:
          description: The total number of ports currently installed in the chassis.
          format: int64
          minimum: 0
          type: integer
        operation_status:
          description: >-
            Device operational status. DOWN is applicable to both Fabric Engine
            and
            	Switch Engine. UP, TESTING, UNKNOWN, DORMANT, UP_MASTER, UP_DORMANT and
            	UP_WARMSTANDBY are applicable to Fabric Engine. The others are
            	applicable to Switch Engine.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        part_number:
          description: Device part number.
          type: string
        serial_number:
          description: Device unique serial number.
          type: string
        slot_number:
          description: Device Slot Number.
          type: integer
        sys_build_time:
          description: System build time.
          type: string
        sys_up_time:
          description: |-
            The time (in hundredths of a second) since the network
            	management portion of the system was last re-initialized.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - serial_number
        - slot_number
      type: object
    AssetSlotGetRequest:
      description: AssetSlotGetRequest represents the retrieval request for AssetSlot.
      properties:
        admin_status:
          description: >
            List of AdminStatus. AdminStatus represents Device administrative
            status. UP, DOWN, TESTING and RESET are applicable
            	to Fabric Engine, ENABLED is applicable to Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id.
          items:
            type: string
          type: array
        base_mac_address:
          description: |
            List of BaseMacAddress. BaseMacAddress represents Mac Address.
          items:
            type: string
          type: array
        brand_name:
          description: |
            List of BrandName. BrandName represents Device brand name.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        firmware_version:
          description: >
            List of FirmwareVersion. FirmwareVersion represents The
            vendor-specific firmware revision.
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          items:
            type: string
          type: array
        hardware_rev:
          description: >
            List of HardwareRev. HardwareRev represents Device hardware revision
            level.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        is_power_enabled:
          description: >
            List of IsPowerEnabled. IsPowerEnabled represents Administratively
            turn on or off power on this slot. only applicable on Standby
            	CP & IO and SF Card.
          items:
            type: boolean
          type: array
        mac_address_capacity:
          description: >
            List of MacAddressCapacity. MacAddressCapacity represents MAC
            address capacity.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        model_name:
          description: |
            List of ModelName. ModelName represents Device model name.
          items:
            type: string
          type: array
        node_state:
          description: >
            List of NodeState. NodeState represents Node state - Switch Engine
            (EXOS) only. Configuration commands are
            	prevented when the stack's backup node is in BACKUP state.
            	Administrative failover operations are prevented when the stack's backup
            	node is in BACKUP or BACKUP_CONFIG_SYNCED states. It is safe to perform
            	a failover when the stack's backup node is in BACKUP_IN_SYNC state.
          items:
            format: int64
            type: integer
          type: array
        num_ports:
          description: >
            List of NumPorts. NumPorts represents The total number of ports
            currently installed in the chassis.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        operation_status:
          description: >
            List of OperationStatus. OperationStatus represents Device
            operational status. DOWN is applicable to both Fabric Engine and
            	Switch Engine. UP, TESTING, UNKNOWN, DORMANT, UP_MASTER, UP_DORMANT and
            	UP_WARMSTANDBY are applicable to Fabric Engine. The others are
            	applicable to Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        part_number:
          description: |
            List of PartNumber. PartNumber represents Device part number.
          items:
            type: string
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        slot_number:
          description: |
            List of SlotNumber. SlotNumber represents Device Slot Number.
          items:
            type: integer
          type: array
        sys_build_time:
          description: |
            List of SysBuildTime. SysBuildTime represents System build time.
          items:
            type: string
          type: array
        sys_up_time:
          description: >
            List of SysUpTime. SysUpTime represents The time (in hundredths of a
            second) since the network
            	management portion of the system was last re-initialized.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSlotGetRequest
      type: object
    AssetSlotGetResponse:
      properties:
        AssetSlot:
          items:
            $ref: "#/components/schemas/AssetSlot"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSlotGetResponse
      type: object
    AssetSlppConfig:
      description: AssetSlppConfig - DB Model for SLPP Global config list
        SlppPorts,SlppVlans can be created while creating AssetSlppConfig.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        enabled:
          default: false
          description: SLPP protocol enabled/disabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        slpp_ports:
          items:
            $ref: "#/components/schemas/AssetSlppPortConfig"
          nullable: true
          type: array
        slpp_vlans:
          items:
            $ref: "#/components/schemas/AssetSlppVlanConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - enabled
      type: object
    AssetSlppConfigGetRequest:
      description: AssetSlppConfigGetRequest represents the retrieval request for
        AssetSlppConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SLPP protocol enabled/disabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSlppConfigGetRequest
      type: object
    AssetSlppConfigGetResponse:
      properties:
        AssetSlppConfig:
          items:
            $ref: "#/components/schemas/AssetSlppConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSlppConfigGetResponse
      type: object
    AssetSlppPortConfig:
      description: AssetSlppPortConfig - DB Model for SLPP PORTs config list
      properties:
        asset_port_config_id:
          description: UUID of the Interface.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enableguard:
          default: false
          description: SLPP protocol enable/disable guard.
          type: boolean
        enablepacketrx:
          default: false
          description: SLPP protocol flag indicate whether the port entry is active or not
            for SLPP
          type: boolean
        guardtimeout:
          description: SLPP protocol time period for which a port will remain disabled
            before being re-enabled.
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        packetrxthreshold:
          default: 1
          description: "SLPP protocol: Once the number of SLPP packets received on this
            port reaches this threshold, action will be taken"
          type: integer
        port_name:
          description: SLPP port name.
          type: string
        slpp_config_id:
          description: SLPP Config Id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_port_config_id
        - enableguard
        - enablepacketrx
        - packetrxthreshold
        - port_name
        - slpp_config_id
      type: object
    AssetSlppPortConfigGetRequest:
      description: AssetSlppPortConfigGetRequest represents the retrieval request for
        AssetSlppPortConfig.
      properties:
        asset_port_config_id:
          description: >
            List of AssetPortConfigId. AssetPortConfigId represents UUID of the
            Interface.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enableguard:
          description: >
            List of EnableGuard. EnableGuard represents SLPP protocol
            enable/disable guard.
          items:
            type: boolean
          type: array
        enablepacketrx:
          description: >
            List of EnablePacketRx. EnablePacketRx represents SLPP protocol flag
            indicate whether the port entry is active or not for SLPP
          items:
            type: boolean
          type: array
        guardtimeout:
          description: >
            List of GuardTimeout. GuardTimeout represents SLPP protocol time
            period for which a port will remain disabled before being
            re-enabled.
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        packetrxthreshold:
          description: >
            List of PacketRxThreshold. PacketRxThreshold represents SLPP
            protocol: Once the number of SLPP packets received on this port
            reaches this threshold, action will be taken
          items:
            type: integer
          type: array
        port_name:
          description: |
            List of PortName. PortName represents SLPP port name.
          items:
            type: string
          type: array
        slpp_config_id:
          description: |
            List of SlppConfigId. SlppConfigId represents SLPP Config Id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSlppPortConfigGetRequest
      type: object
    AssetSlppPortConfigGetResponse:
      properties:
        AssetSlppPortConfig:
          items:
            $ref: "#/components/schemas/AssetSlppPortConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSlppPortConfigGetResponse
      type: object
    AssetSlppVlanConfig:
      description: AssetSlppVlanConfig - DB Model for SLPP VLANs config list
      properties:
        asset_vlan_config_id:
          description: UUID of Device VLAN.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          default: false
          description: SLPP protocol enabled/disabled on Vlan.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        slpp_config_id:
          description: SLPP Config Id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: SLPP Vlan ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_vlan_config_id
        - enabled
        - slpp_config_id
        - vlan_id
      type: object
    AssetSlppVlanConfigGetRequest:
      description: AssetSlppVlanConfigGetRequest represents the retrieval request for
        AssetSlppVlanConfig.
      properties:
        asset_vlan_config_id:
          description: >
            List of AssetVlanConfigId. AssetVlanConfigId represents UUID of
            Device VLAN.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents SLPP protocol enabled/disabled
            on Vlan.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        slpp_config_id:
          description: |
            List of SlppConfigId. SlppConfigId represents SLPP Config Id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents SLPP Vlan ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetSlppVlanConfigGetRequest
      type: object
    AssetSlppVlanConfigGetResponse:
      properties:
        AssetSlppVlanConfig:
          items:
            $ref: "#/components/schemas/AssetSlppVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSlppVlanConfigGetResponse
      type: object
    AssetSnmpAccessConfig:
      description: AssetSnmpAccessConfig - DB model SNMP access Config list
      nullable: true
      properties:
        access_view:
          description: The SNMP v1/v2 access view. This applies only to Switch Engine
            (EXOS).
          format: int64
          type: integer
        community:
          description: The SNMP community string
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        group_name:
          description: Specifies the group name for data access. This is Fabric
            Engine(VOSS) specific.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: The index associated with this entry. This property only applies to
            Fabric Engine (VOSS)
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        security_name:
          description: The security name associated with this entry. This property only
            applies to Fabric Engine (VOSS)
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        v1_v2_config_id:
          description: SNMP V1V2 Config ID
          type: string
        vr_name:
          description: The name of the Virtual Router. This property only applies to
            Fabric Engine (VOSS)
          type: string
      required:
        - community
        - v1_v2_config_id
      type: object
    AssetSnmpAccessConfigGetRequest:
      description: AssetSnmpAccessConfigGetRequest represents the retrieval request
        for AssetSnmpAccessConfig.
      properties:
        access_view:
          description: >
            List of AccessView. AccessView represents The SNMP v1/v2 access
            view. This applies only to Switch Engine (EXOS).
          items:
            format: int64
            type: integer
          type: array
        community:
          description: |
            List of Community. Community represents The SNMP community string
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        group_name:
          description: >
            List of GroupName. GroupName represents Specifies the group name for
            data access. This is Fabric Engine(VOSS) specific.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: >
            List of Index. Index represents The index associated with this
            entry. This property only applies to Fabric Engine (VOSS)
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        security_name:
          description: >
            List of SecurityName. SecurityName represents The security name
            associated with this entry. This property only applies to Fabric
            Engine (VOSS)
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        v1_v2_config_id:
          description: |
            List of V1V2ConfigId. V1V2ConfigId represents SNMP V1V2 Config ID
          items:
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents The name of the Virtual Router.
            This property only applies to Fabric Engine (VOSS)
          items:
            type: string
          type: array
      title: AssetSnmpAccessConfigGetRequest
      type: object
    AssetSnmpAccessConfigGetResponse:
      properties:
        AssetSnmpAccessConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpAccessConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpAccessConfigGetResponse
      type: object
    AssetSnmpGlobalConfig:
      description: AssetSnmpGlobalConfig - DB model SNMP Global Config list
        Groups,V1V2Config,V3Config,View can be created while creating
        AssetSnmpGlobalConfig.
      properties:
        asset_device_id:
          description: Device Id.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        groups:
          items:
            $ref: "#/components/schemas/AssetSnmpGroupConfig"
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        sys_contact:
          description: System Contact.
          type: string
        sys_engine_id:
          description: System Engine Id.
          type: string
        sys_location:
          description: System Location.
          type: string
        sys_name:
          description: System Name.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        v1_v2_config:
          $ref: "#/components/schemas/AssetSnmpV1V2Config"
        v3_config:
          $ref: "#/components/schemas/AssetSnmpV3Config"
        view:
          items:
            $ref: "#/components/schemas/AssetSnmpViewConfig"
          nullable: true
          type: array
      required:
        - asset_device_id
      type: object
    AssetSnmpGlobalConfigGetRequest:
      description: AssetSnmpGlobalConfigGetRequest represents the retrieval request
        for AssetSnmpGlobalConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sys_contact:
          description: |
            List of SysContact. SysContact represents System Contact.
          items:
            type: string
          type: array
        sys_engine_id:
          description: |
            List of SysEngineId. SysEngineId represents System Engine Id.
          items:
            type: string
          type: array
        sys_location:
          description: |
            List of SysLocation. SysLocation represents System Location.
          items:
            type: string
          type: array
        sys_name:
          description: |
            List of SysName. SysName represents System Name.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSnmpGlobalConfigGetRequest
      type: object
    AssetSnmpGlobalConfigGetResponse:
      properties:
        AssetSnmpGlobalConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpGlobalConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpGlobalConfigGetResponse
      type: object
    AssetSnmpGroupConfig:
      description: AssetSnmpGroupConfig - DB model SNMP Group Config list
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        group_name:
          description: Specifies the group name for data access. This is Fabric
            Engine(VOSS) specific.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        model:
          description: >-
            The type of SNMP(v1/v2/v3) the group is used for. By default, when a
            group is
            	just created, it is assigned to v3. If the group is associated with a community,
            	there will be created an entry for v1 and v2.
          type: string
        notify_view_name:
          description: >-
            Specifies the notify view name. When the SNMP
            user(V3)/community(V1/V2) is created,
            	it can be added to a group which specifies the data access level.
            	This property only applies to Fabric Engine (VOSS).
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        read_view_name:
          description: >-
            Specifies the read view name. When the SNMP
            user(V3)/community(V1/V2) is created,
            	it can be added to a group which specifies the data access level.
            	This property only applies to Fabric Engine (VOSS).
          type: string
        security_level:
          description: Represents types of Security levels
          format: int64
          type: integer
        security_name:
          description: The security name associated with this entry. This property only
            applies to Fabric Engine (VOSS).
          type: string
        snmp_config_id:
          description: SNMP Config ID
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_name:
          description: The name of the Virtual Router. This property only applies to
            Fabric Engine (VOSS).
          type: string
        write_view_name:
          description: >-
            Specifies the write view name. When the SNMP
            user(V3)/community(V1/V2) is created,
            	it can be added to a group which specifies the data access level.
            	This property only applies to Fabric Engine (VOSS).
          type: string
      required:
        - group_name
        - model
        - security_level
        - snmp_config_id
      type: object
    AssetSnmpGroupConfigGetRequest:
      description: AssetSnmpGroupConfigGetRequest represents the retrieval request for
        AssetSnmpGroupConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        group_name:
          description: >
            List of GroupName. GroupName represents Specifies the group name for
            data access. This is Fabric Engine(VOSS) specific.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        model:
          description: >
            List of Model. Model represents The type of SNMP(v1/v2/v3) the group
            is used for. By default, when a group is
            	just created, it is assigned to v3. If the group is associated with a community,
            	there will be created an entry for v1 and v2.
          items:
            type: string
          type: array
        notify_view_name:
          description: >
            List of NotifyViewName. NotifyViewName represents Specifies the
            notify view name. When the SNMP user(V3)/community(V1/V2) is
            created,
            	it can be added to a group which specifies the data access level.
            	This property only applies to Fabric Engine (VOSS).
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        read_view_name:
          description: >
            List of ReadViewName. ReadViewName represents Specifies the read
            view name. When the SNMP user(V3)/community(V1/V2) is created,
            	it can be added to a group which specifies the data access level.
            	This property only applies to Fabric Engine (VOSS).
          items:
            type: string
          type: array
        security_level:
          description: >
            List of SecurityLevel. SecurityLevel represents Represents types of
            Security levels
          items:
            format: int64
            type: integer
          type: array
        security_name:
          description: >
            List of SecurityName. SecurityName represents The security name
            associated with this entry. This property only applies to Fabric
            Engine (VOSS).
          items:
            type: string
          type: array
        snmp_config_id:
          description: |
            List of SnmpConfigId. SnmpConfigId represents SNMP Config ID
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents The name of the Virtual Router.
            This property only applies to Fabric Engine (VOSS).
          items:
            type: string
          type: array
        write_view_name:
          description: >
            List of WriteViewName. WriteViewName represents Specifies the write
            view name. When the SNMP user(V3)/community(V1/V2) is created,
            	it can be added to a group which specifies the data access level.
            	This property only applies to Fabric Engine (VOSS).
          items:
            type: string
          type: array
      title: AssetSnmpGroupConfigGetRequest
      type: object
    AssetSnmpGroupConfigGetResponse:
      properties:
        AssetSnmpGroupConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpGroupConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpGroupConfigGetResponse
      type: object
    AssetSnmpTrapReceiverConfig:
      description: AssetSnmpTrapReceiverConfig - DB model SNMP trap receiver Config list
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inform:
          description: This property specifies the notify type (applies to Fabric Engine).
          type: boolean
        ip_address:
          description: IP Address
          type: string
        ip_address_type:
          description: IP Address Type
          format: int64
          type: integer
        name:
          description: >-
            The user name associated with this entry. This property represents
            	the security name for Fabric Engine (VOSS) and the community for Switch Engine (EXOS).
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port:
          description: The UDP port number associated with this user.
          format: int64
          type: integer
        trap_receiver_name:
          description: The name of the trap receiver. This property applies to Switch
            Engine (EXOS) only.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        v1_v2_config_id:
          description: V1V2 config Id
          type: string
        version:
          description: The SNMP version. This property applies to Fabric Engine (VOSS).
          type: string
        vr_name:
          description: The name of the Virtual Router. This property only applies to
            Switch Engine (EXOS).
          type: string
      required:
        - name
        - trap_receiver_name
        - v1_v2_config_id
      type: object
    AssetSnmpTrapReceiverConfigGetRequest:
      description: AssetSnmpTrapReceiverConfigGetRequest represents the retrieval
        request for AssetSnmpTrapReceiverConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        inform:
          description: >
            List of Inform. Inform represents This property specifies the notify
            type (applies to Fabric Engine).
          items:
            type: boolean
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents IP Address
          items:
            type: string
          type: array
        ip_address_type:
          description: |
            List of IpAddressType. IpAddressType represents IP Address Type
          items:
            format: int64
            type: integer
          type: array
        name:
          description: >
            List of Name. Name represents The user name associated with this
            entry. This property represents
            	the security name for Fabric Engine (VOSS) and the community for Switch Engine (EXOS).
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port:
          description: >
            List of Port. Port represents The UDP port number associated with
            this user.
          items:
            format: int64
            type: integer
          type: array
        trap_receiver_name:
          description: >
            List of TrapReceiverName. TrapReceiverName represents The name of
            the trap receiver. This property applies to Switch Engine (EXOS)
            only.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        v1_v2_config_id:
          description: |
            List of V1V2ConfigId. V1V2ConfigId represents V1V2 config Id
          items:
            type: string
          type: array
        version:
          description: >
            List of Version. Version represents The SNMP version. This property
            applies to Fabric Engine (VOSS).
          items:
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents The name of the Virtual Router.
            This property only applies to Switch Engine (EXOS).
          items:
            type: string
          type: array
      title: AssetSnmpTrapReceiverConfigGetRequest
      type: object
    AssetSnmpTrapReceiverConfigGetResponse:
      properties:
        AssetSnmpTrapReceiverConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpTrapReceiverConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpTrapReceiverConfigGetResponse
      type: object
    AssetSnmpV1V2Config:
      description: AssetSnmpV1V2Config - DB model SNMP V1-V2 Config list
        AccessList,TrapReceivers can be created while creating
        AssetSnmpV1V2Config.
      nullable: true
      properties:
        access_list:
          items:
            $ref: "#/components/schemas/AssetSnmpAccessConfig"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Enabled state for SNMP
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        snmp_config_id:
          description: SNMP Config ID
          type: string
        trap_receivers:
          items:
            $ref: "#/components/schemas/AssetSnmpTrapReceiverConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - enabled
        - snmp_config_id
      type: object
    AssetSnmpV1V2ConfigGetRequest:
      description: AssetSnmpV1V2ConfigGetRequest represents the retrieval request for
        AssetSnmpV1V2Config.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents Enabled state for SNMP
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        snmp_config_id:
          description: |
            List of SnmpConfigId. SnmpConfigId represents SNMP Config ID
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSnmpV1V2ConfigGetRequest
      type: object
    AssetSnmpV1V2ConfigGetResponse:
      properties:
        AssetSnmpV1V2Config:
          items:
            $ref: "#/components/schemas/AssetSnmpV1V2Config"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpV1V2ConfigGetResponse
      type: object
    AssetSnmpV3AccessConfig:
      description: AssetSnmpV3AccessConfig - DB model SNMP v3 access Config list
      nullable: true
      properties:
        authentication_method:
          description: The SNMP version 3 authentication method.
          type: string
        authentication_password:
          description: SNMP secret (i.e. password).
          type: string
        authentication_password_encrypted:
          description: A value of true indicates the password is encrypted.  This property
            applies to Switch Engine (EXOS) only.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        encryption_method:
          description: The SNMP version 3 encryption method. The values of AES_192,
            AES_256 and 3_DES apply only to Switch Engine (EXOS)
          format: int64
          type: integer
        encryption_password:
          description: Encryption secret.
          type: string
        encryption_password_encrypted:
          description: A value of true indicates the encryption password is encrypted.
          type: boolean
        group_name:
          description: Specifies the group name for data access. This is Fabric
            Engine(VOSS) specific.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        type:
          description: The SNMP version 3 type.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        username:
          description: The user name for this entry
          type: string
        v3_config_id:
          description: SNMP V3 Config ID
          type: string
      required:
        - username
        - v3_config_id
      type: object
    AssetSnmpV3AccessConfigGetRequest:
      description: AssetSnmpV3AccessConfigGetRequest represents the retrieval request
        for AssetSnmpV3AccessConfig.
      properties:
        authentication_method:
          description: >
            List of AuthenticationMethod. AuthenticationMethod represents The
            SNMP version 3 authentication method.
          items:
            type: string
          type: array
        authentication_password:
          description: >
            List of AuthenticationPassword. AuthenticationPassword represents
            SNMP secret (i.e. password).
          items:
            type: string
          type: array
        authentication_password_encrypted:
          description: >
            List of AuthenticationPasswordEncrypted.
            AuthenticationPasswordEncrypted represents A value of true indicates
            the password is encrypted.  This property applies to Switch Engine
            (EXOS) only.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        encryption_method:
          description: >
            List of EncryptionMethod. EncryptionMethod represents The SNMP
            version 3 encryption method. The values of AES_192, AES_256 and
            3_DES apply only to Switch Engine (EXOS)
          items:
            format: int64
            type: integer
          type: array
        encryption_password:
          description: >
            List of EncryptionPassword. EncryptionPassword represents Encryption
            secret.
          items:
            type: string
          type: array
        encryption_password_encrypted:
          description: >
            List of EncryptionPasswordEncrypted. EncryptionPasswordEncrypted
            represents A value of true indicates the encryption password is
            encrypted.
          items:
            type: boolean
          type: array
        group_name:
          description: >
            List of GroupName. GroupName represents Specifies the group name for
            data access. This is Fabric Engine(VOSS) specific.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        type:
          description: |
            List of Type. Type represents The SNMP version 3 type.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        username:
          description: |
            List of Username. Username represents The user name for this entry
          items:
            type: string
          type: array
        v3_config_id:
          description: |
            List of V3ConfigId. V3ConfigId represents SNMP V3 Config ID
          items:
            type: string
          type: array
      title: AssetSnmpV3AccessConfigGetRequest
      type: object
    AssetSnmpV3AccessConfigGetResponse:
      properties:
        AssetSnmpV3AccessConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpV3AccessConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpV3AccessConfigGetResponse
      type: object
    AssetSnmpV3Config:
      description: AssetSnmpV3Config - DB model SNMP v3 Config list
        AccessList,TrapReceivers can be created while creating
        AssetSnmpV3Config.
      nullable: true
      properties:
        access_list:
          items:
            $ref: "#/components/schemas/AssetSnmpV3AccessConfig"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Enabled state for SNMP
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        snmp_config_id:
          description: SNMP Config ID
          type: string
        trap_receivers:
          items:
            $ref: "#/components/schemas/AssetSnmpV3TrapReceiverConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - enabled
        - snmp_config_id
      type: object
    AssetSnmpV3ConfigGetRequest:
      description: AssetSnmpV3ConfigGetRequest represents the retrieval request for
        AssetSnmpV3Config.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents Enabled state for SNMP
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        snmp_config_id:
          description: |
            List of SnmpConfigId. SnmpConfigId represents SNMP Config ID
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSnmpV3ConfigGetRequest
      type: object
    AssetSnmpV3ConfigGetResponse:
      properties:
        AssetSnmpV3Config:
          items:
            $ref: "#/components/schemas/AssetSnmpV3Config"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpV3ConfigGetResponse
      type: object
    AssetSnmpV3TrapReceiverConfig:
      description: AssetSnmpV3TrapReceiverConfig - DB model SNMP V3 trap receiver
        Config list
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inform:
          description: This property specifies the notify type (applies to Fabric Engine).
          type: boolean
        ip_address:
          description: IP Address
          type: string
        ip_address_type:
          description: IP Address Type
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port:
          description: The UDP port number associated with this user.
          format: int64
          type: integer
        security_level:
          description: The SNMP security level
          format: int64
          type: integer
        trap_receiver_name:
          description: The name of the trap receiver. This property applies to Switch
            Engine (EXOS) only.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        user_name:
          description: The user name associated with this entry.
          type: string
        v3_config_id:
          description: SNMP V3 Config ID
          type: string
        vr_name:
          description: The name of the Virtual Router. This property only applies to
            Switch Engine (EXOS).
          type: string
      required:
        - trap_receiver_name
        - user_name
        - v3_config_id
      type: object
    AssetSnmpV3TrapReceiverConfigGetRequest:
      description: AssetSnmpV3TrapReceiverConfigGetRequest represents the retrieval
        request for AssetSnmpV3TrapReceiverConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        inform:
          description: >
            List of Inform. Inform represents This property specifies the notify
            type (applies to Fabric Engine).
          items:
            type: boolean
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents IP Address
          items:
            type: string
          type: array
        ip_address_type:
          description: |
            List of IpAddressType. IpAddressType represents IP Address Type
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port:
          description: >
            List of Port. Port represents The UDP port number associated with
            this user.
          items:
            format: int64
            type: integer
          type: array
        security_level:
          description: >
            List of SecurityLevel. SecurityLevel represents The SNMP security
            level
          items:
            format: int64
            type: integer
          type: array
        trap_receiver_name:
          description: >
            List of TrapReceiverName. TrapReceiverName represents The name of
            the trap receiver. This property applies to Switch Engine (EXOS)
            only.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        user_name:
          description: >
            List of UserName. UserName represents The user name associated with
            this entry.
          items:
            type: string
          type: array
        v3_config_id:
          description: |
            List of V3ConfigId. V3ConfigId represents SNMP V3 Config ID
          items:
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents The name of the Virtual Router.
            This property only applies to Switch Engine (EXOS).
          items:
            type: string
          type: array
      title: AssetSnmpV3TrapReceiverConfigGetRequest
      type: object
    AssetSnmpV3TrapReceiverConfigGetResponse:
      properties:
        AssetSnmpV3TrapReceiverConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpV3TrapReceiverConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpV3TrapReceiverConfigGetResponse
      type: object
    AssetSnmpViewConfig:
      description: AssetSnmpViewConfig - DB model SNMP Group Config list
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        oids:
          description: Defines the list of MIB objects accessible by the SNMP entity.
          items:
            type: string
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        snmp_config_id:
          description: SNMP Config ID
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        view_name:
          description: >-
            Specifies the view name. When the SNMP user(V3)/community(V1/V2) is
            created, it can be added to a group
            	which specifies the data access level. This property only applies to Fabric Engine (VOSS).
          type: string
      required:
        - snmp_config_id
      type: object
    AssetSnmpViewConfigGetRequest:
      description: AssetSnmpViewConfigGetRequest represents the retrieval request for
        AssetSnmpViewConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        oids:
          description: >
            List of Oids. Oids represents Defines the list of MIB objects
            accessible by the SNMP entity.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        snmp_config_id:
          description: |
            List of SnmpConfigId. SnmpConfigId represents SNMP Config ID
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        view_name:
          description: >
            List of ViewName. ViewName represents Specifies the view name. When
            the SNMP user(V3)/community(V1/V2) is created, it can be added to a
            group
            	which specifies the data access level. This property only applies to Fabric Engine (VOSS).
          items:
            type: string
          type: array
      title: AssetSnmpViewConfigGetRequest
      type: object
    AssetSnmpViewConfigGetResponse:
      properties:
        AssetSnmpViewConfig:
          items:
            $ref: "#/components/schemas/AssetSnmpViewConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSnmpViewConfigGetResponse
      type: object
    AssetSntpConfig:
      description: AssetSntpConfig - DB model for SNTP configuration Servers can be
        created while creating AssetSntpConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        broadcast_vr_name:
          description: Virtual Router (VR) instance on which SNTP is enabled
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          default: false
          description: SNTP configuration enabled state
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        servers:
          items:
            $ref: "#/components/schemas/AssetSntpServerConfig"
          nullable: true
          type: array
        update_interval:
          description: Update interval time
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - broadcast_vr_name
        - enabled
        - update_interval
      type: object
    AssetSntpConfigGetRequest:
      description: AssetSntpConfigGetRequest represents the retrieval request for
        AssetSntpConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        broadcast_vr_name:
          description: >
            List of BroadcastVrName. BroadcastVrName represents Virtual Router
            (VR) instance on which SNTP is enabled
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SNTP configuration enabled state
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        update_interval:
          description: >
            List of UpdateInterval. UpdateInterval represents Update interval
            time
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSntpConfigGetRequest
      type: object
    AssetSntpConfigGetResponse:
      properties:
        AssetSntpConfig:
          items:
            $ref: "#/components/schemas/AssetSntpConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSntpConfigGetResponse
      type: object
    AssetSntpServerConfig:
      description: AssetSntpServerConfig - DB model for SNTP server configuration
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        host_name_ip:
          description: Host Name or IP address (v4 or v6)
          type: string
        host_type:
          description: Type of the Host Name or IP address
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        index:
          description: SNTP config index
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        sntp_config_id:
          description: Id of the SNTP configuration
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_name:
          description: Virtual Router (VR) instance on which SNTP is enabled
          type: string
      required:
        - host_name_ip
        - host_type
        - sntp_config_id
        - vr_name
      type: object
    AssetSntpServerConfigGetRequest:
      description: AssetSntpServerConfigGetRequest represents the retrieval request
        for AssetSntpServerConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        host_name_ip:
          description: >
            List of HostNameIp. HostNameIp represents Host Name or IP address
            (v4 or v6)
          items:
            type: string
          type: array
        host_type:
          description: >
            List of HostType. HostType represents Type of the Host Name or IP
            address
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        index:
          description: |
            List of Index. Index represents SNTP config index
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sntp_config_id:
          description: >
            List of SntpConfigId. SntpConfigId represents Id of the SNTP
            configuration
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents Virtual Router (VR) instance on
            which SNTP is enabled
          items:
            type: string
          type: array
      title: AssetSntpServerConfigGetRequest
      type: object
    AssetSntpServerConfigGetResponse:
      properties:
        AssetSntpServerConfig:
          items:
            $ref: "#/components/schemas/AssetSntpServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSntpServerConfigGetResponse
      type: object
    AssetSpbmCfmGlobal:
      description: >-
        AssetSpbmCfmGlobal - The object that represents CFM global settings for
        SPBM VLANs

        on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Global CFM status for SPBM VLANs.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        level:
          description: Global CFM maintenance level for SPBM VLANs.
          format: int64
          type: integer
        mep_id:
          description: Global CFM maintenance end point ID value for SPBM VLANs.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetSpbmCfmGlobalGetRequest:
      description: AssetSpbmCfmGlobalGetRequest represents the retrieval request for
        AssetSpbmCfmGlobal.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents Global CFM status for SPBM VLANs.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        level:
          description: >
            List of Level. Level represents Global CFM maintenance level for
            SPBM VLANs.
          items:
            format: int64
            type: integer
          type: array
        mep_id:
          description: >
            List of MepId. MepId represents Global CFM maintenance end point ID
            value for SPBM VLANs.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSpbmCfmGlobalGetRequest
      type: object
    AssetSpbmCfmGlobalGetResponse:
      properties:
        AssetSpbmCfmGlobal:
          items:
            $ref: "#/components/schemas/AssetSpbmCfmGlobal"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSpbmCfmGlobalGetResponse
      type: object
    AssetSpbmGlobalSettingsConfig:
      description: >-
        AssetSpbmGlobalSettingsConfig - The object that represents Configuration
        of SPBM global settings

        on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: SPBM global status.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        nickname_server_enabled:
          description: Nick-name Server enabled state. Disabled via duplicate detection.
          type: boolean
        nickname_server_prefix:
          description: >-
            The prefix is the value of the first octet of the Nick-name. There
            are 256 possible
            	Nick-name prefixes in total: 0.00.00 - F.F0.00 corresponding to 256 ranges: 0.0x.xx -
            	F.Fx.xx, with 4k Nick-names each. This configuration is only used when the SPBM
            	Nick-name Server feature is enabled.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetSpbmGlobalSettingsConfigGetRequest:
      description: AssetSpbmGlobalSettingsConfigGetRequest represents the retrieval
        request for AssetSpbmGlobalSettingsConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SPBM global status.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        nickname_server_enabled:
          description: >
            List of NicknameServerEnabled. NicknameServerEnabled represents
            Nick-name Server enabled state. Disabled via duplicate detection.
          items:
            type: boolean
          type: array
        nickname_server_prefix:
          description: >
            List of NicknameServerPrefix. NicknameServerPrefix represents The
            prefix is the value of the first octet of the Nick-name. There are
            256 possible
            	Nick-name prefixes in total: 0.00.00 - F.F0.00 corresponding to 256 ranges: 0.0x.xx -
            	F.Fx.xx, with 4k Nick-names each. This configuration is only used when the SPBM
            	Nick-name Server feature is enabled.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSpbmGlobalSettingsConfigGetRequest
      type: object
    AssetSpbmGlobalSettingsConfigGetResponse:
      properties:
        AssetSpbmGlobalSettingsConfig:
          items:
            $ref: "#/components/schemas/AssetSpbmGlobalSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSpbmGlobalSettingsConfigGetResponse
      type: object
    AssetSpbmInstance:
      description: AssetSpbmInstance - The object that represents settings for SPBM
        instances configuration.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_device_id:
          description: Device Id
          type: string
        asset_isis_global_config_id:
          description: ISIS Global Config Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        instance_id:
          description: SPBM instance ID.
          type: integer
        ip_shortcut_enabled:
          description: A value of True indicates Ipv4 shortcut is enabled.
          type: boolean
        ipv6_shortcut_enabled:
          description: A value of True indicates Ipv6 shortcut is enabled.
          type: boolean
        lsdb_trap_enabled:
          description: A value of True indicates trap is enabled on this SPBM instance.
          type: boolean
        multicast_enabled:
          description: A value of True indicates multicast is enabled.
          type: boolean
        multicast_fwd_cache_timeout:
          description: Multicast forward cache in seconds.
          type: integer
        node_nick_name:
          description: SPBM node`s nickname. It is used to calculate ISID multicast mac
            address.
          type: string
        origin:
          description: SPBM instance origin.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        peer_sys_id:
          description: The ID of peer.
          type: string
        primary_vlan:
          description: >-
            Primary VLAN id for this SPBM instance. This attribute makes sense
            only
            	if the vlans attribute contains 2 vlans. Otherwise it would be 0.
          format: int64
          minimum: 0
          type: integer
        smlt_bmac:
          description: SMLT Bmac
          type: string
        stp_multi_homing:
          description: A value of True indicates Fabric Multi-Homing is enabled.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlans:
          description: |-
            ISIS SPBM instance data vlans.
            	There can be only 2 vlans set.
          items:
            type: string
          nullable: true
          type: array
      required:
        - asset_device_id
        - asset_isis_global_config_id
        - instance_id
      type: object
    AssetSpbmInstanceGetRequest:
      description: AssetSpbmInstanceGetRequest represents the retrieval request for
        AssetSpbmInstance.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        asset_isis_global_config_id:
          description: >
            List of AssetIsisGlobalConfigId. AssetIsisGlobalConfigId represents
            ISIS Global Config Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instance_id:
          description: |
            List of InstanceId. InstanceId represents SPBM instance ID.
          items:
            type: integer
          type: array
        ip_shortcut_enabled:
          description: >
            List of IpShortcutEnabled. IpShortcutEnabled represents A value of
            True indicates Ipv4 shortcut is enabled.
          items:
            type: boolean
          type: array
        ipv6_shortcut_enabled:
          description: >
            List of Ipv6ShortcutEnabled. Ipv6ShortcutEnabled represents A value
            of True indicates Ipv6 shortcut is enabled.
          items:
            type: boolean
          type: array
        lsdb_trap_enabled:
          description: >
            List of LsdbTrapEnabled. LsdbTrapEnabled represents A value of True
            indicates trap is enabled on this SPBM instance.
          items:
            type: boolean
          type: array
        multicast_enabled:
          description: >
            List of MulticastEnabled. MulticastEnabled represents A value of
            True indicates multicast is enabled.
          items:
            type: boolean
          type: array
        multicast_fwd_cache_timeout:
          description: >
            List of MulticastFwdCacheTimeout. MulticastFwdCacheTimeout
            represents Multicast forward cache in seconds.
          items:
            type: integer
          type: array
        node_nick_name:
          description: >
            List of NodeNickName. NodeNickName represents SPBM node`s nickname.
            It is used to calculate ISID multicast mac address.
          items:
            type: string
          type: array
        origin:
          description: |
            List of Origin. Origin represents SPBM instance origin.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        peer_sys_id:
          description: |
            List of PeerSysId. PeerSysId represents The ID of peer.
          items:
            type: string
          type: array
        primary_vlan:
          description: >
            List of PrimaryVlan. PrimaryVlan represents Primary VLAN id for this
            SPBM instance. This attribute makes sense only
            	if the vlans attribute contains 2 vlans. Otherwise it would be 0.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        smlt_bmac:
          description: |
            List of SmltBmac. SmltBmac represents SMLT Bmac
          items:
            type: string
          type: array
        stp_multi_homing:
          description: >
            List of StpMultiHoming. StpMultiHoming represents A value of True
            indicates Fabric Multi-Homing is enabled.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlans:
          description: |
            List of Vlans. Vlans represents ISIS SPBM instance data vlans.
            	There can be only 2 vlans set.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
      title: AssetSpbmInstanceGetRequest
      type: object
    AssetSpbmInstanceGetResponse:
      properties:
        AssetSpbmInstance:
          items:
            $ref: "#/components/schemas/AssetSpbmInstance"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSpbmInstanceGetResponse
      type: object
    AssetSsidConfig:
      description: AssetSsidConfig - DB Model for settings for asset SSID configuration.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: SSID Enabled
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        if_names:
          description: Interface Names
          type: string
        name:
          description: SSID Name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - name
      type: object
    AssetSsidConfigGetRequest:
      description: AssetSsidConfigGetRequest represents the retrieval request for
        AssetSsidConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SSID Enabled
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        if_names:
          description: |
            List of IfNames. IfNames represents Interface Names
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents SSID Name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSsidConfigGetRequest
      type: object
    AssetSsidConfigGetResponse:
      properties:
        AssetSsidConfig:
          items:
            $ref: "#/components/schemas/AssetSsidConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSsidConfigGetResponse
      type: object
    AssetSsidState:
      description: AssetSsidState - DB Model for settings for asset SSID state.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        encryption:
          description: SSID Encryption
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        if_names:
          description: Interface Names
          type: string
        name:
          description: SSID Name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - encryption
        - name
      type: object
    AssetSsidStateGetRequest:
      description: AssetSsidStateGetRequest represents the retrieval request for
        AssetSsidState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        encryption:
          description: |
            List of Encryption. Encryption represents SSID Encryption
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        if_names:
          description: |
            List of IfNames. IfNames represents Interface Names
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents SSID Name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSsidStateGetRequest
      type: object
    AssetSsidStateGetResponse:
      properties:
        AssetSsidState:
          items:
            $ref: "#/components/schemas/AssetSsidState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSsidStateGetResponse
      type: object
    AssetStormControlPortConfig:
      description: >-
        AssetStormControlPortConfig - The object that represents settings for
        storm control settings for every port

        on the device, for unicast, broadcast and multicast traffic.
      properties:
        asset_interface_id:
          description: Interface Id.
          type: string
        broadcast_actions:
          items:
            format: int64
            type: integer
          type: array
        broadcast_enabled:
          default: false
          description: Storm control enabled state on the current port for broadcast
            packets.
          type: boolean
        broadcast_rate:
          description: Allowed traffic rate in packets per seconds for broadcast packets.
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        multicast_actions:
          items:
            format: int64
            type: integer
          type: array
        multicast_enabled:
          default: false
          description: Storm control enabled state on the current port for multicast
            packets.
          type: boolean
        multicast_rate:
          description: Allowed traffic rate in packets per seconds for multicast packets.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        unicast_actions:
          items:
            format: int64
            type: integer
          type: array
        unicast_enabled:
          default: false
          description: Storm control enabled state on the current port for unicast packets.
          type: boolean
        unicast_rate:
          description: Allowed traffic rate in packets per seconds for unicast packets.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_interface_id
        - broadcast_enabled
        - device_id
        - interface_name
        - multicast_enabled
        - unicast_enabled
      type: object
    AssetStormControlPortConfigGetRequest:
      description: AssetStormControlPortConfigGetRequest represents the retrieval
        request for AssetStormControlPortConfig.
      properties:
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id.
          items:
            type: string
          type: array
        broadcast_actions:
          description: >
            List of BroadcastActions. BroadcastActions represents Storm control
            actions taken in addition to dropping packets above the specified
            rate.
            	On Fabric Engine (VOSS), actions are not supported for broadcast packets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        broadcast_enabled:
          description: >
            List of BroadcastEnabled. BroadcastEnabled represents Storm control
            enabled state on the current port for broadcast packets.
          items:
            type: boolean
          type: array
        broadcast_rate:
          description: >
            List of BroadcastRate. BroadcastRate represents Allowed traffic rate
            in packets per seconds for broadcast packets.
          items:
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        multicast_actions:
          description: >
            List of MulticastActions. MulticastActions represents Storm control
            actions taken in addition to dropping packets above the specified
            rate.
            	On Fabric Engine (VOSS), actions are not supported for multicast packets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        multicast_enabled:
          description: >
            List of MulticastEnabled. MulticastEnabled represents Storm control
            enabled state on the current port for multicast packets.
          items:
            type: boolean
          type: array
        multicast_rate:
          description: >
            List of MulticastRate. MulticastRate represents Allowed traffic rate
            in packets per seconds for multicast packets.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        unicast_actions:
          description: >
            List of UnicastActions. UnicastActions represents Storm control
            actions taken in addition to dropping packets above the specified
            rate.
            	On Fabric Engine (VOSS), actions are not supported for unicast packets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        unicast_enabled:
          description: >
            List of UnicastEnabled. UnicastEnabled represents Storm control
            enabled state on the current port for unicast packets.
          items:
            type: boolean
          type: array
        unicast_rate:
          description: >
            List of UnicastRate. UnicastRate represents Allowed traffic rate in
            packets per seconds for unicast packets.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetStormControlPortConfigGetRequest
      type: object
    AssetStormControlPortConfigGetResponse:
      properties:
        AssetStormControlPortConfig:
          items:
            $ref: "#/components/schemas/AssetStormControlPortConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStormControlPortConfigGetResponse
      type: object
    AssetStpDomainState:
      description: AssetStpDomainState - The object that represents stp state in a STP
        domain. AssetStpPortStateEntry can be created while creating
        AssetStpDomainState.
      properties:
        asset_device_id:
          description: Asset Device Id.
          type: string
        asset_stp_port_state_entry:
          items:
            $ref: "#/components/schemas/AssetStpPortStateEntry"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        designated_root:
          description: Designated root
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        stp_name:
          description: STP name
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - stp_name
      type: object
    AssetStpDomainStateGetRequest:
      description: AssetStpDomainStateGetRequest represents the retrieval request for
        AssetStpDomainState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Asset Device Id.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        designated_root:
          description: |
            List of DesignatedRoot. DesignatedRoot represents Designated root
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        stp_name:
          description: |
            List of StpName. StpName represents STP name
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetStpDomainStateGetRequest
      type: object
    AssetStpDomainStateGetResponse:
      properties:
        AssetStpDomainState:
          items:
            $ref: "#/components/schemas/AssetStpDomainState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStpDomainStateGetResponse
      type: object
    AssetStpGlobalConfig:
      description: AssetStpGlobalConfig - The object that represents STP global
        settings configuration. StpProtocolSettingsEntries can be created while
        creating AssetStpGlobalConfig.
      properties:
        asset_device_id:
          description: Asset Device Id.
          type: string
        boot_mode:
          description: >-
            Spanning-tree admin(configurable) boot mode. If this is sent in a
            request,
            	all the other attributes will be ignored. After changing this attribute, save
            	config and reboot are required. This is applicable only to Fabric Engine(VOSS).
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mstp_format:
          description: The format of the MSTP
          type: integer
        mstp_region:
          description: The name of the MSTP region that this device belongs to.
          type: string
        mstp_revision:
          default: 3
          description: The revision number of the MSTP region.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        protocol:
          description: STP Protocol.
          format: int64
          type: integer
        stp_protocol_settings_entries:
          items:
            $ref: "#/components/schemas/AssetStpProtocolSettingsConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetStpGlobalConfigGetRequest:
      description: AssetStpGlobalConfigGetRequest represents the retrieval request for
        AssetStpGlobalConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Asset Device Id.
          items:
            type: string
          type: array
        boot_mode:
          description: >
            List of BootMode. BootMode represents Spanning-tree
            admin(configurable) boot mode. If this is sent in a request,
            	all the other attributes will be ignored. After changing this attribute, save
            	config and reboot are required. This is applicable only to Fabric Engine(VOSS).
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mstp_format:
          description: |
            List of MstpFormat. MstpFormat represents The format of the MSTP
          items:
            type: integer
          type: array
        mstp_region:
          description: >
            List of MstpRegion. MstpRegion represents The name of the MSTP
            region that this device belongs to.
          items:
            type: string
          type: array
        mstp_revision:
          description: >
            List of MstpRevision. MstpRevision represents The revision number of
            the MSTP region.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        protocol:
          description: |
            List of Protocol. Protocol represents STP Protocol.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetStpGlobalConfigGetRequest
      type: object
    AssetStpGlobalConfigGetResponse:
      properties:
        AssetStpGlobalConfig:
          items:
            $ref: "#/components/schemas/AssetStpGlobalConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStpGlobalConfigGetResponse
      type: object
    AssetStpInterfaceSettingsConfig:
      description: AssetStpInterfaceSettingsConfig - The object that represents
        interfaces in a STP domain.
      nullable: true
      properties:
        auto_edge_enabled:
          description: The setting indicating if auto edge detection enabled
          type: boolean
        bpdu_origin:
          description: BPDU Origin
          format: int64
          type: integer
        bpdu_protection:
          description: Bridge Protocol Data Unit (BPDU) Protection setting.
          format: int64
          type: integer
        bpdu_restrict_timeout:
          description: BPDU restrict timeout.
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enable_bpdu_restrict:
          description: |-
            This is a loop prevention mechanism configured with bpduProtection.
            	When set to True, this port is disabled as soon as an STP BPDU
            	is received. This setting applies to Switch Engine (EXOS) only.
            	For Fabric Engine (VOSS), this setting is not configurable but
            	is always on when bpduProtection is turned on.
          type: boolean
        enable_edge_port:
          description: |-
            The admin enabled setting indicates if edge detection
            	is enabled on this port
          type: boolean
        enabled:
          description: |-
            Current STP state of the port for the associated STP domain/instance
            	which can be enabled or disabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: STP interface name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        path_cost:
          description: |-
            The contribution of this port to the path cost of paths towards the
            	spanning tree root which include this port. A value of 0 indicates
            	auto, specifies to configure appropriate default value.
          format: int64
          type: integer
        priority:
          description: |-
            The value of the priority field which is contained in the first
            	(in network byte order) octet of the (2 octet long) Port ID.
            	The value must be in steps of 16.
          type: integer
        protocol_settings_id:
          description: STP protocol settings config id.
          type: string
        stp_interface_mode:
          description: Mode of stp interface on port
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - bpdu_protection
        - bpdu_restrict_timeout
        - enable_edge_port
        - enabled
        - interface_name
        - path_cost
        - priority
        - protocol_settings_id
      type: object
    AssetStpInterfaceSettingsConfigGetRequest:
      description: AssetStpInterfaceSettingsConfigGetRequest represents the retrieval
        request for AssetStpInterfaceSettingsConfig.
      properties:
        auto_edge_enabled:
          description: >
            List of AutoEdgeEnabled. AutoEdgeEnabled represents The setting
            indicating if auto edge detection enabled
          items:
            type: boolean
          type: array
        bpdu_origin:
          description: |
            List of BpduOrigin. BpduOrigin represents BPDU Origin
          items:
            format: int64
            type: integer
          type: array
        bpdu_protection:
          description: >
            List of BpduProtection. BpduProtection represents Bridge Protocol
            Data Unit (BPDU) Protection setting.
          items:
            format: int64
            type: integer
          type: array
        bpdu_restrict_timeout:
          description: >
            List of BpduRestrictTimeout. BpduRestrictTimeout represents BPDU
            restrict timeout.
          items:
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enable_bpdu_restrict:
          description: >
            List of EnableBpduRestrict. EnableBpduRestrict represents This is a
            loop prevention mechanism configured with bpduProtection.
            	When set to True, this port is disabled as soon as an STP BPDU
            	is received. This setting applies to Switch Engine (EXOS) only.
            	For Fabric Engine (VOSS), this setting is not configurable but
            	is always on when bpduProtection is turned on.
          items:
            type: boolean
          type: array
        enable_edge_port:
          description: >
            List of EnableEdgePort. EnableEdgePort represents The admin enabled
            setting indicates if edge detection
            	is enabled on this port
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents Current STP state of the port
            for the associated STP domain/instance
            	which can be enabled or disabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents STP interface name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        path_cost:
          description: >
            List of PathCost. PathCost represents The contribution of this port
            to the path cost of paths towards the
            	spanning tree root which include this port. A value of 0 indicates
            	auto, specifies to configure appropriate default value.
          items:
            format: int64
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents The value of the priority
            field which is contained in the first
            	(in network byte order) octet of the (2 octet long) Port ID.
            	The value must be in steps of 16.
          items:
            type: integer
          type: array
        protocol_settings_id:
          description: >
            List of ProtocolSettingsId. ProtocolSettingsId represents STP
            protocol settings config id.
          items:
            type: string
          type: array
        stp_interface_mode:
          description: >
            List of StpInterfaceMode. StpInterfaceMode represents Mode of stp
            interface on port
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetStpInterfaceSettingsConfigGetRequest
      type: object
    AssetStpInterfaceSettingsConfigGetResponse:
      properties:
        AssetStpInterfaceSettingsConfig:
          items:
            $ref: "#/components/schemas/AssetStpInterfaceSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStpInterfaceSettingsConfigGetResponse
      type: object
    AssetStpPortStateEntry:
      description: AssetStpPortStateEntry - The object that represents port state in a
        STP domain.
      nullable: true
      properties:
        asset_stp_domain_state_id:
          description: STP state detail id
          type: string
        bpdu_timer_count:
          description: BPDU timer count setting
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        designated_bridge:
          description: Designated bridge
          type: string
        designated_port:
          description: Designated port
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        path_cost:
          description: Path cost for the STP protocol
          type: integer
        port_name:
          description: Port Name
          type: string
        port_state:
          description: Port state with respect to stp
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_stp_domain_state_id
        - bpdu_timer_count
        - path_cost
        - port_name
        - port_state
      type: object
    AssetStpPortStateEntryGetRequest:
      description: AssetStpPortStateEntryGetRequest represents the retrieval request
        for AssetStpPortStateEntry.
      properties:
        asset_stp_domain_state_id:
          description: >
            List of AssetStpDomainStateId. AssetStpDomainStateId represents STP
            state detail id
          items:
            type: string
          type: array
        bpdu_timer_count:
          description: >
            List of BpduTimerCount. BpduTimerCount represents BPDU timer count
            setting
          items:
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        designated_bridge:
          description: >
            List of DesignatedBridge. DesignatedBridge represents Designated
            bridge
          items:
            type: string
          type: array
        designated_port:
          description: |
            List of DesignatedPort. DesignatedPort represents Designated port
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        path_cost:
          description: |
            List of PathCost. PathCost represents Path cost for the STP protocol
          items:
            type: integer
          type: array
        port_name:
          description: |
            List of PortName. PortName represents Port Name
          items:
            type: string
          type: array
        port_state:
          description: >
            List of PortState. PortState represents Port state with respect to
            stp
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetStpPortStateEntryGetRequest
      type: object
    AssetStpPortStateEntryGetResponse:
      properties:
        AssetStpPortStateEntry:
          items:
            $ref: "#/components/schemas/AssetStpPortStateEntry"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStpPortStateEntryGetResponse
      type: object
    AssetStpProtocolSettingsConfig:
      description: AssetStpProtocolSettingsConfig - The object that represents
        settings for protocol settings configuration in STP domain.
        InterfaceSettings,VlanSettings can be created while creating
        AssetStpProtocolSettingsConfig.
      properties:
        auto_bind_vlans:
          description: |-
            The list of VLANs participating in auto-bind feature. Ports added to
            	an auto-bind VLAN are automatically added to the STP domain.
            	This is Switch Engine (EXOS) specific.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: |-
            The admin enabled setting that indicates if STP is enabled
            	in this STP domain.
          type: boolean
        forwarding_delay:
          description: |-
            The time in seconds that the ports in this STP domain spend in the
            	listening and learning states when the switch is the root bridge.
            	For Switch Engine, this setting is not configurable on
            	non-CIST MSTP domains.
          type: integer
        hold_count:
          description: Maximum hold count for BPDU
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        instance_name:
          description: |-
            STP instance name.
            	For Fabric Engine (VOSS) STP instance range is 0-63.
            	For Fabric Engine (VOSS) - in MSTP mode, stp instance
            	name and MSTI id should match.
          type: string
        interface_settings:
          items:
            $ref: "#/components/schemas/AssetStpInterfaceSettingsConfig"
          nullable: true
          type: array
        max_age:
          description: |-
            The maximum age of a BPDU in seconds.
            	For Switch Engine, this setting is not configurable on
            	non-CIST MSTP domains.
          type: integer
        max_hops:
          description: Maximum hops
          type: integer
        mst_instance_id:
          description: MST instance Id.
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        priority:
          description: |-
            The bridge priority of the STP domain. For MSTP, the value
            	must be in steps of 4096. The maximum value for MSTP is 61440.
            	For RSTP, the value can be in steps of 1 for Switch Engine,
            	but the value must be in steps of 4096 for Fabric Engine.
          type: integer
        protocol_type:
          description: STP Protocol types.
          format: int64
          type: integer
        stp_global_config_id:
          description: STP global config id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_settings:
          items:
            $ref: "#/components/schemas/AssetStpVlanSettingsConfig"
          nullable: true
          type: array
      required:
        - enabled
        - instance_name
        - protocol_type
        - stp_global_config_id
      type: object
    AssetStpProtocolSettingsConfigGetRequest:
      description: AssetStpProtocolSettingsConfigGetRequest represents the retrieval
        request for AssetStpProtocolSettingsConfig.
      properties:
        auto_bind_vlans:
          description: >
            List of AutoBindVlans. AutoBindVlans represents The list of VLANs
            participating in auto-bind feature. Ports added to
            	an auto-bind VLAN are automatically added to the STP domain.
            	This is Switch Engine (EXOS) specific.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents The admin enabled setting that
            indicates if STP is enabled
            	in this STP domain.
          items:
            type: boolean
          type: array
        forwarding_delay:
          description: >
            List of ForwardingDelay. ForwardingDelay represents The time in
            seconds that the ports in this STP domain spend in the
            	listening and learning states when the switch is the root bridge.
            	For Switch Engine, this setting is not configurable on
            	non-CIST MSTP domains.
          items:
            type: integer
          type: array
        hold_count:
          description: |
            List of HoldCount. HoldCount represents Maximum hold count for BPDU
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instance_name:
          description: |
            List of InstanceName. InstanceName represents STP instance name.
            	For Fabric Engine (VOSS) STP instance range is 0-63.
            	For Fabric Engine (VOSS) - in MSTP mode, stp instance
            	name and MSTI id should match.
          items:
            type: string
          type: array
        max_age:
          description: >
            List of MaxAge. MaxAge represents The maximum age of a BPDU in
            seconds.
            	For Switch Engine, this setting is not configurable on
            	non-CIST MSTP domains.
          items:
            type: integer
          type: array
        max_hops:
          description: |
            List of MaxHops. MaxHops represents Maximum hops
          items:
            type: integer
          type: array
        mst_instance_id:
          description: |
            List of MstInstanceId. MstInstanceId represents MST instance Id.
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents The bridge priority of the STP
            domain. For MSTP, the value
            	must be in steps of 4096. The maximum value for MSTP is 61440.
            	For RSTP, the value can be in steps of 1 for Switch Engine,
            	but the value must be in steps of 4096 for Fabric Engine.
          items:
            type: integer
          type: array
        protocol_type:
          description: |
            List of ProtocolType. ProtocolType represents STP Protocol types.
          items:
            format: int64
            type: integer
          type: array
        stp_global_config_id:
          description: >
            List of StpGlobalConfigId. StpGlobalConfigId represents STP global
            config id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetStpProtocolSettingsConfigGetRequest
      type: object
    AssetStpProtocolSettingsConfigGetResponse:
      properties:
        AssetStpProtocolSettingsConfig:
          items:
            $ref: "#/components/schemas/AssetStpProtocolSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStpProtocolSettingsConfigGetResponse
      type: object
    AssetStpVlanSettingsConfig:
      description: AssetStpVlanSettingsConfig - The object that represents ports in a
        specified VLAN part of a STP domain.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dynamic_ports:
          description: Object representing a list of dynamic ports
          items:
            type: string
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_list:
          description: An object representing a list of ports.
          items:
            type: string
          nullable: true
          type: array
        protocol_settings_id:
          description: STP protocol settings config id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config_id:
          description: VLAN Config Id
          type: string
        vlan_id:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - protocol_settings_id
        - vlan_id
      type: object
    AssetStpVlanSettingsConfigGetRequest:
      description: AssetStpVlanSettingsConfigGetRequest represents the retrieval
        request for AssetStpVlanSettingsConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dynamic_ports:
          description: >
            List of DynamicPorts. DynamicPorts represents Object representing a
            list of dynamic ports
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_list:
          description: >
            List of PortList. PortList represents An object representing a list
            of ports.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        protocol_settings_id:
          description: >
            List of ProtocolSettingsId. ProtocolSettingsId represents STP
            protocol settings config id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_config_id:
          description: |
            List of VlanConfigId. VlanConfigId represents VLAN Config Id
          items:
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents This object represents a VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetStpVlanSettingsConfigGetRequest
      type: object
    AssetStpVlanSettingsConfigGetResponse:
      properties:
        AssetStpVlanSettingsConfig:
          items:
            $ref: "#/components/schemas/AssetStpVlanSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetStpVlanSettingsConfigGetResponse
      type: object
    AssetSysCapabilities:
      description: AssetSysCapabilities - DB Model for Device capabilities like
        supported VLANs, rules etc.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        max_vlan_supported:
          description: >-
            The maximum number of vlans supported.
            	This number can be different depending on the flags that are activated on the device.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        policy_maximum_roles:
          description: The maximum number of policy roles supported on this device.
          format: int64
          minimum: 0
          type: integer
        policy_maximum_rules:
          description: The maximum number of policy rules supported on this device.
          format: int64
          minimum: 0
          type: integer
        policy_role_tci_overwrite:
          description: |-
            A value of true indicates policy roles supports TCI overwrite
            	on this device.
          type: boolean
        radius_supported_vendor_ids:
          description: The supported RADIUS vendors ids (Fabric Engine only).
          items:
            type: string
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetSysCapabilitiesGetRequest:
      description: AssetSysCapabilitiesGetRequest represents the retrieval request for
        AssetSysCapabilities.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        max_vlan_supported:
          description: >
            List of MaxVlanSupported. MaxVlanSupported represents The maximum
            number of vlans supported.
            	This number can be different depending on the flags that are activated on the device.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        policy_maximum_roles:
          description: >
            List of PolicyMaximumRoles. PolicyMaximumRoles represents The
            maximum number of policy roles supported on this device.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        policy_maximum_rules:
          description: >
            List of PolicyMaximumRules. PolicyMaximumRules represents The
            maximum number of policy rules supported on this device.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        policy_role_tci_overwrite:
          description: >
            List of PolicyRoleTciOverwrite. PolicyRoleTciOverwrite represents A
            value of true indicates policy roles supports TCI overwrite
            	on this device.
          items:
            type: boolean
          type: array
        radius_supported_vendor_ids:
          description: >
            List of RadiusSupportedVendorIds. RadiusSupportedVendorIds
            represents The supported RADIUS vendors ids (Fabric Engine only).
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSysCapabilitiesGetRequest
      type: object
    AssetSysCapabilitiesGetResponse:
      properties:
        AssetSysCapabilities:
          items:
            $ref: "#/components/schemas/AssetSysCapabilities"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSysCapabilitiesGetResponse
      type: object
    AssetSyslogConfig:
      description: AssetSyslogConfig - DB model for Syslog configuration
        SyslogServerEntries can be created while creating AssetSyslogConfig.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Syslog global enabled state (Fabric Engine only)
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        syslog_server_entries:
          items:
            $ref: "#/components/schemas/AssetSyslogServerConfig"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
      type: object
    AssetSyslogConfigGetRequest:
      description: AssetSyslogConfigGetRequest represents the retrieval request for
        AssetSyslogConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents Syslog global enabled state
            (Fabric Engine only)
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSyslogConfigGetRequest
      type: object
    AssetSyslogConfigGetResponse:
      properties:
        AssetSyslogConfig:
          items:
            $ref: "#/components/schemas/AssetSyslogConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSyslogConfigGetResponse
      type: object
    AssetSyslogServerConfig:
      description: AssetSyslogServerConfig - DB model for Syslog server configuration
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Syslog server enabled state
          type: boolean
        facility:
          description: Syslog server facility
          format: int64
          type: integer
        host_id:
          description: Syslog server host id (Fabric Engine only)
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_address:
          description: Syslog server ip address
          type: string
        ip_address_type:
          description: Syslog server ip address type
          format: int64
          type: integer
        log_level:
          description: >-
            Fabric Engine (VOSS) supports all combinations of Severities
            (starting with, only and
            	combination of severities). Switch Engine (EXOS) supports starting with and only severity.
            	logLevel will contain either one severity or a set of consecutive severities ending in
            	Critical. Examples of valid logLevel for VOSS: [ERROR], [ERROR,CRITICAL,ALERT,EMERGENCY],
            	[ERROR,ALERT]. Examples of valid logLevel for EXOS: [ERROR],
            	[ERROR,CRITICAL,ALERT,EMERGENCY]
          items:
            type: string
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port:
          description: >-
            UDP port used for syslog server (Fabric Engine range is 514-530).
            Default port for syslog
            	is 514.
          format: int64
          type: integer
        syslog_config_id:
          description: Id of the Syslog configuration
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_name:
          description: Syslog server Virtual Router (VR) name (Switch Engine only)
          type: string
      required:
        - enabled
        - facility
        - ip_address
        - port
        - syslog_config_id
        - vr_name
      type: object
    AssetSyslogServerConfigGetRequest:
      description: AssetSyslogServerConfigGetRequest represents the retrieval request
        for AssetSyslogServerConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents Syslog server enabled state
          items:
            type: boolean
          type: array
        facility:
          description: |
            List of Facility. Facility represents Syslog server facility
          items:
            format: int64
            type: integer
          type: array
        host_id:
          description: >
            List of HostId. HostId represents Syslog server host id (Fabric
            Engine only)
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents Syslog server ip address
          items:
            type: string
          type: array
        ip_address_type:
          description: >
            List of IpAddressType. IpAddressType represents Syslog server ip
            address type
          items:
            format: int64
            type: integer
          type: array
        log_level:
          description: >
            List of LogLevel. LogLevel represents Fabric Engine (VOSS) supports
            all combinations of Severities (starting with, only and
            	combination of severities). Switch Engine (EXOS) supports starting with and only severity.
            	logLevel will contain either one severity or a set of consecutive severities ending in
            	Critical. Examples of valid logLevel for VOSS: [ERROR], [ERROR,CRITICAL,ALERT,EMERGENCY],
            	[ERROR,ALERT]. Examples of valid logLevel for EXOS: [ERROR],
            	[ERROR,CRITICAL,ALERT,EMERGENCY]
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port:
          description: >
            List of Port. Port represents UDP port used for syslog server
            (Fabric Engine range is 514-530). Default port for syslog
            	is 514.
          items:
            format: int64
            type: integer
          type: array
        syslog_config_id:
          description: >
            List of SyslogConfigId. SyslogConfigId represents Id of the Syslog
            configuration
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_name:
          description: >
            List of VrName. VrName represents Syslog server Virtual Router (VR)
            name (Switch Engine only)
          items:
            type: string
          type: array
      title: AssetSyslogServerConfigGetRequest
      type: object
    AssetSyslogServerConfigGetResponse:
      properties:
        AssetSyslogServerConfig:
          items:
            $ref: "#/components/schemas/AssetSyslogServerConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSyslogServerConfigGetResponse
      type: object
    AssetSystemSettings:
      description: AssetSystemSettings - Db Model for storing system applicable settings
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        cloud_connector_enabled:
          description: The flag to indicate if the cloud connector is enabled.
          type: boolean
        cloud_connector_server_address:
          description: The cloud connector server address.
          type: string
        cloud_connector_vr_name:
          description: The cloud connector VR name.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        iq_agent_enabled:
          description: The flag to indicate if the IQ agent is enabled.
          type: boolean
        iq_agent_server_address:
          description: The IQ agent server address.
          type: string
        iq_agent_vlan_id:
          description: The IQ agent VLAN ID.
          format: int64
          type: integer
        iq_agent_vr_name:
          description: The IQ agent VR name.
          type: string
        l2_mtu_size:
          description: The L2 MTU size.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        stack_ports_enabled:
          description: The flag to indicate if the stack ports are enabled.
          type: boolean
        stacking_enabled:
          description: The flag to indicate if the application staking is enabled.
          type: boolean
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - stack_ports_enabled
        - stacking_enabled
      type: object
    AssetSystemSettingsGetRequest:
      description: AssetSystemSettingsGetRequest represents the retrieval request for
        AssetSystemSettings.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        cloud_connector_enabled:
          description: >
            List of CloudConnectorEnabled. CloudConnectorEnabled represents The
            flag to indicate if the cloud connector is enabled.
          items:
            type: boolean
          type: array
        cloud_connector_server_address:
          description: >
            List of CloudConnectorServerAddress. CloudConnectorServerAddress
            represents The cloud connector server address.
          items:
            type: string
          type: array
        cloud_connector_vr_name:
          description: >
            List of CloudConnectorVrName. CloudConnectorVrName represents The
            cloud connector VR name.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        iq_agent_enabled:
          description: >
            List of IqAgentEnabled. IqAgentEnabled represents The flag to
            indicate if the IQ agent is enabled.
          items:
            type: boolean
          type: array
        iq_agent_server_address:
          description: >
            List of IqAgentServerAddress. IqAgentServerAddress represents The IQ
            agent server address.
          items:
            type: string
          type: array
        iq_agent_vlan_id:
          description: >
            List of IqAgentVlanId. IqAgentVlanId represents The IQ agent VLAN ID.
          items:
            format: int64
            type: integer
          type: array
        iq_agent_vr_name:
          description: >
            List of IqAgentVrName. IqAgentVrName represents The IQ agent VR name.
          items:
            type: string
          type: array
        l2_mtu_size:
          description: |
            List of L2MtuSize. L2MtuSize represents The L2 MTU size.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        stack_ports_enabled:
          description: >
            List of StackPortsEnabled. StackPortsEnabled represents The flag to
            indicate if the stack ports are enabled.
          items:
            type: boolean
          type: array
        stacking_enabled:
          description: >
            List of StackingEnabled. StackingEnabled represents The flag to
            indicate if the application staking is enabled.
          items:
            type: boolean
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetSystemSettingsGetRequest
      type: object
    AssetSystemSettingsGetResponse:
      properties:
        AssetSystemSettings:
          items:
            $ref: "#/components/schemas/AssetSystemSettings"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetSystemSettingsGetResponse
      type: object
    AssetTimeZoneConfig:
      description: AssetTimeZoneConfig - DB model for TimeZone configuration
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        hours:
          description: Offset from GMT in hours
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        minutes:
          description: Offset from GMT in minutes
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        time_zone_id:
          description: Time zone identifier
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - hours
        - minutes
        - time_zone_id
      type: object
    AssetTimeZoneConfigGetRequest:
      description: AssetTimeZoneConfigGetRequest represents the retrieval request for
        AssetTimeZoneConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        hours:
          description: |
            List of Hours. Hours represents Offset from GMT in hours
          items:
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        minutes:
          description: |
            List of Minutes. Minutes represents Offset from GMT in minutes
          items:
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        time_zone_id:
          description: |
            List of TimeZoneId. TimeZoneId represents Time zone identifier
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetTimeZoneConfigGetRequest
      type: object
    AssetTimeZoneConfigGetResponse:
      properties:
        AssetTimeZoneConfig:
          items:
            $ref: "#/components/schemas/AssetTimeZoneConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetTimeZoneConfigGetResponse
      type: object
    AssetUrlCapabilities:
      description: AssetUrlCapabilities - DB Model for URL Capabilities
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dev_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/stacking
            	2. /v0/configuration/poe-power
          format: int64
          minimum: 0
          type: integer
        fabric_attach_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/fabric-attach
            	2. /v0/state/fabric-attach
          format: int64
          minimum: 0
          type: integer
        feature_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/dhcp-snoop
            	2. /v0/configuration/stp
            	4. /v0/configuration/elrp
            	8. /v0/configuration/igmp
            	16. /v0/configuration/storm-control/ports
            	32. /v0/configuration/instant-port
            	64. /v0/configuration/node-alias
            	128. /v1/configuration/snmp
            	256. /v1/configuration/stp
            	512. /v0/state/stp
          format: int64
          minimum: 0
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/forwarding
            	2. /v0/state/forwarding/mac-fdb
            	4. /v0/state/forwarding/mac-fdb/isids
            	8. /v0/state/forwarding/ip-arp
            	16. /v0/state/forwarding/ipv6-nd
            	32. /v0/configuration/mlag/rsmlt
            	64. /v0/state/mlag/rsmlt
            	128. /v0/configuration/lag
            	256. /v0/state/lag
            	512. /v0/configuration/lldp
            	1024. /v0/state/lldp
            	2048. /v0/configuration/cdp/ports
            	4096. /v0/state/cdp
            	8192. /v0/configuration/ports
            	16384. /v0/state/ports
            	32768. /v1/state/ports
          format: int64
          minimum: 0
          type: integer
        l2_vsn_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/spbm/l2/isid
            	2. /v0/state/spbm/l2/isid/
          format: int64
          minimum: 0
          type: integer
        l3_vsn_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/spbm/l3vsn/vrf
            	2. /v0/configuration/spbm/l3/accept-policy
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        spbm_isis_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/state/spbm/isis/multicast-fib
            	2. /v0/state/spbm/isis/lsp
            	4. /v0/state/spbm/isis/ip-mcast-routes
            	8. /v0/state/spbm/isis/inter-isid/unicast-fib
            	16. /v0/state/spbm/isis/unicast-fib
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/vlan
            	2. /v0/state/vlan
            	4. /v1/state/vlan
            	8. /v0/configuration/vlan/ports
            	16. /v0/configuration/dhcp-relay/vlan
            	32. /v0/operation/service-probe/instances
            	64. /v0/state/service-probe/instance
          format: int64
          minimum: 0
          type: integer
        vrf_url_caps:
          description: |-
            Below, the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/vrf
            	2. /v0/configuration/route
            	4. /v0/configuration/route-protection
            	8. /v0/state/route
            	16. /v0/state/route-protection
            	32. /v0/state/route/summary
            	64. /v0/configuration/vrrp
            	128. /v0/state/vrrp
            	256. /v0/configuration/dhcp-relay/vr
            	512. /v0/state/dhcp-relay/vr
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_device_id
      type: object
    AssetUrlCapabilitiesGetRequest:
      description: AssetUrlCapabilitiesGetRequest represents the retrieval request for
        AssetUrlCapabilities.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dev_url_caps:
          description: >
            List of DevUrlCaps. DevUrlCaps represents Below, the number
            indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/stacking
            	2. /v0/configuration/poe-power
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        fabric_attach_url_caps:
          description: >
            List of FabricAttachUrlCaps. FabricAttachUrlCaps represents Below,
            the number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/fabric-attach
            	2. /v0/state/fabric-attach
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        feature_url_caps:
          description: >
            List of FeatureUrlCaps. FeatureUrlCaps represents Below, the number
            indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/dhcp-snoop
            	2. /v0/configuration/stp
            	4. /v0/configuration/elrp
            	8. /v0/configuration/igmp
            	16. /v0/configuration/storm-control/ports
            	32. /v0/configuration/instant-port
            	64. /v0/configuration/node-alias
            	128. /v1/configuration/snmp
            	256. /v1/configuration/stp
            	512. /v0/state/stp
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_url_caps:
          description: >
            List of InterfaceUrlCaps. InterfaceUrlCaps represents Below, the
            number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/forwarding
            	2. /v0/state/forwarding/mac-fdb
            	4. /v0/state/forwarding/mac-fdb/isids
            	8. /v0/state/forwarding/ip-arp
            	16. /v0/state/forwarding/ipv6-nd
            	32. /v0/configuration/mlag/rsmlt
            	64. /v0/state/mlag/rsmlt
            	128. /v0/configuration/lag
            	256. /v0/state/lag
            	512. /v0/configuration/lldp
            	1024. /v0/state/lldp
            	2048. /v0/configuration/cdp/ports
            	4096. /v0/state/cdp
            	8192. /v0/configuration/ports
            	16384. /v0/state/ports
            	32768. /v1/state/ports
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l2_vsn_url_caps:
          description: >
            List of L2VsnUrlCaps. L2VsnUrlCaps represents Below, the number
            indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/spbm/l2/isid
            	2. /v0/state/spbm/l2/isid/
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l3_vsn_url_caps:
          description: >
            List of L3VsnUrlCaps. L3VsnUrlCaps represents Below, the number
            indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/spbm/l3vsn/vrf
            	2. /v0/configuration/spbm/l3/accept-policy
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        spbm_isis_url_caps:
          description: >
            List of SpbmIsisUrlCaps. SpbmIsisUrlCaps represents Below, the
            number indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/state/spbm/isis/multicast-fib
            	2. /v0/state/spbm/isis/lsp
            	4. /v0/state/spbm/isis/ip-mcast-routes
            	8. /v0/state/spbm/isis/inter-isid/unicast-fib
            	16. /v0/state/spbm/isis/unicast-fib
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_url_caps:
          description: >
            List of VlanUrlCaps. VlanUrlCaps represents Below, the number
            indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/vlan
            	2. /v0/state/vlan
            	4. /v1/state/vlan
            	8. /v0/configuration/vlan/ports
            	16. /v0/configuration/dhcp-relay/vlan
            	32. /v0/operation/service-probe/instances
            	64. /v0/state/service-probe/instance
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_url_caps:
          description: >
            List of VrfUrlCaps. VrfUrlCaps represents Below, the number
            indicates the bit position
            	which when set, enables the URL specified
            	against it.

            	1. /v0/configuration/vrf
            	2. /v0/configuration/route
            	4. /v0/configuration/route-protection
            	8. /v0/state/route
            	16. /v0/state/route-protection
            	32. /v0/state/route/summary
            	64. /v0/configuration/vrrp
            	128. /v0/state/vrrp
            	256. /v0/configuration/dhcp-relay/vr
            	512. /v0/state/dhcp-relay/vr
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetUrlCapabilitiesGetRequest
      type: object
    AssetUrlCapabilitiesGetResponse:
      properties:
        AssetUrlCapabilities:
          items:
            $ref: "#/components/schemas/AssetUrlCapabilities"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetUrlCapabilitiesGetResponse
      type: object
    AssetVim:
      description: AssetVim - This object represents the list of Vim on particular Slot.
      properties:
        admin_status:
          description: >-
            Device administrative status. UP, DOWN, TESTING and RESET are
            applicable
            	to Fabric Engine, ENABLED is applicable to Switch Engine.
          format: int64
          type: integer
        asset_slot_id:
          description: Slot Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        model_name:
          description: Device model name.
          type: string
        operation_status:
          description: >-
            Device operational status. DOWN is applicable to both Fabric Engine
            and
            	Switch Engine. UP, TESTING, UNKNOWN, DORMANT, UP_MASTER, UP_DORMANT and
            	UP_WARMSTANDBY are applicable to Fabric Engine. The others are
            	applicable to Switch Engine.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        part_number:
          description: Device part number.
          type: string
        serial_number:
          description: Device unique serial number.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - serial_number
      type: object
    AssetVimGetRequest:
      description: AssetVimGetRequest represents the retrieval request for AssetVim.
      properties:
        admin_status:
          description: >
            List of AdminStatus. AdminStatus represents Device administrative
            status. UP, DOWN, TESTING and RESET are applicable
            	to Fabric Engine, ENABLED is applicable to Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        asset_slot_id:
          description: |
            List of AssetSlotId. AssetSlotId represents Slot Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        model_name:
          description: |
            List of ModelName. ModelName represents Device model name.
          items:
            type: string
          type: array
        operation_status:
          description: >
            List of OperationStatus. OperationStatus represents Device
            operational status. DOWN is applicable to both Fabric Engine and
            	Switch Engine. UP, TESTING, UNKNOWN, DORMANT, UP_MASTER, UP_DORMANT and
            	UP_WARMSTANDBY are applicable to Fabric Engine. The others are
            	applicable to Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        part_number:
          description: |
            List of PartNumber. PartNumber represents Device part number.
          items:
            type: string
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetVimGetRequest
      type: object
    AssetVimGetResponse:
      properties:
        AssetVim:
          items:
            $ref: "#/components/schemas/AssetVim"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVimGetResponse
      type: object
    AssetVlanConfig:
      description: AssetVlanConfig - This object represents the configuration for all
        VLANs. AssetVlanConfigIpArpLearnFrom,AssetVrfVlanConfig can be created
        while creating AssetVlanConfig.
      properties:
        asset_interface_id:
          description: Interface Id
          type: string
        asset_vlan_config_ip_arp_learn_from:
          items:
            $ref: "#/components/schemas/AssetVlanConfigIpArpLearnFrom"
          nullable: true
          type: array
        asset_vrf_vlan_config:
          $ref: "#/components/schemas/AssetVrfVlanConfig"
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        dhcp_enabled:
          description: >-
            A value of true indicates that the VLAN is configured to be a DHCP
            client.
            	This attribute is Ipv4 only.
          type: boolean
        enabled:
          description: This is Switch Engine (EXOS) specific and indicates if this VLAN is
            enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_arp_gratuitous_protect_enabled:
          description: >-
            A value of true indicates gratuitous ARP protection is enabled on
            the VLAN.
            	This parameter applies to Switch Engine only.
          type: boolean
        ip_arp_inspection_enabled:
          description: >-
            A value of true indicates IP ARP inspection is enabled on the VLAN.
            	ARP inspection intercepts and discards the ARP packets with invalid IP to MAC address bindings.
            	The address binding table is created using the information gathered from the DHCP requests and
            	replies when DHCP snooping is enabled.
            	This parameter applies to Fabric Engine only.
            	VOSS ONLY
          type: boolean
        ip_interface_enabled:
          description: >-
            This is Fabric Engine (VOSS) specific and indicates if the IP is
            enabled on this interface.
            	This does not affect the state of the interface itself, only its connection to an IP stack.
            	VOSS ONLY
          type: boolean
        ip_routing_enabled:
          description: >-
            A value of true indicates IP routing is enabled on the VLAN. This
            setting is only applicable to
            	Switch Engine (EXOS). This attribute is Ipv4 only.
          type: boolean
        is_dynamic:
          description: A value of true indicates that the associated VLAN was dynamically
            created.
          type: boolean
        is_loopback:
          description: |-
            Loopback flag (True if the VLAN interface is a loopback).
            	This attribute is not supported on Fabric Engine.
          type: boolean
        is_mgmt_interface:
          description: |-
            Management interface flag (applies to Fabric Engine only).
            	VOSS ONLY
          type: boolean
        isid_id:
          description: ISID ID. This setting is only applicable to Fabric Engine (VOSS).
          format: int64
          minimum: 0
          type: integer
        isid_name:
          description: ISID name. This setting is only applicable to Fabric Engine (VOSS).
          type: string
        l2_forwarding_enabled:
          description: This is Switch Engine (EXOS) specific and indicates if L2 fowarding
            is enabled on this VLAN.
          type: boolean
        l3_mtu:
          description: >-
            This indicates the MTU value for a frame. For Switch Engine(EXOS)
            the values are between 1500 and 9216
            	For Fabric Engine (VOSS), the Ipv4 MTU is 1500 and is fixed, but for Ipv6 the range is between 1280 and
            	9500 depending on the system MTU.
          type: integer
        mvpn_isid:
          description: |-
            I-SID used for SPB multicast with no IP address.
            	Set to 0 to enable SPB multicast in the global routing table(GRT).
            	Set to -1 in order to remove MVPN I-SID.
          type: integer
        mvpn_mac_offset:
          description: MVPN I-SID MAC offset. Can only be set together with the I-SID value.
          type: integer
        ntp_enabled:
          description: >-
            A value of true indicates that the VLAN is configured to be a NTP
            client.  This setting is only
            	applicable to Switch Engine (EXOS). This attribute is Ipv4 only.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        pvlan_id:
          description: |-
            This object represents a VLAN ID object including Mgmt VLAN.
            	VOSS ONLY
          format: int64
          minimum: 0
          type: integer
        qos_queue:
          description: >-
            This is Switch Engine specific. The QOS queue assigned to the VLAN.
            Values of 1
            	through 8 correspond to QOS profiles QP1 through QP8 respectively. QOS profiles
            	QP2 through QP7 do not exist by default and must be created by the user
            	before assigning to a VLAN.  A value of 0 indicates no QOS queue is assigned
            	to the VLAN.
          type: integer
        spb_mcast_enabled:
          description: |-
            A value of true indicates that this VLAN is SPB Multicast enabled.
            	This is a Fabric Engine specific setting.
            	VOSS ONLY
          type: boolean
        stp_name:
          description: >-
            The name of the STP instance this VLAN is assigned to.  For Fabric
            Engine an empty or not present string
            	indicates the VLAN is assigned to STP instance 0.  For Switch Engine an empty or not present string indicates
            	the VLAN is not assigned to an STP domain.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_name:
          description: >-
            VLAN name (For Switch Engine the maximum length is 32 characters).
            An empty string may be
            	returned by the implementation in cases where a name is not associated with the VLAN.
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vlan_type:
          description: >-
            The type of VLAN (Applicability to each NOS is shown below):
            	|    Type                         |   NOS           |  Description
            	|---------------------------------|-----------------|----------------------------------------
            	|    PORT_MSTP_RSTP               |   Fabric Engine |  VLAN by port                         |
            	|    PROTOCOL_MSTP_RSTP           |   Fabric Engine |  VLAN by protocol                     |
            	|    PVLAN_MSTP_RSTP              |   Fabric Engine |  Private VLAN by port                 |
            	|    SPBM_BVLAN                   |   Fabric Engine |  SPBM backbone VLAN (B-VLAN)          |
            	|    VLAN                         |   Switch Engine |  Virtual Local Area Network           |
            	|    VMAN                         |   Switch Engine |  Virtual Metropolitan Area Network    |
            	|    BROUTER                      |   Fabric Engine |  Brouter VLAN                         |
            	|    PVLAN_ISOLATED_SUBSCRIBER    |   Switch Engine |  Private VLAN isolated subscriber     |
            	|    PVLAN_NONISOLATED_SUBSCRIBER |   Switch Engine |  Private VLAN non-isolated subscriber |
            	|    PVLAN_NETWORK                |   Switch Engine |  Private VLAN network                 |
            	Note:
            	Type is required for VLAN creation on Fabric Engine.  The default type for
            	Switch Engine is VLAN. BROUTER type cannot be used for VLAN creation.
            	Only one VLAN of type PROTOCOL_MSTP_RSTP can be configured on Fabric Engine.
            	vlanType of PVLAN_ISOLATED_SUBSCRIBER, PVLAN_NONISOLATED_SUBSCRIBER, and
            	PVLAN_NETWORK must also include the privateVlanName attribute.
          format: int64
          type: integer
        vr_name:
          description: VR Name.
          type: string
      required:
        - asset_interface_id
        - device_id
        - vlan_number
        - vlan_type
      type: object
    AssetVlanConfigGetRequest:
      description: AssetVlanConfigGetRequest represents the retrieval request for
        AssetVlanConfig.
      properties:
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        dhcp_enabled:
          description: >
            List of DhcpEnabled. DhcpEnabled represents A value of true
            indicates that the VLAN is configured to be a DHCP client.
            	This attribute is Ipv4 only.
          items:
            type: boolean
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents This is Switch Engine (EXOS)
            specific and indicates if this VLAN is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_arp_gratuitous_protect_enabled:
          description: >
            List of IpArpGratuitousProtectEnabled. IpArpGratuitousProtectEnabled
            represents A value of true indicates gratuitous ARP protection is
            enabled on the VLAN.
            	This parameter applies to Switch Engine only.
          items:
            type: boolean
          type: array
        ip_arp_inspection_enabled:
          description: >
            List of IpArpInspectionEnabled. IpArpInspectionEnabled represents A
            value of true indicates IP ARP inspection is enabled on the VLAN.
            	ARP inspection intercepts and discards the ARP packets with invalid IP to MAC address bindings.
            	The address binding table is created using the information gathered from the DHCP requests and
            	replies when DHCP snooping is enabled.
            	This parameter applies to Fabric Engine only.
            	VOSS ONLY
          items:
            type: boolean
          type: array
        ip_interface_enabled:
          description: >
            List of IpInterfaceEnabled. IpInterfaceEnabled represents This is
            Fabric Engine (VOSS) specific and indicates if the IP is enabled on
            this interface.
            	This does not affect the state of the interface itself, only its connection to an IP stack.
            	VOSS ONLY
          items:
            type: boolean
          type: array
        ip_routing_enabled:
          description: >
            List of IpRoutingEnabled. IpRoutingEnabled represents A value of
            true indicates IP routing is enabled on the VLAN. This setting is
            only applicable to
            	Switch Engine (EXOS). This attribute is Ipv4 only.
          items:
            type: boolean
          type: array
        is_dynamic:
          description: >
            List of IsDynamic. IsDynamic represents A value of true indicates
            that the associated VLAN was dynamically created.
          items:
            type: boolean
          type: array
        is_loopback:
          description: >
            List of IsLoopback. IsLoopback represents Loopback flag (True if the
            VLAN interface is a loopback).
            	This attribute is not supported on Fabric Engine.
          items:
            type: boolean
          type: array
        is_mgmt_interface:
          description: >
            List of IsMgmtInterface. IsMgmtInterface represents Management
            interface flag (applies to Fabric Engine only).
            	VOSS ONLY
          items:
            type: boolean
          type: array
        isid_id:
          description: >
            List of IsidId. IsidId represents ISID ID. This setting is only
            applicable to Fabric Engine (VOSS).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_name:
          description: >
            List of IsidName. IsidName represents ISID name. This setting is
            only applicable to Fabric Engine (VOSS).
          items:
            type: string
          type: array
        l2_forwarding_enabled:
          description: >
            List of L2ForwardingEnabled. L2ForwardingEnabled represents This is
            Switch Engine (EXOS) specific and indicates if L2 fowarding is
            enabled on this VLAN.
          items:
            type: boolean
          type: array
        l3_mtu:
          description: >
            List of L3Mtu. L3Mtu represents This indicates the MTU value for a
            frame. For Switch Engine(EXOS) the values are between 1500 and 9216
            	For Fabric Engine (VOSS), the Ipv4 MTU is 1500 and is fixed, but for Ipv6 the range is between 1280 and
            	9500 depending on the system MTU.
          items:
            type: integer
          type: array
        mvpn_isid:
          description: >
            List of MvpnIsid. MvpnIsid represents I-SID used for SPB multicast
            with no IP address.
            	Set to 0 to enable SPB multicast in the global routing table(GRT).
            	Set to -1 in order to remove MVPN I-SID.
          items:
            type: integer
          type: array
        mvpn_mac_offset:
          description: >
            List of MvpnMacOffset. MvpnMacOffset represents MVPN I-SID MAC
            offset. Can only be set together with the I-SID value.
          items:
            type: integer
          type: array
        ntp_enabled:
          description: >
            List of NtpEnabled. NtpEnabled represents A value of true indicates
            that the VLAN is configured to be a NTP client.  This setting is
            only
            	applicable to Switch Engine (EXOS). This attribute is Ipv4 only.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        pvlan_id:
          description: >
            List of PvlanId. PvlanId represents This object represents a VLAN ID
            object including Mgmt VLAN.
            	VOSS ONLY
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        qos_queue:
          description: >
            List of QosQueue. QosQueue represents This is Switch Engine
            specific. The QOS queue assigned to the VLAN. Values of 1
            	through 8 correspond to QOS profiles QP1 through QP8 respectively. QOS profiles
            	QP2 through QP7 do not exist by default and must be created by the user
            	before assigning to a VLAN.  A value of 0 indicates no QOS queue is assigned
            	to the VLAN.
          items:
            type: integer
          type: array
        spb_mcast_enabled:
          description: >
            List of SpbMcastEnabled. SpbMcastEnabled represents A value of true
            indicates that this VLAN is SPB Multicast enabled.
            	This is a Fabric Engine specific setting.
            	VOSS ONLY
          items:
            type: boolean
          type: array
        stp_name:
          description: >
            List of StpName. StpName represents The name of the STP instance
            this VLAN is assigned to.  For Fabric Engine an empty or not present
            string
            	indicates the VLAN is assigned to STP instance 0.  For Switch Engine an empty or not present string indicates
            	the VLAN is not assigned to an STP domain.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_name:
          description: >
            List of VlanName. VlanName represents VLAN name (For Switch Engine
            the maximum length is 32 characters). An empty string may be
            	returned by the implementation in cases where a name is not associated with the VLAN.
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_type:
          description: >
            List of VlanType. VlanType represents The type of VLAN
            (Applicability to each NOS is shown below):
            	|    Type                         |   NOS           |  Description
            	|---------------------------------|-----------------|----------------------------------------
            	|    PORT_MSTP_RSTP               |   Fabric Engine |  VLAN by port                         |
            	|    PROTOCOL_MSTP_RSTP           |   Fabric Engine |  VLAN by protocol                     |
            	|    PVLAN_MSTP_RSTP              |   Fabric Engine |  Private VLAN by port                 |
            	|    SPBM_BVLAN                   |   Fabric Engine |  SPBM backbone VLAN (B-VLAN)          |
            	|    VLAN                         |   Switch Engine |  Virtual Local Area Network           |
            	|    VMAN                         |   Switch Engine |  Virtual Metropolitan Area Network    |
            	|    BROUTER                      |   Fabric Engine |  Brouter VLAN                         |
            	|    PVLAN_ISOLATED_SUBSCRIBER    |   Switch Engine |  Private VLAN isolated subscriber     |
            	|    PVLAN_NONISOLATED_SUBSCRIBER |   Switch Engine |  Private VLAN non-isolated subscriber |
            	|    PVLAN_NETWORK                |   Switch Engine |  Private VLAN network                 |
            	Note:
            	Type is required for VLAN creation on Fabric Engine.  The default type for
            	Switch Engine is VLAN. BROUTER type cannot be used for VLAN creation.
            	Only one VLAN of type PROTOCOL_MSTP_RSTP can be configured on Fabric Engine.
            	vlanType of PVLAN_ISOLATED_SUBSCRIBER, PVLAN_NONISOLATED_SUBSCRIBER, and
            	PVLAN_NETWORK must also include the privateVlanName attribute.
          items:
            format: int64
            type: integer
          type: array
        vr_name:
          description: |
            List of VrName. VrName represents VR Name.
          items:
            type: string
          type: array
      title: AssetVlanConfigGetRequest
      type: object
    AssetVlanConfigGetResponse:
      properties:
        AssetVlanConfig:
          items:
            $ref: "#/components/schemas/AssetVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVlanConfigGetResponse
      type: object
    AssetVlanConfigIpArpLearnFrom:
      description: >-
        AssetVlanConfigIpArpLearnFrom - This object represents the configuration
        of ARP Learning for port list

        on the VLAN. This can be from DHCP or ARP.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_arp_learn_from:
          description: |-
            IP Arp Learn From.
            	EXOS ONLY
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_name:
          description: |-
            Port number (maps to the front panel port of a switch);
            	also the key for the port list
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config_id:
          description: Vlan Config Id
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - ip_arp_learn_from
        - port_name
        - vlan_config_id
        - vlan_number
      type: object
    AssetVlanConfigIpArpLearnFromGetRequest:
      description: AssetVlanConfigIpArpLearnFromGetRequest represents the retrieval
        request for AssetVlanConfigIpArpLearnFrom.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_arp_learn_from:
          description: |
            List of IpArpLearnFrom. IpArpLearnFrom represents IP Arp Learn From.
            	EXOS ONLY
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_name:
          description: >
            List of PortName. PortName represents Port number (maps to the front
            panel port of a switch);
            	also the key for the port list
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_config_id:
          description: |
            List of VlanConfigId. VlanConfigId represents Vlan Config Id
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVlanConfigIpArpLearnFromGetRequest
      type: object
    AssetVlanConfigIpArpLearnFromGetResponse:
      properties:
        AssetVlanConfigIpArpLearnFrom:
          items:
            $ref: "#/components/schemas/AssetVlanConfigIpArpLearnFrom"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVlanConfigIpArpLearnFromGetResponse
      type: object
    AssetVlanMacFdbState:
      description: >-
        AssetVlanMacFdbState - This object represents the State information for
        the Forwarding Database entries

        filtered by Vlan number.
      properties:
        age:
          description: |-
            The age of the entry in seconds. This attribute applies to
            	Switch Engine only. A value of 0 indicates that entry should
            	never be aged out.
          format: int64
          minimum: 0
          type: integer
        bmac:
          description: >-
            If the Forwarding Database entry is learned from a
            	SPBM network, use backbone MAC address to track where the MAC address
            	comes from instead of port name. This attribute applies
            	to Fabric Engine only.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        cvid:
          description: >-
            Used to identify customer vid of the switched uni port that
            	this entry is associated with. 4095 value is not used. 4096 value is
            	reserved for untagged case. 0 means there is no switched uni association.
            	This attribute applies to Fabric Engine only.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        flags:
          items:
            format: int64
            type: integer
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: |-
            Either the value 0, or the port name or associated LAG ID
            	of the port on which a frame having a source address equal to the
            	value of the corresponding MAC addrress has been seen.
            	A value of 0 indicates that the port number has not been
            	learned but that the bridge does have some forwarding/filtering
            	information about this address.
            	A value of 'cpp' indicates a self-assigned MAC address.
          type: string
        interface_type:
          description: |-
            This attribute indicates the interface type on which the MAC
            	is learnt. LAG type is applicable for Fabric Engine only.
          format: int64
          type: integer
        mac_address:
          description: |-
            A unicast MAC address for which the bridge has
            	forwarding and/or filtering information.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        smlt_remote:
          description: |-
            Indicate if the MAC address was learned remotely or locally.
            	This attribute applies to Fabric Engine only.
          type: boolean
        status:
          description: |-
            This attribute indicates the status of the Forwarding
            	Database entry. This attribute applies to Fabric Engine only.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: The identifier of the VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - interface_name
        - interface_type
        - mac_address
        - status
        - vlan_number
      type: object
    AssetVlanMacFdbStateGetRequest:
      description: AssetVlanMacFdbStateGetRequest represents the retrieval request for
        AssetVlanMacFdbState.
      properties:
        age:
          description: >
            List of Age. Age represents The age of the entry in seconds. This
            attribute applies to
            	Switch Engine only. A value of 0 indicates that entry should
            	never be aged out.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        bmac:
          description: >
            List of Bmac. Bmac represents If the Forwarding Database entry is
            learned from a
            	SPBM network, use backbone MAC address to track where the MAC address
            	comes from instead of port name. This attribute applies
            	to Fabric Engine only.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        cvid:
          description: >
            List of Cvid. Cvid represents Used to identify customer vid of the
            switched uni port that
            	this entry is associated with. 4095 value is not used. 4096 value is
            	reserved for untagged case. 0 means there is no switched uni association.
            	This attribute applies to Fabric Engine only.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        flags:
          description: >
            List of Flags. Flags represents Flags describing the entry state.
            This attribute applies to
            	Switch Engine only.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Either the value 0,
            or the port name or associated LAG ID
            	of the port on which a frame having a source address equal to the
            	value of the corresponding MAC addrress has been seen.
            	A value of 0 indicates that the port number has not been
            	learned but that the bridge does have some forwarding/filtering
            	information about this address.
            	A value of 'cpp' indicates a self-assigned MAC address.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents This attribute
            indicates the interface type on which the MAC
            	is learnt. LAG type is applicable for Fabric Engine only.
          items:
            format: int64
            type: integer
          type: array
        mac_address:
          description: >
            List of MacAddress. MacAddress represents A unicast MAC address for
            which the bridge has
            	forwarding and/or filtering information.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        smlt_remote:
          description: >
            List of SmltRemote. SmltRemote represents Indicate if the MAC
            address was learned remotely or locally.
            	This attribute applies to Fabric Engine only.
          items:
            type: boolean
          type: array
        status:
          description: >
            List of Status. Status represents This attribute indicates the
            status of the Forwarding
            	Database entry. This attribute applies to Fabric Engine only.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents The identifier of the VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVlanMacFdbStateGetRequest
      type: object
    AssetVlanMacFdbStateGetResponse:
      properties:
        AssetVlanMacFdbState:
          items:
            $ref: "#/components/schemas/AssetVlanMacFdbState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVlanMacFdbStateGetResponse
      type: object
    AssetVlanState:
      description: AssetVlanState - This object represents the state information for
        all VLANs on the device. AssetVlanStateMemberInterfaces can be created
        while creating AssetVlanState.
      properties:
        asset_interface_id:
          description: Interface Id
          type: string
        asset_vlan_state_member_interfaces:
          items:
            $ref: "#/components/schemas/AssetVlanStateMemberInterfaces"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_arp_inspection_origin:
          description: State of the VLAN. 'DISABLE' is Switch Engine specific.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        state:
          description: State of the VLAN. 'DISABLE' is Switch Engine specific.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_name:
          description: >-
            VLAN name (For Switch Engine the maximum length is 32 characters).
            An empty string may be
            	returned by the implementation in cases where a name is not associated with the VLAN.
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_interface_id
        - device_id
        - ip_arp_inspection_origin
        - state
        - vlan_number
      type: object
    AssetVlanStateGetRequest:
      description: AssetVlanStateGetRequest represents the retrieval request for
        AssetVlanState.
      properties:
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_arp_inspection_origin:
          description: >
            List of IpArpInspectionOrigin. IpArpInspectionOrigin represents
            State of the VLAN. 'DISABLE' is Switch Engine specific.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        state:
          description: >
            List of State. State represents State of the VLAN. 'DISABLE' is
            Switch Engine specific.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_name:
          description: >
            List of VlanName. VlanName represents VLAN name (For Switch Engine
            the maximum length is 32 characters). An empty string may be
            	returned by the implementation in cases where a name is not associated with the VLAN.
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVlanStateGetRequest
      type: object
    AssetVlanStateGetResponse:
      properties:
        AssetVlanState:
          items:
            $ref: "#/components/schemas/AssetVlanState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVlanStateGetResponse
      type: object
    AssetVlanStateMemberInterfaces:
      description: AssetVlanStateMemberInterfaces - This object represents the
        configuration of member ports added to the VLAN.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: The interface name.
          type: string
        interface_type:
          description: The interface type.
          format: int64
          type: integer
        is_dynamic:
          description: A value of true indicates that the associated VLAN was dynamically
            created.
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_status:
          description: Port Status
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vlan_state_id:
          description: Vlan State Id
          type: string
      required:
        - device_id
        - interface_name
        - interface_type
        - vlan_number
        - vlan_state_id
      type: object
    AssetVlanStateMemberInterfacesGetRequest:
      description: AssetVlanStateMemberInterfacesGetRequest represents the retrieval
        request for AssetVlanStateMemberInterfaces.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents The interface name.
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents The interface type.
          items:
            format: int64
            type: integer
          type: array
        is_dynamic:
          description: >
            List of IsDynamic. IsDynamic represents A value of true indicates
            that the associated VLAN was dynamically created.
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_status:
          description: |
            List of PortStatus. PortStatus represents Port Status
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_state_id:
          description: |
            List of VlanStateId. VlanStateId represents Vlan State Id
          items:
            type: string
          type: array
      title: AssetVlanStateMemberInterfacesGetRequest
      type: object
    AssetVlanStateMemberInterfacesGetResponse:
      properties:
        AssetVlanStateMemberInterfaces:
          items:
            $ref: "#/components/schemas/AssetVlanStateMemberInterfaces"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVlanStateMemberInterfacesGetResponse
      type: object
    AssetVrfConfig:
      description: AssetVrfConfig - This object represents the configuration for all
        VRF. AssetVrfRouteConfig,AssetVrfVlanConfig can be created while
        creating AssetVrfConfig.
      properties:
        asset_vrf_route_config:
          items:
            $ref: "#/components/schemas/AssetVrfRouteConfig"
          nullable: true
          type: array
        asset_vrf_vlan_config:
          items:
            $ref: "#/components/schemas/AssetVrfVlanConfig"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        dynamic:
          description: VRF management setting (True indicates this VRF is the management
            VRF).
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_routing_enabled:
          description: |-
            A value of true indicates IP routing is enabled on this VRF. This
            	setting is only applicable to Fabric Engine (VOSS).
            	VOSS ONLY.
          type: boolean
        is_mgmt:
          description: VRF management setting (True indicates this VRF is the management
            VRF).
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vr_type:
          description: >-
            The virtual router type (VR or VRF). For Switch Engine (EXOS) VRF's
            are associated with
            	a parent VR and allow multiple instances of a protocol to operate as part of a single process.  VRF's
            	also support Layer 3 VPN's. Fabric Engine (VOSS) does not support VR value.
          format: int64
          type: integer
        vrf_id:
          description: VRF identifier
          format: int64
          minimum: 0
          type: integer
        vrf_name:
          description: VRF name
          type: string
      required:
        - device_id
        - vrf_name
      type: object
    AssetVrfConfigGetRequest:
      description: AssetVrfConfigGetRequest represents the retrieval request for
        AssetVrfConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        dynamic:
          description: >
            List of Dynamic. Dynamic represents VRF management setting (True
            indicates this VRF is the management VRF).
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_routing_enabled:
          description: >
            List of IpRoutingEnabled. IpRoutingEnabled represents A value of
            true indicates IP routing is enabled on this VRF. This
            	setting is only applicable to Fabric Engine (VOSS).
            	VOSS ONLY.
          items:
            type: boolean
          type: array
        is_mgmt:
          description: >
            List of IsMgmt. IsMgmt represents VRF management setting (True
            indicates this VRF is the management VRF).
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vr_type:
          description: >
            List of VrType. VrType represents The virtual router type (VR or
            VRF). For Switch Engine (EXOS) VRF's are associated with
            	a parent VR and allow multiple instances of a protocol to operate as part of a single process.  VRF's
            	also support Layer 3 VPN's. Fabric Engine (VOSS) does not support VR value.
          items:
            format: int64
            type: integer
          type: array
        vrf_id:
          description: |
            List of VrfId. VrfId represents VRF identifier
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name
          items:
            type: string
          type: array
      title: AssetVrfConfigGetRequest
      type: object
    AssetVrfConfigGetResponse:
      properties:
        AssetVrfConfig:
          items:
            $ref: "#/components/schemas/AssetVrfConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrfConfigGetResponse
      type: object
    AssetVrfProtectedRouteGateway:
      description: >-
        AssetVrfProtectedRouteGateway - This object represents the list of
        gateways for each VR

        which have been configured as part of protected static

        routes.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        gateway_address:
          description: The gateway associated with the static route.
          type: string
        gateway_address_type:
          description: The gateway type.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        route_count:
          description: The route count.
          format: int64
          minimum: 0
          type: integer
        route_state:
          description: The route state.
          type: string
        route_type:
          description: The route type.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_state_id:
          description: VRF State Id
          type: string
      required:
        - gateway_address
        - gateway_address_type
        - vrf_state_id
      type: object
    AssetVrfProtectedRouteGatewayGetRequest:
      description: AssetVrfProtectedRouteGatewayGetRequest represents the retrieval
        request for AssetVrfProtectedRouteGateway.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        gateway_address:
          description: >
            List of GatewayAddress. GatewayAddress represents The gateway
            associated with the static route.
          items:
            type: string
          type: array
        gateway_address_type:
          description: >
            List of GatewayAddressType. GatewayAddressType represents The
            gateway type.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        route_count:
          description: |
            List of RouteCount. RouteCount represents The route count.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        route_state:
          description: |
            List of RouteState. RouteState represents The route state.
          items:
            type: string
          type: array
        route_type:
          description: |
            List of RouteType. RouteType represents The route type.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_state_id:
          description: |
            List of VrfStateId. VrfStateId represents VRF State Id
          items:
            type: string
          type: array
      title: AssetVrfProtectedRouteGatewayGetRequest
      type: object
    AssetVrfProtectedRouteGatewayGetResponse:
      properties:
        AssetVrfProtectedRouteGateway:
          items:
            $ref: "#/components/schemas/AssetVrfProtectedRouteGateway"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrfProtectedRouteGatewayGetResponse
      type: object
    AssetVrfRouteConfig:
      description: >-
        AssetVrfRouteConfig - This object represents the state information of
        list of configured routes

        across all of the VRF's on the device.
      nullable: true
      properties:
        blackhole:
          description: A value of True indicates this route is a blackhole route.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        default_route:
          description: A value of True indicates this route is a default route (0.0.0.0/0
            or ::/0).
          type: boolean
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: >-
            A value of True indicates this route is enabled.  This parameter
            only applies to Fabric Engine (VOSS),
            	for Switch Engine (EXOS) all configured static routes are considered as enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_version:
          description: |-
            ISIS SPBM IP address. Define an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          format: int64
          type: integer
        next_hop_address:
          description: The next hop address (Ipv4 or Ipv6) of the static route.
          type: string
        next_hop_address_type:
          default: 0
          description: The next hop address type (Ipv4 or Ipv6) of the static route.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        ping_enabled:
          description: >-
            A value of true indicates this route is configured to use ping to
            monitor the
            	health of the next-hop gateway. The route will not become active until an echo
            	reply is received from the gateway. A route will transition from active to
            	inactive after a configured number of echo requests are not replied to.
            	This property applies to Switch Engine (EXOS) only. There is also a global
            	setting to enable and disable the sending of pings to a gateway and is enabled
            	by default. This property can only be set at the time of creation of the static
            	route.
          type: boolean
        preference:
          description: >-
            The preference (administrative distance) associated with this route.
            This parameter allows preference
            	to be set on a per prefix basis and only applies to Fabric Engine (VOSS). For Switch Engine (EXOS),
            	the preference can only be set on a per route type basis. The preference is used for route selection
            	when the same prefix is learned from multiple protocols. A lower value is more preferential. For Fabric
            	Engine the range is 1-255 on a static route.
          format: int64
          minimum: 0
          type: integer
        prefix_address:
          description: The prefix IP Address (Ipv4 or Ipv6) associated with the static
            route.
          type: string
        prefix_address_type:
          default: 0
          description: The type of IP prefix (Ipv4 or Ipv6)
          format: int64
          type: integer
        prefix_mask_length:
          description: The type of IP prefix (Ipv4 or Ipv6)
          format: int64
          minimum: 0
          type: integer
        route_name:
          description: An optional name associated with this route. This parameter only
            applies to Fabric Engine (VOSS).
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_config_id:
          description: VRF Config Id
          type: string
        weight:
          description: >-
            The route metric (cost) associated with this route. The weight is
            used for route selection
            	when multiple instance of the same prefix are learned from the same protocol. For Switch Engine (EXOS)
            	the value range is 1 to 255, whereas Fabric Engine (VOSS) supports of a range of 1 to 64,000
          format: int64
          minimum: 0
          type: integer
      required:
        - next_hop_address
        - next_hop_address_type
        - prefix_address
        - prefix_address_type
        - vrf_config_id
      type: object
    AssetVrfRouteConfigGetRequest:
      description: AssetVrfRouteConfigGetRequest represents the retrieval request for
        AssetVrfRouteConfig.
      properties:
        blackhole:
          description: >
            List of Blackhole. Blackhole represents A value of True indicates
            this route is a blackhole route.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        default_route:
          description: >
            List of DefaultRoute. DefaultRoute represents A value of True
            indicates this route is a default route (0.0.0.0/0 or ::/0).
          items:
            type: boolean
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of True indicates this
            route is enabled.  This parameter only applies to Fabric Engine
            (VOSS),
            	for Switch Engine (EXOS) all configured static routes are considered as enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_version:
          description: >
            List of IpVersion. IpVersion represents ISIS SPBM IP address. Define
            an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          items:
            format: int64
            type: integer
          type: array
        next_hop_address:
          description: >
            List of NextHopAddress. NextHopAddress represents The next hop
            address (Ipv4 or Ipv6) of the static route.
          items:
            type: string
          type: array
        next_hop_address_type:
          description: >
            List of NextHopAddressType. NextHopAddressType represents The next
            hop address type (Ipv4 or Ipv6) of the static route.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        ping_enabled:
          description: >
            List of PingEnabled. PingEnabled represents A value of true
            indicates this route is configured to use ping to monitor the
            	health of the next-hop gateway. The route will not become active until an echo
            	reply is received from the gateway. A route will transition from active to
            	inactive after a configured number of echo requests are not replied to.
            	This property applies to Switch Engine (EXOS) only. There is also a global
            	setting to enable and disable the sending of pings to a gateway and is enabled
            	by default. This property can only be set at the time of creation of the static
            	route.
          items:
            type: boolean
          type: array
        preference:
          description: >
            List of Preference. Preference represents The preference
            (administrative distance) associated with this route. This parameter
            allows preference
            	to be set on a per prefix basis and only applies to Fabric Engine (VOSS). For Switch Engine (EXOS),
            	the preference can only be set on a per route type basis. The preference is used for route selection
            	when the same prefix is learned from multiple protocols. A lower value is more preferential. For Fabric
            	Engine the range is 1-255 on a static route.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        prefix_address:
          description: >
            List of PrefixAddress. PrefixAddress represents The prefix IP
            Address (Ipv4 or Ipv6) associated with the static route.
          items:
            type: string
          type: array
        prefix_address_type:
          description: >
            List of PrefixAddressType. PrefixAddressType represents The type of
            IP prefix (Ipv4 or Ipv6)
          items:
            format: int64
            type: integer
          type: array
        prefix_mask_length:
          description: >
            List of PrefixMaskLength. PrefixMaskLength represents The type of IP
            prefix (Ipv4 or Ipv6)
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        route_name:
          description: >
            List of RouteName. RouteName represents An optional name associated
            with this route. This parameter only applies to Fabric Engine
            (VOSS).
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_config_id:
          description: |
            List of VrfConfigId. VrfConfigId represents VRF Config Id
          items:
            type: string
          type: array
        weight:
          description: >
            List of Weight. Weight represents The route metric (cost) associated
            with this route. The weight is used for route selection
            	when multiple instance of the same prefix are learned from the same protocol. For Switch Engine (EXOS)
            	the value range is 1 to 255, whereas Fabric Engine (VOSS) supports of a range of 1 to 64,000
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVrfRouteConfigGetRequest
      type: object
    AssetVrfRouteConfigGetResponse:
      properties:
        AssetVrfRouteConfig:
          items:
            $ref: "#/components/schemas/AssetVrfRouteConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrfRouteConfigGetResponse
      type: object
    AssetVrfRouteState:
      description: >-
        AssetVrfRouteState - This object represents the state information of all
        routes configured

        across VRF on device.
      nullable: true
      properties:
        age:
          description: |-
            The number of seconds since this route was last updated or
            	otherwise determined to be correct.
          format: int64
          minimum: 0
          type: integer
        blackhole:
          description: A value of True indicates this route is a blackhole route.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        default_route:
          description: A value of True indicates this route is a default route (0.0.0.0/0
            or ::/0).
          type: boolean
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_version:
          description: |-
            ISIS SPBM IP address. Define an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          format: int64
          type: integer
        next_hop_address:
          description: The next hop address (Ipv4 or Ipv6) of the static route.
          type: string
        next_hop_address_type:
          default: 0
          description: The next hop address type (Ipv4 or Ipv6) of the static route.
          format: int64
          type: integer
        next_hop_id:
          description: |-
            Identifier of the next-hop, hostname or mac address. This is
            	Fabric Engine specific.
            	This is internally used by SPBM.
          type: string
        origin:
          description: |-
            The origin of the route. The table below shows which route types
            	correspond to which NOS. For Switch Engine some of the
            	origins are general names that have more specific origins
            	associated with them and the general name is a shortcut.
            	For example, BGP routes are categorized as either EBGP or IBGP
            	in the show command output.  Configuration of export of BGP
            	routes into another protocol will automatically be converted
            	into separate commands for IBGP and EBGP. These instances have been
            	denoted with an asterisk in the table.  In addition, HARD_CODED is
            	used for debugging and can not be configured.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        preference:
          description: >-
            The preference (administrative distance) associated with this route.
            This parameter allows preference
            	to be set on a per prefix basis and only applies to Fabric Engine (VOSS). For Switch Engine (EXOS),
            	the preference can only be set on a per route type basis. The preference is used for route selection
            	when the same prefix is learned from multiple protocols. A lower value is more preferential. For Fabric
            	Engine the range is 1-255 on a static route.
          format: int64
          minimum: 0
          type: integer
        prefix_address:
          description: The prefix IP Address (Ipv4 or Ipv6) associated with the static
            route.
          type: string
        prefix_address_type:
          default: 0
          description: The type of IP prefix (Ipv4 or Ipv6)
          format: int64
          type: integer
        prefix_mask_length:
          description: The type of IP prefix (Ipv4 or Ipv6)
          format: int64
          minimum: 0
          type: integer
        route_name:
          description: An optional name associated with this route. This parameter only
            applies to Fabric Engine (VOSS).
          type: string
        status:
          description: The status of the route.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_name:
          description: Vlan name
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vrf_state_id:
          description: VRF State Id
          type: string
        weight:
          description: >-
            The route metric (cost) associated with this route. The weight is
            used for route selection
            	when multiple instance of the same prefix are learned from the same protocol. For Switch Engine (EXOS)
            	the value range is 1 to 255, whereas Fabric Engine (VOSS) supports of a range of 1 to 64,000
          format: int64
          minimum: 0
          type: integer
      required:
        - next_hop_address
        - next_hop_address_type
        - origin
        - prefix_address
        - prefix_address_type
        - vrf_state_id
      type: object
    AssetVrfRouteStateGetRequest:
      description: AssetVrfRouteStateGetRequest represents the retrieval request for
        AssetVrfRouteState.
      properties:
        age:
          description: >
            List of Age. Age represents  The number of seconds since this route
            was last updated or
            	otherwise determined to be correct.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        blackhole:
          description: >
            List of Blackhole. Blackhole represents A value of True indicates
            this route is a blackhole route.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        default_route:
          description: >
            List of DefaultRoute. DefaultRoute represents A value of True
            indicates this route is a default route (0.0.0.0/0 or ::/0).
          items:
            type: boolean
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_version:
          description: >
            List of IpVersion. IpVersion represents ISIS SPBM IP address. Define
            an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          items:
            format: int64
            type: integer
          type: array
        next_hop_address:
          description: >
            List of NextHopAddress. NextHopAddress represents The next hop
            address (Ipv4 or Ipv6) of the static route.
          items:
            type: string
          type: array
        next_hop_address_type:
          description: >
            List of NextHopAddressType. NextHopAddressType represents The next
            hop address type (Ipv4 or Ipv6) of the static route.
          items:
            format: int64
            type: integer
          type: array
        next_hop_id:
          description: >
            List of NextHopId. NextHopId represents Identifier of the next-hop,
            hostname or mac address. This is
            	Fabric Engine specific.
            	This is internally used by SPBM.
          items:
            type: string
          type: array
        origin:
          description: >
            List of Origin. Origin represents The origin of the route. The table
            below shows which route types
            	correspond to which NOS. For Switch Engine some of the
            	origins are general names that have more specific origins
            	associated with them and the general name is a shortcut.
            	For example, BGP routes are categorized as either EBGP or IBGP
            	in the show command output.  Configuration of export of BGP
            	routes into another protocol will automatically be converted
            	into separate commands for IBGP and EBGP. These instances have been
            	denoted with an asterisk in the table.  In addition, HARD_CODED is
            	used for debugging and can not be configured.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        preference:
          description: >
            List of Preference. Preference represents The preference
            (administrative distance) associated with this route. This parameter
            allows preference
            	to be set on a per prefix basis and only applies to Fabric Engine (VOSS). For Switch Engine (EXOS),
            	the preference can only be set on a per route type basis. The preference is used for route selection
            	when the same prefix is learned from multiple protocols. A lower value is more preferential. For Fabric
            	Engine the range is 1-255 on a static route.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        prefix_address:
          description: >
            List of PrefixAddress. PrefixAddress represents The prefix IP
            Address (Ipv4 or Ipv6) associated with the static route.
          items:
            type: string
          type: array
        prefix_address_type:
          description: >
            List of PrefixAddressType. PrefixAddressType represents The type of
            IP prefix (Ipv4 or Ipv6)
          items:
            format: int64
            type: integer
          type: array
        prefix_mask_length:
          description: >
            List of PrefixMaskLength. PrefixMaskLength represents The type of IP
            prefix (Ipv4 or Ipv6)
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        route_name:
          description: >
            List of RouteName. RouteName represents An optional name associated
            with this route. This parameter only applies to Fabric Engine
            (VOSS).
          items:
            type: string
          type: array
        status:
          description: |
            List of Status. Status represents The status of the route.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_name:
          description: |
            List of VlanName. VlanName represents Vlan name
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_state_id:
          description: |
            List of VrfStateId. VrfStateId represents VRF State Id
          items:
            type: string
          type: array
        weight:
          description: >
            List of Weight. Weight represents The route metric (cost) associated
            with this route. The weight is used for route selection
            	when multiple instance of the same prefix are learned from the same protocol. For Switch Engine (EXOS)
            	the value range is 1 to 255, whereas Fabric Engine (VOSS) supports of a range of 1 to 64,000
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVrfRouteStateGetRequest
      type: object
    AssetVrfRouteStateGetResponse:
      properties:
        AssetVrfRouteState:
          items:
            $ref: "#/components/schemas/AssetVrfRouteState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrfRouteStateGetResponse
      type: object
    AssetVrfState:
      description: AssetVrfState - This object represents the state information of all
        VRF on the device. AssetVrfProtectedRouteGateway,AssetVrfRouteState can
        be created while creating AssetVrfState.
      properties:
        asset_vrf_protected_route_gateway:
          items:
            $ref: "#/components/schemas/AssetVrfProtectedRouteGateway"
          nullable: true
          type: array
        asset_vrf_route_state:
          items:
            $ref: "#/components/schemas/AssetVrfRouteState"
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        route_count:
          description: |-
            Total number of routes (statically created or dynamically learned)
            	for a virtual router instance.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_id:
          description: VRF identifier
          format: int64
          minimum: 0
          type: integer
        vrf_name:
          description: VRF name
          type: string
      required:
        - device_id
        - vrf_name
      type: object
    AssetVrfStateGetRequest:
      description: AssetVrfStateGetRequest represents the retrieval request for
        AssetVrfState.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        route_count:
          description: >
            List of RouteCount. RouteCount represents Total number of routes
            (statically created or dynamically learned)
            	for a virtual router instance.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_id:
          description: |
            List of VrfId. VrfId represents VRF identifier
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name
          items:
            type: string
          type: array
      title: AssetVrfStateGetRequest
      type: object
    AssetVrfStateGetResponse:
      properties:
        AssetVrfState:
          items:
            $ref: "#/components/schemas/AssetVrfState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrfStateGetResponse
      type: object
    AssetVrfVlanConfig:
      description: AssetVrfVlanConfig - This object represents the Virtual Router
        Forwarding (VRF) settings with summary VLAN information.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_config_id:
          description: VLAN Config Id
          type: string
        vlan_number:
          description: >-
            Untagged packets received on this port are assigned to the VLAN
            specified by this property. A value of 0,
            	is used to unset this parameter.
          format: int64
          minimum: 0
          type: integer
        vrf_config_id:
          description: VRF Config Id
          type: string
      required:
        - vlan_config_id
        - vlan_number
        - vrf_config_id
      type: object
    AssetVrfVlanConfigGetRequest:
      description: AssetVrfVlanConfigGetRequest represents the retrieval request for
        AssetVrfVlanConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_config_id:
          description: |
            List of VlanConfigId. VlanConfigId represents VLAN Config Id
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents Untagged packets received
            on this port are assigned to the VLAN specified by this property. A
            value of 0,
            	is used to unset this parameter.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrf_config_id:
          description: |
            List of VrfConfigId. VrfConfigId represents VRF Config Id
          items:
            type: string
          type: array
      title: AssetVrfVlanConfigGetRequest
      type: object
    AssetVrfVlanConfigGetResponse:
      properties:
        AssetVrfVlanConfig:
          items:
            $ref: "#/components/schemas/AssetVrfVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrfVlanConfigGetResponse
      type: object
    AssetVrrpConfig:
      description: >-
        AssetVrrpConfig - This object represents the configuration of VRRP
        settings for a virtual router instance

        (excludes the virtual router ID). AssetVrrpVirtualIpAddressConfig can be
        created while creating AssetVrrpConfig.
      properties:
        advertisement_interval_units:
          description: >-
            The time units of the advertisement interval.  For Fabric Engine
            (VOSS)
            	the interval ranges are [1-255] seconds for version 2 and [1-40] seconds
            	for version 3. Fabric Engine (VOSS) supports only SECONDS for this attribute.
            	Fabric Engine (VOSS) has a separate 'fast advertisement interval'
            	which is expressed in milliseconds [200-1000]. For Switch Engine (EXOS)
            	the interval can be expressed in seconds [1-40] or centiseconds [10-4095].
          format: int64
          type: integer
        advertisement_interval_value:
          description: |-
            The advertisement interval in the units specified
            	by the parameter 'units'
          format: int64
          minimum: 0
          type: integer
        asset_interface_id:
          description: Interface Id
          type: string
        asset_vrrp_virtual_ip_address_config:
          items:
            $ref: "#/components/schemas/AssetVrrpVirtualIpAddressConfig"
          nullable: true
          type: array
        backup_routing:
          description: |-
            A value of true indicates that the backup routes traffic instead of
            	switching it to the master.  This is a configurable setting in both
            	Fabric Engine (VOSS) and Switch Engine (EXOS) and is referred to as
            	Fabric Routing in the latter NOS.
          type: boolean
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        enabled:
          description: A value of true indicates that this VRRP virtual router is enabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        preempt_enabled:
          description: >-
            A value of true indicates a higher priority backup will
            automatically become the
            	master according to the value of the preemptHoldTimer. A value of false
            	indicates the backup will not become the master unless the current master fails.
          type: boolean
        preempt_hold_timer:
          description: >-
            The time interval (seconds) for a higher priority backup to preempt
            the master
            	and become master itself. For Fabric Engine a value of 0 indicates the timer does
            	not run and the allowable range is specified by the minimum and maximum values shown
            	below. For Switch Engine a value of 0 indicates the timer is 3 times the advertisement
            	interval and the allowable range is [0-3600] seconds.
          format: int64
          minimum: 0
          type: integer
        priority:
          description: >-
            The priority value used in advertisements sent from the local router
            	for this VRRP instance. A higher priority value determines master election
            	for the instance. Switch Engine (EXOS) supports a maximum
            	value of 255 which indicates that the sending router is the owner
            	(i.e. it is locally configured on an interface) of the virtual IP address.
            	Fabric Engine (VOSS) supports a maximum value of 254.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        version:
          description: >-
            The VRRP version for this virtual router.  The default for Switch
            Engine (EXOS) is
            	'V3-V2-Compatible'. Fabric Engine (VOSS) does not provide support for V3-V2-Compatible.
          format: int64
          type: integer
        vlan_id:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vrrp_id:
          description: VRRP identifier
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_interface_id
        - device_id
        - vrrp_id
      type: object
    AssetVrrpConfigGetRequest:
      description: AssetVrrpConfigGetRequest represents the retrieval request for
        AssetVrrpConfig.
      properties:
        advertisement_interval_units:
          description: >
            List of AdvertisementIntervalUnits. AdvertisementIntervalUnits
            represents The time units of the advertisement interval.  For Fabric
            Engine (VOSS)
            	the interval ranges are [1-255] seconds for version 2 and [1-40] seconds
            	for version 3. Fabric Engine (VOSS) supports only SECONDS for this attribute.
            	Fabric Engine (VOSS) has a separate 'fast advertisement interval'
            	which is expressed in milliseconds [200-1000]. For Switch Engine (EXOS)
            	the interval can be expressed in seconds [1-40] or centiseconds [10-4095].
          items:
            format: int64
            type: integer
          type: array
        advertisement_interval_value:
          description: >
            List of AdvertisementIntervalValue. AdvertisementIntervalValue
            represents The advertisement interval in the units specified
            	by the parameter 'units'
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        backup_routing:
          description: >
            List of BackupRouting. BackupRouting represents A value of true
            indicates that the backup routes traffic instead of
            	switching it to the master.  This is a configurable setting in both
            	Fabric Engine (VOSS) and Switch Engine (EXOS) and is referred to as
            	Fabric Routing in the latter NOS.
          items:
            type: boolean
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        enabled:
          description: >
            List of Enabled. Enabled represents A value of true indicates that
            this VRRP virtual router is enabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        preempt_enabled:
          description: >
            List of PreemptEnabled. PreemptEnabled represents A value of true
            indicates a higher priority backup will automatically become the
            	master according to the value of the preemptHoldTimer. A value of false
            	indicates the backup will not become the master unless the current master fails.
          items:
            type: boolean
          type: array
        preempt_hold_timer:
          description: >
            List of PreemptHoldTimer. PreemptHoldTimer represents The time
            interval (seconds) for a higher priority backup to preempt the
            master
            	and become master itself. For Fabric Engine a value of 0 indicates the timer does
            	not run and the allowable range is specified by the minimum and maximum values shown
            	below. For Switch Engine a value of 0 indicates the timer is 3 times the advertisement
            	interval and the allowable range is [0-3600] seconds.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        priority:
          description: >
            List of Priority. Priority represents The priority value used in
            advertisements sent from the local router
            	for this VRRP instance. A higher priority value determines master election
            	for the instance. Switch Engine (EXOS) supports a maximum
            	value of 255 which indicates that the sending router is the owner
            	(i.e. it is locally configured on an interface) of the virtual IP address.
            	Fabric Engine (VOSS) supports a maximum value of 254.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        version:
          description: >
            List of Version. Version represents The VRRP version for this
            virtual router.  The default for Switch Engine (EXOS) is
            	'V3-V2-Compatible'. Fabric Engine (VOSS) does not provide support for V3-V2-Compatible.
          items:
            format: int64
            type: integer
          type: array
        vlan_id:
          description: >
            List of VlanId. VlanId represents This object represents a VLAN ID
            object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrrp_id:
          description: |
            List of VrrpId. VrrpId represents VRRP identifier
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVrrpConfigGetRequest
      type: object
    AssetVrrpConfigGetResponse:
      properties:
        AssetVrrpConfig:
          items:
            $ref: "#/components/schemas/AssetVrrpConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrrpConfigGetResponse
      type: object
    AssetVrrpState:
      description: AssetVrrpState - This object represents the state information for
        all VRRP instances. AssetVrrpVirtualIpAddressState can be created while
        creating AssetVrrpState.
      properties:
        advertise_interval_errors:
          description: |-
            The total number of VRRP advertisement packets received
            	for which the advertisement interval is different than the
            	one configured for the local virtual router.
          type: integer
        advertise_received:
          description: The total number of VRRP advertisements received by this virtual
            router.
          type: integer
        asset_interface_id:
          description: Interface Id
          type: string
        asset_vrrp_virtual_ip_address_state:
          items:
            $ref: "#/components/schemas/AssetVrrpVirtualIpAddressState"
          nullable: true
          type: array
        become_master:
          description: The total number of times that this virtual router`s state has
            transitioned to MASTER.
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        master_ip_address:
          description: Master IP address (Ipv4 or Ipv6)
          type: string
        master_ip_version:
          description: Master IP Version
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        state:
          description: >-
            The current state for a virtual router. 'INIT' indicates that
            virtual router is waiting for a startup event.
            	'BACKUP' indicates the virtual router is monitoring the availability of the master router. 'MASTER' indicates
            	that the virtual router is forwarding packets for IP address that are associated with this router.
            	Fabric Engine (VOSS) does not provide support for UNKNOWN value.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vrrp_id:
          description: VRRP identifier
          format: int64
          minimum: 0
          type: integer
      required:
        - asset_interface_id
        - device_id
        - vrrp_id
      type: object
    AssetVrrpStateGetRequest:
      description: AssetVrrpStateGetRequest represents the retrieval request for
        AssetVrrpState.
      properties:
        advertise_interval_errors:
          description: >
            List of AdvertiseIntervalErrors. AdvertiseIntervalErrors represents
            The total number of VRRP advertisement packets received
            	for which the advertisement interval is different than the
            	one configured for the local virtual router.
          items:
            type: integer
          type: array
        advertise_received:
          description: >
            List of AdvertiseReceived. AdvertiseReceived represents The total
            number of VRRP advertisements received by this virtual router.
          items:
            type: integer
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        become_master:
          description: >
            List of BecomeMaster. BecomeMaster represents The total number of
            times that this virtual router`s state has transitioned to MASTER.
          items:
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        master_ip_address:
          description: >
            List of MasterIpAddress. MasterIpAddress represents Master IP
            address (Ipv4 or Ipv6)
          items:
            type: string
          type: array
        master_ip_version:
          description: >
            List of MasterIpVersion. MasterIpVersion represents Master IP Version
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        state:
          description: >
            List of State. State represents The current state for a virtual
            router. 'INIT' indicates that virtual router is waiting for a
            startup event.
            	'BACKUP' indicates the virtual router is monitoring the availability of the master router. 'MASTER' indicates
            	that the virtual router is forwarding packets for IP address that are associated with this router.
            	Fabric Engine (VOSS) does not provide support for UNKNOWN value.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: >
            List of VlanId. VlanId represents This object represents a VLAN ID
            object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vrrp_id:
          description: |
            List of VrrpId. VrrpId represents VRRP identifier
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: AssetVrrpStateGetRequest
      type: object
    AssetVrrpStateGetResponse:
      properties:
        AssetVrrpState:
          items:
            $ref: "#/components/schemas/AssetVrrpState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrrpStateGetResponse
      type: object
    AssetVrrpVirtualIpAddressConfig:
      description: AssetVrrpVirtualIpAddressConfig - This object represents the list
        of virtual IP addresses for this virtual router.
      properties:
        address:
          description: >-
            The list of virtual IP addresses for this virtual router.
            	Fabric Engine and Switch Engine supports multiple virtual IP addresses
            	per virtual router. On Switch Engine all virtual IP addresses on a given
            	virtual router must be the same address type (Ipv4 or Ipv6). Fabric Engine
            	supports both Ipv4 and Ipv6 address types on a single virtual router.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_version:
          description: IP Version
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrrp_config_id:
          description: VRRP Config Id
          type: string
      required:
        - address
        - ip_version
        - vrrp_config_id
      type: object
    AssetVrrpVirtualIpAddressConfigGetRequest:
      description: AssetVrrpVirtualIpAddressConfigGetRequest represents the retrieval
        request for AssetVrrpVirtualIpAddressConfig.
      properties:
        address:
          description: >
            List of Address. Address represents The list of virtual IP addresses
            for this virtual router.
            	Fabric Engine and Switch Engine supports multiple virtual IP addresses
            	per virtual router. On Switch Engine all virtual IP addresses on a given
            	virtual router must be the same address type (Ipv4 or Ipv6). Fabric Engine
            	supports both Ipv4 and Ipv6 address types on a single virtual router.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_version:
          description: |
            List of IpVersion. IpVersion represents IP Version
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrrp_config_id:
          description: |
            List of VrrpConfigId. VrrpConfigId represents VRRP Config Id
          items:
            type: string
          type: array
      title: AssetVrrpVirtualIpAddressConfigGetRequest
      type: object
    AssetVrrpVirtualIpAddressConfigGetResponse:
      properties:
        AssetVrrpVirtualIpAddressConfig:
          items:
            $ref: "#/components/schemas/AssetVrrpVirtualIpAddressConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrrpVirtualIpAddressConfigGetResponse
      type: object
    AssetVrrpVirtualIpAddressState:
      description: AssetVrrpVirtualIpAddressState - This object represents the state
        of virtual IP addresses for this virtual router.
      properties:
        address:
          description: >-
            The list of virtual IP addresses for this virtual router.
            	Fabric Engine and Switch Engine supports multiple virtual IP addresses
            	per virtual router. On Switch Engine all virtual IP addresses on a given
            	virtual router must be the same address type (Ipv4 or Ipv6). Fabric Engine
            	supports both Ipv4 and Ipv6 address types on a single virtual router.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        ip_version:
          description: IP Version
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrrp_state_id:
          description: VRRP State Id.
          type: string
      required:
        - address
        - ip_version
        - vrrp_state_id
      type: object
    AssetVrrpVirtualIpAddressStateGetRequest:
      description: AssetVrrpVirtualIpAddressStateGetRequest represents the retrieval
        request for AssetVrrpVirtualIpAddressState.
      properties:
        address:
          description: >
            List of Address. Address represents The list of virtual IP addresses
            for this virtual router.
            	Fabric Engine and Switch Engine supports multiple virtual IP addresses
            	per virtual router. On Switch Engine all virtual IP addresses on a given
            	virtual router must be the same address type (Ipv4 or Ipv6). Fabric Engine
            	supports both Ipv4 and Ipv6 address types on a single virtual router.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_version:
          description: |
            List of IpVersion. IpVersion represents IP Version
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrrp_state_id:
          description: |
            List of VrrpStateId. VrrpStateId represents VRRP State Id.
          items:
            type: string
          type: array
      title: AssetVrrpVirtualIpAddressStateGetRequest
      type: object
    AssetVrrpVirtualIpAddressStateGetResponse:
      properties:
        AssetVrrpVirtualIpAddressState:
          items:
            $ref: "#/components/schemas/AssetVrrpVirtualIpAddressState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetVrrpVirtualIpAddressStateGetResponse
      type: object
    AssetWirelessInterface:
      description: AssetWirelessInterface - This object represents the asset wireless
        interface.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: Admin state
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        name:
          description: Interface Name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - asset_interface_id
        - name
      type: object
    AssetWirelessInterfaceGetRequest:
      description: AssetWirelessInterfaceGetRequest represents the retrieval request
        for AssetWirelessInterface.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents Admin state
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Interface Name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetWirelessInterfaceGetRequest
      type: object
    AssetWirelessInterfaceGetResponse:
      properties:
        AssetWirelessInterface:
          items:
            $ref: "#/components/schemas/AssetWirelessInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetWirelessInterfaceGetResponse
      type: object
    AssetWirelessInterfaceState:
      description: AssetWirelessInterfaceState - This object represents the asset
        wireless interface state.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        asset_interface_id:
          description: Interface Id
          type: string
        band:
          description: Band
          type: string
        bssid:
          description: BSSID
          type: string
        channel:
          description: Channel
          type: integer
        channel_width:
          description: Channel Width
          type: integer
        chassis_id:
          description: Chassis ID
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        eirp:
          description: EIRP
          type: number
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        if_index:
          description: Interface Index
          format: int64
          minimum: 0
          type: integer
        ip_address:
          description: IP Address
          type: string
        name:
          description: Interface Name
          type: string
        net_mask:
          description: Net Mask
          type: string
        noise_floor:
          description: Noise Floor
          format: int64
          type: integer
        oper_state:
          description: Operational state
          type: string
        operation_mode:
          description: Operation Mode
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        power:
          description: Power
          type: integer
        radio_mode:
          description: Radio Mode
          type: string
        ssid_name:
          description: SSID Name
          type: string
        system_name:
          description: System Name
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_device_id
        - asset_interface_id
        - name
      type: object
    AssetWirelessInterfaceStateGetRequest:
      description: AssetWirelessInterfaceStateGetRequest represents the retrieval
        request for AssetWirelessInterfaceState.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        band:
          description: |
            List of Band. Band represents Band
          items:
            type: string
          type: array
        bssid:
          description: |
            List of Bssid. Bssid represents BSSID
          items:
            type: string
          type: array
        channel:
          description: |
            List of Channel. Channel represents Channel
          items:
            type: integer
          type: array
        channel_width:
          description: |
            List of ChannelWidth. ChannelWidth represents Channel Width
          items:
            type: integer
          type: array
        chassis_id:
          description: |
            List of ChassisId. ChassisId represents Chassis ID
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        eirp:
          description: |
            List of Eirp. Eirp represents EIRP
          items:
            type: number
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        if_index:
          description: |
            List of IfIndex. IfIndex represents Interface Index
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        ip_address:
          description: |
            List of IpAddress. IpAddress represents IP Address
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Interface Name
          items:
            type: string
          type: array
        net_mask:
          description: |
            List of NetMask. NetMask represents Net Mask
          items:
            type: string
          type: array
        noise_floor:
          description: |
            List of NoiseFloor. NoiseFloor represents Noise Floor
          items:
            format: int64
            type: integer
          type: array
        oper_state:
          description: |
            List of OperState. OperState represents Operational state
          items:
            type: string
          type: array
        operation_mode:
          description: |
            List of OperationMode. OperationMode represents Operation Mode
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        power:
          description: |
            List of Power. Power represents Power
          items:
            type: integer
          type: array
        radio_mode:
          description: |
            List of RadioMode. RadioMode represents Radio Mode
          items:
            type: string
          type: array
        ssid_name:
          description: |
            List of SsidName. SsidName represents SSID Name
          items:
            type: string
          type: array
        system_name:
          description: |
            List of SystemName. SystemName represents System Name
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: AssetWirelessInterfaceStateGetRequest
      type: object
    AssetWirelessInterfaceStateGetResponse:
      properties:
        AssetWirelessInterfaceState:
          items:
            $ref: "#/components/schemas/AssetWirelessInterfaceState"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: AssetWirelessInterfaceStateGetResponse
      type: object
    Error:
      description: Standard JSON error
      properties:
        message:
          type: string
        statusCode:
          maximum: 600
          minimum: 100
          type: integer
      type: object
    InferredArea:
      description: InferredArea - This object describes the inferred area list.
        InferredDevice can be created while creating InferredArea.
      properties:
        address:
          description: |-
            A manually configured ISIS manual area address
            	for this system. This attribute is available in
            	both areas.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_id:
          description: ID of the parent Fabric
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_device:
          items:
            $ref: "#/components/schemas/InferredDevice"
          nullable: true
          type: array
        name:
          description: |-
            The ISIS system area-name. This attribute is
            	available in both areas and if the device does
            	not support multi-area, it won`t be included
            	in the home response
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredAreaGetRequest:
      description: InferredAreaGetRequest represents the retrieval request for InferredArea.
      properties:
        address:
          description: >
            List of Address. Address represents A manually configured ISIS
            manual area address
            	for this system. This attribute is available in
            	both areas.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_id:
          description: |
            List of FabricId. FabricId represents ID of the parent Fabric
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: >
            List of Name. Name represents The ISIS system area-name. This
            attribute is
            	available in both areas and if the device does
            	not support multi-area, it won`t be included
            	in the home response
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredAreaGetRequest
      type: object
    InferredAreaGetResponse:
      properties:
        InferredArea:
          items:
            $ref: "#/components/schemas/InferredArea"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredAreaGetResponse
      type: object
    InferredBoundaryDetails:
      description: InferredBoundaryDetails - This object describes the boundary
        details list. InferredIsisRemoteDetails can be created while creating
        InferredBoundaryDetails.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_spbm_detail_id:
          description: ID of the parent device-spbm-detail
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_isis_remote_details:
          items:
            $ref: "#/components/schemas/InferredIsisRemoteDetails"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredBoundaryDetailsGetRequest:
      description: InferredBoundaryDetailsGetRequest represents the retrieval request
        for InferredBoundaryDetails.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_spbm_detail_id:
          description: >
            List of DeviceSpbmDetailId. DeviceSpbmDetailId represents ID of the
            parent device-spbm-detail
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredBoundaryDetailsGetRequest
      type: object
    InferredBoundaryDetailsGetResponse:
      properties:
        InferredBoundaryDetails:
          items:
            $ref: "#/components/schemas/InferredBoundaryDetails"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredBoundaryDetailsGetResponse
      type: object
    InferredCluster:
      description: InferredCluster - This object represents the Inferred Cluster
        information.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_one_id:
          description: User device one that is part of the cluster
          type: string
        device_one_peer_name:
          description: A unique name assigned to primary device in the cluster.
          type: string
        device_two_id:
          description: User device two that is part of the cluster
          type: string
        device_two_peer_name:
          description: A unique name assigned to second device in the cluster.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        local_one_identifier:
          description: >-
            Local identifier for primary node. Local Identifier will be local
            switch MAC addresses in case of EXOS devices.
            	In case of VOSS devices, they represent local sysID.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        peer_one_ip_address:
          description: IP address of primary or initial node in the cluster.
          type: string
        peer_two_ip_address:
          description: IP address of second node in the cluster.
          type: string
        remote_one_identifier:
          description: >-
            Remote identifier for primary node. Remote Identifier will be remote
            switch MAC addresses in case of EXOS devices.
            	In case of VOSS devices, they represent remote sysID
          type: string
        type:
          description: Cluster Type.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredClusterGetRequest:
      description: InferredClusterGetRequest represents the retrieval request for
        InferredCluster.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_one_id:
          description: >
            List of DeviceOneId. DeviceOneId represents User device one that is
            part of the cluster
          items:
            type: string
          type: array
        device_one_peer_name:
          description: >
            List of DeviceOnePeerName. DeviceOnePeerName represents A unique
            name assigned to primary device in the cluster.
          items:
            type: string
          type: array
        device_two_id:
          description: >
            List of DeviceTwoId. DeviceTwoId represents User device two that is
            part of the cluster
          items:
            type: string
          type: array
        device_two_peer_name:
          description: >
            List of DeviceTwoPeerName. DeviceTwoPeerName represents A unique
            name assigned to second device in the cluster.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        local_one_identifier:
          description: >
            List of LocalOneIdentifier. LocalOneIdentifier represents Local
            identifier for primary node. Local Identifier will be local switch
            MAC addresses in case of EXOS devices.
            	In case of VOSS devices, they represent local sysID.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        peer_one_ip_address:
          description: >
            List of PeerOneIpAddress. PeerOneIpAddress represents IP address of
            primary or initial node in the cluster.
          items:
            type: string
          type: array
        peer_two_ip_address:
          description: >
            List of PeerTwoIpAddress. PeerTwoIpAddress represents IP address of
            second node in the cluster.
          items:
            type: string
          type: array
        remote_one_identifier:
          description: >
            List of RemoteOneIdentifier. RemoteOneIdentifier represents Remote
            identifier for primary node. Remote Identifier will be remote switch
            MAC addresses in case of EXOS devices.
            	In case of VOSS devices, they represent remote sysID
          items:
            type: string
          type: array
        type:
          description: |
            List of Type. Type represents Cluster Type.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredClusterGetRequest
      type: object
    InferredClusterGetResponse:
      properties:
        InferredCluster:
          items:
            $ref: "#/components/schemas/InferredCluster"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredClusterGetResponse
      type: object
    InferredCustomerVlanUni:
      description: InferredCustomerVlanUni - This object describes the customer vlan list.
      nullable: true
      properties:
        associated_vlan_numbers:
          description: >-
            Vlan IDs associated with this vlan. For a PVLAN, when id is
            	Primary/Secondary VLAN ID, this will be Secondary/Primary VLAN IDs respectively.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        isid:
          description: A value that uniquely identifies the Layer 2 services instances
            network-wide.
          format: int64
          minimum: 0
          type: integer
        l2_access_id:
          description: L2 Access Id.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID.
          format: int64
          minimum: 0
          type: integer
        vlan_type:
          description: Vlan types
          format: int64
          type: integer
      required:
        - device_id
        - l2_access_id
        - vlan_number
        - vlan_type
      type: object
    InferredCustomerVlanUniGetRequest:
      description: InferredCustomerVlanUniGetRequest represents the retrieval request
        for InferredCustomerVlanUni.
      properties:
        associated_vlan_numbers:
          description: >
            List of AssociatedVlanNumbers. AssociatedVlanNumbers represents Vlan
            IDs associated with this vlan. For a PVLAN, when id is
            	Primary/Secondary VLAN ID, this will be Secondary/Primary VLAN IDs respectively.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 services instances network-wide.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l2_access_id:
          description: |
            List of L2AccessId. L2AccessId represents L2 Access Id.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_type:
          description: |
            List of VlanType. VlanType represents Vlan types
          items:
            format: int64
            type: integer
          type: array
      title: InferredCustomerVlanUniGetRequest
      type: object
    InferredCustomerVlanUniGetResponse:
      properties:
        InferredCustomerVlanUni:
          items:
            $ref: "#/components/schemas/InferredCustomerVlanUni"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredCustomerVlanUniGetResponse
      type: object
    InferredDevice:
      description: InferredDevice - This object represents the Inferred device
        information.
        InferredDeviceFaDetail,InferredDeviceSpbmDetail,InferredDiscoveryStatusError,InferredInterface,DeviceL2EpDetail,DeviceL3EpDetail,InferredLag,InferredSlot,DeviceVlanDetail,DeviceVrfDetail
        can be created while creating InferredDevice.
      nullable: true
      properties:
        area_id:
          description: ID of the parent Area
          type: string
        asset_device_id:
          description: Asset Device Id
          type: string
        chassis_id:
          description: >-
            The string value used to identify the chassis component
            	associated with the local system as described by IEEE 802.1AB-2005 9.5.2.3.
          type: string
        common_service_id:
          description: ID of the common Service
          format: int64
          minimum: 0
          type: integer
        connection_status:
          description: Connection Status.
          format: int64
          type: integer
        connection_timestamp:
          description: Connection timestamp.
          format: date-time
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        description:
          description: Device type name (must be unique within a given profile).
          type: string
        device_l2_ep_detail:
          items:
            $ref: "#/components/schemas/InferredL2ServiceEndPoint"
          nullable: true
          type: array
        device_l3_ep_detail:
          items:
            $ref: "#/components/schemas/InferredL3ServiceEndPoint"
          nullable: true
          type: array
        device_type:
          description: Device Type.
          format: int64
          type: integer
        device_vlan_detail:
          items:
            $ref: "#/components/schemas/InferredVlan"
          nullable: true
          type: array
        device_vrf_detail:
          items:
            $ref: "#/components/schemas/InferredVrf"
          nullable: true
          type: array
        discovery_status_last_update_time:
          description: |-
            Holds the time at which above parameter
            	discovery-status is last updated.
          format: date-time
          type: string
        discovery_status_trace_id:
          description: trace ID of the discovery status.
          type: string
        extreme_common_types_discovery_status_enum:
          description: Discovery status of the device.
          format: int64
          type: integer
        fabric_device_state:
          description: State of fabric, outside of configuration.
          format: int64
          type: integer
        firmware_version:
          description: |-
            The vendor-specific firmware revision
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          type: string
        functional_device_type:
          description: Functional Device Type.
          format: int64
          type: integer
        group_id:
          description: ID of the parent Group
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_device_fa_detail:
          $ref: "#/components/schemas/InferredDeviceFaDetail"
        inferred_device_spbm_detail:
          $ref: "#/components/schemas/InferredDeviceSpbmDetail"
        inferred_discovery_status_error:
          items:
            $ref: "#/components/schemas/InferredDiscoveryStatusError"
          nullable: true
          type: array
        inferred_interface:
          items:
            $ref: "#/components/schemas/InferredInterface"
          nullable: true
          type: array
        inferred_lag:
          items:
            $ref: "#/components/schemas/InferredLag"
          nullable: true
          type: array
        inferred_slot:
          items:
            $ref: "#/components/schemas/InferredSlot"
          nullable: true
          type: array
        ip_address:
          description: IP address (Ipv4 or Ipv6) of the device.
          type: string
        is_stack:
          description: |-
            Stack state. This attribute is Switch Engine
            	(EXOS) applicable only.
          type: boolean
        location:
          description: Location of the Device.
          type: string
        mac_address:
          description: Mac Address of the device
          type: string
        managed:
          description: |-
            Managed indicates that the device is actively
            	managed
          type: boolean
        model_name:
          description: Device model name.
          type: string
        name:
          description: Device type name (must be unique within a given profile).
          type: string
        operating_system:
          description: Operating System.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        scheduler_refresh_in_progress:
          description: |-
            Indicates if scheduler refresh for this device
            	is in progress.
          type: boolean
        scheduler_refresh_update_time:
          description: |-
            Holds the time at which above parameter
            	SchedulerRefreshInProgress is updated.
          format: date-time
          type: string
        serial_number:
          description: Device unique serial number.
          type: string
        stack_id:
          description: Stack ID
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        uptime:
          description: |-
            The amount of time since the network operating system was
            	initialized.
          format: int64
          minimum: 0
          type: integer
      required:
        - owner_id
        - mac_address
      type: object
    InferredDeviceFaDetail:
      description: InferredDeviceFaDetail - This object describes the device fa
        details list.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        device_system_id:
          description: |-
            DeviceSystemID - Fabric Attach System ID. MAC
            	Address of device. Commonly displayed with link
            	info append to end.
            	Managed: Device MAC.
            	Unmanaged: Device MAC. Remote
            	AssetFabricAttachStateDiscoveryElement's
            	system_id when Device MAC is invalid.
          type: string
        fa_assignment_timeout:
          description: |-
            FAAssignmentTimeout - Timeout until Agent
            	assignment is aged out.
            	Managed: AssetFabricAttachConfig's
            	assignment_timeout.
          format: int64
          minimum: 0
          type: integer
        fa_discovery_timeout:
          description: |-
            FADiscoveryTimeout - Timeout until Agent
            	assignment is aged out.
            	Managed: AssetFabricAttachConfig's
            	discovery_timeout.
          format: int64
          minimum: 0
          type: integer
        fa_forward_mgmt_vlan_enabled:
          description: |-
            FAForwardMgmtVLANEnabled - Global flag
            	stating if Fabric Attach Proxy should send
            	management VLAN data to clients.
            	Managed: AssetFabricAttachConfig's forwarding.
          type: boolean
        fa_forward_mgmt_vlan_port_list:
          description: |-
            FAForwardMgmtVLANPortList - List of all ports
            	with FA Proxy management VLAN transmission to
            	clients enabled.
            	Managed: AssetFabricAttachConfigPorts'
            	forwarding.
          type: string
        fa_server_id:
          description: |-
            FAServerID - SystemID of Primary Fabric
            	Attach Server to this proxy device.
            	Managed: Local
            	AssetFabricAttachStateDiscoveryElement's type
            	SERVER
          type: string
        fa_server_name:
          description: |-
            FAServerName - Primary Fabric Attach Server
            	Device name
            	Managed: FAServerID's inferred device
            	description.
          type: string
        fa_standalone_proxy_uplink:
          description: |-
            FaStandaloneProxyUplink - The uplink port
            	name for standalone proxy operation.
            	Managed: AssetFabricAttachConfig's uplink_port
          type: string
        fa_type:
          description: |-
            FAType - Fabric Attach mode the device is
            	running in.
            	Managed: AssetFabricAttachConfig's
            	fabric_attach_mode.
            	Unmanaged: Remote
            	AssetFabricAttachStateDiscoveryElement's type.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        mgmt_vlan:
          description: |-
            MgmtVlan - The VLAN advertised to fabric
            	attach clients for them to use as the
            	management VLAN.
            	Managed: AssetFabricAttachConfig's mgmt_vlan.
            	Unmanaged: Remote
            	AssetFabricAttachStateDiscoveryElement's mgmt_vlan.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        remote_managed_asset_device_id:
          description: UUID of the device
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredDeviceFaDetailGetRequest:
      description: InferredDeviceFaDetailGetRequest represents the retrieval request
        for InferredDeviceFaDetail.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        device_system_id:
          description: >
            List of DeviceSystemId. DeviceSystemId represents DeviceSystemID -
            Fabric Attach System ID. MAC
            	Address of device. Commonly displayed with link
            	info append to end.
            	Managed: Device MAC.
            	Unmanaged: Device MAC. Remote
            	AssetFabricAttachStateDiscoveryElement's
            	system_id when Device MAC is invalid.
          items:
            type: string
          type: array
        fa_assignment_timeout:
          description: >
            List of FaAssignmentTimeout. FaAssignmentTimeout represents
            FAAssignmentTimeout - Timeout until Agent
            	assignment is aged out.
            	Managed: AssetFabricAttachConfig's
            	assignment_timeout.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        fa_discovery_timeout:
          description: >
            List of FaDiscoveryTimeout. FaDiscoveryTimeout represents
            FADiscoveryTimeout - Timeout until Agent
            	assignment is aged out.
            	Managed: AssetFabricAttachConfig's
            	discovery_timeout.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        fa_forward_mgmt_vlan_enabled:
          description: >
            List of FaForwardMgmtVlanEnabled. FaForwardMgmtVlanEnabled
            represents FAForwardMgmtVLANEnabled - Global flag
            	stating if Fabric Attach Proxy should send
            	management VLAN data to clients.
            	Managed: AssetFabricAttachConfig's forwarding.
          items:
            type: boolean
          type: array
        fa_forward_mgmt_vlan_port_list:
          description: >
            List of FaForwardMgmtVlanPortList. FaForwardMgmtVlanPortList
            represents FAForwardMgmtVLANPortList - List of all ports
            	with FA Proxy management VLAN transmission to
            	clients enabled.
            	Managed: AssetFabricAttachConfigPorts'
            	forwarding.
          items:
            type: string
          type: array
        fa_server_id:
          description: >
            List of FaServerId. FaServerId represents FAServerID - SystemID of
            Primary Fabric
            	Attach Server to this proxy device.
            	Managed: Local
            	AssetFabricAttachStateDiscoveryElement's type
            	SERVER
          items:
            type: string
          type: array
        fa_server_name:
          description: >
            List of FaServerName. FaServerName represents FAServerName - Primary
            Fabric Attach Server
            	Device name
            	Managed: FAServerID's inferred device
            	description.
          items:
            type: string
          type: array
        fa_standalone_proxy_uplink:
          description: >
            List of FaStandaloneProxyUplink. FaStandaloneProxyUplink represents
            FaStandaloneProxyUplink - The uplink port
            	name for standalone proxy operation.
            	Managed: AssetFabricAttachConfig's uplink_port
          items:
            type: string
          type: array
        fa_type:
          description: >
            List of FaType. FaType represents FAType - Fabric Attach mode the
            device is
            	running in.
            	Managed: AssetFabricAttachConfig's
            	fabric_attach_mode.
            	Unmanaged: Remote
            	AssetFabricAttachStateDiscoveryElement's type.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        mgmt_vlan:
          description: >
            List of MgmtVlan. MgmtVlan represents MgmtVlan - The VLAN advertised
            to fabric
            	attach clients for them to use as the
            	management VLAN.
            	Managed: AssetFabricAttachConfig's mgmt_vlan.
            	Unmanaged: Remote
            	AssetFabricAttachStateDiscoveryElement's mgmt_vlan.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        remote_managed_asset_device_id:
          description: >
            List of RemoteManagedAssetDeviceId. RemoteManagedAssetDeviceId
            represents UUID of the device
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredDeviceFaDetailGetRequest
      type: object
    InferredDeviceFaDetailGetResponse:
      properties:
        InferredDeviceFaDetail:
          items:
            $ref: "#/components/schemas/InferredDeviceFaDetail"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredDeviceFaDetailGetResponse
      type: object
    InferredDeviceGetRequest:
      description: InferredDeviceGetRequest represents the retrieval request for
        InferredDevice.
      properties:
        area_id:
          description: |
            List of AreaId. AreaId represents ID of the parent Area
          items:
            type: string
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Asset Device Id
          items:
            type: string
          type: array
        chassis_id:
          description: >
            List of ChassisId. ChassisId represents The string value used to
            identify the chassis component
            	associated with the local system as described by IEEE 802.1AB-2005 9.5.2.3.
          items:
            type: string
          type: array
        common_service_id:
          description: >
            List of CommonServiceId. CommonServiceId represents ID of the common
            Service
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        connection_status:
          description: >
            List of ConnectionStatus. ConnectionStatus represents Connection
            Status.
          items:
            format: int64
            type: integer
          type: array
        connection_timestamp:
          description: >
            List of ConnectionTimestamp. ConnectionTimestamp represents
            Connection timestamp.
          items:
            format: date-time
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        description:
          description: >
            List of Description. Description represents Device type name (must
            be unique within a given profile).
          items:
            type: string
          type: array
        device_type:
          description: |
            List of DeviceType. DeviceType represents Device Type.
          items:
            format: int64
            type: integer
          type: array
        discovery_status_last_update_time:
          description: >
            List of DiscoveryStatusLastUpdateTime. DiscoveryStatusLastUpdateTime
            represents Holds the time at which above parameter
            	discovery-status is last updated.
          items:
            format: date-time
            type: string
          type: array
        discovery_status_trace_id:
          description: >
            List of DiscoveryStatusTraceId. DiscoveryStatusTraceId represents
            trace ID of the discovery status.
          items:
            type: string
          type: array
        extreme_common_types_discovery_status_enum:
          description: >
            List of ExtremeCommonTypesDiscoveryStatusEnum.
            ExtremeCommonTypesDiscoveryStatusEnum represents Discovery status of
            the device.
          items:
            format: int64
            type: integer
          type: array
        fabric_device_state:
          description: >
            List of FabricDeviceState. FabricDeviceState represents State of
            fabric, outside of configuration.
          items:
            format: int64
            type: integer
          type: array
        firmware_version:
          description: >
            List of FirmwareVersion. FirmwareVersion represents The
            vendor-specific firmware revision
            	For details see ANSI/TIA-1057, Section 10.2.6.2.
          items:
            type: string
          type: array
        functional_device_type:
          description: >
            List of FunctionalDeviceType. FunctionalDeviceType represents
            Functional Device Type.
          items:
            format: int64
            type: integer
          type: array
        group_id:
          description: |
            List of GroupId. GroupId represents ID of the parent Group
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        ip_address:
          description: >
            List of IpAddress. IpAddress represents IP address (Ipv4 or Ipv6) of
            the device.
          items:
            type: string
          type: array
        is_stack:
          description: >
            List of IsStack. IsStack represents Stack state. This attribute is
            Switch Engine
            	(EXOS) applicable only.
          items:
            type: boolean
          type: array
        location:
          description: |
            List of Location. Location represents Location of the Device.
          items:
            type: string
          type: array
        mac_address:
          description: |
            List of MacAddress. MacAddress represents Mac Address of the device
          items:
            type: string
          type: array
        managed:
          description: >
            List of Managed. Managed represents Managed indicates that the
            device is actively
            	managed
          items:
            type: boolean
          type: array
        model_name:
          description: |
            List of ModelName. ModelName represents Device model name.
          items:
            type: string
          type: array
        name:
          description: >
            List of Name. Name represents Device type name (must be unique
            within a given profile).
          items:
            type: string
          type: array
        operating_system:
          description: >
            List of OperatingSystem. OperatingSystem represents Operating System.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        scheduler_refresh_in_progress:
          description: >
            List of SchedulerRefreshInProgress. SchedulerRefreshInProgress
            represents Indicates if scheduler refresh for this device
            	is in progress.
          items:
            type: boolean
          type: array
        scheduler_refresh_update_time:
          description: >
            List of SchedulerRefreshUpdateTime. SchedulerRefreshUpdateTime
            represents Holds the time at which above parameter
            	SchedulerRefreshInProgress is updated.
          items:
            format: date-time
            type: string
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        stack_id:
          description: |
            List of StackId. StackId represents Stack ID
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        uptime:
          description: >
            List of Uptime. Uptime represents The amount of time since the
            network operating system was
            	initialized.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: InferredDeviceGetRequest
      type: object
    InferredDeviceGetResponse:
      properties:
        InferredDevice:
          items:
            $ref: "#/components/schemas/InferredDevice"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredDeviceGetResponse
      type: object
    InferredDeviceSpbmDetail:
      description: InferredDeviceSpbmDetail - This object describes the device spbm
        details list. InferredBoundaryDetails,InferredIsisDetails can be created
        while creating InferredDeviceSpbmDetail.
      nullable: true
      properties:
        bvlan1:
          description: Backbone vlan ID 1
          format: int64
          minimum: 0
          type: integer
        bvlan2:
          description: Backbone vlan ID 2
          format: int64
          minimum: 0
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: ID of the parent device
          type: string
        global_spbm_detail_id:
          description: ID of the parent global-spbm-detail
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_boundary_details:
          $ref: "#/components/schemas/InferredBoundaryDetails"
        inferred_isis_details:
          $ref: "#/components/schemas/InferredIsisDetails"
        instance_name:
          description: Instance Name
          format: int64
          minimum: 0
          type: integer
        is_boundary_node:
          description: Indicates if the node is a boundary node
          type: boolean
        isis_admin_state:
          description: Admin state of isis on device
          format: int64
          type: integer
        nick_name:
          description: Nick name of the SPBM node.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        spbm_state:
          description: Admin state of spbm on device
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredDeviceSpbmDetailGetRequest:
      description: InferredDeviceSpbmDetailGetRequest represents the retrieval request
        for InferredDeviceSpbmDetail.
      properties:
        bvlan1:
          description: |
            List of Bvlan1. Bvlan1 represents Backbone vlan ID 1
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        bvlan2:
          description: |
            List of Bvlan2. Bvlan2 represents Backbone vlan ID 2
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents ID of the parent device
          items:
            type: string
          type: array
        global_spbm_detail_id:
          description: >
            List of GlobalSpbmDetailId. GlobalSpbmDetailId represents ID of the
            parent global-spbm-detail
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instance_name:
          description: |
            List of InstanceName. InstanceName represents Instance Name
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        is_boundary_node:
          description: >
            List of IsBoundaryNode. IsBoundaryNode represents Indicates if the
            node is a boundary node
          items:
            type: boolean
          type: array
        isis_admin_state:
          description: >
            List of IsisAdminState. IsisAdminState represents Admin state of
            isis on device
          items:
            format: int64
            type: integer
          type: array
        nick_name:
          description: |
            List of NickName. NickName represents Nick name of the SPBM node.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        spbm_state:
          description: >
            List of SpbmState. SpbmState represents Admin state of spbm on device
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredDeviceSpbmDetailGetRequest
      type: object
    InferredDeviceSpbmDetailGetResponse:
      properties:
        InferredDeviceSpbmDetail:
          items:
            $ref: "#/components/schemas/InferredDeviceSpbmDetail"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredDeviceSpbmDetailGetResponse
      type: object
    InferredDiscoveryStatusError:
      description: InferredDiscoveryStatusError - This object represents the error
        object during discovery
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        error_code:
          description: Integer error code
          format: int64
          type: integer
        error_detail:
          description: Detailed error description
          type: string
        error_message:
          description: Short error message
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredDiscoveryStatusErrorGetRequest:
      description: InferredDiscoveryStatusErrorGetRequest represents the retrieval
        request for InferredDiscoveryStatusError.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        error_code:
          description: |
            List of ErrorCode. ErrorCode represents Integer error code
          items:
            format: int64
            type: integer
          type: array
        error_detail:
          description: >
            List of ErrorDetail. ErrorDetail represents Detailed error
            description
          items:
            type: string
          type: array
        error_message:
          description: |
            List of ErrorMessage. ErrorMessage represents Short error message
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredDiscoveryStatusErrorGetRequest
      type: object
    InferredDiscoveryStatusErrorGetResponse:
      properties:
        InferredDiscoveryStatusError:
          items:
            $ref: "#/components/schemas/InferredDiscoveryStatusError"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredDiscoveryStatusErrorGetResponse
      type: object
    InferredFabric:
      description: InferredFabric - This object describes the fabric list.
        InferredArea,InferredGlobalSpbmDetails can be created while creating
        InferredFabric.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        description:
          description: Description of the fabric
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_area:
          items:
            $ref: "#/components/schemas/InferredArea"
          nullable: true
          type: array
        inferred_global_spbm_details:
          $ref: "#/components/schemas/InferredGlobalSpbmDetails"
        name:
          description: Name of Fabric
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredFabricAttachDomain:
      description: InferredFabricAttachDomain - This object describes the fabric
        attach domain list. FabricAttachPorts,VlanAssignments can be created
        while creating InferredFabricAttachDomain.
      properties:
        connected_client:
          description: Connected client.
          items:
            type: string
          nullable: true
          type: array
        connected_proxy:
          description: Connected proxy.
          items:
            type: string
          nullable: true
          type: array
        connected_server:
          description: Connected server.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        fabric_attach_ports:
          items:
            $ref: "#/components/schemas/InferredFabricAttachPort"
          nullable: true
          type: array
        fabric_attach_role:
          description: Fabric attach role.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_assignments:
          items:
            $ref: "#/components/schemas/InferredFabricAttachVlan"
          nullable: true
          type: array
      required:
        - device_id
      type: object
    InferredFabricAttachDomainGetRequest:
      description: InferredFabricAttachDomainGetRequest represents the retrieval
        request for InferredFabricAttachDomain.
      properties:
        connected_client:
          description: >
            List of ConnectedClient. ConnectedClient represents Connected client.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        connected_proxy:
          description: |
            List of ConnectedProxy. ConnectedProxy represents Connected proxy.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        connected_server:
          description: >
            List of ConnectedServer. ConnectedServer represents Connected server.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        fabric_attach_role:
          description: >
            List of FabricAttachRole. FabricAttachRole represents Fabric attach
            role.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredFabricAttachDomainGetRequest
      type: object
    InferredFabricAttachDomainGetResponse:
      properties:
        InferredFabricAttachDomain:
          items:
            $ref: "#/components/schemas/InferredFabricAttachDomain"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricAttachDomainGetResponse
      type: object
    InferredFabricAttachPort:
      description: InferredFabricAttachPort - This object describes the fabric attach
        port list. VlanAssignments can be created while creating
        InferredFabricAttachPort.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        fabric_attach_domain_id:
          description: Fabric attach domain Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_assignments:
          items:
            $ref: "#/components/schemas/InferredFabricAttachVlan"
          nullable: true
          type: array
      required:
        - device_id
        - fabric_attach_domain_id
        - interface_name
        - interface_type
      type: object
    InferredFabricAttachPortGetRequest:
      description: InferredFabricAttachPortGetRequest represents the retrieval request
        for InferredFabricAttachPort.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        fabric_attach_domain_id:
          description: >
            List of FabricAttachDomainId. FabricAttachDomainId represents Fabric
            attach domain Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredFabricAttachPortGetRequest
      type: object
    InferredFabricAttachPortGetResponse:
      properties:
        InferredFabricAttachPort:
          items:
            $ref: "#/components/schemas/InferredFabricAttachPort"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricAttachPortGetResponse
      type: object
    InferredFabricAttachVlan:
      description: InferredFabricAttachVlan - This object describes the fabric attach
        vlan list.
      properties:
        assignment_origin:
          description: Assignment origin.
          format: int64
          type: integer
        assignment_state:
          description: Assignment state.
          format: int64
          type: integer
        assignment_type:
          description: Assignment type.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        fabric_attach_domain_id:
          description: Fabric attach domain Id
          type: string
        fabric_attach_port_id:
          description: Fabric attach port Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        isid:
          description: Isid.
          format: int64
          minimum: 0
          type: integer
        isid_name:
          description: Isid Name.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID. For PVLAN, this will be
            Primary/Secondary VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - interface_name
        - interface_type
        - isid
        - vlan_number
      type: object
    InferredFabricAttachVlanGetRequest:
      description: InferredFabricAttachVlanGetRequest represents the retrieval request
        for InferredFabricAttachVlan.
      properties:
        assignment_origin:
          description: >
            List of AssignmentOrigin. AssignmentOrigin represents Assignment
            origin.
          items:
            format: int64
            type: integer
          type: array
        assignment_state:
          description: >
            List of AssignmentState. AssignmentState represents Assignment state.
          items:
            format: int64
            type: integer
          type: array
        assignment_type:
          description: |
            List of AssignmentType. AssignmentType represents Assignment type.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        fabric_attach_domain_id:
          description: >
            List of FabricAttachDomainId. FabricAttachDomainId represents Fabric
            attach domain Id
          items:
            type: string
          type: array
        fabric_attach_port_id:
          description: >
            List of FabricAttachPortId. FabricAttachPortId represents Fabric
            attach port Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: |
            List of Isid. Isid represents Isid.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        isid_name:
          description: |
            List of IsidName. IsidName represents Isid Name.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID. For PVLAN, this will be Primary/Secondary VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: InferredFabricAttachVlanGetRequest
      type: object
    InferredFabricAttachVlanGetResponse:
      properties:
        InferredFabricAttachVlan:
          items:
            $ref: "#/components/schemas/InferredFabricAttachVlan"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricAttachVlanGetResponse
      type: object
    InferredFabricGetRequest:
      description: InferredFabricGetRequest represents the retrieval request for
        InferredFabric.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        description:
          description: >
            List of Description. Description represents Description of the fabric
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Name of Fabric
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredFabricGetRequest
      type: object
    InferredFabricGetResponse:
      properties:
        InferredFabric:
          items:
            $ref: "#/components/schemas/InferredFabric"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricGetResponse
      type: object
    InferredFabricLink:
      description: InferredFabricLink - DB model for links between fabric devices.
        FaDetail can be created while creating InferredFabricLink.
      nullable: true
      properties:
        adj_state:
          description: Fabric links adjacency active state
          format: int64
          type: integer
        admin_status:
          description: Admin status of the fabric link
          format: int64
          type: integer
        admin_update_time:
          description: The amount of time since the last admin status was updated.
          format: date-time
          type: string
        area_address:
          description: Area of the address
          type: string
        asset_fa_config_one_id:
          description: Asset fabric attach config one
          type: string
        asset_fa_config_two_id:
          description: Asset fabric attach config two
          type: string
        asset_fa_element_one_id:
          description: Asset fabric attach state discovery element one
          type: string
        asset_fa_element_two_id:
          description: Asset fabric attach state discovery element two
          type: string
        asset_isis_circuit_config_one_id:
          description: Asset ISIS circuit config one
          type: string
        asset_isis_circuit_config_two_id:
          description: Asset ISIS circuit config two
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_one_id:
          description: User device one that form the fabric link
          type: string
        device_two_id:
          description: User device two that form the fabric link
          type: string
        fa_detail:
          $ref: "#/components/schemas/InferredFabricLinkFaDetail"
        fabric_link_type:
          description: Type of the fabric link
          format: int64
          type: integer
        fabric_topology_id:
          description: Fabric topology
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name_one:
          description: Interface one name
          type: string
        interface_name_two:
          description: Interface two name
          type: string
        interface_type_one:
          description: Interface one type
          format: int64
          type: integer
        interface_type_two:
          description: Interface two type
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        physical_link_id:
          description: Physical Link
          type: string
        status:
          description: Link Status
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredFabricLinkFaDetail:
      description: InferredFabricLinkFaDetail - DB model for fabric link fa detail
      nullable: true
      properties:
        active_fa_binding_one:
          description: |-
            The endpoints that have successfully completed the binding process
            	and are now fully integrated into the network fabric
          format: int64
          type: integer
        active_fa_binding_two:
          description: |-
            The endpoints that have successfully completed the binding process
            	and are now fully integrated into the network fabric
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_link_id:
          description: ID of fabric link
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        pending_fa_binding_one:
          description: The endpoints waiting to complete the binding process to the
            network fabric
          format: int64
          type: integer
        pending_fa_binding_two:
          description: The endpoints waiting to complete the binding process to the
            network fabric
          format: int64
          type: integer
        rejected_fa_binding_one:
          description: The endpoints failed to bind to the network fabric
          format: int64
          type: integer
        rejected_fa_binding_two:
          description: The endpoints failed to bind to the network fabric
          format: int64
          type: integer
        total_fa_binding_one:
          description: The total number of endpoints that have been successfully bound to
            the network fabric
          format: int64
          type: integer
        total_fa_binding_two:
          description: The total number of endpoints that have been successfully bound to
            the network fabric
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredFabricLinkFaDetailGetRequest:
      description: InferredFabricLinkFaDetailGetRequest represents the retrieval
        request for InferredFabricLinkFaDetail.
      properties:
        active_fa_binding_one:
          description: >
            List of ActiveFaBindingOne. ActiveFaBindingOne represents The
            endpoints that have successfully completed the binding process
            	and are now fully integrated into the network fabric
          items:
            format: int64
            type: integer
          type: array
        active_fa_binding_two:
          description: >
            List of ActiveFaBindingTwo. ActiveFaBindingTwo represents The
            endpoints that have successfully completed the binding process
            	and are now fully integrated into the network fabric
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_link_id:
          description: |
            List of FabricLinkId. FabricLinkId represents ID of fabric link
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        pending_fa_binding_one:
          description: >
            List of PendingFaBindingOne. PendingFaBindingOne represents The
            endpoints waiting to complete the binding process to the network
            fabric
          items:
            format: int64
            type: integer
          type: array
        pending_fa_binding_two:
          description: >
            List of PendingFaBindingTwo. PendingFaBindingTwo represents The
            endpoints waiting to complete the binding process to the network
            fabric
          items:
            format: int64
            type: integer
          type: array
        rejected_fa_binding_one:
          description: >
            List of RejectedFaBindingOne. RejectedFaBindingOne represents The
            endpoints failed to bind to the network fabric
          items:
            format: int64
            type: integer
          type: array
        rejected_fa_binding_two:
          description: >
            List of RejectedFaBindingTwo. RejectedFaBindingTwo represents The
            endpoints failed to bind to the network fabric
          items:
            format: int64
            type: integer
          type: array
        total_fa_binding_one:
          description: >
            List of TotalFaBindingOne. TotalFaBindingOne represents The total
            number of endpoints that have been successfully bound to the network
            fabric
          items:
            format: int64
            type: integer
          type: array
        total_fa_binding_two:
          description: >
            List of TotalFaBindingTwo. TotalFaBindingTwo represents The total
            number of endpoints that have been successfully bound to the network
            fabric
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredFabricLinkFaDetailGetRequest
      type: object
    InferredFabricLinkFaDetailGetResponse:
      properties:
        InferredFabricLinkFaDetail:
          items:
            $ref: "#/components/schemas/InferredFabricLinkFaDetail"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricLinkFaDetailGetResponse
      type: object
    InferredFabricLinkGetRequest:
      description: InferredFabricLinkGetRequest represents the retrieval request for
        InferredFabricLink.
      properties:
        adj_state:
          description: >
            List of AdjState. AdjState represents Fabric links adjacency active
            state
          items:
            format: int64
            type: integer
          type: array
        admin_status:
          description: >
            List of AdminStatus. AdminStatus represents Admin status of the
            fabric link
          items:
            format: int64
            type: integer
          type: array
        admin_update_time:
          description: >
            List of AdminUpdateTime. AdminUpdateTime represents The amount of
            time since the last admin status was updated.
          items:
            format: date-time
            type: string
          type: array
        area_address:
          description: |
            List of AreaAddress. AreaAddress represents Area of the address
          items:
            type: string
          type: array
        asset_fa_config_one_id:
          description: >
            List of AssetFaConfigOneId. AssetFaConfigOneId represents Asset
            fabric attach config one
          items:
            type: string
          type: array
        asset_fa_config_two_id:
          description: >
            List of AssetFaConfigTwoId. AssetFaConfigTwoId represents Asset
            fabric attach config two
          items:
            type: string
          type: array
        asset_fa_element_one_id:
          description: >
            List of AssetFaElementOneId. AssetFaElementOneId represents Asset
            fabric attach state discovery element one
          items:
            type: string
          type: array
        asset_fa_element_two_id:
          description: >
            List of AssetFaElementTwoId. AssetFaElementTwoId represents Asset
            fabric attach state discovery element two
          items:
            type: string
          type: array
        asset_isis_circuit_config_one_id:
          description: >
            List of AssetIsisCircuitConfigOneId. AssetIsisCircuitConfigOneId
            represents Asset ISIS circuit config one
          items:
            type: string
          type: array
        asset_isis_circuit_config_two_id:
          description: >
            List of AssetIsisCircuitConfigTwoId. AssetIsisCircuitConfigTwoId
            represents Asset ISIS circuit config two
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_one_id:
          description: >
            List of DeviceOneId. DeviceOneId represents User device one that
            form the fabric link
          items:
            type: string
          type: array
        device_two_id:
          description: >
            List of DeviceTwoId. DeviceTwoId represents User device two that
            form the fabric link
          items:
            type: string
          type: array
        fabric_link_type:
          description: >
            List of FabricLinkType. FabricLinkType represents Type of the fabric
            link
          items:
            format: int64
            type: integer
          type: array
        fabric_topology_id:
          description: >
            List of FabricTopologyId. FabricTopologyId represents Fabric topology
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name_one:
          description: >
            List of InterfaceNameOne. InterfaceNameOne represents Interface one
            name
          items:
            type: string
          type: array
        interface_name_two:
          description: >
            List of InterfaceNameTwo. InterfaceNameTwo represents Interface two
            name
          items:
            type: string
          type: array
        interface_type_one:
          description: >
            List of InterfaceTypeOne. InterfaceTypeOne represents Interface one
            type
          items:
            format: int64
            type: integer
          type: array
        interface_type_two:
          description: >
            List of InterfaceTypeTwo. InterfaceTypeTwo represents Interface two
            type
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        physical_link_id:
          description: |
            List of PhysicalLinkId. PhysicalLinkId represents Physical Link
          items:
            type: string
          type: array
        status:
          description: |
            List of Status. Status represents Link Status
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredFabricLinkGetRequest
      type: object
    InferredFabricLinkGetResponse:
      properties:
        InferredFabricLink:
          items:
            $ref: "#/components/schemas/InferredFabricLink"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricLinkGetResponse
      type: object
    InferredFabricTopology:
      description: InferredFabricTopology - DB model for fabric topology list.
        InferredFabricLink,InferredUserLink can be created while creating
        InferredFabricTopology.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_id:
          description: Fabric id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_fabric_link:
          items:
            $ref: "#/components/schemas/InferredFabricLink"
          nullable: true
          type: array
        inferred_user_link:
          items:
            $ref: "#/components/schemas/InferredUserLink"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredFabricTopologyGetRequest:
      description: InferredFabricTopologyGetRequest represents the retrieval request
        for InferredFabricTopology.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_id:
          description: |
            List of FabricId. FabricId represents Fabric id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredFabricTopologyGetRequest
      type: object
    InferredFabricTopologyGetResponse:
      properties:
        InferredFabricTopology:
          items:
            $ref: "#/components/schemas/InferredFabricTopology"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredFabricTopologyGetResponse
      type: object
    InferredGlobalSpbmDetails:
      description: InferredGlobalSpbmDetails - This object describes the Global SPBM
        details list. InferredDeviceSpbmDetail can be created while creating
        InferredGlobalSpbmDetails.
      properties:
        bvlan1:
          description: Backbone vlan ID 1
          format: int64
          minimum: 0
          type: integer
        bvlan2:
          description: Backbone vlan ID 2
          format: int64
          minimum: 0
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_id:
          description: ID of the parent Fabric
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_device_spbm_detail:
          items:
            $ref: "#/components/schemas/InferredDeviceSpbmDetail"
          nullable: true
          type: array
        instance_name:
          description: InstanceName indicates the value of the SPBM instance. Only one can
            be configured
          format: int64
          minimum: 0
          type: integer
        mepid:
          description: >-
            Global CFM maintenance end point ID value for SPBM VLANs.
            	MEPID stands for Multicast Ethernet Port Identifier.
            	It’s a unique identifier assigned to each port in the network.
            	MEPIDs help manage multicast traffic by ensuring efficient delivery across the fabric.
          format: int64
          minimum: 0
          type: integer
        nick_name:
          description: Nick name of the SPBM node.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        primary_bvlan:
          description: Primary Backbone vlan ID
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredGlobalSpbmDetailsGetRequest:
      description: InferredGlobalSpbmDetailsGetRequest represents the retrieval
        request for InferredGlobalSpbmDetails.
      properties:
        bvlan1:
          description: |
            List of Bvlan1. Bvlan1 represents Backbone vlan ID 1
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        bvlan2:
          description: |
            List of Bvlan2. Bvlan2 represents Backbone vlan ID 2
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_id:
          description: |
            List of FabricId. FabricId represents ID of the parent Fabric
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instance_name:
          description: >
            List of InstanceName. InstanceName represents InstanceName indicates
            the value of the SPBM instance. Only one can be configured
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        mepid:
          description: >
            List of Mepid. Mepid represents Global CFM maintenance end point ID
            value for SPBM VLANs.
            	MEPID stands for Multicast Ethernet Port Identifier.
            	It’s a unique identifier assigned to each port in the network.
            	MEPIDs help manage multicast traffic by ensuring efficient delivery across the fabric.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        nick_name:
          description: |
            List of NickName. NickName represents Nick name of the SPBM node.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        primary_bvlan:
          description: >
            List of PrimaryBvlan. PrimaryBvlan represents Primary Backbone vlan
            ID
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredGlobalSpbmDetailsGetRequest
      type: object
    InferredGlobalSpbmDetailsGetResponse:
      properties:
        InferredGlobalSpbmDetails:
          items:
            $ref: "#/components/schemas/InferredGlobalSpbmDetails"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredGlobalSpbmDetailsGetResponse
      type: object
    InferredGroupInfo:
      description: InferredGroupInfo - This object represents the Inferred group
        information.
      properties:
        border_devices:
          items:
            $ref: "#/components/schemas/InferredDevice"
          nullable: true
          type: array
        border_links:
          items:
            $ref: "#/components/schemas/InferredPhysicalLink"
          nullable: true
          type: array
        children:
          items:
            $ref: "#/components/schemas/InferredGroupInfo"
          type: array
        common_service_id:
          description: Common service identifier
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_count:
          description: Count of devices at floor level only. Needed only for UI, will not
            be in database
          format: int64
          type: integer
        devices:
          items:
            $ref: "#/components/schemas/InferredDevice"
          nullable: true
          type: array
        expanded:
          description: Group expansion state. Needed only for UI, will not be in database
          type: boolean
        group_type:
          description: Group Type.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        is_boundary_floor:
          description: Boundary floor state. Needed only for UI, will not be in database
          type: boolean
        latitude:
          description: Location coordinates
          type: number
        longitude:
          description: Location coordinates
          type: number
        name:
          description: Name of the group
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        parent_id:
          description: It refers to the uuid of group itself which is the parent of
            current group
          type: string
        path:
          description: UUID in XPath
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredGroupInfoGetRequest:
      description: InferredGroupInfoGetRequest represents the retrieval request for
        InferredGroupInfo.
      properties:
        common_service_id:
          description: >
            List of CommonServiceId. CommonServiceId represents Common service
            identifier
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_count:
          description: >
            List of DeviceCount. DeviceCount represents Count of devices at
            floor level only. Needed only for UI, will not be in database
          items:
            format: int64
            type: integer
          type: array
        expanded:
          description: >
            List of Expanded. Expanded represents Group expansion state. Needed
            only for UI, will not be in database
          items:
            type: boolean
          type: array
        group_type:
          description: |
            List of GroupType. GroupType represents Group Type.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        is_boundary_floor:
          description: >
            List of IsBoundaryFloor. IsBoundaryFloor represents Boundary floor
            state. Needed only for UI, will not be in database
          items:
            type: boolean
          type: array
        latitude:
          description: |
            List of Latitude. Latitude represents Location coordinates
          items:
            type: number
          type: array
        longitude:
          description: |
            List of Longitude. Longitude represents Location coordinates
          items:
            type: number
          type: array
        name:
          description: |
            List of Name. Name represents Name of the group
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        parent_id:
          description: >
            List of ParentId. ParentId represents It refers to the uuid of group
            itself which is the parent of current group
          items:
            type: string
          type: array
        path:
          description: |
            List of Path. Path represents UUID in XPath
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredGroupInfoGetRequest
      type: object
    InferredGroupInfoGetResponse:
      properties:
        InferredGroupInfo:
          items:
            $ref: "#/components/schemas/InferredGroupInfo"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredGroupInfoGetResponse
      type: object
    InferredInterface:
      description: InferredInterface - This object represents the Inferred Interface
        information.
      nullable: true
      properties:
        asset_interface_id:
          description: UUID of the asset interface
          type: string
        cluster_id:
          description: UUID of the cluster
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: |-
            Specifies the name of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          type: string
        interface_type:
          description: Ethernet(port), Lag(link aggregation) or ISIS logical
            interface(ISIS tunnel)
          format: int64
          type: integer
        mlag_id:
          description: MLAG ID - Switch Engine (EXOS) only, Fabric Engine (VOSS) uses MLT ID
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredInterfaceGetRequest:
      description: InferredInterfaceGetRequest represents the retrieval request for
        InferredInterface.
      properties:
        asset_interface_id:
          description: >
            List of AssetInterfaceId. AssetInterfaceId represents UUID of the
            asset interface
          items:
            type: string
          type: array
        cluster_id:
          description: |
            List of ClusterId. ClusterId represents UUID of the cluster
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Specifies the name
            of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents Ethernet(port),
            Lag(link aggregation) or ISIS logical interface(ISIS tunnel)
          items:
            format: int64
            type: integer
          type: array
        mlag_id:
          description: >
            List of MlagId. MlagId represents MLAG ID - Switch Engine (EXOS)
            only, Fabric Engine (VOSS) uses MLT ID
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredInterfaceGetRequest
      type: object
    InferredInterfaceGetResponse:
      properties:
        InferredInterface:
          items:
            $ref: "#/components/schemas/InferredInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredInterfaceGetResponse
      type: object
    InferredInterfaceVlanMap:
      description: InferredInterfaceVlanMap - This object represents the Port VLAN Map.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: |-
            Specifies the name of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          type: string
        interface_type:
          description: Interface type.
          format: int64
          type: integer
        member_ports:
          description: List of member ports. Applicable only when InterfaceType is lag
          items:
            type: string
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        private_vlan_port_mode:
          description: Private Vlan port mode.
          format: int64
          type: integer
        stp_enabled_on_ports:
          description: Represents if stp is enabled on port
          type: boolean
        stp_interface_status:
          description: Stp interface status
          format: int64
          type: integer
        switched_port_mode:
          description: |-
            A value of True will enable the port on the current vlan to perform
            	tagging on the incoming and outgoing traffic. This is Fabric Engine
            	specific.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: Vlan Id
          type: string
      required:
        - interface_name
        - interface_type
        - switched_port_mode
        - vlan_id
      type: object
    InferredInterfaceVlanMapGetRequest:
      description: InferredInterfaceVlanMapGetRequest represents the retrieval request
        for InferredInterfaceVlanMap.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Specifies the name
            of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents Interface type.
          items:
            format: int64
            type: integer
          type: array
        member_ports:
          description: >
            List of MemberPorts. MemberPorts represents List of member ports.
            Applicable only when InterfaceType is lag
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        private_vlan_port_mode:
          description: >
            List of PrivateVlanPortMode. PrivateVlanPortMode represents Private
            Vlan port mode.
          items:
            format: int64
            type: integer
          type: array
        stp_enabled_on_ports:
          description: >
            List of StpEnabledOnPorts. StpEnabledOnPorts represents Represents
            if stp is enabled on port
          items:
            type: boolean
          type: array
        stp_interface_status:
          description: >
            List of StpInterfaceStatus. StpInterfaceStatus represents Stp
            interface status
          items:
            format: int64
            type: integer
          type: array
        switched_port_mode:
          description: >
            List of SwitchedPortMode. SwitchedPortMode represents A value of
            True will enable the port on the current vlan to perform
            	tagging on the incoming and outgoing traffic. This is Fabric Engine
            	specific.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: |
            List of VlanId. VlanId represents Vlan Id
          items:
            type: string
          type: array
      title: InferredInterfaceVlanMapGetRequest
      type: object
    InferredInterfaceVlanMapGetResponse:
      properties:
        InferredInterfaceVlanMap:
          items:
            $ref: "#/components/schemas/InferredInterfaceVlanMap"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredInterfaceVlanMapGetResponse
      type: object
    InferredIsisDetails:
      description: InferredIsisDetails - This object describes the ISIS details list.
      nullable: true
      properties:
        asset_device_id:
          description: UUID of the asset device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_spbm_detail_id:
          description: ID of the parent device-spbm-detail
          type: string
        enabled:
          description: ISIS enabled or disabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        manual_area:
          description: |-
            A manually configured area address for this
            	system. This attribute is available in both
            	areas.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        system_id:
          description: |-
            The ID for this Intermediate System. This value
            	is appended to each of the area addresses to
            	form the Network Entity Titles. This attribute
            	is available in both areas.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredIsisDetailsGetRequest:
      description: InferredIsisDetailsGetRequest represents the retrieval request for
        InferredIsisDetails.
      properties:
        asset_device_id:
          description: >
            List of AssetDeviceId. AssetDeviceId represents UUID of the asset
            device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_spbm_detail_id:
          description: >
            List of DeviceSpbmDetailId. DeviceSpbmDetailId represents ID of the
            parent device-spbm-detail
          items:
            type: string
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents ISIS enabled or disabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        manual_area:
          description: >
            List of ManualArea. ManualArea represents A manually configured area
            address for this
            	system. This attribute is available in both
            	areas.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        system_id:
          description: >
            List of SystemId. SystemId represents The ID for this Intermediate
            System. This value
            	is appended to each of the area addresses to
            	form the Network Entity Titles. This attribute
            	is available in both areas.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredIsisDetailsGetRequest
      type: object
    InferredIsisDetailsGetResponse:
      properties:
        InferredIsisDetails:
          items:
            $ref: "#/components/schemas/InferredIsisDetails"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredIsisDetailsGetResponse
      type: object
    InferredIsisRemoteDetails:
      description: InferredIsisRemoteDetails - This object describes the ISIS remote
        details list.
      nullable: true
      properties:
        area_name:
          description: |-
            The ISIS system area-name. This attribute is
            	available in both areas and if the device does
            	not support multi-area, it won`t be included
            	in the home response.
          type: string
        asset_device_id:
          description: UUID of the asset device
          type: string
        boundary_detail_id:
          description: ID of the parent boundary-detail
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          description: ISIS enabled or disabled.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        manual_area:
          description: |-
            A manually configured area address for this
            	system. This attribute is available in both
            	areas.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        system_id:
          description: |-
            The ID for this Intermediate System. This value
            	is appended to each of the area addresses to
            	form the Network Entity Titles. This attribute
            	is available in both areas.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredIsisRemoteDetailsGetRequest:
      description: InferredIsisRemoteDetailsGetRequest represents the retrieval
        request for InferredIsisRemoteDetails.
      properties:
        area_name:
          description: >
            List of AreaName. AreaName represents The ISIS system area-name.
            This attribute is
            	available in both areas and if the device does
            	not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        asset_device_id:
          description: >
            List of AssetDeviceId. AssetDeviceId represents UUID of the asset
            device
          items:
            type: string
          type: array
        boundary_detail_id:
          description: >
            List of BoundaryDetailId. BoundaryDetailId represents ID of the
            parent boundary-detail
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents ISIS enabled or disabled.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        manual_area:
          description: >
            List of ManualArea. ManualArea represents A manually configured area
            address for this
            	system. This attribute is available in both
            	areas.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        system_id:
          description: >
            List of SystemId. SystemId represents The ID for this Intermediate
            System. This value
            	is appended to each of the area addresses to
            	form the Network Entity Titles. This attribute
            	is available in both areas.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredIsisRemoteDetailsGetRequest
      type: object
    InferredIsisRemoteDetailsGetResponse:
      properties:
        InferredIsisRemoteDetails:
          items:
            $ref: "#/components/schemas/InferredIsisRemoteDetails"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredIsisRemoteDetailsGetResponse
      type: object
    InferredL2Access:
      description: InferredL2Access - This object describes the L2 access info list.
        CustomerVlan,SwitchedVlan,TransparentVlan can be created while creating
        InferredL2Access.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        customer_vlan:
          $ref: "#/components/schemas/InferredCustomerVlanUni"
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l2_service_end_point_id:
          description: L2 Service End Point Id.
          type: string
        origin:
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        switched_vlan:
          $ref: "#/components/schemas/InferredSwitchedVlanUni"
        transparent_vlan:
          $ref: "#/components/schemas/InferredTransparentVlanUni"
        type:
          description: L2 Access type.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - l2_service_end_point_id
        - type
      type: object
    InferredL2AccessGetRequest:
      description: InferredL2AccessGetRequest represents the retrieval request for
        InferredL2Access.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        l2_service_end_point_id:
          description: >
            List of L2ServiceEndPointId. L2ServiceEndPointId represents L2
            Service End Point Id.
          items:
            type: string
          type: array
        origin:
          description: |
            List of Origin. Origin represents List of L2 Access Origin.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        type:
          description: |
            List of Type. Type represents L2 Access type.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredL2AccessGetRequest
      type: object
    InferredL2AccessGetResponse:
      properties:
        InferredL2Access:
          items:
            $ref: "#/components/schemas/InferredL2Access"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL2AccessGetResponse
      type: object
    InferredL2Service:
      description: InferredL2Service - This object represents the settings for Layer-2
        services. EndPoints can be created while creating InferredL2Service.
      properties:
        associated_subnets:
          items:
            format: int64
            type: integer
          type: array
        associated_vlans:
          description: Associated Vlan numbers.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Customer Id
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled_features:
          items:
            format: int64
            type: integer
          type: array
        end_points:
          items:
            $ref: "#/components/schemas/InferredL2ServiceEndPoint"
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Owner Id
          format: int64
          minimum: 0
          type: integer
        service_name:
          description: Service Name in devices.
          type: string
        service_number:
          description: Service Number in Devices.
          format: int64
          minimum: 0
          type: integer
        sub_type:
          description: Service sub types
          format: int64
          type: integer
        type:
          description: Overlay types
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - customer_id
        - owner_id
        - service_name
        - service_number
      type: object
    InferredL2ServiceEndPoint:
      description: InferredL2ServiceEndPoint - This object describes the L2 service
        endpoint list. L2Access can be created while creating
        InferredL2ServiceEndPoint.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        device_l2_isid_name:
          description: L2 Service Name configured for this endpoint
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l2_access:
          items:
            $ref: "#/components/schemas/InferredL2Access"
          nullable: true
          type: array
        l2_service_id:
          description: L2 Service Id
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - l2_service_id
      type: object
    InferredL2ServiceEndPointGetRequest:
      description: InferredL2ServiceEndPointGetRequest represents the retrieval
        request for InferredL2ServiceEndPoint.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        device_l2_isid_name:
          description: >
            List of DeviceL2IsidName. DeviceL2IsidName represents L2 Service
            Name configured for this endpoint
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        l2_service_id:
          description: |
            List of L2ServiceId. L2ServiceId represents L2 Service Id
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredL2ServiceEndPointGetRequest
      type: object
    InferredL2ServiceEndPointGetResponse:
      properties:
        InferredL2ServiceEndPoint:
          items:
            $ref: "#/components/schemas/InferredL2ServiceEndPoint"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL2ServiceEndPointGetResponse
      type: object
    InferredL2ServiceGetRequest:
      description: InferredL2ServiceGetRequest represents the retrieval request for
        InferredL2Service.
      properties:
        associated_subnets:
          description: >
            List of AssociatedSubnets. AssociatedSubnets represents Associated
            Service Subnets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        associated_vlans:
          description: >
            List of AssociatedVlans. AssociatedVlans represents Associated Vlan
            numbers.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: |
            List of CustomerId. CustomerId represents Customer Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled_features:
          description: >
            List of EnabledFeatures. EnabledFeatures represents Enabled Features.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Owner Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        service_name:
          description: |
            List of ServiceName. ServiceName represents Service Name in devices.
          items:
            type: string
          type: array
        service_number:
          description: >
            List of ServiceNumber. ServiceNumber represents Service Number in
            Devices.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sub_type:
          description: |
            List of SubType. SubType represents Service sub types
          items:
            format: int64
            type: integer
          type: array
        type:
          description: |
            List of Type. Type represents Overlay types
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredL2ServiceGetRequest
      type: object
    InferredL2ServiceGetResponse:
      properties:
        InferredL2Service:
          items:
            $ref: "#/components/schemas/InferredL2Service"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL2ServiceGetResponse
      type: object
    InferredL3Access:
      description: InferredL3Access - This object represents the settings for Layer-3
        access.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        gateway_ip:
          description: Gateway Ipv4 address.
          type: string
        gateway_ip_mask_length:
          description: Gateway IP Mask Length.
          format: int64
          minimum: 0
          type: integer
        gateway_type:
          description: Gateway Type.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l3_gateway_interface_id:
          description: L3 Gateway Interface Id.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_id:
          description: VRF Id
          type: string
        vrf_name:
          description: VRF name
          type: string
        vrf_number:
          description: VRF Number.
          format: int64
          minimum: 0
          type: integer
      required:
        - gateway_ip
        - l3_gateway_interface_id
        - vrf_id
        - vrf_name
        - vrf_number
      type: object
    InferredL3AccessGetRequest:
      description: InferredL3AccessGetRequest represents the retrieval request for
        InferredL3Access.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        gateway_ip:
          description: |
            List of GatewayIp. GatewayIp represents Gateway Ipv4 address.
          items:
            type: string
          type: array
        gateway_ip_mask_length:
          description: >
            List of GatewayIpMaskLength. GatewayIpMaskLength represents Gateway
            IP Mask Length.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        gateway_type:
          description: |
            List of GatewayType. GatewayType represents Gateway Type.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        l3_gateway_interface_id:
          description: >
            List of L3GatewayInterfaceId. L3GatewayInterfaceId represents L3
            Gateway Interface Id.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_id:
          description: |
            List of VrfId. VrfId represents VRF Id
          items:
            type: string
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name
          items:
            type: string
          type: array
        vrf_number:
          description: |
            List of VrfNumber. VrfNumber represents VRF Number.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: InferredL3AccessGetRequest
      type: object
    InferredL3AccessGetResponse:
      properties:
        InferredL3Access:
          items:
            $ref: "#/components/schemas/InferredL3Access"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL3AccessGetResponse
      type: object
    InferredL3GatewayInterface:
      description: InferredL3GatewayInterface - This object represents the L3 gateway
        interface information. InferredVrrpInstance can be created while
        creating InferredL3GatewayInterface.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_vrrp_instance:
          items:
            $ref: "#/components/schemas/InferredVrrpInstance"
          nullable: true
          type: array
        interface_name:
          description: |-
            Specifies the name of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          type: string
        interface_type:
          description: Interface type.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - interface_name
        - interface_type
      type: object
    InferredL3GatewayInterfaceGetRequest:
      description: InferredL3GatewayInterfaceGetRequest represents the retrieval
        request for InferredL3GatewayInterface.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Specifies the name
            of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents Interface type.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredL3GatewayInterfaceGetRequest
      type: object
    InferredL3GatewayInterfaceGetResponse:
      properties:
        InferredL3GatewayInterface:
          items:
            $ref: "#/components/schemas/InferredL3GatewayInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL3GatewayInterfaceGetResponse
      type: object
    InferredL3Service:
      description: InferredL3Service - This object represents the settings for Layer-3
        services. EndPoints can be created while creating InferredL3Service.
      properties:
        associated_subnets:
          items:
            format: int64
            type: integer
          type: array
        associated_vlans:
          description: Associated Vlan numbers.
          items:
            type: string
          nullable: true
          type: array
        associated_vrfs:
          description: Associated VRFs.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Customer Id
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled_features:
          items:
            format: int64
            type: integer
          type: array
        end_points:
          items:
            $ref: "#/components/schemas/InferredL3ServiceEndPoint"
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        network_overlay_type:
          description: Network Overlay types
          format: int64
          type: integer
        owner_id:
          description: Owner Id
          format: int64
          minimum: 0
          type: integer
        service_name:
          description: Service Name in devices.
          type: string
        service_number:
          description: Service Number in Devices.
          format: int64
          minimum: 0
          type: integer
        sub_type:
          description: Service sub types
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - customer_id
        - owner_id
        - service_name
        - service_number
      type: object
    InferredL3ServiceEndPoint:
      description: InferredL3ServiceEndPoint - This object represents the settings for
        Layer-3 services endpoints.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        device_l3_isid_name:
          description: L3 service name configured on the endpoint
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l3_service_id:
          description: L3 Service Id
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vrf_id:
          description: VRF Id
          type: string
        vrf_name:
          description: VRF name.
          type: string
      required:
        - device_id
        - l3_service_id
        - vrf_name
      type: object
    InferredL3ServiceEndPointGetRequest:
      description: InferredL3ServiceEndPointGetRequest represents the retrieval
        request for InferredL3ServiceEndPoint.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        device_l3_isid_name:
          description: >
            List of DeviceL3IsidName. DeviceL3IsidName represents L3 service
            name configured on the endpoint
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        l3_service_id:
          description: |
            List of L3ServiceId. L3ServiceId represents L3 Service Id
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vrf_id:
          description: |
            List of VrfId. VrfId represents VRF Id
          items:
            type: string
          type: array
        vrf_name:
          description: |
            List of VrfName. VrfName represents VRF name.
          items:
            type: string
          type: array
      title: InferredL3ServiceEndPointGetRequest
      type: object
    InferredL3ServiceEndPointGetResponse:
      properties:
        InferredL3ServiceEndPoint:
          items:
            $ref: "#/components/schemas/InferredL3ServiceEndPoint"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL3ServiceEndPointGetResponse
      type: object
    InferredL3ServiceGetRequest:
      description: InferredL3ServiceGetRequest represents the retrieval request for
        InferredL3Service.
      properties:
        associated_subnets:
          description: >
            List of AssociatedSubnets. AssociatedSubnets represents Associated
            Service Subnets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        associated_vlans:
          description: >
            List of AssociatedVlans. AssociatedVlans represents Associated Vlan
            numbers.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        associated_vrfs:
          description: |
            List of AssociatedVrfs. AssociatedVrfs represents Associated VRFs.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: |
            List of CustomerId. CustomerId represents Customer Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled_features:
          description: >
            List of EnabledFeatures. EnabledFeatures represents Enabled Features.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        network_overlay_type:
          description: >
            List of NetworkOverlayType. NetworkOverlayType represents Network
            Overlay types
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Owner Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        service_name:
          description: |
            List of ServiceName. ServiceName represents Service Name in devices.
          items:
            type: string
          type: array
        service_number:
          description: >
            List of ServiceNumber. ServiceNumber represents Service Number in
            Devices.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sub_type:
          description: |
            List of SubType. SubType represents Service sub types
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredL3ServiceGetRequest
      type: object
    InferredL3ServiceGetResponse:
      properties:
        InferredL3Service:
          items:
            $ref: "#/components/schemas/InferredL3Service"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredL3ServiceGetResponse
      type: object
    InferredLag:
      description: InferredLag - This object represents the LAG interface information.
        LagPortMembers can be created while creating InferredLag.
      nullable: true
      properties:
        asset_lag_id:
          description: >-
            LAG config ID - Master port on Switch Engine, MLT ID on Fabric
            Engine.
            	On Fabric Engine, the ID can be between 1 and 512.
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_id:
          description: UUID of the interface
          type: string
        lag_number:
          description: The Lag Number
          type: string
        lag_port_members:
          items:
            $ref: "#/components/schemas/InferredPortLagMemberMap"
          nullable: true
          type: array
        name:
          description: The Lag Name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredLagGetRequest:
      description: InferredLagGetRequest represents the retrieval request for InferredLag.
      properties:
        asset_lag_id:
          description: >
            List of AssetLagId. AssetLagId represents LAG config ID - Master
            port on Switch Engine, MLT ID on Fabric Engine.
            	On Fabric Engine, the ID can be between 1 and 512.
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_id:
          description: |
            List of InterfaceId. InterfaceId represents UUID of the interface
          items:
            type: string
          type: array
        lag_number:
          description: |
            List of LagNumber. LagNumber represents The Lag Number
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents The Lag Name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredLagGetRequest
      type: object
    InferredLagGetResponse:
      properties:
        InferredLag:
          items:
            $ref: "#/components/schemas/InferredLag"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredLagGetResponse
      type: object
    InferredLldpEdgeDeviceGrouping:
      description: InferredLldpEdgeDeviceGrouping - This object represents the LLDP
        edge device grouping information.
      properties:
        common_service_id:
          description: Common service identifier
          format: int64
          type: integer
        count:
          description: Number of Aps/Camera/Phone/Other – Based on deviceType
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        edge_device_csid:
          description: The CSID of the edge device
          type: string
        edge_device_type:
          description: Holds type for managed and unmanaged device types -
            AP/Phone/Camera/Other.
          format: int64
          type: integer
        edge_device_uuid:
          description: The UUID of the edge device
          type: string
        group_id:
          description: ID of the parent Group
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        is_dual_homed:
          description: Indicates if the edge device is dual homed
          type: boolean
        managed:
          description: Indicates if the edge device is actively managed
          type: boolean
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        status:
          description: The connection status of the edge device
            (connected/disconnected/unknown)
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - customer_id
        - owner_id
        - device_id
        - edge_device_type
      type: object
    InferredLldpEdgeDeviceGroupingGetRequest:
      description: InferredLldpEdgeDeviceGroupingGetRequest represents the retrieval
        request for InferredLldpEdgeDeviceGrouping.
      properties:
        common_service_id:
          description: >
            List of CommonServiceId. CommonServiceId represents Common service
            identifier
          items:
            format: int64
            type: integer
          type: array
        count:
          description: >
            List of Count. Count represents Number of Aps/Camera/Phone/Other –
            Based on deviceType
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        edge_device_csid:
          description: >
            List of EdgeDeviceCsid. EdgeDeviceCsid represents The CSID of the
            edge device
          items:
            type: string
          type: array
        edge_device_type:
          description: >
            List of EdgeDeviceType. EdgeDeviceType represents Holds type for
            managed and unmanaged device types - AP/Phone/Camera/Other.
          items:
            format: int64
            type: integer
          type: array
        edge_device_uuid:
          description: >
            List of EdgeDeviceUuid. EdgeDeviceUuid represents The UUID of the
            edge device
          items:
            type: string
          type: array
        group_id:
          description: |
            List of GroupId. GroupId represents ID of the parent Group
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        is_dual_homed:
          description: >
            List of IsDualHomed. IsDualHomed represents Indicates if the edge
            device is dual homed
          items:
            type: boolean
          type: array
        managed:
          description: >
            List of Managed. Managed represents Indicates if the edge device is
            actively managed
          items:
            type: boolean
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        status:
          description: >
            List of Status. Status represents The connection status of the edge
            device (connected/disconnected/unknown)
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredLldpEdgeDeviceGroupingGetRequest
      type: object
    InferredLldpEdgeDeviceGroupingGetResponse:
      properties:
        InferredLldpEdgeDeviceGrouping:
          items:
            $ref: "#/components/schemas/InferredLldpEdgeDeviceGrouping"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredLldpEdgeDeviceGroupingGetResponse
      type: object
    InferredPhysicalLink:
      description: InferredPhysicalLink - The object that represents settings for a
        Physical Link
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_one_id:
          description: UUID of DeviceOne
          type: string
        device_two_id:
          description: UUID of Device Two
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_one_id:
          description: UUID of InterfaceOne
          type: string
        interface_two_id:
          description: UUID of Interface Two
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        parent_lag_id:
          description: ParentLAGID string value
          type: string
        physical_topology_id:
          description: PhysicalTopologyID
          type: string
        status:
          description: Link Status
          format: int64
          type: integer
        type:
          description: Type of interface
          format: int64
          type: integer
        update_time:
          description: |-
            The amount of time since the network operating system was
            	initialized.
          format: date-time
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredPhysicalLinkGetRequest:
      description: InferredPhysicalLinkGetRequest represents the retrieval request for
        InferredPhysicalLink.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_one_id:
          description: |
            List of DeviceOneId. DeviceOneId represents UUID of DeviceOne
          items:
            type: string
          type: array
        device_two_id:
          description: |
            List of DeviceTwoId. DeviceTwoId represents UUID of Device Two
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_one_id:
          description: >
            List of InterfaceOneId. InterfaceOneId represents UUID of
            InterfaceOne
          items:
            type: string
          type: array
        interface_two_id:
          description: >
            List of InterfaceTwoId. InterfaceTwoId represents UUID of Interface
            Two
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        parent_lag_id:
          description: |
            List of ParentLagId. ParentLagId represents ParentLAGID string value
          items:
            type: string
          type: array
        physical_topology_id:
          description: >
            List of PhysicalTopologyId. PhysicalTopologyId represents
            PhysicalTopologyID
          items:
            type: string
          type: array
        status:
          description: |
            List of Status. Status represents Link Status
          items:
            format: int64
            type: integer
          type: array
        type:
          description: |
            List of Type. Type represents Type of interface
          items:
            format: int64
            type: integer
          type: array
        update_time:
          description: >
            List of UpdateTime. UpdateTime represents The amount of time since
            the network operating system was
            	initialized.
          items:
            format: date-time
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPhysicalLinkGetRequest
      type: object
    InferredPhysicalLinkGetResponse:
      properties:
        InferredPhysicalLink:
          items:
            $ref: "#/components/schemas/InferredPhysicalLink"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPhysicalLinkGetResponse
      type: object
    InferredPhysicalTopology:
      description: InferredPhysicalTopology - This object represents the Inferred
        Physical Topology information. InferredPhysicalLink can be created while
        creating InferredPhysicalTopology.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_physical_link:
          items:
            $ref: "#/components/schemas/InferredPhysicalLink"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredPhysicalTopologyGetRequest:
      description: InferredPhysicalTopologyGetRequest represents the retrieval request
        for InferredPhysicalTopology.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPhysicalTopologyGetRequest
      type: object
    InferredPhysicalTopologyGetResponse:
      properties:
        InferredPhysicalTopology:
          items:
            $ref: "#/components/schemas/InferredPhysicalTopology"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPhysicalTopologyGetResponse
      type: object
    InferredPort:
      description: InferredPort - This object describes the inferred port list.
        PortErrorStats,PortInOutStats,PortUtilizationInfo can be created while
        creating InferredPort.
      properties:
        asset_port_id:
          description: Id of the physical port associated with the inferred port
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Id of the device associated with the port
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_id:
          description: Id of the interface associated with the port
          type: string
        name:
          description: Name of Fabric
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_error_stats:
          $ref: "#/components/schemas/InferredPortErrorStats"
        port_in_out_stats:
          $ref: "#/components/schemas/InferredPortInOutStats"
        port_utilization_info:
          $ref: "#/components/schemas/InferredPortUtilizationInfo"
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - interface_id
      type: object
    InferredPortErrorStats:
      description: InferredPortErrorStats - This object describes the inferred port
        error stats list.
      nullable: true
      properties:
        alignment:
          description: Alighment
          format: int64
          type: integer
        carrier:
          description: Communication medium
          format: int64
          type: integer
        carrier_sense:
          description: Checks for communication medium currently in use
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deferred_tx:
          description: Deffered Transmissions
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        excessive_collision:
          description: Count of excessive collision (16 consecutive collisions)
          format: int64
          type: integer
        fcs:
          description: Frames failed the FCS checks.
          format: int64
          type: integer
        frame_too_long:
          description: Frames exceed the maximum allowable size.
          format: int64
          type: integer
        frame_too_short:
          description: Frames smaller than the minimum allowable size.
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        in_discards:
          description: Packets dropped at ingress port.
          format: int64
          type: integer
        in_errors:
          description: Errors encountered with incoming packets.
          format: int64
          type: integer
        late_collision:
          description: Count of late collision (detected after the first 512 bits of the
            frame have been transmitted)
          format: int64
          type: integer
        link_failures:
          description: Port link failures.
          format: int64
          type: integer
        link_inactive:
          description: Port not active/operational.
          format: int64
          type: integer
        mac_rx:
          description: Issues with the receiption of frames at Media Access Control layer.
          format: int64
          type: integer
        mac_tx:
          description: Issues with the transmission of frames at Media Access Control layer.
          format: int64
          type: integer
        multiple_collision:
          description: Frames experienced more than one collisions before successful
            transmission.
          format: int64
          type: integer
        out_discards:
          description: Packets droppet at egress port.
          format: int64
          type: integer
        out_errors:
          description: Errors encountered with outgoing packets.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        packet:
          description: Number of pakcets.
          format: int64
          type: integer
        port_id:
          description: Id of the inferred port for which the error stats are associated with
          type: string
        single_collision:
          description: Frames experienced exactly one collisions before successful
            transmission.
          format: int64
          type: integer
        sqe_test:
          description: Signal quality error tests
          format: int64
          type: integer
        unknown_protos:
          description: Packets received with unknown protocols
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - port_id
      type: object
    InferredPortErrorStatsGetRequest:
      description: InferredPortErrorStatsGetRequest represents the retrieval request
        for InferredPortErrorStats.
      properties:
        alignment:
          description: |
            List of Alignment. Alignment represents Alighment
          items:
            format: int64
            type: integer
          type: array
        carrier:
          description: |
            List of Carrier. Carrier represents Communication medium
          items:
            format: int64
            type: integer
          type: array
        carrier_sense:
          description: >
            List of CarrierSense. CarrierSense represents Checks for
            communication medium currently in use
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deferred_tx:
          description: |
            List of DeferredTx. DeferredTx represents Deffered Transmissions
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        excessive_collision:
          description: >
            List of ExcessiveCollision. ExcessiveCollision represents Count of
            excessive collision (16 consecutive collisions)
          items:
            format: int64
            type: integer
          type: array
        fcs:
          description: |
            List of Fcs. Fcs represents Frames failed the FCS checks.
          items:
            format: int64
            type: integer
          type: array
        frame_too_long:
          description: >
            List of FrameTooLong. FrameTooLong represents Frames exceed the
            maximum allowable size.
          items:
            format: int64
            type: integer
          type: array
        frame_too_short:
          description: >
            List of FrameTooShort. FrameTooShort represents Frames smaller than
            the minimum allowable size.
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        in_discards:
          description: >
            List of InDiscards. InDiscards represents Packets dropped at ingress
            port.
          items:
            format: int64
            type: integer
          type: array
        in_errors:
          description: >
            List of InErrors. InErrors represents Errors encountered with
            incoming packets.
          items:
            format: int64
            type: integer
          type: array
        late_collision:
          description: >
            List of LateCollision. LateCollision represents Count of late
            collision (detected after the first 512 bits of the frame have been
            transmitted)
          items:
            format: int64
            type: integer
          type: array
        link_failures:
          description: |
            List of LinkFailures. LinkFailures represents Port link failures.
          items:
            format: int64
            type: integer
          type: array
        link_inactive:
          description: >
            List of LinkInactive. LinkInactive represents Port not
            active/operational.
          items:
            format: int64
            type: integer
          type: array
        mac_rx:
          description: >
            List of MacRx. MacRx represents Issues with the receiption of frames
            at Media Access Control layer.
          items:
            format: int64
            type: integer
          type: array
        mac_tx:
          description: >
            List of MacTx. MacTx represents Issues with the transmission of
            frames at Media Access Control layer.
          items:
            format: int64
            type: integer
          type: array
        multiple_collision:
          description: >
            List of MultipleCollision. MultipleCollision represents Frames
            experienced more than one collisions before successful transmission.
          items:
            format: int64
            type: integer
          type: array
        out_discards:
          description: >
            List of OutDiscards. OutDiscards represents Packets droppet at
            egress port.
          items:
            format: int64
            type: integer
          type: array
        out_errors:
          description: >
            List of OutErrors. OutErrors represents Errors encountered with
            outgoing packets.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        packet:
          description: |
            List of Packet. Packet represents Number of pakcets.
          items:
            format: int64
            type: integer
          type: array
        port_id:
          description: >
            List of PortId. PortId represents Id of the inferred port for which
            the error stats are associated with
          items:
            type: string
          type: array
        single_collision:
          description: >
            List of SingleCollision. SingleCollision represents Frames
            experienced exactly one collisions before successful transmission.
          items:
            format: int64
            type: integer
          type: array
        sqe_test:
          description: |
            List of SqeTest. SqeTest represents Signal quality error tests
          items:
            format: int64
            type: integer
          type: array
        unknown_protos:
          description: >
            List of UnknownProtos. UnknownProtos represents Packets received
            with unknown protocols
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPortErrorStatsGetRequest
      type: object
    InferredPortErrorStatsGetResponse:
      properties:
        InferredPortErrorStats:
          items:
            $ref: "#/components/schemas/InferredPortErrorStats"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPortErrorStatsGetResponse
      type: object
    InferredPortGetRequest:
      description: InferredPortGetRequest represents the retrieval request for InferredPort.
      properties:
        asset_port_id:
          description: >
            List of AssetPortId. AssetPortId represents Id of the physical port
            associated with the inferred port
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: >
            List of DeviceId. DeviceId represents Id of the device associated
            with the port
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_id:
          description: >
            List of InterfaceId. InterfaceId represents Id of the interface
            associated with the port
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Name of Fabric
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPortGetRequest
      type: object
    InferredPortGetResponse:
      properties:
        InferredPort:
          items:
            $ref: "#/components/schemas/InferredPort"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPortGetResponse
      type: object
    InferredPortInOutStats:
      description: InferredPortInOutStats - This object describes the inferred port
        in/out stats list.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        in_broadcast_pkts:
          description: Total number of broadcast packets received on the port.
          format: int64
          type: integer
        in_flow_ctrl_pkts:
          description: Total number of flow control packets (also known as pause frames)
            received on the port.
          format: int64
          type: integer
        in_multicast_pkts:
          description: Total number of multicast packets received on the port.
          format: int64
          type: integer
        in_octets:
          description: Total number of octets (bytes) received on the port.
          format: int64
          type: integer
        in_pfc_pkts:
          description: Total number of Priority Flow Control (PFC) packets received on the
            port.
          format: int64
          type: integer
        in_ucast_pkts:
          description: Total number of unicast packets received on the port.
          format: int64
          type: integer
        out_broadcast_pkts:
          description: Total number of broadcast packets transmitted from the port.
          format: int64
          type: integer
        out_flow_ctrl_pkts:
          description: Total number of flow control packets (also known as pause frames)
            transmitted from the port.
          format: int64
          type: integer
        out_multicast_pkts:
          description: Total number of multicast packets transmitted from the port.
          format: int64
          type: integer
        out_octets:
          description: Total number of octets (bytes) transmitted from the port.
          format: int64
          type: integer
        out_pfc_pkts:
          description: Total number of Priority Flow Control (PFC) packets transmitted
            from the port.
          format: int64
          type: integer
        out_ucast_pkts:
          description: Total number of unicast packets transmitted from the port.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_id:
          description: Id of the inferred port for which the utilization is associated with
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - port_id
      type: object
    InferredPortInOutStatsGetRequest:
      description: InferredPortInOutStatsGetRequest represents the retrieval request
        for InferredPortInOutStats.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        in_broadcast_pkts:
          description: >
            List of InBroadcastPkts. InBroadcastPkts represents Total number of
            broadcast packets received on the port.
          items:
            format: int64
            type: integer
          type: array
        in_flow_ctrl_pkts:
          description: >
            List of InFlowCtrlPkts. InFlowCtrlPkts represents Total number of
            flow control packets (also known as pause frames) received on the
            port.
          items:
            format: int64
            type: integer
          type: array
        in_multicast_pkts:
          description: >
            List of InMulticastPkts. InMulticastPkts represents Total number of
            multicast packets received on the port.
          items:
            format: int64
            type: integer
          type: array
        in_octets:
          description: >
            List of InOctets. InOctets represents Total number of octets (bytes)
            received on the port.
          items:
            format: int64
            type: integer
          type: array
        in_pfc_pkts:
          description: >
            List of InPfcPkts. InPfcPkts represents Total number of Priority
            Flow Control (PFC) packets received on the port.
          items:
            format: int64
            type: integer
          type: array
        in_ucast_pkts:
          description: >
            List of InUcastPkts. InUcastPkts represents Total number of unicast
            packets received on the port.
          items:
            format: int64
            type: integer
          type: array
        out_broadcast_pkts:
          description: >
            List of OutBroadcastPkts. OutBroadcastPkts represents Total number
            of broadcast packets transmitted from the port.
          items:
            format: int64
            type: integer
          type: array
        out_flow_ctrl_pkts:
          description: >
            List of OutFlowCtrlPkts. OutFlowCtrlPkts represents Total number of
            flow control packets (also known as pause frames) transmitted from
            the port.
          items:
            format: int64
            type: integer
          type: array
        out_multicast_pkts:
          description: >
            List of OutMulticastPkts. OutMulticastPkts represents Total number
            of multicast packets transmitted from the port.
          items:
            format: int64
            type: integer
          type: array
        out_octets:
          description: >
            List of OutOctets. OutOctets represents Total number of octets
            (bytes) transmitted from the port.
          items:
            format: int64
            type: integer
          type: array
        out_pfc_pkts:
          description: >
            List of OutPfcPkts. OutPfcPkts represents Total number of Priority
            Flow Control (PFC) packets transmitted from the port.
          items:
            format: int64
            type: integer
          type: array
        out_ucast_pkts:
          description: >
            List of OutUcastPkts. OutUcastPkts represents Total number of
            unicast packets transmitted from the port.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_id:
          description: >
            List of PortId. PortId represents Id of the inferred port for which
            the utilization is associated with
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPortInOutStatsGetRequest
      type: object
    InferredPortInOutStatsGetResponse:
      properties:
        InferredPortInOutStats:
          items:
            $ref: "#/components/schemas/InferredPortInOutStats"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPortInOutStatsGetResponse
      type: object
    InferredPortLagMemberMap:
      description: InferredPortLagMemberMap - This object represents the Port Lag
        Member map.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: |-
            Specifies the name of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          type: string
        lag_id:
          description: UUID of the lag
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredPortLagMemberMapGetRequest:
      description: InferredPortLagMemberMapGetRequest represents the retrieval request
        for InferredPortLagMemberMap.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: >
            List of InterfaceName. InterfaceName represents Specifies the name
            of interface based on interface type. In case of
            	PORT interfaces, this name represents physical port format;
            	In case of LAG interfaces, this name indicates the LAG ID.
            	In case of logical interfaces, this name will be in format
            	the logical interface id.
          items:
            type: string
          type: array
        lag_id:
          description: |
            List of LagId. LagId represents UUID of the lag
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPortLagMemberMapGetRequest
      type: object
    InferredPortLagMemberMapGetResponse:
      properties:
        InferredPortLagMemberMap:
          items:
            $ref: "#/components/schemas/InferredPortLagMemberMap"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPortLagMemberMapGetResponse
      type: object
    InferredPortUtilizationInfo:
      description: InferredPortUtilizationInfo - This object describes the inferred
        port utilization information list.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        port_id:
          description: Id of the inferred port for which the utilization is associated with
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        utilization_max_rx:
          description: The maximum rate(%) at which the data is received on the port
          type: number
        utilization_max_tx:
          description: The maximum rate(%) at which the data is transmitted from the port
          type: number
        utilization_rx:
          description: The rate(%) at which the data is received on the port
          type: number
        utilization_tx:
          description: The rate(%) at which the data is transmitted from the port
          type: number
      required:
        - port_id
      type: object
    InferredPortUtilizationInfoGetRequest:
      description: InferredPortUtilizationInfoGetRequest represents the retrieval
        request for InferredPortUtilizationInfo.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        port_id:
          description: >
            List of PortId. PortId represents Id of the inferred port for which
            the utilization is associated with
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        utilization_max_rx:
          description: >
            List of UtilizationMaxRx. UtilizationMaxRx represents The maximum
            rate(%) at which the data is received on the port
          items:
            type: number
          type: array
        utilization_max_tx:
          description: >
            List of UtilizationMaxTx. UtilizationMaxTx represents The maximum
            rate(%) at which the data is transmitted from the port
          items:
            type: number
          type: array
        utilization_rx:
          description: >
            List of UtilizationRx. UtilizationRx represents The rate(%) at which
            the data is received on the port
          items:
            type: number
          type: array
        utilization_tx:
          description: >
            List of UtilizationTx. UtilizationTx represents The rate(%) at which
            the data is transmitted from the port
          items:
            type: number
          type: array
      title: InferredPortUtilizationInfoGetRequest
      type: object
    InferredPortUtilizationInfoGetResponse:
      properties:
        InferredPortUtilizationInfo:
          items:
            $ref: "#/components/schemas/InferredPortUtilizationInfo"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPortUtilizationInfoGetResponse
      type: object
    InferredPrimaryPrivateVlan:
      description: InferredPrimaryPrivateVlan - This object represents the list of
        primary private vlan. Interfaces can be created while creating
        InferredPrimaryPrivateVlan.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interfaces:
          items:
            $ref: "#/components/schemas/InferredPrivateVlanInterface"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        private_vlan_domain_id:
          description: Parivate Vlan domain Id
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: |-
            This object represents a VLAN ID. For PVLAN, this will be
            	Primary/Secondary VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - private_vlan_domain_id
        - vlan_id
      type: object
    InferredPrimaryPrivateVlanGetRequest:
      description: InferredPrimaryPrivateVlanGetRequest represents the retrieval
        request for InferredPrimaryPrivateVlan.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        private_vlan_domain_id:
          description: >
            List of PrivateVlanDomainId. PrivateVlanDomainId represents Parivate
            Vlan domain Id
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: >
            List of VlanId. VlanId represents This object represents a VLAN ID.
            For PVLAN, this will be
            	Primary/Secondary VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: InferredPrimaryPrivateVlanGetRequest
      type: object
    InferredPrimaryPrivateVlanGetResponse:
      properties:
        InferredPrimaryPrivateVlan:
          items:
            $ref: "#/components/schemas/InferredPrimaryPrivateVlan"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPrimaryPrivateVlanGetResponse
      type: object
    InferredPrivateVlanDomain:
      description: InferredPrivateVlanDomain - This object represents the list of
        private vlan domain. PrimaryVlan,SecondaryVlans can be created while
        creating InferredPrivateVlanDomain.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        name:
          description: Name of Private VLAN.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        primary_vlan:
          $ref: "#/components/schemas/InferredPrimaryPrivateVlan"
        primary_vlan_id:
          description: This object represents a VLAN ID. For PVLAN, this will be
            Primary/Secondary VLAN ID.
          format: int64
          minimum: 0
          type: integer
        secondary_vlans:
          items:
            $ref: "#/components/schemas/InferredSecondaryPrivateVlan"
          nullable: true
          type: array
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - name
        - primary_vlan_id
      type: object
    InferredPrivateVlanDomainGetRequest:
      description: InferredPrivateVlanDomainGetRequest represents the retrieval
        request for InferredPrivateVlanDomain.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Name of Private VLAN.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        primary_vlan_id:
          description: >
            List of PrimaryVlanId. PrimaryVlanId represents This object
            represents a VLAN ID. For PVLAN, this will be Primary/Secondary VLAN
            ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPrivateVlanDomainGetRequest
      type: object
    InferredPrivateVlanDomainGetResponse:
      properties:
        InferredPrivateVlanDomain:
          items:
            $ref: "#/components/schemas/InferredPrivateVlanDomain"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPrivateVlanDomainGetResponse
      type: object
    InferredPrivateVlanInterface:
      description: InferredPrivateVlanInterface - This object represents the list of
        private vlan interfaces.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name.
          type: string
        interface_type:
          description: Interface type.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        primary_private_vlan_id:
          description: Primary private Vlan Id.
          type: string
        private_vlan_port_mode:
          description: Private Vlan port mode.
          format: int64
          type: integer
        secondary_private_vlan_id:
          description: Secondary private Vlan Id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - interface_name
        - interface_type
      type: object
    InferredPrivateVlanInterfaceGetRequest:
      description: InferredPrivateVlanInterfaceGetRequest represents the retrieval
        request for InferredPrivateVlanInterface.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name.
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents Interface type.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        primary_private_vlan_id:
          description: >
            List of PrimaryPrivateVlanId. PrimaryPrivateVlanId represents
            Primary private Vlan Id.
          items:
            type: string
          type: array
        private_vlan_port_mode:
          description: >
            List of PrivateVlanPortMode. PrivateVlanPortMode represents Private
            Vlan port mode.
          items:
            format: int64
            type: integer
          type: array
        secondary_private_vlan_id:
          description: >
            List of SecondaryPrivateVlanId. SecondaryPrivateVlanId represents
            Secondary private Vlan Id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredPrivateVlanInterfaceGetRequest
      type: object
    InferredPrivateVlanInterfaceGetResponse:
      properties:
        InferredPrivateVlanInterface:
          items:
            $ref: "#/components/schemas/InferredPrivateVlanInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredPrivateVlanInterfaceGetResponse
      type: object
    InferredSecondaryPrivateVlan:
      description: InferredSecondaryPrivateVlan - This object represents the list of
        secondary private vlan. Interfaces can be created while creating
        InferredSecondaryPrivateVlan.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id.
          type: string
        device_internal:
          description: |-
            Represents whether the VLAN is a device internal and doesn't
            	show up in the VLAN API/CLI response.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interfaces:
          items:
            $ref: "#/components/schemas/InferredPrivateVlanInterface"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        primary_vlan_id:
          description: This object represents primary VLAN ID.
          format: int64
          minimum: 0
          type: integer
        private_vlan_domain_id:
          description: Private Vlan domain Id
          type: string
        subscriber_type:
          description: Private Vlan subscriber type.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_id:
          description: This object represents secondary VLAN ID.
          format: int64
          minimum: 0
          type: integer
      required:
        - device_id
        - primary_vlan_id
        - private_vlan_domain_id
        - subscriber_type
        - vlan_id
      type: object
    InferredSecondaryPrivateVlanGetRequest:
      description: InferredSecondaryPrivateVlanGetRequest represents the retrieval
        request for InferredSecondaryPrivateVlan.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id.
          items:
            type: string
          type: array
        device_internal:
          description: >
            List of DeviceInternal. DeviceInternal represents Represents whether
            the VLAN is a device internal and doesn't
            	show up in the VLAN API/CLI response.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        primary_vlan_id:
          description: >
            List of PrimaryVlanId. PrimaryVlanId represents This object
            represents primary VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        private_vlan_domain_id:
          description: >
            List of PrivateVlanDomainId. PrivateVlanDomainId represents Private
            Vlan domain Id
          items:
            type: string
          type: array
        subscriber_type:
          description: >
            List of SubscriberType. SubscriberType represents Private Vlan
            subscriber type.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_id:
          description: >
            List of VlanId. VlanId represents This object represents secondary
            VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
      title: InferredSecondaryPrivateVlanGetRequest
      type: object
    InferredSecondaryPrivateVlanGetResponse:
      properties:
        InferredSecondaryPrivateVlan:
          items:
            $ref: "#/components/schemas/InferredSecondaryPrivateVlan"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredSecondaryPrivateVlanGetResponse
      type: object
    InferredSlot:
      description: InferredSlot - This object represents the list of slots.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: UUID of the device
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        model:
          description: Device model name.
          type: string
        node_state:
          description: |-
            Node state - Switch Engine (EXOS) only.
            	Configuration commands are  prevented when the
            	stack's backup node is in BACKUP state.
            	Administrative failover operations are
            	prevented when the stack's backup  node is in
            	BACKUP or BACKUP_CONFIG_SYNCED states. It is
            	safe to perform  a failover when the stack's
            	backup node is in BACKUP_IN_SYNC state.
          format: int64
          type: integer
        onboarded_to_cloud:
          description: |-
            Indicates if the slot has been onboarded to
            	cloud
          type: boolean
        operation_status:
          description: |-
            Device operational status. DOWN is applicable
            	to both Fabric Engine and Switch Engine. UP,
            	TESTING, UNKNOWN, DORMANT, UP_MASTER,
            	UP_DORMANT and UP_WARMSTANDBY are applicable to
            	Fabric Engine. The others are applicable to
            	Switch Engine.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        serial_number:
          description: Device unique serial number.
          type: string
        slot_number:
          description: Device Slot Number.
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - serial_number
      type: object
    InferredSlotGetRequest:
      description: InferredSlotGetRequest represents the retrieval request for InferredSlot.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents UUID of the device
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        model:
          description: |
            List of Model. Model represents Device model name.
          items:
            type: string
          type: array
        node_state:
          description: >
            List of NodeState. NodeState represents Node state - Switch Engine
            (EXOS) only.
            	Configuration commands are  prevented when the
            	stack's backup node is in BACKUP state.
            	Administrative failover operations are
            	prevented when the stack's backup  node is in
            	BACKUP or BACKUP_CONFIG_SYNCED states. It is
            	safe to perform  a failover when the stack's
            	backup node is in BACKUP_IN_SYNC state.
          items:
            format: int64
            type: integer
          type: array
        onboarded_to_cloud:
          description: >
            List of OnboardedToCloud. OnboardedToCloud represents Indicates if
            the slot has been onboarded to
            	cloud
          items:
            type: boolean
          type: array
        operation_status:
          description: >
            List of OperationStatus. OperationStatus represents Device
            operational status. DOWN is applicable
            	to both Fabric Engine and Switch Engine. UP,
            	TESTING, UNKNOWN, DORMANT, UP_MASTER,
            	UP_DORMANT and UP_WARMSTANDBY are applicable to
            	Fabric Engine. The others are applicable to
            	Switch Engine.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        slot_number:
          description: |
            List of SlotNumber. SlotNumber represents Device Slot Number.
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredSlotGetRequest
      type: object
    InferredSlotGetResponse:
      properties:
        InferredSlot:
          items:
            $ref: "#/components/schemas/InferredSlot"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredSlotGetResponse
      type: object
    InferredSwitchedVlanUni:
      description: InferredSwitchedVlanUni - This object represents the list of
        Switched Vlan UNI.
      nullable: true
      properties:
        associated_vlan_numbers:
          description: >-
            Vlan IDs associated with this vlan. For a PVLAN, when id is
            	Primary/Secondary VLAN ID, this will be Secondary/Primary VLAN IDs respectively.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        isid:
          description: A value that uniquely identifies the Layer 2 services instances
            network-wide.
          format: int64
          minimum: 0
          type: integer
        l2_access_id:
          description: L2 Access Id.
          type: string
        member_ports:
          description: |-
            List of members associated with the lag. This is applicable
            	only when the interface is of type LAG (Lag_Interface).
          items:
            type: string
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        private_vlan_port_mode:
          description: Private Vlan port mode.
          format: int64
          type: integer
        switched_port_mode_type:
          description: Switched port mode.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID. For PVLAN, this will be
            Primary/Secondary VLAN ID.
          format: int64
          minimum: 0
          type: integer
        vlan_type:
          description: Vlan types
          format: int64
          type: integer
      required:
        - device_id
        - interface_name
        - interface_type
        - l2_access_id
        - switched_port_mode_type
        - vlan_number
        - vlan_type
      type: object
    InferredSwitchedVlanUniGetRequest:
      description: InferredSwitchedVlanUniGetRequest represents the retrieval request
        for InferredSwitchedVlanUni.
      properties:
        associated_vlan_numbers:
          description: >
            List of AssociatedVlanNumbers. AssociatedVlanNumbers represents Vlan
            IDs associated with this vlan. For a PVLAN, when id is
            	Primary/Secondary VLAN ID, this will be Secondary/Primary VLAN IDs respectively.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 services instances network-wide.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l2_access_id:
          description: |
            List of L2AccessId. L2AccessId represents L2 Access Id.
          items:
            type: string
          type: array
        member_ports:
          description: >
            List of MemberPorts. MemberPorts represents List of members
            associated with the lag. This is applicable
            	only when the interface is of type LAG (Lag_Interface).
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        private_vlan_port_mode:
          description: >
            List of PrivateVlanPortMode. PrivateVlanPortMode represents Private
            Vlan port mode.
          items:
            format: int64
            type: integer
          type: array
        switched_port_mode_type:
          description: >
            List of SwitchedPortModeType. SwitchedPortModeType represents
            Switched port mode.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID. For PVLAN, this will be Primary/Secondary VLAN ID.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_type:
          description: |
            List of VlanType. VlanType represents Vlan types
          items:
            format: int64
            type: integer
          type: array
      title: InferredSwitchedVlanUniGetRequest
      type: object
    InferredSwitchedVlanUniGetResponse:
      properties:
        InferredSwitchedVlanUni:
          items:
            $ref: "#/components/schemas/InferredSwitchedVlanUni"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredSwitchedVlanUniGetResponse
      type: object
    InferredTag:
      description: InferredTag - This object represents the list of Tags.
        InferredTagCriteria can be created while creating InferredTag.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        devices:
          items:
            $ref: "#/components/schemas/InferredDevice"
          nullable: true
          type: array
        fabric_links:
          items:
            $ref: "#/components/schemas/InferredFabricLink"
          nullable: true
          type: array
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_tag_criteria:
          $ref: "#/components/schemas/InferredTagCriteria"
        l2_services:
          items:
            $ref: "#/components/schemas/InferredL2Service"
          nullable: true
          type: array
        l3_services:
          items:
            $ref: "#/components/schemas/InferredL3Service"
          nullable: true
          type: array
        name:
          description: Name of the tag.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        physical_links:
          items:
            $ref: "#/components/schemas/InferredPhysicalLink"
          nullable: true
          type: array
        tag_origin:
          description: Tag origin point which specify from where tag is generated.
          format: int64
          type: integer
        tag_type:
          description: Tag type.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_services:
          items:
            $ref: "#/components/schemas/InferredVlanService"
          nullable: true
          type: array
        vrf_services:
          items:
            $ref: "#/components/schemas/InferredVrfService"
          nullable: true
          type: array
      type: object
    InferredTagCriteria:
      description: InferredTagCriteria - Tag criteria which defines the policy/rules
        based on which devices will be tagged. InferredTagOuterElement can be
        created while creating InferredTagCriteria.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_tag_outer_element:
          items:
            $ref: "#/components/schemas/InferredTagOuterElement"
          nullable: true
          type: array
        outer_condition:
          description: Logical condition between criteria Entities.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        tag_id:
          description: Tag Id
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredTagCriteriaGetRequest:
      description: InferredTagCriteriaGetRequest represents the retrieval request for
        InferredTagCriteria.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        outer_condition:
          description: >
            List of OuterCondition. OuterCondition represents Logical condition
            between criteria Entities.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        tag_id:
          description: |
            List of TagId. TagId represents Tag Id
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredTagCriteriaGetRequest
      type: object
    InferredTagCriteriaGetResponse:
      properties:
        InferredTagCriteria:
          items:
            $ref: "#/components/schemas/InferredTagCriteria"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredTagCriteriaGetResponse
      type: object
    InferredTagElement:
      description: InferredTagElement - This object represents the list of tag element.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        identifier:
          description: Tag match identifier.
          format: int64
          type: integer
        match_cond:
          description: Matching condition.
          format: int64
          type: integer
        match_values:
          description: Matching values.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        tag_outer_element_id:
          description: Tag Outer Element Id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredTagElementGetRequest:
      description: InferredTagElementGetRequest represents the retrieval request for
        InferredTagElement.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        identifier:
          description: |
            List of Identifier. Identifier represents Tag match identifier.
          items:
            format: int64
            type: integer
          type: array
        match_cond:
          description: |
            List of MatchCond. MatchCond represents Matching condition.
          items:
            format: int64
            type: integer
          type: array
        match_values:
          description: |
            List of MatchValues. MatchValues represents Matching values.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        tag_outer_element_id:
          description: >
            List of TagOuterElementId. TagOuterElementId represents Tag Outer
            Element Id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredTagElementGetRequest
      type: object
    InferredTagElementGetResponse:
      properties:
        InferredTagElement:
          items:
            $ref: "#/components/schemas/InferredTagElement"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredTagElementGetResponse
      type: object
    InferredTagGetRequest:
      description: InferredTagGetRequest represents the retrieval request for InferredTag.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Name of the tag.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        tag_origin:
          description: >
            List of TagOrigin. TagOrigin represents Tag origin point which
            specify from where tag is generated.
          items:
            format: int64
            type: integer
          type: array
        tag_type:
          description: |
            List of TagType. TagType represents Tag type.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredTagGetRequest
      type: object
    InferredTagGetResponse:
      properties:
        InferredTag:
          items:
            $ref: "#/components/schemas/InferredTag"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredTagGetResponse
      type: object
    InferredTagOuterElement:
      description: InferredTagOuterElement - This object represents the list of tag
        outer element. InferredTagElement can be created while creating
        InferredTagOuterElement.
      nullable: true
      properties:
        condition:
          description: Logical condition between criteria Entities.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_tag_element:
          items:
            $ref: "#/components/schemas/InferredTagElement"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        tag_criteria_id:
          description: Tag Criteria Id.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      type: object
    InferredTagOuterElementGetRequest:
      description: InferredTagOuterElementGetRequest represents the retrieval request
        for InferredTagOuterElement.
      properties:
        condition:
          description: >
            List of Condition. Condition represents Logical condition between
            criteria Entities.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        tag_criteria_id:
          description: |
            List of TagCriteriaId. TagCriteriaId represents Tag Criteria Id.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredTagOuterElementGetRequest
      type: object
    InferredTagOuterElementGetResponse:
      properties:
        InferredTagOuterElement:
          items:
            $ref: "#/components/schemas/InferredTagOuterElement"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredTagOuterElementGetResponse
      type: object
    InferredTransparentVlanUni:
      description: InferredTransparentVlanUni - This object represents the list of
        Transparent Vlan UNI.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: The type of interface.
          format: int64
          type: integer
        isid:
          description: A value that uniquely identifies the Layer 2 services instances
            network-wide.
          format: int64
          minimum: 0
          type: integer
        l2_access_id:
          description: L2 Access Id.
          type: string
        member_ports:
          description: |-
            List of members associated with the lag. This is applicable
            	only when the interface is of type LAG (Lag_Interface).
          items:
            type: string
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        switched_port_mode_type:
          description: Switched port mode.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - device_id
        - interface_name
        - interface_type
        - l2_access_id
        - switched_port_mode_type
      type: object
    InferredTransparentVlanUniGetRequest:
      description: InferredTransparentVlanUniGetRequest represents the retrieval
        request for InferredTransparentVlanUni.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: >
            List of InterfaceType. InterfaceType represents The type of
            interface.
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 services instances network-wide.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        l2_access_id:
          description: |
            List of L2AccessId. L2AccessId represents L2 Access Id.
          items:
            type: string
          type: array
        member_ports:
          description: >
            List of MemberPorts. MemberPorts represents List of members
            associated with the lag. This is applicable
            	only when the interface is of type LAG (Lag_Interface).
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        switched_port_mode_type:
          description: >
            List of SwitchedPortModeType. SwitchedPortModeType represents
            Switched port mode.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredTransparentVlanUniGetRequest
      type: object
    InferredTransparentVlanUniGetResponse:
      properties:
        InferredTransparentVlanUni:
          items:
            $ref: "#/components/schemas/InferredTransparentVlanUni"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredTransparentVlanUniGetResponse
      type: object
    InferredUserLink:
      description: InferredUserLink - DB model for the list of links between fabric
        and non-fabric devices.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        fabric_device_id:
          description: Fabric device
          type: string
        fabric_topology_id:
          description: Fabric topology id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        physical_link_id:
          description: Physical Link ID
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        user_device_id:
          description: User device
          type: string
        user_link_type:
          description: Type of user link
          type: string
      type: object
    InferredUserLinkGetRequest:
      description: InferredUserLinkGetRequest represents the retrieval request for
        InferredUserLink.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        fabric_device_id:
          description: |
            List of FabricDeviceId. FabricDeviceId represents Fabric device
          items:
            type: string
          type: array
        fabric_topology_id:
          description: >
            List of FabricTopologyId. FabricTopologyId represents Fabric
            topology id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        physical_link_id:
          description: |
            List of PhysicalLinkId. PhysicalLinkId represents Physical Link ID
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        user_device_id:
          description: |
            List of UserDeviceId. UserDeviceId represents User device
          items:
            type: string
          type: array
        user_link_type:
          description: |
            List of UserLinkType. UserLinkType represents Type of user link
          items:
            type: string
          type: array
      title: InferredUserLinkGetRequest
      type: object
    InferredUserLinkGetResponse:
      properties:
        InferredUserLink:
          items:
            $ref: "#/components/schemas/InferredUserLink"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredUserLinkGetResponse
      type: object
    InferredVlan:
      description: InferredVlan - This object that represents settings for a Vlan.
        Members can be created while creating InferredVlan.
      nullable: true
      properties:
        associated_vlan_numbers:
          description: When VlanNumber is Primary/Secondary VLAN ID, this will be
            Secondary/Primary VLAN IDs respectively.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        device_internal:
          description: Represents whether the VLAN is a device internal and doesn't show
            up in the VLAN API/CLI response.
          type: boolean
        device_vlan_name:
          description: Vlan name configured on the device
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        members:
          items:
            $ref: "#/components/schemas/InferredInterfaceVlanMap"
          nullable: true
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        stp_domain_name:
          description: STP domain name
          type: string
        stp_enabled:
          description: Represents if STP is enabled on port
          type: boolean
        stp_root_bridge:
          description: STP Root Bridge
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vlan_type:
          description: Vlan types
          format: int64
          type: integer
      required:
        - device_id
        - vlan_number
        - vlan_type
      type: object
    InferredVlanGetRequest:
      description: InferredVlanGetRequest represents the retrieval request for InferredVlan.
      properties:
        associated_vlan_numbers:
          description: >
            List of AssociatedVlanNumbers. AssociatedVlanNumbers represents When
            VlanNumber is Primary/Secondary VLAN ID, this will be
            Secondary/Primary VLAN IDs respectively.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        device_internal:
          description: >
            List of DeviceInternal. DeviceInternal represents Represents whether
            the VLAN is a device internal and doesn't show up in the VLAN
            API/CLI response.
          items:
            type: boolean
          type: array
        device_vlan_name:
          description: >
            List of DeviceVlanName. DeviceVlanName represents Vlan name
            configured on the device
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        stp_domain_name:
          description: |
            List of StpDomainName. StpDomainName represents STP domain name
          items:
            type: string
          type: array
        stp_enabled:
          description: >
            List of StpEnabled. StpEnabled represents Represents if STP is
            enabled on port
          items:
            type: boolean
          type: array
        stp_root_bridge:
          description: |
            List of StpRootBridge. StpRootBridge represents STP Root Bridge
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_type:
          description: |
            List of VlanType. VlanType represents Vlan types
          items:
            format: int64
            type: integer
          type: array
      title: InferredVlanGetRequest
      type: object
    InferredVlanGetResponse:
      properties:
        InferredVlan:
          items:
            $ref: "#/components/schemas/InferredVlan"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredVlanGetResponse
      type: object
    InferredVlanService:
      description: InferredVlanService - This object that represents settings for a
        Vlan service.
      properties:
        associated_subnets:
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Customer Id
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled_features:
          items:
            format: int64
            type: integer
          type: array
        end_point_count:
          description: Total L2 endpoints.
          format: int64
          minimum: 0
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Owner Id
          format: int64
          minimum: 0
          type: integer
        service_number:
          description: Service Number in Devices.
          format: int64
          minimum: 0
          type: integer
        stp_enabled:
          description: Represents if STP is enabled on port
          type: boolean
        stp_root_bridges:
          description: List of STP Root Bridges
          items:
            type: string
          nullable: true
          type: array
        sub_type:
          description: Service sub types
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_service_name:
          description: Default vlan service Name
          type: string
      required:
        - customer_id
        - owner_id
        - service_number
      type: object
    InferredVlanServiceGetRequest:
      description: InferredVlanServiceGetRequest represents the retrieval request for
        InferredVlanService.
      properties:
        associated_subnets:
          description: >
            List of AssociatedSubnets. AssociatedSubnets represents Associated
            Service Subnets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: |
            List of CustomerId. CustomerId represents Customer Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled_features:
          description: >
            List of EnabledFeatures. EnabledFeatures represents Enabled Features.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        end_point_count:
          description: |
            List of EndPointCount. EndPointCount represents Total L2 endpoints.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Owner Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        service_number:
          description: >
            List of ServiceNumber. ServiceNumber represents Service Number in
            Devices.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        stp_enabled:
          description: >
            List of StpEnabled. StpEnabled represents Represents if STP is
            enabled on port
          items:
            type: boolean
          type: array
        stp_root_bridges:
          description: >
            List of StpRootBridges. StpRootBridges represents List of STP Root
            Bridges
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        sub_type:
          description: |
            List of SubType. SubType represents Service sub types
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_service_name:
          description: >
            List of VlanServiceName. VlanServiceName represents Default vlan
            service Name
          items:
            type: string
          type: array
      title: InferredVlanServiceGetRequest
      type: object
    InferredVlanServiceGetResponse:
      properties:
        InferredVlanService:
          items:
            $ref: "#/components/schemas/InferredVlanService"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredVlanServiceGetResponse
      type: object
    InferredVrf:
      description: InferredVrf - This object that represents settings for a VRF.
        L3Access can be created while creating InferredVrf.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        l3_access:
          items:
            $ref: "#/components/schemas/InferredL3Access"
          nullable: true
          type: array
        name:
          description: VRF name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlans:
          description: List of Vlans.
          items:
            type: string
          nullable: true
          type: array
      required:
        - device_id
        - name
      type: object
    InferredVrfGetRequest:
      description: InferredVrfGetRequest represents the retrieval request for InferredVrf.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents VRF name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlans:
          description: |
            List of Vlans. Vlans represents List of Vlans.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
      title: InferredVrfGetRequest
      type: object
    InferredVrfGetResponse:
      properties:
        InferredVrf:
          items:
            $ref: "#/components/schemas/InferredVrf"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredVrfGetResponse
      type: object
    InferredVrfService:
      description: InferredVrfService - This object that represents settings for a VRF
        service.
      properties:
        associated_subnets:
          items:
            format: int64
            type: integer
          type: array
        associated_vlans:
          description: List of Vlans.
          items:
            type: string
          nullable: true
          type: array
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Customer Id
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled_features:
          items:
            format: int64
            type: integer
          type: array
        end_point_count:
          description: Total L2 endpoints.
          format: int64
          minimum: 0
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        owner_id:
          description: Owner Id
          format: int64
          minimum: 0
          type: integer
        service_name:
          description: Service Number in Devices.
          type: string
        sub_type:
          description: Service sub types
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - customer_id
        - owner_id
        - service_name
      type: object
    InferredVrfServiceGetRequest:
      description: InferredVrfServiceGetRequest represents the retrieval request for
        InferredVrfService.
      properties:
        associated_subnets:
          description: >
            List of AssociatedSubnets. AssociatedSubnets represents Associated
            Service Subnets.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        associated_vlans:
          description: |
            List of AssociatedVlans. AssociatedVlans represents List of Vlans.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: |
            List of CustomerId. CustomerId represents Customer Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled_features:
          description: >
            List of EnabledFeatures. EnabledFeatures represents Enabled Features.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        end_point_count:
          description: |
            List of EndPointCount. EndPointCount represents Total L2 endpoints.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Owner Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        service_name:
          description: >
            List of ServiceName. ServiceName represents Service Number in
            Devices.
          items:
            type: string
          type: array
        sub_type:
          description: |
            List of SubType. SubType represents Service sub types
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredVrfServiceGetRequest
      type: object
    InferredVrfServiceGetResponse:
      properties:
        InferredVrfService:
          items:
            $ref: "#/components/schemas/InferredVrfService"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredVrfServiceGetResponse
      type: object
    InferredVrrpInstance:
      description: InferredVrrpInstance - This object that represents settings for
        list of VRRP instance.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        instance_id:
          description: VRRP Instance Id
          format: int64
          minimum: 0
          type: integer
        is_active:
          description: VRRP active or not.
          type: boolean
        l3_gateway_interface_id:
          description: L3 Gateway Interface Id.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        state:
          description: The VRRP State.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        virtual_ips:
          description: Virtual IPs
          items:
            type: string
          nullable: true
          type: array
      required:
        - instance_id
        - l3_gateway_interface_id
      type: object
    InferredVrrpInstanceGetRequest:
      description: InferredVrrpInstanceGetRequest represents the retrieval request for
        InferredVrrpInstance.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instance_id:
          description: |
            List of InstanceId. InstanceId represents VRRP Instance Id
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        is_active:
          description: |
            List of IsActive. IsActive represents VRRP active or not.
          items:
            type: boolean
          type: array
        l3_gateway_interface_id:
          description: >
            List of L3GatewayInterfaceId. L3GatewayInterfaceId represents L3
            Gateway Interface Id.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        state:
          description: |
            List of State. State represents The VRRP State.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        virtual_ips:
          description: |
            List of VirtualIps. VirtualIps represents Virtual IPs
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
      title: InferredVrrpInstanceGetRequest
      type: object
    InferredVrrpInstanceGetResponse:
      properties:
        InferredVrrpInstance:
          items:
            $ref: "#/components/schemas/InferredVrrpInstance"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredVrrpInstanceGetResponse
      type: object
    InferredWirelessInterface:
      description: InferredWirelessInterface - This object represents the inferred
        wireless interface.
      properties:
        asset_wireless_interface_id:
          description: Asset Wireless Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_device_id:
          description: Device Id
          type: string
        inferred_interface_id:
          description: Interface Id
          type: string
        name:
          description: Name
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - asset_wireless_interface_id
        - inferred_device_id
        - inferred_interface_id
        - name
      type: object
    InferredWirelessInterfaceErrorStats:
      description: InferredWirelessInterfaceErrorStats - This object represents the
        inferred wireless interface error stats.
      properties:
        alignment:
          description: Alignment
          format: int64
          type: integer
        carrier:
          description: Carrier
          format: int64
          type: integer
        carrier_sense:
          description: Carrier Sense
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deferred_tx:
          description: Deferred Tx
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        excessive_collision:
          description: Excessive Collision
          format: int64
          type: integer
        fcs:
          description: Fcs
          format: int64
          type: integer
        frame_too_long:
          description: Frame Too Long
          format: int64
          type: integer
        frame_too_short:
          description: Frame Too Short
          format: int64
          type: integer
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        in_discards:
          description: In Discards
          format: int64
          type: integer
        in_errors:
          description: In Errors
          format: int64
          type: integer
        inferred_wireless_interface_id:
          description: Wireless Interface Id
          type: string
        late_collision:
          description: Late Collision
          format: int64
          type: integer
        link_failures:
          description: Link Failures
          format: int64
          type: integer
        link_inactive:
          description: Link Inactive
          format: int64
          type: integer
        mac_rx:
          description: Mac Rx
          format: int64
          type: integer
        mac_tx:
          description: Mac Tx
          format: int64
          type: integer
        multiple_collision:
          description: Multiple Collision
          format: int64
          type: integer
        out_discards:
          description: Out Discards
          format: int64
          type: integer
        out_errors:
          description: Out Errors
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        packet:
          description: Packet
          format: int64
          type: integer
        single_collision:
          description: Single Collision
          format: int64
          type: integer
        sqe_test:
          description: Sqe Test
          format: int64
          type: integer
        unknown_protos:
          description: Unknown Protos
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - inferred_wireless_interface_id
      type: object
    InferredWirelessInterfaceErrorStatsGetRequest:
      description: InferredWirelessInterfaceErrorStatsGetRequest represents the
        retrieval request for InferredWirelessInterfaceErrorStats.
      properties:
        alignment:
          description: |
            List of Alignment. Alignment represents Alignment
          items:
            format: int64
            type: integer
          type: array
        carrier:
          description: |
            List of Carrier. Carrier represents Carrier
          items:
            format: int64
            type: integer
          type: array
        carrier_sense:
          description: |
            List of CarrierSense. CarrierSense represents Carrier Sense
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deferred_tx:
          description: |
            List of DeferredTx. DeferredTx represents Deferred Tx
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        excessive_collision:
          description: >
            List of ExcessiveCollision. ExcessiveCollision represents Excessive
            Collision
          items:
            format: int64
            type: integer
          type: array
        fcs:
          description: |
            List of Fcs. Fcs represents Fcs
          items:
            format: int64
            type: integer
          type: array
        frame_too_long:
          description: |
            List of FrameTooLong. FrameTooLong represents Frame Too Long
          items:
            format: int64
            type: integer
          type: array
        frame_too_short:
          description: |
            List of FrameTooShort. FrameTooShort represents Frame Too Short
          items:
            format: int64
            type: integer
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        in_discards:
          description: |
            List of InDiscards. InDiscards represents In Discards
          items:
            format: int64
            type: integer
          type: array
        in_errors:
          description: |
            List of InErrors. InErrors represents In Errors
          items:
            format: int64
            type: integer
          type: array
        inferred_wireless_interface_id:
          description: >
            List of InferredWirelessInterfaceId. InferredWirelessInterfaceId
            represents Wireless Interface Id
          items:
            type: string
          type: array
        late_collision:
          description: |
            List of LateCollision. LateCollision represents Late Collision
          items:
            format: int64
            type: integer
          type: array
        link_failures:
          description: |
            List of LinkFailures. LinkFailures represents Link Failures
          items:
            format: int64
            type: integer
          type: array
        link_inactive:
          description: |
            List of LinkInactive. LinkInactive represents Link Inactive
          items:
            format: int64
            type: integer
          type: array
        mac_rx:
          description: |
            List of MacRx. MacRx represents Mac Rx
          items:
            format: int64
            type: integer
          type: array
        mac_tx:
          description: |
            List of MacTx. MacTx represents Mac Tx
          items:
            format: int64
            type: integer
          type: array
        multiple_collision:
          description: >
            List of MultipleCollision. MultipleCollision represents Multiple
            Collision
          items:
            format: int64
            type: integer
          type: array
        out_discards:
          description: |
            List of OutDiscards. OutDiscards represents Out Discards
          items:
            format: int64
            type: integer
          type: array
        out_errors:
          description: |
            List of OutErrors. OutErrors represents Out Errors
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        packet:
          description: |
            List of Packet. Packet represents Packet
          items:
            format: int64
            type: integer
          type: array
        single_collision:
          description: |
            List of SingleCollision. SingleCollision represents Single Collision
          items:
            format: int64
            type: integer
          type: array
        sqe_test:
          description: |
            List of SqeTest. SqeTest represents Sqe Test
          items:
            format: int64
            type: integer
          type: array
        unknown_protos:
          description: |
            List of UnknownProtos. UnknownProtos represents Unknown Protos
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredWirelessInterfaceErrorStatsGetRequest
      type: object
    InferredWirelessInterfaceErrorStatsGetResponse:
      properties:
        InferredWirelessInterfaceErrorStats:
          items:
            $ref: "#/components/schemas/InferredWirelessInterfaceErrorStats"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredWirelessInterfaceErrorStatsGetResponse
      type: object
    InferredWirelessInterfaceGetRequest:
      description: InferredWirelessInterfaceGetRequest represents the retrieval
        request for InferredWirelessInterface.
      properties:
        asset_wireless_interface_id:
          description: >
            List of AssetWirelessInterfaceId. AssetWirelessInterfaceId
            represents Asset Wireless Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        inferred_device_id:
          description: |
            List of InferredDeviceId. InferredDeviceId represents Device Id
          items:
            type: string
          type: array
        inferred_interface_id:
          description: >
            List of InferredInterfaceId. InferredInterfaceId represents
            Interface Id
          items:
            type: string
          type: array
        name:
          description: |
            List of Name. Name represents Name
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredWirelessInterfaceGetRequest
      type: object
    InferredWirelessInterfaceGetResponse:
      properties:
        InferredWirelessInterface:
          items:
            $ref: "#/components/schemas/InferredWirelessInterface"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredWirelessInterfaceGetResponse
      type: object
    InferredWirelessInterfaceInOutStats:
      description: InferredWirelessInterfaceInOutStats - This object represents the
        inferred wireless interface in-out stats.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        in_broadcast_pkts:
          description: Incoming broadcast packets
          format: int64
          type: integer
        in_flow_ctrl_pkts:
          description: Incoming flow control packets
          format: int64
          type: integer
        in_multicast_pkts:
          description: Incoming multicast packets
          format: int64
          type: integer
        in_octets:
          description: Incoming octets
          format: int64
          type: integer
        in_pfc_pkts:
          description: Incoming priority-based flow control packets
          format: int64
          type: integer
        in_ucast_pkts:
          description: Incoming unicast packets
          format: int64
          type: integer
        inferred_wireless_interface_id:
          description: Wireless Interface Id
          type: string
        out_broadcast_pkts:
          description: Outgoing broadcast packets
          format: int64
          type: integer
        out_flow_ctrl_pkts:
          description: Outgoing flow control packets
          format: int64
          type: integer
        out_multicast_pkts:
          description: Outgoing multicast packets
          format: int64
          type: integer
        out_octets:
          description: Outgoing octets
          format: int64
          type: integer
        out_pfc_pkts:
          description: Outgoing priority-based flow control packets
          format: int64
          type: integer
        out_ucast_pkts:
          description: Outgoing unicast packets
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - inferred_wireless_interface_id
      type: object
    InferredWirelessInterfaceInOutStatsGetRequest:
      description: InferredWirelessInterfaceInOutStatsGetRequest represents the
        retrieval request for InferredWirelessInterfaceInOutStats.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        in_broadcast_pkts:
          description: >
            List of InBroadcastPkts. InBroadcastPkts represents Incoming
            broadcast packets
          items:
            format: int64
            type: integer
          type: array
        in_flow_ctrl_pkts:
          description: >
            List of InFlowCtrlPkts. InFlowCtrlPkts represents Incoming flow
            control packets
          items:
            format: int64
            type: integer
          type: array
        in_multicast_pkts:
          description: >
            List of InMulticastPkts. InMulticastPkts represents Incoming
            multicast packets
          items:
            format: int64
            type: integer
          type: array
        in_octets:
          description: |
            List of InOctets. InOctets represents Incoming octets
          items:
            format: int64
            type: integer
          type: array
        in_pfc_pkts:
          description: >
            List of InPfcPkts. InPfcPkts represents Incoming priority-based flow
            control packets
          items:
            format: int64
            type: integer
          type: array
        in_ucast_pkts:
          description: |
            List of InUcastPkts. InUcastPkts represents Incoming unicast packets
          items:
            format: int64
            type: integer
          type: array
        inferred_wireless_interface_id:
          description: >
            List of InferredWirelessInterfaceId. InferredWirelessInterfaceId
            represents Wireless Interface Id
          items:
            type: string
          type: array
        out_broadcast_pkts:
          description: >
            List of OutBroadcastPkts. OutBroadcastPkts represents Outgoing
            broadcast packets
          items:
            format: int64
            type: integer
          type: array
        out_flow_ctrl_pkts:
          description: >
            List of OutFlowCtrlPkts. OutFlowCtrlPkts represents Outgoing flow
            control packets
          items:
            format: int64
            type: integer
          type: array
        out_multicast_pkts:
          description: >
            List of OutMulticastPkts. OutMulticastPkts represents Outgoing
            multicast packets
          items:
            format: int64
            type: integer
          type: array
        out_octets:
          description: |
            List of OutOctets. OutOctets represents Outgoing octets
          items:
            format: int64
            type: integer
          type: array
        out_pfc_pkts:
          description: >
            List of OutPfcPkts. OutPfcPkts represents Outgoing priority-based
            flow control packets
          items:
            format: int64
            type: integer
          type: array
        out_ucast_pkts:
          description: >
            List of OutUcastPkts. OutUcastPkts represents Outgoing unicast
            packets
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: InferredWirelessInterfaceInOutStatsGetRequest
      type: object
    InferredWirelessInterfaceInOutStatsGetResponse:
      properties:
        InferredWirelessInterfaceInOutStats:
          items:
            $ref: "#/components/schemas/InferredWirelessInterfaceInOutStats"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredWirelessInterfaceInOutStatsGetResponse
      type: object
    InferredWirelessInterfaceUtilizationInfo:
      description: InferredWirelessInterfaceUtilizationInfo - This object represents
        the inferred wireless interface utilization info.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        inferred_wireless_interface_id:
          description: Wireless Interface Id
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        utilization_max_rx:
          description: Utilization Max Rx
          type: number
        utilization_max_tx:
          description: Utilization Max Tx
          type: number
        utilization_rx:
          description: Utilization Rx
          type: number
        utilization_tx:
          description: Utilization Tx
          type: number
      required:
        - inferred_wireless_interface_id
      type: object
    InferredWirelessInterfaceUtilizationInfoGetRequest:
      description: InferredWirelessInterfaceUtilizationInfoGetRequest represents the
        retrieval request for InferredWirelessInterfaceUtilizationInfo.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        inferred_wireless_interface_id:
          description: >
            List of InferredWirelessInterfaceId. InferredWirelessInterfaceId
            represents Wireless Interface Id
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        utilization_max_rx:
          description: >
            List of UtilizationMaxRx. UtilizationMaxRx represents Utilization
            Max Rx
          items:
            type: number
          type: array
        utilization_max_tx:
          description: >
            List of UtilizationMaxTx. UtilizationMaxTx represents Utilization
            Max Tx
          items:
            type: number
          type: array
        utilization_rx:
          description: |
            List of UtilizationRx. UtilizationRx represents Utilization Rx
          items:
            type: number
          type: array
        utilization_tx:
          description: |
            List of UtilizationTx. UtilizationTx represents Utilization Tx
          items:
            type: number
          type: array
      title: InferredWirelessInterfaceUtilizationInfoGetRequest
      type: object
    InferredWirelessInterfaceUtilizationInfoGetResponse:
      properties:
        InferredWirelessInterfaceUtilizationInfo:
          items:
            $ref: "#/components/schemas/InferredWirelessInterfaceUtilizationInfo"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: InferredWirelessInterfaceUtilizationInfoGetResponse
      type: object
    IntentAutoSenseFabricAttach:
      description: IntentAutoSenseFabricAttach - Fabric attach authentication key for
        auto-sense ports.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        auth_is_encrypted:
          description: Specifies if the secret is encrypted (true) or clear text (false)
          type: boolean
        auth_value:
          description: >-
            The fabric attach authentication key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 32 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          type: string
        camera_dot1x_state:
          description: >-
            Specifies the fabric attach 802.1x status for camera I-SID used by
            auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          type: string
        camera_isid:
          description: |-
            Specifies the fabric attach camera I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        msg_auth_enabled:
          description: Enable fabric attach MsgAuth for auto-sense ports.
          type: boolean
        ovs_isid:
          description: |-
            Specifies the fabric attach OVS I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        ovs_status:
          description: >-
            Specifies the fabric attach 802.1x status for OVS I-SID used by
            auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        proxy_mgmt_cvid:
          description: >-
            Specifies the fabric attach proxy management Cvid used by auto-sense
            ports.
            	Cvid is set only for tagged traffic.
          format: int64
          type: integer
        proxy_mgmt_isid:
          description: >-
            Specifies the fabric attach proxy management I-SID used by
            auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        proxy_no_auth_isid:
          description: >-
            Specifies the fabric attach proxy no-auth I-SID used by auto-sense
            ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        wap_type1_isid:
          description: >-
            Specifies the fabric attach WAP type-1 I-SID used by auto-sense
            ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        wap_type1_status:
          description: >-
            Specifies the fabric attach 802.1x status for WAP type-1 I-SID used
            by auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          type: string
      required:
        - intent_device_id
      type: object
    IntentAutoSenseFabricAttachGetRequest:
      description: IntentAutoSenseFabricAttachGetRequest represents the retrieval
        request for IntentAutoSenseFabricAttach.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        auth_is_encrypted:
          description: >
            List of AuthIsEncrypted. AuthIsEncrypted represents Specifies if the
            secret is encrypted (true) or clear text (false)
          items:
            type: boolean
          type: array
        auth_value:
          description: >
            List of AuthValue. AuthValue represents  The fabric attach
            authentication key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 32 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          items:
            type: string
          type: array
        camera_dot1x_state:
          description: >
            List of CameraDot1xState. CameraDot1xState represents Specifies the
            fabric attach 802.1x status for camera I-SID used by auto-sense
            ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          items:
            type: string
          type: array
        camera_isid:
          description: >
            List of CameraIsid. CameraIsid represents Specifies the fabric
            attach camera I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        msg_auth_enabled:
          description: >
            List of MsgAuthEnabled. MsgAuthEnabled represents Enable fabric
            attach MsgAuth for auto-sense ports.
          items:
            type: boolean
          type: array
        ovs_isid:
          description: >
            List of OvsIsid. OvsIsid represents Specifies the fabric attach OVS
            I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        ovs_status:
          description: >
            List of OvsStatus. OvsStatus represents Specifies the fabric attach
            802.1x status for OVS I-SID used by auto-sense ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        proxy_mgmt_cvid:
          description: >
            List of ProxyMgmtCvid. ProxyMgmtCvid represents Specifies the fabric
            attach proxy management Cvid used by auto-sense ports.
            	Cvid is set only for tagged traffic.
          items:
            format: int64
            type: integer
          type: array
        proxy_mgmt_isid:
          description: >
            List of ProxyMgmtIsid. ProxyMgmtIsid represents Specifies the fabric
            attach proxy management I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        proxy_no_auth_isid:
          description: >
            List of ProxyNoAuthIsid. ProxyNoAuthIsid represents Specifies the
            fabric attach proxy no-auth I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        wap_type1_isid:
          description: >
            List of WapType1Isid. WapType1Isid represents Specifies the fabric
            attach WAP type-1 I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        wap_type1_status:
          description: >
            List of WapType1Status. WapType1Status represents Specifies the
            fabric attach 802.1x status for WAP type-1 I-SID used by auto-sense
            ports.
            	AUTO - EAPOL is enabled and based on the 802.1x authentication, traffic is allowed.
            	FORCE_AUTHORIZED - EAPOL is disabled on the port and the port allows all traffic
            	irrespective of 802.1x authentication.
          items:
            type: string
          type: array
      title: IntentAutoSenseFabricAttachGetRequest
      type: object
    IntentAutoSenseFabricAttachGetResponse:
      properties:
        IntentAutoSenseFabricAttach:
          items:
            $ref: "#/components/schemas/IntentAutoSenseFabricAttach"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentAutoSenseFabricAttachGetResponse
      type: object
    IntentAutoSenseGlobal:
      description: IntentAutoSenseGlobal - This object represents the global
        auto-sense settings.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        data_isid:
          description: |-
            Specifies the data I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        dhcp_detection_enabled:
          description: Enable DHCP detection for auto-sense ports.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        onboarding_isid:
          description: |-
            Specifies the onboarding I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        voice_cvid:
          description: |-
            Specifies the voice Cvid associated with
            	the voice I-SID used by auto-sense ports. Cvid is
            	set only for tagged voice traffic. This needs to be
            	set along the I-SID.
          format: int64
          type: integer
        voice_dot1x_lldp_auth_enabled:
          description: Enable 802.1x LLDP authentication for voice auto-sense ports.
          type: boolean
        voice_isid:
          description: |-
            Specifies the voice I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          format: int64
          type: integer
        wait_interval:
          description: Specifies the wait interval in seconds for the 'WAIT' state of
            auto-sense's finite state machine.
          format: int64
          type: integer
      required:
        - intent_device_id
      type: object
    IntentAutoSenseGlobalGetRequest:
      description: IntentAutoSenseGlobalGetRequest represents the retrieval request
        for IntentAutoSenseGlobal.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        data_isid:
          description: >
            List of DataIsid. DataIsid represents Specifies the data I-SID used
            by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        dhcp_detection_enabled:
          description: >
            List of DhcpDetectionEnabled. DhcpDetectionEnabled represents Enable
            DHCP detection for auto-sense ports.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        onboarding_isid:
          description: >
            List of OnboardingIsid. OnboardingIsid represents Specifies the
            onboarding I-SID used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        voice_cvid:
          description: >
            List of VoiceCvid. VoiceCvid represents Specifies the voice Cvid
            associated with
            	the voice I-SID used by auto-sense ports. Cvid is
            	set only for tagged voice traffic. This needs to be
            	set along the I-SID.
          items:
            format: int64
            type: integer
          type: array
        voice_dot1x_lldp_auth_enabled:
          description: >
            List of VoiceDot1xLldpAuthEnabled. VoiceDot1xLldpAuthEnabled
            represents Enable 802.1x LLDP authentication for voice auto-sense
            ports.
          items:
            type: boolean
          type: array
        voice_isid:
          description: >
            List of VoiceIsid. VoiceIsid represents Specifies the voice I-SID
            used by auto-sense ports.
            	A value of 0 means the I-SID is not set.
          items:
            format: int64
            type: integer
          type: array
        wait_interval:
          description: >
            List of WaitInterval. WaitInterval represents Specifies the wait
            interval in seconds for the 'WAIT' state of auto-sense's finite
            state machine.
          items:
            format: int64
            type: integer
          type: array
      title: IntentAutoSenseGlobalGetRequest
      type: object
    IntentAutoSenseGlobalGetResponse:
      properties:
        IntentAutoSenseGlobal:
          items:
            $ref: "#/components/schemas/IntentAutoSenseGlobal"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentAutoSenseGlobalGetResponse
      type: object
    IntentAutoSenseIsis:
      description: IntentAutoSenseIsis - This object represents the global auto-sense
        ISIS settings.
      properties:
        asset_device_id:
          description: UUID of the device
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        hello_auth_is_encrypted:
          description: Specifies if the secret is encrypted (true) or clear text (false)
          type: boolean
        hello_auth_value:
          description: >-
            The ISIS HelloAuth key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 16 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          type: string
        hello_key_id:
          description: |-
            Specifies ISIS HelloAuth key ID for auto-sense ports.
            	This needs to be set along helloAuthType and helloAuthKey.
          format: int64
          type: integer
        hello_type:
          description: Set ISIS HelloAuth type for auto-sense ports.
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_device_id
      type: object
    IntentAutoSenseIsisGetRequest:
      description: IntentAutoSenseIsisGetRequest represents the retrieval request for
        IntentAutoSenseIsis.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents UUID of the device
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        hello_auth_is_encrypted:
          description: >
            List of HelloAuthIsEncrypted. HelloAuthIsEncrypted represents
            Specifies if the secret is encrypted (true) or clear text (false)
          items:
            type: boolean
          type: array
        hello_auth_value:
          description: >
            List of HelloAuthValue. HelloAuthValue represents The ISIS HelloAuth
            key for auto-sense ports.
            	The maximum length for configuration of a cleartext password is 16 characters.
            	The maximum length of 512 characters accommodates the longest possible encrypted password.
          items:
            type: string
          type: array
        hello_key_id:
          description: >
            List of HelloKeyId. HelloKeyId represents Specifies ISIS HelloAuth
            key ID for auto-sense ports.
            	This needs to be set along helloAuthType and helloAuthKey.
          items:
            format: int64
            type: integer
          type: array
        hello_type:
          description: >
            List of HelloType. HelloType represents Set ISIS HelloAuth type for
            auto-sense ports.
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentAutoSenseIsisGetRequest
      type: object
    IntentAutoSenseIsisGetResponse:
      properties:
        IntentAutoSenseIsis:
          items:
            $ref: "#/components/schemas/IntentAutoSenseIsis"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentAutoSenseIsisGetResponse
      type: object
    IntentDevice:
      description: IntentDevice - This object represents the Intent device information.
      properties:
        asset_device_id:
          description: Asset Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_type:
          description: Intent device type.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        serial_number:
          description: Device unique serial number.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_device_type
        - serial_number
      type: object
    IntentDeviceGetRequest:
      description: IntentDeviceGetRequest represents the retrieval request for IntentDevice.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Asset Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_type:
          description: >
            List of IntentDeviceType. IntentDeviceType represents Intent device
            type.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        serial_number:
          description: >
            List of SerialNumber. SerialNumber represents Device unique serial
            number.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentDeviceGetRequest
      type: object
    IntentDeviceGetResponse:
      properties:
        IntentDevice:
          items:
            $ref: "#/components/schemas/IntentDevice"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentDeviceGetResponse
      type: object
    IntentIsisCircuitConfig:
      description: IntentIsisCircuitConfig - This object represents the ISIS
        configuration per interface. IntentIsisCircuitSpbmSetting can be created
        while creating IntentIsisCircuitConfig.
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_interface_id:
          description: Interface Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          default: false
          description: ISIS circuit state.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_isis_circuit_spbm_setting:
          $ref: "#/components/schemas/IntentIsisCircuitSpbmSetting"
        intent_isis_global_config_id:
          description: ISIS Global Config Id
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        interface_name:
          description: Interface name
          type: string
        interface_type:
          description: ISIS circuit type.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - area_type
        - intent_isis_global_config_id
        - interface_name
        - interface_type
      type: object
    IntentIsisCircuitConfigGetRequest:
      description: IntentIsisCircuitConfigGetRequest represents the retrieval request
        for IntentIsisCircuitConfig.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_interface_id:
          description: |
            List of AssetInterfaceId. AssetInterfaceId represents Interface Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents ISIS circuit state.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_isis_global_config_id:
          description: >
            List of IntentIsisGlobalConfigId. IntentIsisGlobalConfigId
            represents ISIS Global Config Id
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        interface_name:
          description: |
            List of InterfaceName. InterfaceName represents Interface name
          items:
            type: string
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents ISIS circuit type.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentIsisCircuitConfigGetRequest
      type: object
    IntentIsisCircuitConfigGetResponse:
      properties:
        IntentIsisCircuitConfig:
          items:
            $ref: "#/components/schemas/IntentIsisCircuitConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentIsisCircuitConfigGetResponse
      type: object
    IntentIsisCircuitSpbmSetting:
      description: IntentIsisCircuitSpbmSetting - This object represents the ISIS SPBM
        interface configuration.
      nullable: true
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          default: true
          description: SPBM Instance State.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_isis_circuit_config_id:
          description: ISIS Circuit Config Id
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        interface_type:
          description: ISIS circuit type.
          format: int64
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        spbm_id:
          description: SPBM Id
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_isis_circuit_config_id
      type: object
    IntentIsisCircuitSpbmSettingGetRequest:
      description: IntentIsisCircuitSpbmSettingGetRequest represents the retrieval
        request for IntentIsisCircuitSpbmSetting.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SPBM Instance State.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_isis_circuit_config_id:
          description: >
            List of IntentIsisCircuitConfigId. IntentIsisCircuitConfigId
            represents ISIS Circuit Config Id
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        interface_type:
          description: |
            List of InterfaceType. InterfaceType represents ISIS circuit type.
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        spbm_id:
          description: |
            List of SpbmId. SpbmId represents SPBM Id
          items:
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentIsisCircuitSpbmSettingGetRequest
      type: object
    IntentIsisCircuitSpbmSettingGetResponse:
      properties:
        IntentIsisCircuitSpbmSetting:
          items:
            $ref: "#/components/schemas/IntentIsisCircuitSpbmSetting"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentIsisCircuitSpbmSettingGetResponse
      type: object
    IntentIsisGlobalConfig:
      description: >-
        IntentIsisGlobalConfig - This object represents the list of ISIS global
        settings configured per each ISIS area

        type: home and remote area. The area type is present in the payload and
        based on that,

        the attributes for each type is different. For devices which do not
        support multi-area,

        response will not include remote area and in the home area will not
        include areaName,

        areaVNodeNickname, areaVNodeSysId and areaVNodeSysName
        IntentSpbmInstance can be created while creating IntentIsisGlobalConfig.
      properties:
        area_name:
          description: |-
            The ISIS system area-name. This attribute is available in both areas
            	and if the device does not support multi-area, it won`t be included
            	in the home response.
          type: string
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        intent_spbm_instance:
          items:
            $ref: "#/components/schemas/IntentSpbmInstance"
          nullable: true
          type: array
        ip_source_address:
          description: |-
            ISIS SPBM Ipv4 address. Define an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          type: string
        manual_area_address:
          description: |-
            A manually configured area address for this system. This
            	attribute is available in both areas.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        sys_admin_state:
          default: 1
          description: |-
            The administrative state of this Intermediate System.
            	Setting this object to the value 'on' when its current
            	value is 'off' enables the Intermediate System. This
            	attribute is available in both areas.
          format: int64
          type: integer
        sys_id:
          description: |-
            The ID for this Intermediate System. This value is
            	appended to each of the area addresses to form the
            	Network Entity Titles. This attribute is available
            	in both areas.
          type: string
        sys_name:
          description: |-
            The hostname listed in LSP, or the system name if host
            	name is not configured.
          type: string
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - area_type
        - intent_device_id
      type: object
    IntentIsisGlobalConfigGetRequest:
      description: IntentIsisGlobalConfigGetRequest represents the retrieval request
        for IntentIsisGlobalConfig.
      properties:
        area_name:
          description: >
            List of AreaName. AreaName represents The ISIS system area-name.
            This attribute is available in both areas
            	and if the device does not support multi-area, it won`t be included
            	in the home response.
          items:
            type: string
          type: array
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        ip_source_address:
          description: >
            List of IpSourceAddress. IpSourceAddress represents ISIS SPBM Ipv4
            address. Define an existing router interface
            	for management (ping/traceroute). This attribute is
            	available in home area.
          items:
            type: string
          type: array
        manual_area_address:
          description: >
            List of ManualAreaAddress. ManualAreaAddress represents A manually
            configured area address for this system. This
            	attribute is available in both areas.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        sys_admin_state:
          description: >
            List of SysAdminState. SysAdminState represents The administrative
            state of this Intermediate System.
            	Setting this object to the value 'on' when its current
            	value is 'off' enables the Intermediate System. This
            	attribute is available in both areas.
          items:
            format: int64
            type: integer
          type: array
        sys_id:
          description: >
            List of SysId. SysId represents The ID for this Intermediate System.
            This value is
            	appended to each of the area addresses to form the
            	Network Entity Titles. This attribute is available
            	in both areas.
          items:
            type: string
          type: array
        sys_name:
          description: >
            List of SysName. SysName represents The hostname listed in LSP, or
            the system name if host
            	name is not configured.
          items:
            type: string
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentIsisGlobalConfigGetRequest
      type: object
    IntentIsisGlobalConfigGetResponse:
      properties:
        IntentIsisGlobalConfig:
          items:
            $ref: "#/components/schemas/IntentIsisGlobalConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentIsisGlobalConfigGetResponse
      type: object
    IntentL2VsnCvlanConfig:
      description: IntentL2VsnCvlanConfig - This object represents the CVLAN
        configuration on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        platform_vlan_number:
          description: The identifier of the VLAN.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_device_id
        - isid
        - platform_vlan_number
      type: object
    IntentL2VsnCvlanConfigGetRequest:
      description: IntentL2VsnCvlanConfigGetRequest represents the retrieval request
        for IntentL2VsnCvlanConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        platform_vlan_number:
          description: >
            List of PlatformVlanNumber. PlatformVlanNumber represents The
            identifier of the VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentL2VsnCvlanConfigGetRequest
      type: object
    IntentL2VsnCvlanConfigGetResponse:
      properties:
        IntentL2VsnCvlanConfig:
          items:
            $ref: "#/components/schemas/IntentL2VsnCvlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentL2VsnCvlanConfigGetResponse
      type: object
    IntentL2VsnSuniConfig:
      description: IntentL2VsnSuniConfig - This object represents the SUNI
        configuration on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        platform_vlan_number:
          description: The identifier of the VLAN.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_device_id
        - isid
      type: object
    IntentL2VsnSuniConfigGetRequest:
      description: IntentL2VsnSuniConfigGetRequest represents the retrieval request
        for IntentL2VsnSuniConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        platform_vlan_number:
          description: >
            List of PlatformVlanNumber. PlatformVlanNumber represents The
            identifier of the VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentL2VsnSuniConfigGetRequest
      type: object
    IntentL2VsnSuniConfigGetResponse:
      properties:
        IntentL2VsnSuniConfig:
          items:
            $ref: "#/components/schemas/IntentL2VsnSuniConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentL2VsnSuniConfigGetResponse
      type: object
    IntentL2VsnTuniConfig:
      description: IntentL2VsnTuniConfig - This object represents the TUNI
        configuration on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        isid:
          description: >-
            A value that uniquely identifies the Layer 2 and Layer 3 services
            instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          format: int64
          minimum: 0
          type: integer
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_device_id
        - isid
      type: object
    IntentL2VsnTuniConfigGetRequest:
      description: IntentL2VsnTuniConfigGetRequest represents the retrieval request
        for IntentL2VsnTuniConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        isid:
          description: >
            List of Isid. Isid represents A value that uniquely identifies the
            Layer 2 and Layer 3 services instances network-wide.
            	Values greater than 16000000 are for Layer 2 instances dynamically added. For set operations,
            	the ISID maximum value is 15999999.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentL2VsnTuniConfigGetRequest
      type: object
    IntentL2VsnTuniConfigGetResponse:
      properties:
        IntentL2VsnTuniConfig:
          items:
            $ref: "#/components/schemas/IntentL2VsnTuniConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentL2VsnTuniConfigGetResponse
      type: object
    IntentSpbmGlobalSettingsConfig:
      description: >-
        IntentSpbmGlobalSettingsConfig - The object that represents
        Configuration of SPBM global settings

        on the device.
      properties:
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        enabled:
          default: false
          description: SPBM global status.
          type: boolean
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        nickname_server_enabled:
          default: false
          description: Nick-name Server enabled state. Disabled via duplicate detection.
          type: boolean
        nickname_server_prefix:
          description: >-
            The prefix is the value of the first octet of the Nick-name. There
            are 256 possible
            	Nick-name prefixes in total: 0.00.00 - F.F0.00 corresponding to 256 ranges: 0.0x.xx -
            	F.Fx.xx, with 4k Nick-names each. This configuration is only used when the SPBM
            	Nick-name Server feature is enabled.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
      required:
        - intent_device_id
      type: object
    IntentSpbmGlobalSettingsConfigGetRequest:
      description: IntentSpbmGlobalSettingsConfigGetRequest represents the retrieval
        request for IntentSpbmGlobalSettingsConfig.
      properties:
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        enabled:
          description: |
            List of Enabled. Enabled represents SPBM global status.
          items:
            type: boolean
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        nickname_server_enabled:
          description: >
            List of NicknameServerEnabled. NicknameServerEnabled represents
            Nick-name Server enabled state. Disabled via duplicate detection.
          items:
            type: boolean
          type: array
        nickname_server_prefix:
          description: >
            List of NicknameServerPrefix. NicknameServerPrefix represents The
            prefix is the value of the first octet of the Nick-name. There are
            256 possible
            	Nick-name prefixes in total: 0.00.00 - F.F0.00 corresponding to 256 ranges: 0.0x.xx -
            	F.Fx.xx, with 4k Nick-names each. This configuration is only used when the SPBM
            	Nick-name Server feature is enabled.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
      title: IntentSpbmGlobalSettingsConfigGetRequest
      type: object
    IntentSpbmGlobalSettingsConfigGetResponse:
      properties:
        IntentSpbmGlobalSettingsConfig:
          items:
            $ref: "#/components/schemas/IntentSpbmGlobalSettingsConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentSpbmGlobalSettingsConfigGetResponse
      type: object
    IntentSpbmInstance:
      description: IntentSpbmInstance - The object that represents settings for SPBM
        instances configuration.
      nullable: true
      properties:
        area_type:
          description: ISIS area type.
          format: int64
          type: integer
        asset_device_id:
          description: Device Id
          type: string
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        instance_id:
          description: SPBM instance ID.
          type: integer
        intent_isis_global_config_id:
          description: ISIS Global Config Id
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        ip_shortcut_enabled:
          default: false
          description: A value of True indicates Ipv4 shortcut is enabled.
          type: boolean
        ipv6_shortcut_enabled:
          default: false
          description: A value of True indicates Ipv6 shortcut is enabled.
          type: boolean
        multicast_enabled:
          default: false
          description: A value of True indicates multicast is enabled.
          type: boolean
        node_nick_name:
          description: SPBM node`s nickname. It is used to calculate ISID multicast mac
            address.
          type: string
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        primary_vlan:
          description: >-
            Primary VLAN id for this SPBM instance. This attribute makes sense
            only
            	if the vlans attribute contains 2 vlans. Otherwise it would be 0.
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlans:
          description: |-
            ISIS SPBM instance data vlans.
            	There can be only 2 vlans set.
          items:
            type: string
          nullable: true
          type: array
      required:
        - area_type
        - instance_id
        - intent_isis_global_config_id
      type: object
    IntentSpbmInstanceGetRequest:
      description: IntentSpbmInstanceGetRequest represents the retrieval request for
        IntentSpbmInstance.
      properties:
        area_type:
          description: |
            List of AreaType. AreaType represents ISIS area type.
          items:
            format: int64
            type: integer
          type: array
        asset_device_id:
          description: |
            List of AssetDeviceId. AssetDeviceId represents Device Id
          items:
            type: string
          type: array
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        instance_id:
          description: |
            List of InstanceId. InstanceId represents SPBM instance ID.
          items:
            type: integer
          type: array
        intent_isis_global_config_id:
          description: >
            List of IntentIsisGlobalConfigId. IntentIsisGlobalConfigId
            represents ISIS Global Config Id
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        ip_shortcut_enabled:
          description: >
            List of IpShortcutEnabled. IpShortcutEnabled represents A value of
            True indicates Ipv4 shortcut is enabled.
          items:
            type: boolean
          type: array
        ipv6_shortcut_enabled:
          description: >
            List of Ipv6ShortcutEnabled. Ipv6ShortcutEnabled represents A value
            of True indicates Ipv6 shortcut is enabled.
          items:
            type: boolean
          type: array
        multicast_enabled:
          description: >
            List of MulticastEnabled. MulticastEnabled represents A value of
            True indicates multicast is enabled.
          items:
            type: boolean
          type: array
        node_nick_name:
          description: >
            List of NodeNickName. NodeNickName represents SPBM node`s nickname.
            It is used to calculate ISID multicast mac address.
          items:
            type: string
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        primary_vlan:
          description: >
            List of PrimaryVlan. PrimaryVlan represents Primary VLAN id for this
            SPBM instance. This attribute makes sense only
            	if the vlans attribute contains 2 vlans. Otherwise it would be 0.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlans:
          description: |
            List of Vlans. Vlans represents ISIS SPBM instance data vlans.
            	There can be only 2 vlans set.
          items:
            items:
              type: string
            nullable: true
            type: array
          type: array
      title: IntentSpbmInstanceGetRequest
      type: object
    IntentSpbmInstanceGetResponse:
      properties:
        IntentSpbmInstance:
          items:
            $ref: "#/components/schemas/IntentSpbmInstance"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentSpbmInstanceGetResponse
      type: object
    IntentVlanConfig:
      description: IntentVlanConfig - This object represents the configuration for all
        VLANs.
      properties:
        created_at:
          description: Specifies the date and time when the entry was created.
          format: date-time
          type: string
        customer_id:
          description: Identifier for the Customer.
          format: int64
          minimum: 0
          type: integer
        deleted_at:
          $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
        device_id:
          description: Generated Device Id
          type: string
        id:
          description: An auto-generated primary key identifier of the table in the UUID
            format defined in RFC 4122.
          type: string
        intent_device_id:
          description: UUID of the intent device
          type: string
        intent_set:
          items:
            format: int64
            type: integer
          type: array
        owner_id:
          description: Identifier for the VIQ(Tenant).
          format: int64
          minimum: 0
          type: integer
        updated_at:
          description: Specifies the date and time when the entry was last updated.
          format: date-time
          type: string
        vlan_name:
          description: >-
            VLAN name (For Switch Engine the maximum length is 32 characters).
            An empty string may be
            	returned by the implementation in cases where a name is not associated with the VLAN.
          type: string
        vlan_number:
          description: This object represents a VLAN ID object including Mgmt VLAN.
          format: int64
          minimum: 0
          type: integer
        vlan_type:
          description: >-
            The type of VLAN (Applicability to each NOS is shown below):
            	|    Type                         |   NOS           |  Description
            	|---------------------------------|-----------------|----------------------------------------
            	|    PORT_MSTP_RSTP               |   Fabric Engine |  VLAN by port                         |
            	|    PROTOCOL_MSTP_RSTP           |   Fabric Engine |  VLAN by protocol                     |
            	|    PVLAN_MSTP_RSTP              |   Fabric Engine |  Private VLAN by port                 |
            	|    SPBM_BVLAN                   |   Fabric Engine |  SPBM backbone VLAN (B-VLAN)          |
            	|    VLAN                         |   Switch Engine |  Virtual Local Area Network           |
            	|    VMAN                         |   Switch Engine |  Virtual Metropolitan Area Network    |
            	|    BROUTER                      |   Fabric Engine |  Brouter VLAN                         |
            	|    PVLAN_ISOLATED_SUBSCRIBER    |   Switch Engine |  Private VLAN isolated subscriber     |
            	|    PVLAN_NONISOLATED_SUBSCRIBER |   Switch Engine |  Private VLAN non-isolated subscriber |
            	|    PVLAN_NETWORK                |   Switch Engine |  Private VLAN network                 |
            	Note:
            	Type is required for VLAN creation on Fabric Engine.  The default type for
            	Switch Engine is VLAN. BROUTER type cannot be used for VLAN creation.
            	Only one VLAN of type PROTOCOL_MSTP_RSTP can be configured on Fabric Engine.
            	vlanType of PVLAN_ISOLATED_SUBSCRIBER, PVLAN_NONISOLATED_SUBSCRIBER, and
            	PVLAN_NETWORK must also include the privateVlanName attribute.
          format: int64
          type: integer
        vr_name:
          description: VR Name.
          type: string
      required:
        - intent_device_id
        - vlan_name
        - vlan_number
        - vlan_type
        - vr_name
      type: object
    IntentVlanConfigGetRequest:
      description: IntentVlanConfigGetRequest represents the retrieval request for
        IntentVlanConfig.
      properties:
        created_at:
          description: >
            List of CreatedAt. CreatedAt represents Specifies the date and time
            when the entry was created.
          items:
            format: date-time
            type: string
          type: array
        customer_id:
          description: >
            List of CustomerId. CustomerId represents Identifier for the
            Customer.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        deleted_at:
          description: >
            List of DeletedAt. DeletedAt represents Specifies the date and time
            when the entry was soft deleted.
          items:
            $ref: "#/components/schemas/gorm_io_gorm_DeletedAt"
          type: array
        device_id:
          description: |
            List of DeviceId. DeviceId represents Generated Device Id
          items:
            type: string
          type: array
        id:
          description: >
            List of Id. Id represents An auto-generated primary key identifier
            of the table in the UUID format defined in RFC 4122.
          items:
            type: string
          type: array
        intent_device_id:
          description: >
            List of IntentDeviceId. IntentDeviceId represents UUID of the intent
            device
          items:
            type: string
          type: array
        intent_set:
          description: |
            List of IntentSet. IntentSet represents Fields set in intent.
          items:
            items:
              format: int64
              type: integer
            type: array
          type: array
        owner_id:
          description: |
            List of OwnerId. OwnerId represents Identifier for the VIQ(Tenant).
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        updated_at:
          description: >
            List of UpdatedAt. UpdatedAt represents Specifies the date and time
            when the entry was last updated.
          items:
            format: date-time
            type: string
          type: array
        vlan_name:
          description: >
            List of VlanName. VlanName represents VLAN name (For Switch Engine
            the maximum length is 32 characters). An empty string may be
            	returned by the implementation in cases where a name is not associated with the VLAN.
          items:
            type: string
          type: array
        vlan_number:
          description: >
            List of VlanNumber. VlanNumber represents This object represents a
            VLAN ID object including Mgmt VLAN.
          items:
            format: int64
            minimum: 0
            type: integer
          type: array
        vlan_type:
          description: >
            List of VlanType. VlanType represents The type of VLAN
            (Applicability to each NOS is shown below):
            	|    Type                         |   NOS           |  Description
            	|---------------------------------|-----------------|----------------------------------------
            	|    PORT_MSTP_RSTP               |   Fabric Engine |  VLAN by port                         |
            	|    PROTOCOL_MSTP_RSTP           |   Fabric Engine |  VLAN by protocol                     |
            	|    PVLAN_MSTP_RSTP              |   Fabric Engine |  Private VLAN by port                 |
            	|    SPBM_BVLAN                   |   Fabric Engine |  SPBM backbone VLAN (B-VLAN)          |
            	|    VLAN                         |   Switch Engine |  Virtual Local Area Network           |
            	|    VMAN                         |   Switch Engine |  Virtual Metropolitan Area Network    |
            	|    BROUTER                      |   Fabric Engine |  Brouter VLAN                         |
            	|    PVLAN_ISOLATED_SUBSCRIBER    |   Switch Engine |  Private VLAN isolated subscriber     |
            	|    PVLAN_NONISOLATED_SUBSCRIBER |   Switch Engine |  Private VLAN non-isolated subscriber |
            	|    PVLAN_NETWORK                |   Switch Engine |  Private VLAN network                 |
            	Note:
            	Type is required for VLAN creation on Fabric Engine.  The default type for
            	Switch Engine is VLAN. BROUTER type cannot be used for VLAN creation.
            	Only one VLAN of type PROTOCOL_MSTP_RSTP can be configured on Fabric Engine.
            	vlanType of PVLAN_ISOLATED_SUBSCRIBER, PVLAN_NONISOLATED_SUBSCRIBER, and
            	PVLAN_NETWORK must also include the privateVlanName attribute.
          items:
            format: int64
            type: integer
          type: array
        vr_name:
          description: |
            List of VrName. VrName represents VR Name.
          items:
            type: string
          type: array
      title: IntentVlanConfigGetRequest
      type: object
    IntentVlanConfigGetResponse:
      properties:
        IntentVlanConfig:
          items:
            $ref: "#/components/schemas/IntentVlanConfig"
          nullable: true
          type: array
        Pagination:
          $ref: "#/components/schemas/PaginationModel"
      title: IntentVlanConfigGetResponse
      type: object
    PaginationModel:
      description: PaginationModel represents the details for a paginated response.
      properties:
        count:
          description: The number of items on the current page
          type: integer
        page:
          description: The current page number being viewed
          type: integer
        total_count:
          description: The total number of items across all pages
          type: integer
        total_pages:
          description: The total number of pages available
          type: integer
      type: object
    gorm_io_gorm_DeletedAt:
      properties:
        Time:
          format: date-time
          type: string
        Valid:
          type: boolean
      type: object
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      description: Enter your API Key as a Bearer Token
info:
  contact:
    email: platform-team@extremenetworks.com
    name: Extreme Networks Platform Team
    url: https://www.extremenetworks.com
  description: This specification provides details of the API exposed by Config
    State service.
  title: Config State Service.
  version: 1.0.0
openapi: 3.0.0
servers:
  - url: https://cloudapi.extremecloudiq.com/configstate/v1
    description: Extreme Platform ONE REST API - Config State
paths:
  /retrieve-asset-account:
    post:
      description: Retrieve AssetAccount
      operationId: retrieveAssetAccount
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAccountGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAccountGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAccount
  /retrieve-asset-all-of-lldp-neighbor-xmed-inventory:
    post:
      description: Retrieve AssetAllOfLldpNeighborXmedInventory
      operationId: retrieveAssetAllOfLldpNeighborXmedInventory
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAllOfLldpNeighborXmedInventoryGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAllOfLldpNeighborXmedInventoryGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAllOfLldpNeighborXmedInventory
  /retrieve-asset-authentication-config:
    post:
      description: Retrieve AssetAuthenticationConfig
      operationId: retrieveAssetAuthenticationConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAuthenticationConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAuthenticationConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAuthenticationConfig
  /retrieve-asset-authentication-port-setting-config:
    post:
      description: Retrieve AssetAuthenticationPortSettingConfig
      operationId: retrieveAssetAuthenticationPortSettingConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAuthenticationPortSettingConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAuthenticationPortSettingConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAuthenticationPortSettingConfig
  /retrieve-asset-authentication-state:
    post:
      description: Retrieve AssetAuthenticationState
      operationId: retrieveAssetAuthenticationState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAuthenticationStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAuthenticationStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAuthenticationState
  /retrieve-asset-auto-sense-fabric-attach:
    post:
      description: Retrieve AssetAutoSenseFabricAttach
      operationId: retrieveAssetAutoSenseFabricAttach
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAutoSenseFabricAttachGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAutoSenseFabricAttachGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAutoSenseFabricAttach
  /retrieve-asset-auto-sense-global:
    post:
      description: Retrieve AssetAutoSenseGlobal
      operationId: retrieveAssetAutoSenseGlobal
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAutoSenseGlobalGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAutoSenseGlobalGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAutoSenseGlobal
  /retrieve-asset-auto-sense-isis:
    post:
      description: Retrieve AssetAutoSenseIsis
      operationId: retrieveAssetAutoSenseIsis
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAutoSenseIsisGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAutoSenseIsisGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAutoSenseIsis
  /retrieve-asset-auto-sense-port:
    post:
      description: Retrieve AssetAutoSensePort
      operationId: retrieveAssetAutoSensePort
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetAutoSensePortGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetAutoSensePortGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetAutoSensePort
  /retrieve-asset-cdp-neighbor:
    post:
      description: Retrieve AssetCdpNeighbor
      operationId: retrieveAssetCdpNeighbor
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetCdpNeighborGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetCdpNeighborGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetCdpNeighbor
  /retrieve-asset-cdp-ports-config:
    post:
      description: Retrieve AssetCdpPortsConfig
      operationId: retrieveAssetCdpPortsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetCdpPortsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetCdpPortsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetCdpPortsConfig
  /retrieve-asset-device:
    post:
      description: Retrieve AssetDevice
      operationId: retrieveAssetDevice
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDeviceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDeviceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDevice
  /retrieve-asset-device-connection-detail:
    post:
      description: Retrieve AssetDeviceConnectionDetail
      operationId: retrieveAssetDeviceConnectionDetail
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDeviceConnectionDetailGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDeviceConnectionDetailGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDeviceConnectionDetail
  /retrieve-asset-device-connection-detail-physical-stack:
    post:
      description: Retrieve AssetDeviceConnectionDetailPhysicalStack
      operationId: retrieveAssetDeviceConnectionDetailPhysicalStack
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDeviceConnectionDetailPhysicalStackGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDeviceConnectionDetailPhysicalStackGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDeviceConnectionDetailPhysicalStack
  /retrieve-asset-device-connection-detail-stack-info:
    post:
      description: Retrieve AssetDeviceConnectionDetailStackInfo
      operationId: retrieveAssetDeviceConnectionDetailStackInfo
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDeviceConnectionDetailStackInfoGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDeviceConnectionDetailStackInfoGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDeviceConnectionDetailStackInfo
  /retrieve-asset-dhcp-relay-vlan-config:
    post:
      description: Retrieve AssetDhcpRelayVlanConfig
      operationId: retrieveAssetDhcpRelayVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpRelayVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpRelayVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpRelayVlanConfig
  /retrieve-asset-dhcp-relay-vlan-server-config:
    post:
      description: Retrieve AssetDhcpRelayVlanServerConfig
      operationId: retrieveAssetDhcpRelayVlanServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpRelayVlanServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpRelayVlanServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpRelayVlanServerConfig
  /retrieve-asset-dhcp-relay-vrf-config:
    post:
      description: Retrieve AssetDhcpRelayVrfConfig
      operationId: retrieveAssetDhcpRelayVrfConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpRelayVrfConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpRelayVrfConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpRelayVrfConfig
  /retrieve-asset-dhcp-relay-vrf-server-config:
    post:
      description: Retrieve AssetDhcpRelayVrfServerConfig
      operationId: retrieveAssetDhcpRelayVrfServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpRelayVrfServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpRelayVrfServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpRelayVrfServerConfig
  /retrieve-asset-dhcp-relay-vrf-state:
    post:
      description: Retrieve AssetDhcpRelayVrfState
      operationId: retrieveAssetDhcpRelayVrfState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpRelayVrfStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpRelayVrfStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpRelayVrfState
  /retrieve-asset-dhcp-snoop-config:
    post:
      description: Retrieve AssetDhcpSnoopConfig
      operationId: retrieveAssetDhcpSnoopConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpSnoopConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpSnoopConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpSnoopConfig
  /retrieve-asset-dhcp-snoop-dynamic-vlans-config:
    post:
      description: Retrieve AssetDhcpSnoopDynamicVlansConfig
      operationId: retrieveAssetDhcpSnoopDynamicVlansConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpSnoopDynamicVlansConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpSnoopDynamicVlansConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpSnoopDynamicVlansConfig
  /retrieve-asset-dhcp-snoop-ports-config:
    post:
      description: Retrieve AssetDhcpSnoopPortsConfig
      operationId: retrieveAssetDhcpSnoopPortsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpSnoopPortsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpSnoopPortsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpSnoopPortsConfig
  /retrieve-asset-dhcp-snoop-vlans-config:
    post:
      description: Retrieve AssetDhcpSnoopVlansConfig
      operationId: retrieveAssetDhcpSnoopVlansConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDhcpSnoopVlansConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDhcpSnoopVlansConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDhcpSnoopVlansConfig
  /retrieve-asset-dns-config:
    post:
      description: Retrieve AssetDnsConfig
      operationId: retrieveAssetDnsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDnsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDnsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDnsConfig
  /retrieve-asset-dns-vrf-server-config:
    post:
      description: Retrieve AssetDnsVrfServerConfig
      operationId: retrieveAssetDnsVrfServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetDnsVrfServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetDnsVrfServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetDnsVrfServerConfig
  /retrieve-asset-elrp-config:
    post:
      description: Retrieve AssetElrpConfig
      operationId: retrieveAssetElrpConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetElrpConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetElrpConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetElrpConfig
  /retrieve-asset-elrp-dynamic-vlan-config:
    post:
      description: Retrieve AssetElrpDynamicVlanConfig
      operationId: retrieveAssetElrpDynamicVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetElrpDynamicVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetElrpDynamicVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetElrpDynamicVlanConfig
  /retrieve-asset-elrp-vlan-config:
    post:
      description: Retrieve AssetElrpVlanConfig
      operationId: retrieveAssetElrpVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetElrpVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetElrpVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetElrpVlanConfig
  /retrieve-asset-fabric-attach-config:
    post:
      description: Retrieve AssetFabricAttachConfig
      operationId: retrieveAssetFabricAttachConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFabricAttachConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFabricAttachConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFabricAttachConfig
  /retrieve-asset-fabric-attach-config-port:
    post:
      description: Retrieve AssetFabricAttachConfigPort
      operationId: retrieveAssetFabricAttachConfigPort
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFabricAttachConfigPortGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFabricAttachConfigPortGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFabricAttachConfigPort
  /retrieve-asset-fabric-attach-config-zero-touch-client:
    post:
      description: Retrieve AssetFabricAttachConfigZeroTouchClient
      operationId: retrieveAssetFabricAttachConfigZeroTouchClient
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFabricAttachConfigZeroTouchClientGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFabricAttachConfigZeroTouchClientGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFabricAttachConfigZeroTouchClient
  /retrieve-asset-fabric-attach-state:
    post:
      description: Retrieve AssetFabricAttachState
      operationId: retrieveAssetFabricAttachState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFabricAttachStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFabricAttachStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFabricAttachState
  /retrieve-asset-fabric-attach-state-discovery-element:
    post:
      description: Retrieve AssetFabricAttachStateDiscoveryElement
      operationId: retrieveAssetFabricAttachStateDiscoveryElement
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFabricAttachStateDiscoveryElementGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFabricAttachStateDiscoveryElementGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFabricAttachStateDiscoveryElement
  /retrieve-asset-fabric-attach-state-vlan-assignment:
    post:
      description: Retrieve AssetFabricAttachStateVlanAssignment
      operationId: retrieveAssetFabricAttachStateVlanAssignment
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFabricAttachStateVlanAssignmentGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFabricAttachStateVlanAssignmentGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFabricAttachStateVlanAssignment
  /retrieve-asset-fe-tunnel-config:
    post:
      description: Retrieve AssetFeTunnelConfig
      operationId: retrieveAssetFeTunnelConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetFeTunnelConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetFeTunnelConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetFeTunnelConfig
  /retrieve-asset-igmp-vlan-config:
    post:
      description: Retrieve AssetIgmpVlanConfig
      operationId: retrieveAssetIgmpVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIgmpVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIgmpVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIgmpVlanConfig
  /retrieve-asset-igmp-vr-config:
    post:
      description: Retrieve AssetIgmpVrConfig
      operationId: retrieveAssetIgmpVrConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIgmpVrConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIgmpVrConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIgmpVrConfig
  /retrieve-asset-instant-port-action-config:
    post:
      description: Retrieve AssetInstantPortActionConfig
      operationId: retrieveAssetInstantPortActionConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInstantPortActionConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInstantPortActionConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInstantPortActionConfig
  /retrieve-asset-instant-port-config:
    post:
      description: Retrieve AssetInstantPortConfig
      operationId: retrieveAssetInstantPortConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInstantPortConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInstantPortConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInstantPortConfig
  /retrieve-asset-instant-port-device-types-config:
    post:
      description: Retrieve AssetInstantPortDeviceTypesConfig
      operationId: retrieveAssetInstantPortDeviceTypesConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInstantPortDeviceTypesConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInstantPortDeviceTypesConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInstantPortDeviceTypesConfig
  /retrieve-asset-instant-port-match-condition-config:
    post:
      description: Retrieve AssetInstantPortMatchConditionConfig
      operationId: retrieveAssetInstantPortMatchConditionConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInstantPortMatchConditionConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInstantPortMatchConditionConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInstantPortMatchConditionConfig
  /retrieve-asset-instant-port-profile-config:
    post:
      description: Retrieve AssetInstantPortProfileConfig
      operationId: retrieveAssetInstantPortProfileConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInstantPortProfileConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInstantPortProfileConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInstantPortProfileConfig
  /retrieve-asset-inter-isis-unicast-fib-state:
    post:
      description: Retrieve AssetInterIsisUnicastFibState
      operationId: retrieveAssetInterIsisUnicastFibState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInterIsisUnicastFibStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInterIsisUnicastFibStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInterIsisUnicastFibState
  /retrieve-asset-interface:
    post:
      description: Retrieve AssetInterface
      operationId: retrieveAssetInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInterface
  /retrieve-asset-interface-ip-address:
    post:
      description: Retrieve AssetInterfaceIpAddress
      operationId: retrieveAssetInterfaceIpAddress
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInterfaceIpAddressGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInterfaceIpAddressGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInterfaceIpAddress
  /retrieve-asset-interface-vlan-map:
    post:
      description: Retrieve AssetInterfaceVlanMap
      operationId: retrieveAssetInterfaceVlanMap
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInterfaceVlanMapGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInterfaceVlanMapGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInterfaceVlanMap
  /retrieve-asset-interface-vlan-properties:
    post:
      description: Retrieve AssetInterfaceVlanProperties
      operationId: retrieveAssetInterfaceVlanProperties
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetInterfaceVlanPropertiesGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetInterfaceVlanPropertiesGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetInterfaceVlanProperties
  /retrieve-asset-ip-arp-state:
    post:
      description: Retrieve AssetIpArpState
      operationId: retrieveAssetIpArpState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIpArpStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIpArpStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIpArpState
  /retrieve-asset-isid-mac-fdb-state:
    post:
      description: Retrieve AssetIsidMacFdbState
      operationId: retrieveAssetIsidMacFdbState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsidMacFdbStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsidMacFdbStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsidMacFdbState
  /retrieve-asset-isis-adjacency:
    post:
      description: Retrieve AssetIsisAdjacency
      operationId: retrieveAssetIsisAdjacency
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisAdjacencyGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisAdjacencyGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisAdjacency
  /retrieve-asset-isis-circuit-config:
    post:
      description: Retrieve AssetIsisCircuitConfig
      operationId: retrieveAssetIsisCircuitConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisCircuitConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisCircuitConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisCircuitConfig
  /retrieve-asset-isis-circuit-spbm-setting:
    post:
      description: Retrieve AssetIsisCircuitSpbmSetting
      operationId: retrieveAssetIsisCircuitSpbmSetting
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisCircuitSpbmSettingGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisCircuitSpbmSettingGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisCircuitSpbmSetting
  /retrieve-asset-isis-circuit-state:
    post:
      description: Retrieve AssetIsisCircuitState
      operationId: retrieveAssetIsisCircuitState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisCircuitStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisCircuitStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisCircuitState
  /retrieve-asset-isis-global-config:
    post:
      description: Retrieve AssetIsisGlobalConfig
      operationId: retrieveAssetIsisGlobalConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisGlobalConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisGlobalConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisGlobalConfig
  /retrieve-asset-isis-global-state:
    post:
      description: Retrieve AssetIsisGlobalState
      operationId: retrieveAssetIsisGlobalState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisGlobalStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisGlobalStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisGlobalState
  /retrieve-asset-isis-l2-multi-area-isid-list:
    post:
      description: Retrieve AssetIsisL2MultiAreaIsidList
      operationId: retrieveAssetIsisL2MultiAreaIsidList
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisL2MultiAreaIsidListGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisL2MultiAreaIsidListGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisL2MultiAreaIsidList
  /retrieve-asset-isis-l2-redistribute-multi-area-snoop:
    post:
      description: Retrieve AssetIsisL2RedistributeMultiAreaSnoop
      operationId: retrieveAssetIsisL2RedistributeMultiAreaSnoop
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisL2RedistributeMultiAreaSnoopGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisL2RedistributeMultiAreaSnoopGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisL2RedistributeMultiAreaSnoop
  /retrieve-asset-isis-l3-accept-policy-config:
    post:
      description: Retrieve AssetIsisL3AcceptPolicyConfig
      operationId: retrieveAssetIsisL3AcceptPolicyConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisL3AcceptPolicyConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisL3AcceptPolicyConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisL3AcceptPolicyConfig
  /retrieve-asset-isis-l3-redistribute:
    post:
      description: Retrieve AssetIsisL3Redistribute
      operationId: retrieveAssetIsisL3Redistribute
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisL3RedistributeGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisL3RedistributeGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisL3Redistribute
  /retrieve-asset-isis-l3-redistribute-multi-area-isid-redist:
    post:
      description: Retrieve AssetIsisL3RedistributeMultiAreaIsidRedist
      operationId: retrieveAssetIsisL3RedistributeMultiAreaIsidRedist
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisL3RedistributeMultiAreaIsidRedistGetReques\
                t"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisL3RedistributeMultiAreaIsidRedistGetRespon\
                  se"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisL3RedistributeMultiAreaIsidRedist
  /retrieve-asset-isis-lsp-state:
    post:
      description: Retrieve AssetIsisLspState
      operationId: retrieveAssetIsisLspState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisLspStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisLspStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisLspState
  /retrieve-asset-isis-multicast-fib-state:
    post:
      description: Retrieve AssetIsisMulticastFibState
      operationId: retrieveAssetIsisMulticastFibState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisMulticastFibStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisMulticastFibStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisMulticastFibState
  /retrieve-asset-isis-multicast-route-state:
    post:
      description: Retrieve AssetIsisMulticastRouteState
      operationId: retrieveAssetIsisMulticastRouteState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisMulticastRouteStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisMulticastRouteStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisMulticastRouteState
  /retrieve-asset-isis-redistribute-multi-area:
    post:
      description: Retrieve AssetIsisRedistributeMultiArea
      operationId: retrieveAssetIsisRedistributeMultiArea
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisRedistributeMultiAreaGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisRedistributeMultiAreaGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisRedistributeMultiArea
  /retrieve-asset-isis-redistribute-multi-area-static-isid:
    post:
      description: Retrieve AssetIsisRedistributeMultiAreaStaticIsid
      operationId: retrieveAssetIsisRedistributeMultiAreaStaticIsid
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisRedistributeMultiAreaStaticIsidGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisRedistributeMultiAreaStaticIsidGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisRedistributeMultiAreaStaticIsid
  /retrieve-asset-isis-unicast-fib-state:
    post:
      description: Retrieve AssetIsisUnicastFibState
      operationId: retrieveAssetIsisUnicastFibState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetIsisUnicastFibStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetIsisUnicastFibStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetIsisUnicastFibState
  /retrieve-asset-l2-vsn-cvlan-config:
    post:
      description: Retrieve AssetL2VsnCvlanConfig
      operationId: retrieveAssetL2VsnCvlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnCvlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnCvlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnCvlanConfig
  /retrieve-asset-l2-vsn-suni-config:
    post:
      description: Retrieve AssetL2VsnSuniConfig
      operationId: retrieveAssetL2VsnSuniConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnSuniConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnSuniConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnSuniConfig
  /retrieve-asset-l2-vsn-suni-config-end-point:
    post:
      description: Retrieve AssetL2VsnSuniConfigEndPoint
      operationId: retrieveAssetL2VsnSuniConfigEndPoint
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnSuniConfigEndPointGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnSuniConfigEndPointGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnSuniConfigEndPoint
  /retrieve-asset-l2-vsn-suni-state:
    post:
      description: Retrieve AssetL2VsnSuniState
      operationId: retrieveAssetL2VsnSuniState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnSuniStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnSuniStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnSuniState
  /retrieve-asset-l2-vsn-suni-state-member-intfs:
    post:
      description: Retrieve AssetL2VsnSuniStateMemberIntfs
      operationId: retrieveAssetL2VsnSuniStateMemberIntfs
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnSuniStateMemberIntfsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnSuniStateMemberIntfsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnSuniStateMemberIntfs
  /retrieve-asset-l2-vsn-tuni-config:
    post:
      description: Retrieve AssetL2VsnTuniConfig
      operationId: retrieveAssetL2VsnTuniConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnTuniConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnTuniConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnTuniConfig
  /retrieve-asset-l2-vsn-tuni-config-member-intf:
    post:
      description: Retrieve AssetL2VsnTuniConfigMemberIntf
      operationId: retrieveAssetL2VsnTuniConfigMemberIntf
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL2VsnTuniConfigMemberIntfGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL2VsnTuniConfigMemberIntfGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL2VsnTuniConfigMemberIntf
  /retrieve-asset-l3-vsn-config:
    post:
      description: Retrieve AssetL3VsnConfig
      operationId: retrieveAssetL3VsnConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL3VsnConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL3VsnConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL3VsnConfig
  /retrieve-asset-l3-vsn-isid-list-config:
    post:
      description: Retrieve AssetL3VsnIsidListConfig
      operationId: retrieveAssetL3VsnIsidListConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetL3VsnIsidListConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetL3VsnIsidListConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetL3VsnIsidListConfig
  /retrieve-asset-lag-config:
    post:
      description: Retrieve AssetLagConfig
      operationId: retrieveAssetLagConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLagConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLagConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLagConfig
  /retrieve-asset-lag-config-member-port:
    post:
      description: Retrieve AssetLagConfigMemberPort
      operationId: retrieveAssetLagConfigMemberPort
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLagConfigMemberPortGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLagConfigMemberPortGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLagConfigMemberPort
  /retrieve-asset-lag-state:
    post:
      description: Retrieve AssetLagState
      operationId: retrieveAssetLagState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLagStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLagStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLagState
  /retrieve-asset-lag-state-member-port:
    post:
      description: Retrieve AssetLagStateMemberPort
      operationId: retrieveAssetLagStateMemberPort
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLagStateMemberPortGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLagStateMemberPortGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLagStateMemberPort
  /retrieve-asset-lldp-config:
    post:
      description: Retrieve AssetLldpConfig
      operationId: retrieveAssetLldpConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpConfig
  /retrieve-asset-lldp-dot1-port-protocol-vlan:
    post:
      description: Retrieve AssetLldpDot1PortProtocolVlan
      operationId: retrieveAssetLldpDot1PortProtocolVlan
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpDot1PortProtocolVlanGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpDot1PortProtocolVlanGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpDot1PortProtocolVlan
  /retrieve-asset-lldp-dot1-vlan-object:
    post:
      description: Retrieve AssetLldpDot1VlanObject
      operationId: retrieveAssetLldpDot1VlanObject
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpDot1VlanObjectGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpDot1VlanObjectGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpDot1VlanObject
  /retrieve-asset-lldp-neighbor-fabric-attach:
    post:
      description: Retrieve AssetLldpNeighborFabricAttach
      operationId: retrieveAssetLldpNeighborFabricAttach
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborFabricAttachGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborFabricAttachGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborFabricAttach
  /retrieve-asset-lldp-neighbor-fabric-attach-vlan-isid-map:
    post:
      description: Retrieve AssetLldpNeighborFabricAttachVlanIsidMap
      operationId: retrieveAssetLldpNeighborFabricAttachVlanIsidMap
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborFabricAttachVlanIsidMapGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborFabricAttachVlanIsidMapGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborFabricAttachVlanIsidMap
  /retrieve-asset-lldp-neighbor-management-address-list:
    post:
      description: Retrieve AssetLldpNeighborManagementAddressList
      operationId: retrieveAssetLldpNeighborManagementAddressList
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborManagementAddressListGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborManagementAddressListGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborManagementAddressList
  /retrieve-asset-lldp-neighbor-state:
    post:
      description: Retrieve AssetLldpNeighborState
      operationId: retrieveAssetLldpNeighborState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborState
  /retrieve-asset-lldp-neighbor-xmed:
    post:
      description: Retrieve AssetLldpNeighborXmed
      operationId: retrieveAssetLldpNeighborXmed
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborXmedGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborXmedGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborXmed
  /retrieve-asset-lldp-neighbor-xmed-poe-pd:
    post:
      description: Retrieve AssetLldpNeighborXmedPoePd
      operationId: retrieveAssetLldpNeighborXmedPoePd
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborXmedPoePdGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborXmedPoePdGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborXmedPoePd
  /retrieve-asset-lldp-neighbor-xmed-poe-pse:
    post:
      description: Retrieve AssetLldpNeighborXmedPoePse
      operationId: retrieveAssetLldpNeighborXmedPoePse
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborXmedPoePseGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborXmedPoePseGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborXmedPoePse
  /retrieve-asset-lldp-neighbor-xmed-policy:
    post:
      description: Retrieve AssetLldpNeighborXmedPolicy
      operationId: retrieveAssetLldpNeighborXmedPolicy
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborXmedPolicyGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborXmedPolicyGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborXmedPolicy
  /retrieve-asset-lldp-neighbor-ztf:
    post:
      description: Retrieve AssetLldpNeighborZtf
      operationId: retrieveAssetLldpNeighborZtf
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborZtfGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborZtfGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborZtf
  /retrieve-asset-lldp-neighbor-ztf-tunnel-group:
    post:
      description: Retrieve AssetLldpNeighborZtfTunnelGroup
      operationId: retrieveAssetLldpNeighborZtfTunnelGroup
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpNeighborZtfTunnelGroupGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpNeighborZtfTunnelGroupGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpNeighborZtfTunnelGroup
  /retrieve-asset-lldp-port-advertise-settings-config:
    post:
      description: Retrieve AssetLldpPortAdvertiseSettingsConfig
      operationId: retrieveAssetLldpPortAdvertiseSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpPortAdvertiseSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpPortAdvertiseSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpPortAdvertiseSettingsConfig
  /retrieve-asset-lldp-port-config:
    post:
      description: Retrieve AssetLldpPortConfig
      operationId: retrieveAssetLldpPortConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpPortConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpPortConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpPortConfig
  /retrieve-asset-lldp-port-med-policy-settings-config:
    post:
      description: Retrieve AssetLldpPortMedPolicySettingsConfig
      operationId: retrieveAssetLldpPortMedPolicySettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLldpPortMedPolicySettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLldpPortMedPolicySettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLldpPortMedPolicySettingsConfig
  /retrieve-asset-location:
    post:
      description: Retrieve AssetLocation
      operationId: retrieveAssetLocation
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetLocationGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetLocationGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetLocation
  /retrieve-asset-mac-locking-config:
    post:
      description: Retrieve AssetMacLockingConfig
      operationId: retrieveAssetMacLockingConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetMacLockingConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMacLockingConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetMacLockingConfig
  /retrieve-asset-mlag-peer-config:
    post:
      description: Retrieve AssetMlagPeerConfig
      operationId: retrieveAssetMlagPeerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetMlagPeerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMlagPeerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetMlagPeerConfig
  /retrieve-asset-mlag-peer-counters:
    post:
      description: Retrieve AssetMlagPeerCounters
      operationId: retrieveAssetMlagPeerCounters
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetMlagPeerCountersGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMlagPeerCountersGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetMlagPeerCounters
  /retrieve-asset-mlag-peer-state:
    post:
      description: Retrieve AssetMlagPeerState
      operationId: retrieveAssetMlagPeerState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetMlagPeerStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMlagPeerStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetMlagPeerState
  /retrieve-asset-mlag-port-state:
    post:
      description: Retrieve AssetMlagPortState
      operationId: retrieveAssetMlagPortState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetMlagPortStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMlagPortStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetMlagPortState
  /retrieve-asset-multicast-fib-interface:
    post:
      description: Retrieve AssetMulticastFibInterface
      operationId: retrieveAssetMulticastFibInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetMulticastFibInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetMulticastFibInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetMulticastFibInterface
  /retrieve-asset-neigh-disc-state:
    post:
      description: Retrieve AssetNeighDiscState
      operationId: retrieveAssetNeighDiscState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetNeighDiscStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetNeighDiscStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetNeighDiscState
  /retrieve-asset-network-to-network-interface:
    post:
      description: Retrieve AssetNetworkToNetworkInterface
      operationId: retrieveAssetNetworkToNetworkInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetNetworkToNetworkInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetNetworkToNetworkInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetNetworkToNetworkInterface
  /retrieve-asset-node-alias-config:
    post:
      description: Retrieve AssetNodeAliasConfig
      operationId: retrieveAssetNodeAliasConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetNodeAliasConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetNodeAliasConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetNodeAliasConfig
  /retrieve-asset-node-alias-port-config:
    post:
      description: Retrieve AssetNodeAliasPortConfig
      operationId: retrieveAssetNodeAliasPortConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetNodeAliasPortConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetNodeAliasPortConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetNodeAliasPortConfig
  /retrieve-asset-ntp-config:
    post:
      description: Retrieve AssetNtpConfig
      operationId: retrieveAssetNtpConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetNtpConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetNtpConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetNtpConfig
  /retrieve-asset-ntp-server-config:
    post:
      description: Retrieve AssetNtpServerConfig
      operationId: retrieveAssetNtpServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetNtpServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetNtpServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetNtpServerConfig
  /retrieve-asset-poe-power-config:
    post:
      description: Retrieve AssetPoePowerConfig
      operationId: retrieveAssetPoePowerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPoePowerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPoePowerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPoePowerConfig
  /retrieve-asset-poe-power-ports-config:
    post:
      description: Retrieve AssetPoePowerPortsConfig
      operationId: retrieveAssetPoePowerPortsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPoePowerPortsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPoePowerPortsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPoePowerPortsConfig
  /retrieve-asset-poe-power-ports-state:
    post:
      description: Retrieve AssetPoePowerPortsState
      operationId: retrieveAssetPoePowerPortsState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPoePowerPortsStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPoePowerPortsStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPoePowerPortsState
  /retrieve-asset-poe-power-slot-settings-config:
    post:
      description: Retrieve AssetPoePowerSlotSettingsConfig
      operationId: retrieveAssetPoePowerSlotSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPoePowerSlotSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPoePowerSlotSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPoePowerSlotSettingsConfig
  /retrieve-asset-policy-global:
    post:
      description: Retrieve AssetPolicyGlobal
      operationId: retrieveAssetPolicyGlobal
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPolicyGlobalGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPolicyGlobalGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPolicyGlobal
  /retrieve-asset-port-capabilities:
    post:
      description: Retrieve AssetPortCapabilities
      operationId: retrieveAssetPortCapabilities
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPortCapabilitiesGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPortCapabilitiesGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPortCapabilities
  /retrieve-asset-port-config:
    post:
      description: Retrieve AssetPortConfig
      operationId: retrieveAssetPortConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPortConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPortConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPortConfig
  /retrieve-asset-port-lldp-location:
    post:
      description: Retrieve AssetPortLldpLocation
      operationId: retrieveAssetPortLldpLocation
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPortLldpLocationGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPortLldpLocationGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPortLldpLocation
  /retrieve-asset-port-mac-locking-config:
    post:
      description: Retrieve AssetPortMacLockingConfig
      operationId: retrieveAssetPortMacLockingConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPortMacLockingConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPortMacLockingConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPortMacLockingConfig
  /retrieve-asset-port-state:
    post:
      description: Retrieve AssetPortState
      operationId: retrieveAssetPortState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPortStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPortStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPortState
  /retrieve-asset-ports-global-config:
    post:
      description: Retrieve AssetPortsGlobalConfig
      operationId: retrieveAssetPortsGlobalConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetPortsGlobalConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetPortsGlobalConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetPortsGlobalConfig
  /retrieve-asset-radius-server-config:
    post:
      description: Retrieve AssetRadiusServerConfig
      operationId: retrieveAssetRadiusServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetRadiusServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetRadiusServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetRadiusServerConfig
  /retrieve-asset-radius-server-host-config:
    post:
      description: Retrieve AssetRadiusServerHostConfig
      operationId: retrieveAssetRadiusServerHostConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetRadiusServerHostConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetRadiusServerHostConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetRadiusServerHostConfig
  /retrieve-asset-radius-server-secure-profiles-config:
    post:
      description: Retrieve AssetRadiusServerSecureProfilesConfig
      operationId: retrieveAssetRadiusServerSecureProfilesConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetRadiusServerSecureProfilesConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetRadiusServerSecureProfilesConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetRadiusServerSecureProfilesConfig
  /retrieve-asset-rsmlt-local-instance-state:
    post:
      description: Retrieve AssetRsmltLocalInstanceState
      operationId: retrieveAssetRsmltLocalInstanceState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetRsmltLocalInstanceStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetRsmltLocalInstanceStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetRsmltLocalInstanceState
  /retrieve-asset-rsmlt-remote-instance-state:
    post:
      description: Retrieve AssetRsmltRemoteInstanceState
      operationId: retrieveAssetRsmltRemoteInstanceState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetRsmltRemoteInstanceStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetRsmltRemoteInstanceStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetRsmltRemoteInstanceState
  /retrieve-asset-rsmlt-vlan-instance-config:
    post:
      description: Retrieve AssetRsmltVlanInstanceConfig
      operationId: retrieveAssetRsmltVlanInstanceConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetRsmltVlanInstanceConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetRsmltVlanInstanceConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetRsmltVlanInstanceConfig
  /retrieve-asset-slot:
    post:
      description: Retrieve AssetSlot
      operationId: retrieveAssetSlot
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSlotGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSlotGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSlot
  /retrieve-asset-slpp-config:
    post:
      description: Retrieve AssetSlppConfig
      operationId: retrieveAssetSlppConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSlppConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSlppConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSlppConfig
  /retrieve-asset-slpp-port-config:
    post:
      description: Retrieve AssetSlppPortConfig
      operationId: retrieveAssetSlppPortConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSlppPortConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSlppPortConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSlppPortConfig
  /retrieve-asset-slpp-vlan-config:
    post:
      description: Retrieve AssetSlppVlanConfig
      operationId: retrieveAssetSlppVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSlppVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSlppVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSlppVlanConfig
  /retrieve-asset-snmp-access-config:
    post:
      description: Retrieve AssetSnmpAccessConfig
      operationId: retrieveAssetSnmpAccessConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpAccessConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpAccessConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpAccessConfig
  /retrieve-asset-snmp-global-config:
    post:
      description: Retrieve AssetSnmpGlobalConfig
      operationId: retrieveAssetSnmpGlobalConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpGlobalConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpGlobalConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpGlobalConfig
  /retrieve-asset-snmp-group-config:
    post:
      description: Retrieve AssetSnmpGroupConfig
      operationId: retrieveAssetSnmpGroupConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpGroupConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpGroupConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpGroupConfig
  /retrieve-asset-snmp-trap-receiver-config:
    post:
      description: Retrieve AssetSnmpTrapReceiverConfig
      operationId: retrieveAssetSnmpTrapReceiverConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpTrapReceiverConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpTrapReceiverConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpTrapReceiverConfig
  /retrieve-asset-snmp-v1-v2-config:
    post:
      description: Retrieve AssetSnmpV1V2Config
      operationId: retrieveAssetSnmpV1V2Config
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpV1V2ConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpV1V2ConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpV1V2Config
  /retrieve-asset-snmp-v3-access-config:
    post:
      description: Retrieve AssetSnmpV3AccessConfig
      operationId: retrieveAssetSnmpV3AccessConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpV3AccessConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpV3AccessConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpV3AccessConfig
  /retrieve-asset-snmp-v3-config:
    post:
      description: Retrieve AssetSnmpV3Config
      operationId: retrieveAssetSnmpV3Config
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpV3ConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpV3ConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpV3Config
  /retrieve-asset-snmp-v3-trap-receiver-config:
    post:
      description: Retrieve AssetSnmpV3TrapReceiverConfig
      operationId: retrieveAssetSnmpV3TrapReceiverConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpV3TrapReceiverConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpV3TrapReceiverConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpV3TrapReceiverConfig
  /retrieve-asset-snmp-view-config:
    post:
      description: Retrieve AssetSnmpViewConfig
      operationId: retrieveAssetSnmpViewConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSnmpViewConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSnmpViewConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSnmpViewConfig
  /retrieve-asset-sntp-config:
    post:
      description: Retrieve AssetSntpConfig
      operationId: retrieveAssetSntpConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSntpConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSntpConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSntpConfig
  /retrieve-asset-sntp-server-config:
    post:
      description: Retrieve AssetSntpServerConfig
      operationId: retrieveAssetSntpServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSntpServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSntpServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSntpServerConfig
  /retrieve-asset-spbm-cfm-global:
    post:
      description: Retrieve AssetSpbmCfmGlobal
      operationId: retrieveAssetSpbmCfmGlobal
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSpbmCfmGlobalGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSpbmCfmGlobalGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSpbmCfmGlobal
  /retrieve-asset-spbm-global-settings-config:
    post:
      description: Retrieve AssetSpbmGlobalSettingsConfig
      operationId: retrieveAssetSpbmGlobalSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSpbmGlobalSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSpbmGlobalSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSpbmGlobalSettingsConfig
  /retrieve-asset-spbm-instance:
    post:
      description: Retrieve AssetSpbmInstance
      operationId: retrieveAssetSpbmInstance
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSpbmInstanceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSpbmInstanceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSpbmInstance
  /retrieve-asset-ssid-config:
    post:
      description: Retrieve AssetSsidConfig
      operationId: retrieveAssetSsidConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSsidConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSsidConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSsidConfig
  /retrieve-asset-ssid-state:
    post:
      description: Retrieve AssetSsidState
      operationId: retrieveAssetSsidState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSsidStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSsidStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSsidState
  /retrieve-asset-storm-control-port-config:
    post:
      description: Retrieve AssetStormControlPortConfig
      operationId: retrieveAssetStormControlPortConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStormControlPortConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStormControlPortConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStormControlPortConfig
  /retrieve-asset-stp-domain-state:
    post:
      description: Retrieve AssetStpDomainState
      operationId: retrieveAssetStpDomainState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStpDomainStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStpDomainStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStpDomainState
  /retrieve-asset-stp-global-config:
    post:
      description: Retrieve AssetStpGlobalConfig
      operationId: retrieveAssetStpGlobalConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStpGlobalConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStpGlobalConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStpGlobalConfig
  /retrieve-asset-stp-interface-settings-config:
    post:
      description: Retrieve AssetStpInterfaceSettingsConfig
      operationId: retrieveAssetStpInterfaceSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStpInterfaceSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStpInterfaceSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStpInterfaceSettingsConfig
  /retrieve-asset-stp-port-state-entry:
    post:
      description: Retrieve AssetStpPortStateEntry
      operationId: retrieveAssetStpPortStateEntry
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStpPortStateEntryGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStpPortStateEntryGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStpPortStateEntry
  /retrieve-asset-stp-protocol-settings-config:
    post:
      description: Retrieve AssetStpProtocolSettingsConfig
      operationId: retrieveAssetStpProtocolSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStpProtocolSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStpProtocolSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStpProtocolSettingsConfig
  /retrieve-asset-stp-vlan-settings-config:
    post:
      description: Retrieve AssetStpVlanSettingsConfig
      operationId: retrieveAssetStpVlanSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetStpVlanSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetStpVlanSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetStpVlanSettingsConfig
  /retrieve-asset-sys-capabilities:
    post:
      description: Retrieve AssetSysCapabilities
      operationId: retrieveAssetSysCapabilities
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSysCapabilitiesGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSysCapabilitiesGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSysCapabilities
  /retrieve-asset-syslog-config:
    post:
      description: Retrieve AssetSyslogConfig
      operationId: retrieveAssetSyslogConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSyslogConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSyslogConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSyslogConfig
  /retrieve-asset-syslog-server-config:
    post:
      description: Retrieve AssetSyslogServerConfig
      operationId: retrieveAssetSyslogServerConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSyslogServerConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSyslogServerConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSyslogServerConfig
  /retrieve-asset-system-settings:
    post:
      description: Retrieve AssetSystemSettings
      operationId: retrieveAssetSystemSettings
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetSystemSettingsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetSystemSettingsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetSystemSettings
  /retrieve-asset-time-zone-config:
    post:
      description: Retrieve AssetTimeZoneConfig
      operationId: retrieveAssetTimeZoneConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetTimeZoneConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetTimeZoneConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetTimeZoneConfig
  /retrieve-asset-url-capabilities:
    post:
      description: Retrieve AssetUrlCapabilities
      operationId: retrieveAssetUrlCapabilities
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetUrlCapabilitiesGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetUrlCapabilitiesGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetUrlCapabilities
  /retrieve-asset-vim:
    post:
      description: Retrieve AssetVim
      operationId: retrieveAssetVim
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVimGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVimGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVim
  /retrieve-asset-vlan-config:
    post:
      description: Retrieve AssetVlanConfig
      operationId: retrieveAssetVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVlanConfig
  /retrieve-asset-vlan-config-ip-arp-learn-from:
    post:
      description: Retrieve AssetVlanConfigIpArpLearnFrom
      operationId: retrieveAssetVlanConfigIpArpLearnFrom
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVlanConfigIpArpLearnFromGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVlanConfigIpArpLearnFromGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVlanConfigIpArpLearnFrom
  /retrieve-asset-vlan-mac-fdb-state:
    post:
      description: Retrieve AssetVlanMacFdbState
      operationId: retrieveAssetVlanMacFdbState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVlanMacFdbStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVlanMacFdbStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVlanMacFdbState
  /retrieve-asset-vlan-state:
    post:
      description: Retrieve AssetVlanState
      operationId: retrieveAssetVlanState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVlanStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVlanStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVlanState
  /retrieve-asset-vlan-state-member-interfaces:
    post:
      description: Retrieve AssetVlanStateMemberInterfaces
      operationId: retrieveAssetVlanStateMemberInterfaces
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVlanStateMemberInterfacesGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVlanStateMemberInterfacesGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVlanStateMemberInterfaces
  /retrieve-asset-vrf-config:
    post:
      description: Retrieve AssetVrfConfig
      operationId: retrieveAssetVrfConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrfConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrfConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrfConfig
  /retrieve-asset-vrf-protected-route-gateway:
    post:
      description: Retrieve AssetVrfProtectedRouteGateway
      operationId: retrieveAssetVrfProtectedRouteGateway
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrfProtectedRouteGatewayGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrfProtectedRouteGatewayGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrfProtectedRouteGateway
  /retrieve-asset-vrf-route-config:
    post:
      description: Retrieve AssetVrfRouteConfig
      operationId: retrieveAssetVrfRouteConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrfRouteConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrfRouteConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrfRouteConfig
  /retrieve-asset-vrf-route-state:
    post:
      description: Retrieve AssetVrfRouteState
      operationId: retrieveAssetVrfRouteState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrfRouteStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrfRouteStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrfRouteState
  /retrieve-asset-vrf-state:
    post:
      description: Retrieve AssetVrfState
      operationId: retrieveAssetVrfState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrfStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrfStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrfState
  /retrieve-asset-vrf-vlan-config:
    post:
      description: Retrieve AssetVrfVlanConfig
      operationId: retrieveAssetVrfVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrfVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrfVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrfVlanConfig
  /retrieve-asset-vrrp-config:
    post:
      description: Retrieve AssetVrrpConfig
      operationId: retrieveAssetVrrpConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrrpConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrrpConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrrpConfig
  /retrieve-asset-vrrp-state:
    post:
      description: Retrieve AssetVrrpState
      operationId: retrieveAssetVrrpState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrrpStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrrpStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrrpState
  /retrieve-asset-vrrp-virtual-ip-address-config:
    post:
      description: Retrieve AssetVrrpVirtualIpAddressConfig
      operationId: retrieveAssetVrrpVirtualIpAddressConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrrpVirtualIpAddressConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrrpVirtualIpAddressConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrrpVirtualIpAddressConfig
  /retrieve-asset-vrrp-virtual-ip-address-state:
    post:
      description: Retrieve AssetVrrpVirtualIpAddressState
      operationId: retrieveAssetVrrpVirtualIpAddressState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetVrrpVirtualIpAddressStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetVrrpVirtualIpAddressStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetVrrpVirtualIpAddressState
  /retrieve-asset-wireless-interface:
    post:
      description: Retrieve AssetWirelessInterface
      operationId: retrieveAssetWirelessInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetWirelessInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetWirelessInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetWirelessInterface
  /retrieve-asset-wireless-interface-state:
    post:
      description: Retrieve AssetWirelessInterfaceState
      operationId: retrieveAssetWirelessInterfaceState
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AssetWirelessInterfaceStateGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AssetWirelessInterfaceStateGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - AssetWirelessInterfaceState
  /retrieve-inferred-area:
    post:
      description: Retrieve InferredArea
      operationId: retrieveInferredArea
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredAreaGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredAreaGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredArea
  /retrieve-inferred-boundary-details:
    post:
      description: Retrieve InferredBoundaryDetails
      operationId: retrieveInferredBoundaryDetails
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredBoundaryDetailsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredBoundaryDetailsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredBoundaryDetails
  /retrieve-inferred-cluster:
    post:
      description: Retrieve InferredCluster
      operationId: retrieveInferredCluster
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredClusterGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredClusterGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredCluster
  /retrieve-inferred-customer-vlan-uni:
    post:
      description: Retrieve InferredCustomerVlanUni
      operationId: retrieveInferredCustomerVlanUni
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredCustomerVlanUniGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredCustomerVlanUniGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredCustomerVlanUni
  /retrieve-inferred-device:
    post:
      description: Retrieve InferredDevice
      operationId: retrieveInferredDevice
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredDeviceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredDeviceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredDevice
  /retrieve-inferred-device-fa-detail:
    post:
      description: Retrieve InferredDeviceFaDetail
      operationId: retrieveInferredDeviceFaDetail
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredDeviceFaDetailGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredDeviceFaDetailGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredDeviceFaDetail
  /retrieve-inferred-device-spbm-detail:
    post:
      description: Retrieve InferredDeviceSpbmDetail
      operationId: retrieveInferredDeviceSpbmDetail
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredDeviceSpbmDetailGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredDeviceSpbmDetailGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredDeviceSpbmDetail
  /retrieve-inferred-discovery-status-error:
    post:
      description: Retrieve InferredDiscoveryStatusError
      operationId: retrieveInferredDiscoveryStatusError
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredDiscoveryStatusErrorGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredDiscoveryStatusErrorGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredDiscoveryStatusError
  /retrieve-inferred-fabric:
    post:
      description: Retrieve InferredFabric
      operationId: retrieveInferredFabric
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabric
  /retrieve-inferred-fabric-attach-domain:
    post:
      description: Retrieve InferredFabricAttachDomain
      operationId: retrieveInferredFabricAttachDomain
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricAttachDomainGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricAttachDomainGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabricAttachDomain
  /retrieve-inferred-fabric-attach-port:
    post:
      description: Retrieve InferredFabricAttachPort
      operationId: retrieveInferredFabricAttachPort
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricAttachPortGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricAttachPortGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabricAttachPort
  /retrieve-inferred-fabric-attach-vlan:
    post:
      description: Retrieve InferredFabricAttachVlan
      operationId: retrieveInferredFabricAttachVlan
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricAttachVlanGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricAttachVlanGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabricAttachVlan
  /retrieve-inferred-fabric-link:
    post:
      description: Retrieve InferredFabricLink
      operationId: retrieveInferredFabricLink
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricLinkGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricLinkGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabricLink
  /retrieve-inferred-fabric-link-fa-detail:
    post:
      description: Retrieve InferredFabricLinkFaDetail
      operationId: retrieveInferredFabricLinkFaDetail
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricLinkFaDetailGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricLinkFaDetailGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabricLinkFaDetail
  /retrieve-inferred-fabric-topology:
    post:
      description: Retrieve InferredFabricTopology
      operationId: retrieveInferredFabricTopology
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredFabricTopologyGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredFabricTopologyGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredFabricTopology
  /retrieve-inferred-global-spbm-details:
    post:
      description: Retrieve InferredGlobalSpbmDetails
      operationId: retrieveInferredGlobalSpbmDetails
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredGlobalSpbmDetailsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredGlobalSpbmDetailsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredGlobalSpbmDetails
  /retrieve-inferred-group-info:
    post:
      description: Retrieve InferredGroupInfo
      operationId: retrieveInferredGroupInfo
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredGroupInfoGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredGroupInfoGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredGroupInfo
  /retrieve-inferred-interface:
    post:
      description: Retrieve InferredInterface
      operationId: retrieveInferredInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredInterface
  /retrieve-inferred-interface-vlan-map:
    post:
      description: Retrieve InferredInterfaceVlanMap
      operationId: retrieveInferredInterfaceVlanMap
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredInterfaceVlanMapGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredInterfaceVlanMapGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredInterfaceVlanMap
  /retrieve-inferred-isis-details:
    post:
      description: Retrieve InferredIsisDetails
      operationId: retrieveInferredIsisDetails
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredIsisDetailsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredIsisDetailsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredIsisDetails
  /retrieve-inferred-isis-remote-details:
    post:
      description: Retrieve InferredIsisRemoteDetails
      operationId: retrieveInferredIsisRemoteDetails
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredIsisRemoteDetailsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredIsisRemoteDetailsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredIsisRemoteDetails
  /retrieve-inferred-l2-access:
    post:
      description: Retrieve InferredL2Access
      operationId: retrieveInferredL2Access
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL2AccessGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL2AccessGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL2Access
  /retrieve-inferred-l2-service:
    post:
      description: Retrieve InferredL2Service
      operationId: retrieveInferredL2Service
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL2ServiceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL2ServiceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL2Service
  /retrieve-inferred-l2-service-end-point:
    post:
      description: Retrieve InferredL2ServiceEndPoint
      operationId: retrieveInferredL2ServiceEndPoint
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL2ServiceEndPointGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL2ServiceEndPointGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL2ServiceEndPoint
  /retrieve-inferred-l3-access:
    post:
      description: Retrieve InferredL3Access
      operationId: retrieveInferredL3Access
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL3AccessGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL3AccessGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL3Access
  /retrieve-inferred-l3-gateway-interface:
    post:
      description: Retrieve InferredL3GatewayInterface
      operationId: retrieveInferredL3GatewayInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL3GatewayInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL3GatewayInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL3GatewayInterface
  /retrieve-inferred-l3-service:
    post:
      description: Retrieve InferredL3Service
      operationId: retrieveInferredL3Service
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL3ServiceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL3ServiceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL3Service
  /retrieve-inferred-l3-service-end-point:
    post:
      description: Retrieve InferredL3ServiceEndPoint
      operationId: retrieveInferredL3ServiceEndPoint
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredL3ServiceEndPointGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredL3ServiceEndPointGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredL3ServiceEndPoint
  /retrieve-inferred-lag:
    post:
      description: Retrieve InferredLag
      operationId: retrieveInferredLag
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredLagGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredLagGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredLag
  /retrieve-inferred-lldp-edge-device-grouping:
    post:
      description: Retrieve InferredLldpEdgeDeviceGrouping
      operationId: retrieveInferredLldpEdgeDeviceGrouping
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredLldpEdgeDeviceGroupingGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredLldpEdgeDeviceGroupingGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredLldpEdgeDeviceGrouping
  /retrieve-inferred-physical-link:
    post:
      description: Retrieve InferredPhysicalLink
      operationId: retrieveInferredPhysicalLink
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPhysicalLinkGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPhysicalLinkGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPhysicalLink
  /retrieve-inferred-physical-topology:
    post:
      description: Retrieve InferredPhysicalTopology
      operationId: retrieveInferredPhysicalTopology
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPhysicalTopologyGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPhysicalTopologyGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPhysicalTopology
  /retrieve-inferred-port:
    post:
      description: Retrieve InferredPort
      operationId: retrieveInferredPort
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPortGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPortGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPort
  /retrieve-inferred-port-error-stats:
    post:
      description: Retrieve InferredPortErrorStats
      operationId: retrieveInferredPortErrorStats
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPortErrorStatsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPortErrorStatsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPortErrorStats
  /retrieve-inferred-port-in-out-stats:
    post:
      description: Retrieve InferredPortInOutStats
      operationId: retrieveInferredPortInOutStats
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPortInOutStatsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPortInOutStatsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPortInOutStats
  /retrieve-inferred-port-lag-member-map:
    post:
      description: Retrieve InferredPortLagMemberMap
      operationId: retrieveInferredPortLagMemberMap
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPortLagMemberMapGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPortLagMemberMapGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPortLagMemberMap
  /retrieve-inferred-port-utilization-info:
    post:
      description: Retrieve InferredPortUtilizationInfo
      operationId: retrieveInferredPortUtilizationInfo
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPortUtilizationInfoGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPortUtilizationInfoGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPortUtilizationInfo
  /retrieve-inferred-primary-private-vlan:
    post:
      description: Retrieve InferredPrimaryPrivateVlan
      operationId: retrieveInferredPrimaryPrivateVlan
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPrimaryPrivateVlanGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPrimaryPrivateVlanGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPrimaryPrivateVlan
  /retrieve-inferred-private-vlan-domain:
    post:
      description: Retrieve InferredPrivateVlanDomain
      operationId: retrieveInferredPrivateVlanDomain
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPrivateVlanDomainGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPrivateVlanDomainGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPrivateVlanDomain
  /retrieve-inferred-private-vlan-interface:
    post:
      description: Retrieve InferredPrivateVlanInterface
      operationId: retrieveInferredPrivateVlanInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredPrivateVlanInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredPrivateVlanInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredPrivateVlanInterface
  /retrieve-inferred-secondary-private-vlan:
    post:
      description: Retrieve InferredSecondaryPrivateVlan
      operationId: retrieveInferredSecondaryPrivateVlan
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredSecondaryPrivateVlanGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredSecondaryPrivateVlanGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredSecondaryPrivateVlan
  /retrieve-inferred-slot:
    post:
      description: Retrieve InferredSlot
      operationId: retrieveInferredSlot
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredSlotGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredSlotGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredSlot
  /retrieve-inferred-switched-vlan-uni:
    post:
      description: Retrieve InferredSwitchedVlanUni
      operationId: retrieveInferredSwitchedVlanUni
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredSwitchedVlanUniGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredSwitchedVlanUniGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredSwitchedVlanUni
  /retrieve-inferred-tag:
    post:
      description: Retrieve InferredTag
      operationId: retrieveInferredTag
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredTagGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredTagGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredTag
  /retrieve-inferred-tag-criteria:
    post:
      description: Retrieve InferredTagCriteria
      operationId: retrieveInferredTagCriteria
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredTagCriteriaGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredTagCriteriaGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredTagCriteria
  /retrieve-inferred-tag-element:
    post:
      description: Retrieve InferredTagElement
      operationId: retrieveInferredTagElement
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredTagElementGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredTagElementGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredTagElement
  /retrieve-inferred-tag-outer-element:
    post:
      description: Retrieve InferredTagOuterElement
      operationId: retrieveInferredTagOuterElement
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredTagOuterElementGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredTagOuterElementGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredTagOuterElement
  /retrieve-inferred-transparent-vlan-uni:
    post:
      description: Retrieve InferredTransparentVlanUni
      operationId: retrieveInferredTransparentVlanUni
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredTransparentVlanUniGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredTransparentVlanUniGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredTransparentVlanUni
  /retrieve-inferred-user-link:
    post:
      description: Retrieve InferredUserLink
      operationId: retrieveInferredUserLink
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredUserLinkGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredUserLinkGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredUserLink
  /retrieve-inferred-vlan:
    post:
      description: Retrieve InferredVlan
      operationId: retrieveInferredVlan
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredVlanGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredVlanGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredVlan
  /retrieve-inferred-vlan-service:
    post:
      description: Retrieve InferredVlanService
      operationId: retrieveInferredVlanService
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredVlanServiceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredVlanServiceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredVlanService
  /retrieve-inferred-vrf:
    post:
      description: Retrieve InferredVrf
      operationId: retrieveInferredVrf
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredVrfGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredVrfGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredVrf
  /retrieve-inferred-vrf-service:
    post:
      description: Retrieve InferredVrfService
      operationId: retrieveInferredVrfService
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredVrfServiceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredVrfServiceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredVrfService
  /retrieve-inferred-vrrp-instance:
    post:
      description: Retrieve InferredVrrpInstance
      operationId: retrieveInferredVrrpInstance
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredVrrpInstanceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredVrrpInstanceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredVrrpInstance
  /retrieve-inferred-wireless-interface:
    post:
      description: Retrieve InferredWirelessInterface
      operationId: retrieveInferredWirelessInterface
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredWirelessInterfaceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredWirelessInterfaceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredWirelessInterface
  /retrieve-inferred-wireless-interface-error-stats:
    post:
      description: Retrieve InferredWirelessInterfaceErrorStats
      operationId: retrieveInferredWirelessInterfaceErrorStats
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredWirelessInterfaceErrorStatsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredWirelessInterfaceErrorStatsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredWirelessInterfaceErrorStats
  /retrieve-inferred-wireless-interface-in-out-stats:
    post:
      description: Retrieve InferredWirelessInterfaceInOutStats
      operationId: retrieveInferredWirelessInterfaceInOutStats
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredWirelessInterfaceInOutStatsGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredWirelessInterfaceInOutStatsGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredWirelessInterfaceInOutStats
  /retrieve-inferred-wireless-interface-utilization-info:
    post:
      description: Retrieve InferredWirelessInterfaceUtilizationInfo
      operationId: retrieveInferredWirelessInterfaceUtilizationInfo
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InferredWirelessInterfaceUtilizationInfoGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InferredWirelessInterfaceUtilizationInfoGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - InferredWirelessInterfaceUtilizationInfo
  /retrieve-intent-auto-sense-fabric-attach:
    post:
      description: Retrieve IntentAutoSenseFabricAttach
      operationId: retrieveIntentAutoSenseFabricAttach
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentAutoSenseFabricAttachGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentAutoSenseFabricAttachGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentAutoSenseFabricAttach
  /retrieve-intent-auto-sense-global:
    post:
      description: Retrieve IntentAutoSenseGlobal
      operationId: retrieveIntentAutoSenseGlobal
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentAutoSenseGlobalGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentAutoSenseGlobalGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentAutoSenseGlobal
  /retrieve-intent-auto-sense-isis:
    post:
      description: Retrieve IntentAutoSenseIsis
      operationId: retrieveIntentAutoSenseIsis
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentAutoSenseIsisGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentAutoSenseIsisGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentAutoSenseIsis
  /retrieve-intent-device:
    post:
      description: Retrieve IntentDevice
      operationId: retrieveIntentDevice
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentDeviceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentDeviceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentDevice
  /retrieve-intent-isis-circuit-config:
    post:
      description: Retrieve IntentIsisCircuitConfig
      operationId: retrieveIntentIsisCircuitConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentIsisCircuitConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentIsisCircuitConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentIsisCircuitConfig
  /retrieve-intent-isis-circuit-spbm-setting:
    post:
      description: Retrieve IntentIsisCircuitSpbmSetting
      operationId: retrieveIntentIsisCircuitSpbmSetting
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentIsisCircuitSpbmSettingGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentIsisCircuitSpbmSettingGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentIsisCircuitSpbmSetting
  /retrieve-intent-isis-global-config:
    post:
      description: Retrieve IntentIsisGlobalConfig
      operationId: retrieveIntentIsisGlobalConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentIsisGlobalConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentIsisGlobalConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentIsisGlobalConfig
  /retrieve-intent-l2-vsn-cvlan-config:
    post:
      description: Retrieve IntentL2VsnCvlanConfig
      operationId: retrieveIntentL2VsnCvlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentL2VsnCvlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentL2VsnCvlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentL2VsnCvlanConfig
  /retrieve-intent-l2-vsn-suni-config:
    post:
      description: Retrieve IntentL2VsnSuniConfig
      operationId: retrieveIntentL2VsnSuniConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentL2VsnSuniConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentL2VsnSuniConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentL2VsnSuniConfig
  /retrieve-intent-l2-vsn-tuni-config:
    post:
      description: Retrieve IntentL2VsnTuniConfig
      operationId: retrieveIntentL2VsnTuniConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentL2VsnTuniConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentL2VsnTuniConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentL2VsnTuniConfig
  /retrieve-intent-spbm-global-settings-config:
    post:
      description: Retrieve IntentSpbmGlobalSettingsConfig
      operationId: retrieveIntentSpbmGlobalSettingsConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentSpbmGlobalSettingsConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentSpbmGlobalSettingsConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentSpbmGlobalSettingsConfig
  /retrieve-intent-spbm-instance:
    post:
      description: Retrieve IntentSpbmInstance
      operationId: retrieveIntentSpbmInstance
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentSpbmInstanceGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentSpbmInstanceGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentSpbmInstance
  /retrieve-intent-vlan-config:
    post:
      description: Retrieve IntentVlanConfig
      operationId: retrieveIntentVlanConfig
      parameters:
        - description: Page number to fetch the data
          in: query
          name: page_number
          schema:
            type: string
        - description: Page size to fetch the data
          in: query
          name: page_size
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IntentVlanConfigGetRequest"
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IntentVlanConfigGetResponse"
          description: Successful operation
        "400":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Bad request
        "401":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unauthorized. Authentication is required or has failed.
        "415":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Unsupported media type
        "500":
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
          description: Internal server error
        default:
          description: Unexpected error
      tags:
        - IntentVlanConfig
tags:
  - name: AssetAccount
  - name: AssetAllOfLldpNeighborXmedInventory
  - name: AssetAuthenticationConfig
  - name: AssetAuthenticationPortSettingConfig
  - name: AssetAuthenticationState
  - name: AssetAutoSenseFabricAttach
  - name: AssetAutoSenseGlobal
  - name: AssetAutoSenseIsis
  - name: AssetAutoSensePort
  - name: AssetCdpNeighbor
  - name: AssetCdpPortsConfig
  - name: AssetDevice
  - name: AssetDeviceConnectionDetail
  - name: AssetDeviceConnectionDetailPhysicalStack
  - name: AssetDeviceConnectionDetailStackInfo
  - name: AssetDhcpRelayVlanConfig
  - name: AssetDhcpRelayVlanServerConfig
  - name: AssetDhcpRelayVrfConfig
  - name: AssetDhcpRelayVrfServerConfig
  - name: AssetDhcpRelayVrfState
  - name: AssetDhcpSnoopConfig
  - name: AssetDhcpSnoopDynamicVlansConfig
  - name: AssetDhcpSnoopPortsConfig
  - name: AssetDhcpSnoopVlansConfig
  - name: AssetDnsConfig
  - name: AssetDnsVrfServerConfig
  - name: AssetElrpConfig
  - name: AssetElrpDynamicVlanConfig
  - name: AssetElrpVlanConfig
  - name: AssetFabricAttachConfig
  - name: AssetFabricAttachConfigPort
  - name: AssetFabricAttachConfigZeroTouchClient
  - name: AssetFabricAttachState
  - name: AssetFabricAttachStateDiscoveryElement
  - name: AssetFabricAttachStateVlanAssignment
  - name: AssetFeTunnelConfig
  - name: AssetIgmpVlanConfig
  - name: AssetIgmpVrConfig
  - name: AssetInstantPortActionConfig
  - name: AssetInstantPortConfig
  - name: AssetInstantPortDeviceTypesConfig
  - name: AssetInstantPortMatchConditionConfig
  - name: AssetInstantPortProfileConfig
  - name: AssetInterIsisUnicastFibState
  - name: AssetInterface
  - name: AssetInterfaceIpAddress
  - name: AssetInterfaceVlanMap
  - name: AssetInterfaceVlanProperties
  - name: AssetIpArpState
  - name: AssetIsidMacFdbState
  - name: AssetIsisAdjacency
  - name: AssetIsisCircuitConfig
  - name: AssetIsisCircuitSpbmSetting
  - name: AssetIsisCircuitState
  - name: AssetIsisGlobalConfig
  - name: AssetIsisGlobalState
  - name: AssetIsisL2MultiAreaIsidList
  - name: AssetIsisL2RedistributeMultiAreaSnoop
  - name: AssetIsisL3AcceptPolicyConfig
  - name: AssetIsisL3Redistribute
  - name: AssetIsisL3RedistributeMultiAreaIsidRedist
  - name: AssetIsisLspState
  - name: AssetIsisMulticastFibState
  - name: AssetIsisMulticastRouteState
  - name: AssetIsisRedistributeMultiArea
  - name: AssetIsisRedistributeMultiAreaStaticIsid
  - name: AssetIsisUnicastFibState
  - name: AssetL2VsnCvlanConfig
  - name: AssetL2VsnSuniConfig
  - name: AssetL2VsnSuniConfigEndPoint
  - name: AssetL2VsnSuniState
  - name: AssetL2VsnSuniStateMemberIntfs
  - name: AssetL2VsnTuniConfig
  - name: AssetL2VsnTuniConfigMemberIntf
  - name: AssetL3VsnConfig
  - name: AssetL3VsnIsidListConfig
  - name: AssetLagConfig
  - name: AssetLagConfigMemberPort
  - name: AssetLagState
  - name: AssetLagStateMemberPort
  - name: AssetLldpConfig
  - name: AssetLldpDot1PortProtocolVlan
  - name: AssetLldpDot1VlanObject
  - name: AssetLldpNeighborFabricAttach
  - name: AssetLldpNeighborFabricAttachVlanIsidMap
  - name: AssetLldpNeighborManagementAddressList
  - name: AssetLldpNeighborState
  - name: AssetLldpNeighborXmed
  - name: AssetLldpNeighborXmedPoePd
  - name: AssetLldpNeighborXmedPoePse
  - name: AssetLldpNeighborXmedPolicy
  - name: AssetLldpNeighborZtf
  - name: AssetLldpNeighborZtfTunnelGroup
  - name: AssetLldpPortAdvertiseSettingsConfig
  - name: AssetLldpPortConfig
  - name: AssetLldpPortMedPolicySettingsConfig
  - name: AssetLocation
  - name: AssetMacLockingConfig
  - name: AssetMlagPeerConfig
  - name: AssetMlagPeerCounters
  - name: AssetMlagPeerState
  - name: AssetMlagPortState
  - name: AssetMulticastFibInterface
  - name: AssetNeighDiscState
  - name: AssetNetworkToNetworkInterface
  - name: AssetNodeAliasConfig
  - name: AssetNodeAliasPortConfig
  - name: AssetNtpConfig
  - name: AssetNtpServerConfig
  - name: AssetPoePowerConfig
  - name: AssetPoePowerPortsConfig
  - name: AssetPoePowerPortsState
  - name: AssetPoePowerSlotSettingsConfig
  - name: AssetPolicyGlobal
  - name: AssetPortCapabilities
  - name: AssetPortConfig
  - name: AssetPortLldpLocation
  - name: AssetPortMacLockingConfig
  - name: AssetPortState
  - name: AssetPortsGlobalConfig
  - name: AssetRadiusServerConfig
  - name: AssetRadiusServerHostConfig
  - name: AssetRadiusServerSecureProfilesConfig
  - name: AssetRsmltLocalInstanceState
  - name: AssetRsmltRemoteInstanceState
  - name: AssetRsmltVlanInstanceConfig
  - name: AssetSlot
  - name: AssetSlppConfig
  - name: AssetSlppPortConfig
  - name: AssetSlppVlanConfig
  - name: AssetSnmpAccessConfig
  - name: AssetSnmpGlobalConfig
  - name: AssetSnmpGroupConfig
  - name: AssetSnmpTrapReceiverConfig
  - name: AssetSnmpV1V2Config
  - name: AssetSnmpV3AccessConfig
  - name: AssetSnmpV3Config
  - name: AssetSnmpV3TrapReceiverConfig
  - name: AssetSnmpViewConfig
  - name: AssetSntpConfig
  - name: AssetSntpServerConfig
  - name: AssetSpbmCfmGlobal
  - name: AssetSpbmGlobalSettingsConfig
  - name: AssetSpbmInstance
  - name: AssetSsidConfig
  - name: AssetSsidState
  - name: AssetStormControlPortConfig
  - name: AssetStpDomainState
  - name: AssetStpGlobalConfig
  - name: AssetStpInterfaceSettingsConfig
  - name: AssetStpPortStateEntry
  - name: AssetStpProtocolSettingsConfig
  - name: AssetStpVlanSettingsConfig
  - name: AssetSysCapabilities
  - name: AssetSyslogConfig
  - name: AssetSyslogServerConfig
  - name: AssetSystemSettings
  - name: AssetTimeZoneConfig
  - name: AssetUrlCapabilities
  - name: AssetVim
  - name: AssetVlanConfig
  - name: AssetVlanConfigIpArpLearnFrom
  - name: AssetVlanMacFdbState
  - name: AssetVlanState
  - name: AssetVlanStateMemberInterfaces
  - name: AssetVrfConfig
  - name: AssetVrfProtectedRouteGateway
  - name: AssetVrfRouteConfig
  - name: AssetVrfRouteState
  - name: AssetVrfState
  - name: AssetVrfVlanConfig
  - name: AssetVrrpConfig
  - name: AssetVrrpState
  - name: AssetVrrpVirtualIpAddressConfig
  - name: AssetVrrpVirtualIpAddressState
  - name: AssetWirelessInterface
  - name: AssetWirelessInterfaceState
  - name: InferredArea
  - name: InferredBoundaryDetails
  - name: InferredCluster
  - name: InferredCustomerVlanUni
  - name: InferredDevice
  - name: InferredDeviceFaDetail
  - name: InferredDeviceSpbmDetail
  - name: InferredDiscoveryStatusError
  - name: InferredFabric
  - name: InferredFabricAttachDomain
  - name: InferredFabricAttachPort
  - name: InferredFabricAttachVlan
  - name: InferredFabricLink
  - name: InferredFabricLinkFaDetail
  - name: InferredFabricTopology
  - name: InferredGlobalSpbmDetails
  - name: InferredGroupInfo
  - name: InferredInterface
  - name: InferredInterfaceVlanMap
  - name: InferredIsisDetails
  - name: InferredIsisRemoteDetails
  - name: InferredL2Access
  - name: InferredL2Service
  - name: InferredL2ServiceEndPoint
  - name: InferredL3Access
  - name: InferredL3GatewayInterface
  - name: InferredL3Service
  - name: InferredL3ServiceEndPoint
  - name: InferredLag
  - name: InferredLldpEdgeDeviceGrouping
  - name: InferredPhysicalLink
  - name: InferredPhysicalTopology
  - name: InferredPort
  - name: InferredPortErrorStats
  - name: InferredPortInOutStats
  - name: InferredPortLagMemberMap
  - name: InferredPortUtilizationInfo
  - name: InferredPrimaryPrivateVlan
  - name: InferredPrivateVlanDomain
  - name: InferredPrivateVlanInterface
  - name: InferredSecondaryPrivateVlan
  - name: InferredSlot
  - name: InferredSwitchedVlanUni
  - name: InferredTag
  - name: InferredTagCriteria
  - name: InferredTagElement
  - name: InferredTagOuterElement
  - name: InferredTransparentVlanUni
  - name: InferredUserLink
  - name: InferredVlan
  - name: InferredVlanService
  - name: InferredVrf
  - name: InferredVrfService
  - name: InferredVrrpInstance
  - name: InferredWirelessInterface
  - name: InferredWirelessInterfaceErrorStats
  - name: InferredWirelessInterfaceInOutStats
  - name: InferredWirelessInterfaceUtilizationInfo
  - name: IntentAutoSenseFabricAttach
  - name: IntentAutoSenseGlobal
  - name: IntentAutoSenseIsis
  - name: IntentDevice
  - name: IntentIsisCircuitConfig
  - name: IntentIsisCircuitSpbmSetting
  - name: IntentIsisGlobalConfig
  - name: IntentL2VsnCvlanConfig
  - name: IntentL2VsnSuniConfig
  - name: IntentL2VsnTuniConfig
  - name: IntentSpbmGlobalSettingsConfig
  - name: IntentSpbmInstance
  - name: IntentVlanConfig
security:
  - BearerAuth: []
